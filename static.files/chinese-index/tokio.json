[{"url":"tokio/attr.main.html","crate":"tokio","title":"main","section":"attr","desc":"将 async 函数标记为由所选运行时执行。该宏帮助设置一个 Runtime， 无需用户直接使用 Runtime 或 Builder。 注意：该宏被设计为简单易用，面向不需要复杂配置的应用。如果所提供的功能不够用， 您可以考虑使用 Builder，它提供更强大的接口。 注意：该宏可用于任何函数，不仅仅是 main 函数。在非 main 函数上使用它 会让该函数表现得像同步函数一样（每次调用时启动一个新运行时）。如果该函数被频繁调用， 建议使用 runtime builder 创建运行时，以便跨调用复用。 §Non-worker async function 注意：由该宏标记的 async 函数不会作为 worker 运行。预期行为是此函数内部派生其他任务。 在此处提供的函数中 await 其他 future 的性能不会像 worker 派生的任务那样快。 §Runtime flavors 该宏可通过 flavor 参数配置不同的运行时配置。 §Multi-threaded 要使用多线程运行时，可通过以下方式配置该宏： #[tokio::main(flavor = \"multi_thread\", worker_threads = 10)] worker_threads 选项配置工作线程的数量， 默认为系统上的 CPU 数量。这是默认的 flavor。 注意：多线程运行时需要 rt-multi-thread 特性标志。 §Current-thread 要使用称为 current_thread 运行时的单线程运行时，可通过以下方式配置该宏： #[tokio::main(flavor = \"current_thread\")]§Local 要使用 local runtime，可以按以下方式配置宏 #[tokio::main(flavor = \"local\")]§Function arguments 除 main 是特殊的之外，任何函数都允许使用参数。 §Usage§Set the name of the runtime #[tokio::main(name = \"my-runtime\")] async fn main() { println!(\"Hello world\"); } 不使用 #[tokio::main] 的等价代码 fn main() { tokio::runtime::Builder::new_multi_thread() .enable_all() .name(\"my-runtime\") .build() .unwrap() .block_on(async { println!(\"Hello world\"); }) }§Using the multi-threaded runtime #[tokio::main] async fn main() { println!(\"Hello world\"); } 不使用 #[tokio::main] 的等价代码 fn main() { tokio::runtime::Builder::new_multi_thread() .enable_all() .build() .unwrap() .block_on(async { println!(\"Hello world\"); }) }§Using the current-thread runtime 基础调度器是单线程的。 #[tokio::main(flavor = \"current_thread\")] async fn main() { println!(\"Hello world\"); } 不使用 #[tokio::main] 的等价代码 fn main() { tokio::runtime::Builder::new_current_thread() .enable_all() .build() .unwrap() .block_on(async { println!(\"Hello world\"); }) }§Using the local runtime local runtime 与 current-thread runtime 类似，但 支持 task::spawn_local。 #[tokio::main(flavor = \"local\")] async fn main() { println!(\"Hello world\"); } 不使用 #[tokio::main] 的等价代码 fn main() { tokio::runtime::Builder::new_current_thread() .enable_all() .build_local(tokio::runtime::LocalOptions::default(","module_doc":"将 async 函数标记为由所选运行时执行。该宏帮助设置一个 Runtime， 无需用户直接使用 Runtime 或 Builder。 注意：该宏被设计为简单易用，面向不需要复杂配置的应用。如果所提供的功能不够用， 您可以考虑使用 Builder，它提供更强大的接口。 注意：该宏可用于任何函数，不仅仅是 main 函数。在非 main 函数上使用它 会让该函数表现得像同步函数一样（每次调用时启动一个新运行时）。如果该函数被频繁调用， 建议使用 runtime builder 创建运行时，以便跨调用复用。 注意：由该宏标记的 async 函数不会作为 worker 运行。预期行为是此函数内部派生其他任务。 在此处提供的函数中 await 其他 future 的性能不会像 worker 派生的任务那样快。 该宏可通过 flavor 参数配置不同的运行时配置。 要使用多线程运行时，可通过以下方式配置该宏：","section_headers":[]},{"url":"tokio/attr.test.html","crate":"tokio","title":"test","section":"attr","desc":"将 async 函数标记为由运行时执行，适用于测试环境。该宏帮助设置一个 Runtime， 无需用户直接使用 Runtime 或 Builder。 注意：该宏被设计为简单易用，面向不需要复杂配置的应用。如果所提供的功能不够用， 您可以考虑使用 Builder，它提供更强大的接口。 §Multi-threaded runtime 要使用多线程运行时，可通过以下方式配置该宏： #[tokio::test(flavor = \"multi_thread\", worker_threads = 1)] async fn my_test() { assert!(true); } worker_threads 选项配置工作线程的数量， 默认为系统上的 CPU 数量。 注意：多线程运行时需要 rt-multi-thread 特性标志。 §Current thread runtime 默认的测试运行时是单线程的。每个测试 都获得单独的 current-thread runtime。 #[tokio::test] async fn my_test() { assert!(true); }§Usage§Set the name of the runtime #[tokio::test(name = \"my-test-runtime\")] async fn my_test() { assert!(true); } 不使用 #[tokio::test] 的等价代码 #[test] fn my_test() { tokio::runtime::Builder::new_current_thread() .enable_all() .name(\"my-test-runtime\") .build() .unwrap() .block_on(async { assert!(true); }) }§Using the multi-thread runtime #[tokio::test(flavor = \"multi_thread\")] async fn my_test() { assert!(true); } 不使用 #[tokio::test] 的等价代码 #[test] fn my_test() { tokio::runtime::Builder::new_multi_thread() .enable_all() .build() .unwrap() .block_on(async { assert!(true); }) }§Using current thread runtime #[tokio::test] async fn my_test() { assert!(true); } 不使用 #[tokio::test] 的等价代码 #[test] fn my_test() { tokio::runtime::Builder::new_current_thread() .enable_all() .build() .unwrap() .block_on(async { assert!(true); }) }§Set number of worker threads #[tokio::test(flavor = \"multi_thread\", worker_threads = 2)] async fn my_test() { assert!(true); } 不使用 #[tokio::test] 的等价代码 #[test] fn my_test() { tokio::runtime::Builder::new_multi_thread() .worker_threads(2) .enable_all() .build() .unwrap() .block_on(async { assert!(true); }) }§Configure the runtime to start with time paused #[tokio::test(start_paused = true)] async fn my_test() { assert!(true); } 不使用 #[tokio::test] 的等价代码 #[test] fn my_test() { tokio::runtime::Builder::new_current_thread() .enable_all() .start_paused(true) .build() .unwrap() .block_on(async { assert!(true); }) } 请注意，start_paused 需要启用 test-util feature。 §Rename package use tokio as t","module_doc":"将 async 函数标记为由运行时执行，适用于测试环境。该宏帮助设置一个 Runtime， 无需用户直接使用 Runtime 或 Builder。 注意：该宏被设计为简单易用，面向不需要复杂配置的应用。如果所提供的功能不够用， 您可以考虑使用 Builder，它提供更强大的接口。 要使用多线程运行时，可通过以下方式配置该宏：","section_headers":[]},{"url":"tokio/macro.join.html","crate":"tokio","title":"join","section":"macro","desc":"等待多个并发分支，当所有分支完成时返回。 join! 宏必须在 async 函数、闭包和块内使用。 join! 宏接受一个 async 表达式列表，并在同一任务上并发地执行它们。 每个 async 表达式求值为一个 future，来自每个表达式的 future 在当前任务上多路复用。 对于返回 Result 的异步表达式，join! 将等待 所有分支完成，无论是否有任何分支以 Err 完成。 使用 try_join! 在遇到 Err 时提早返回。 §Notes 所提供的 future 内联存储，无需分配 Vec。 §Runtime characteristics 由于所有异步表达式都在当前任务上运行，这些表达式 能够并发运行，但不能并行运行。这意味着所有 表达式都在同一线程上运行，如果某个分支阻塞了线程， 所有其他表达式都将无法继续。如果需要并行执行， 请使用 tokio::spawn 生成每个异步表达式， 然后将其 join handle 传递给 join!。 §Fairness 默认情况下，每次唤醒时，join! 生成的 future 会轮换首先 轮询哪个包含的 future。 可以通过在宏使用的开头添加 biased; 来覆盖此行为。 有关详细信息，请参阅示例。这将使 join 按 future 出现的顺序（从上到下）进行轮询。 如果您的 future 之间的交互中已知的 poll 顺序很重要，则可能需要这样做。 但此模式有一个重要的注意事项。您需要自行确保 future 的 poll 顺序是公平的。例如，如果您 join 一个流和一个 shutdown future，并且该流每次 poll 需要处理大量消息， 那么该流可能会一直阻止 shutdown future 被 poll，导致 shutdown future 永远无法执行。u should place the shutdown future earlier in the join! list to ensure that it is always polled, and will not be delayed due to the stream future taking a long time to return Poll::Pending. §示例 两个分支的基本 join async fn do_stuff_async() { // async work } async fn more_async_work() { // more here } let (first, second) = tokio::join!( do_stuff_async(), more_async_work()); // do something with the values 使用 biased; 模式控制轮询顺序。 async fn do_stuff_async() { // async work } async fn more_async_work() { // more here } let (first, second) = tokio::join!( biased; do_stuff_async(), more_async_work() ); // do something with the values","module_doc":"等待多个并发分支，当所有分支完成时返回。 join! 宏必须在 async 函数、闭包和块内使用。 join! 宏接受一个 async 表达式列表，并在同一任务上并发地执行它们。 每个 async 表达式求值为一个 future，来自每个表达式的 future 在当前任务上多路复用。 对于返回 Result 的异步表达式，join! 将等待 所有分支完成，无论是否有任何分支以 Err 完成。 使用 try_join! 在遇到 Err 时提早返回。 所提供的 future 内联存储，无需分配 Vec。 由于所有异步表达式都在当前任务上运行，这些表达式 能够并发运行，但不能并行运行。这意味着所有 表达式都在同一线程上运行，如果某个分支阻塞了线程， 所有其他表达式都将无法继续。如果需要并行执行， 请使用 tokio::spawn 生成每个异步表达式， 然后将其 join handle 传递给 join!。 默认情况下，每次唤醒时，join! 生成的 future 会轮换首先 轮询哪个包含的 future。 可以通过在宏使用的开头添加 biased; 来覆盖此行为。 有关详细信息，请参阅示例。","section_headers":[]},{"url":"tokio/macro.pin.html","crate":"tokio","title":"pin","section":"macro","desc":"在栈上 pin 一个值。 对 async fn 的调用返回的是匿名的 Future 值，这些值是 !Unpin 的。必须先将这些值 pin 才能 poll。调用 .await 会处理这一点，但会消费 future。如果需要在 &mut _ 引用上调用 .await，则调用者负责 pin 该 future。 可以通过使用 Box::pin 分配来 pin，也可以使用 pin! 宏在栈上 pin。 下面的代码将无法编译： ⓘasync fn my_async_fn() { // 这里是异步逻辑 } #[tokio::main] async fn main() { let mut future = my_async_fn(); (&mut future).await; } 要使其工作，需要 pin： use tokio::pin; async fn my_async_fn() { // 这里是异步逻辑 } let future = my_async_fn(); pin!(future); (&mut future).await; 在使用需要 T: Stream + Unpin 的 select! 和流操作符时，pin 很有用。 §用法 pin! 宏以标识符作为参数。它不能与表达式一起使用。 下面的代码无法编译，因为将表达式传递给了 pin!。 ⓘasync fn my_async_fn() { // 这里是异步逻辑 } #[tokio::main] async fn main() { let mut future = pin!(my_async_fn()); (&mut future).await; }§示例 与 select 一起使用： use tokio::{pin, select}; use tokio_stream::{self as stream, StreamExt}; async fn my_async_fn() { // 这里是异步逻辑 } let mut stream = stream::iter(vec![1, 2, 3, 4]); let future = my_async_fn(); pin!(future); loop { select! { _ = &mut future => { // 停止循环 `future` 在完成后仍会被轮询 break; } Some(val) = stream.next() => { println!(\"got value = {}\", val); } } } 由于先赋值给变量然后 pin 很常见，所以该宏还有一个变体，支持一次性完成这两个操作。 use tokio::{pin, select}; async fn my_async_fn() { // 这里是异步逻辑 } pin! { let future1 = my_async_fn(); let future2 = my_async_fn(); } select! { _ = &mut future1 => {} _ = &mut future2 => {} }","module_doc":"在栈上 pin 一个值。 对 async fn 的调用返回的是匿名的 Future 值，这些值是 !Unpin 的。必须先将这些值 pin 才能 poll。调用 .await 会处理这一点，但会消费 future。如果需要在 &mut _ 引用上调用 .await，则调用者负责 pin 该 future。 可以通过使用 Box::pin 分配来 pin，也可以使用 pin! 宏在栈上 pin。 下面的代码将无法编译：","section_headers":[]},{"url":"tokio/macro.select.html","crate":"tokio","title":"select","section":"macro","desc":"等待多个并发分支，当第一个分支完成时返回， 并取消其余分支。 select! 宏必须在 async 函数、闭包和块内使用。 select! 宏接受一个或多个具有以下模式的分支： <pattern> = <async expression> (, if <precondition>)? => <handler>, 此外，select! 宏可以包含一个可选的 else 分支， 在没有其他分支匹配其模式时进行求值： else => <expression> 宏会聚合所有 <async expression> 表达式， 并在当前任务上并发运行它们。一旦第一个表达式 以匹配其 <pattern> 的值完成，select! 宏 就会返回已完成分支的 <handler> 表达式的求值结果。 此外，每个分支可以包含一个可选的 if 前置条件。如果 前置条件返回 false，则该分支被禁用。提供的 <async expression> 仍会被求值，但生成的 future 永远不会被 轮询。此功能在循环中使用 select! 时非常有用。 select! 表达式的完整生命周期如下： Evaluate all provided <precondition> expressions. If the precondition returns false, disable the branch for the remainder of the current call to select!. Re-entering select! due to a loop clears the “disabled” state. Aggregate the <async expression>s from each branch, including the disabled ones. If the branch is disabled, <async expression> is still evaluated, but the resulting future is not polled. If all branches are disabled: go to step 6. Concurrently await on the results for all remaining <async expression>s. Once an <async expression> returns a value, attempt to apply the value to the provided <pattern>. If the pattern matches, evaluate the <handler> and return. If the pattern does not match, disable the current branch for the remainder of the current call to select!. Continue from step 3. Evaluate the else expression. If no else expression is provided, panic. §Runtime characteristics 由于所有异步表达式都在当前任务上运行，这些表达式 能够并发运行，但不能并行运行。这意味着所有 表达式都在同一线程上运行，如果某个分支阻塞了线程， 所有其他表达式都将无法继续。如果需要并行执行， 请使用 tokio::spawn 生成每个异步表达式， 然后将其 join handle 传递给 select!。 §Fairness 默认情况下，select! 会随机选择一个分支优先检查。 当在循环中调用 select! 且分支始终就绪时， 这提供了一定程度的公平性。 可以通过在宏使用的开头添加 biased; 来覆盖此行为。 有关详细信息，请参阅示例。这将使 select 按 future 出现的顺序（从上到下）进行轮询。出于以下几种原因，你可能希望这样做： The random number generation of tokio::select! has a non-zero CPU cost Your futures may interact in a way where known polling order is significant 但此模式有一个重要的注意事项。确保你的 future 的轮询顺序是公平的 成为了你的责任。例如，如果你在流和关闭 future 之间进行选择， 而流有大量消息且消息之间的时间间隔为零或几乎为零， 则应将关闭 future 放在 select! 列表的更靠","module_doc":"等待多个并发分支，当第一个分支完成时返回， 并取消其余分支。 select! 宏必须在 async 函数、闭包和块内使用。 select! 宏接受一个或多个具有以下模式的分支：","section_headers":[]},{"url":"tokio/macro.task_local.html","crate":"tokio","title":"task_local","section":"macro","desc":"声明一个类型为 tokio::task::LocalKey 的新任务本地 key。 §语法 该宏包装任意数量的 static 声明，并使它们对当前任务本地化。每个 static 的公开性和属性都会被保留。例如： §示例 task_local! { pub static ONE: u32; #[allow(unused)] static TWO: f32; } 有关更多信息，请参阅 LocalKey 文档。","module_doc":"声明一个类型为 tokio::task::LocalKey 的新任务本地 key。 该宏包装任意数量的 static 声明，并使它们对当前任务本地化。每个 static 的公开性和属性都会被保留。例如：","section_headers":[]},{"url":"tokio/macro.try_join.html","crate":"tokio","title":"try_join","section":"macro","desc":"等待多个并发分支，当所有分支以 Ok(_) 完成时返回， 或在第一个 Err(_) 时返回。 try_join! 宏必须在 async 函数、闭包和块内使用。 与 join! 类似，try_join! 宏接受一组 异步表达式，并在同一任务上并发求值它们。每个异步 表达式求值为一个 future，来自每个表达式的 future 在当前任务上多路复用。try_join! 宏在所有分支 返回 Ok 时或第一个分支返回 Err 时返回。 §Notes 所提供的 future 内联存储，无需分配 Vec。 §Runtime characteristics 由于所有异步表达式都在当前任务上运行，这些表达式 能够并发运行，但不能并行运行。这意味着所有 表达式都在同一线程上运行，如果某个分支阻塞了线程， 所有其他表达式都将无法继续。如果需要并行执行， 请使用 tokio::spawn 生成每个异步表达式， 然后将其 join handle 传递给 try_join!。 §Fairness 默认情况下，每次唤醒时，try_join! 生成的 future 会轮换首先 轮询哪个包含的 future。 可以通过在宏使用的开头添加 biased; 来覆盖此行为。 有关详细信息，请参阅示例。这将使 try_join 按 future 出现的顺序（从上到下）进行轮询。 如果您的 future 之间的交互中已知的 poll 顺序很重要，则可能需要这样做。 但此模式有一个重要的注意事项。您需要自行确保 future 的 poll 顺序是公平的。例如，如果您 join 一个流和一个 shutdown future，并且该流每次 poll 需要处理大量消息， 那么该流可能会一直阻止 shutdown future 被 poll，导致 shutdown future 永远无法执行。u should place the shutdown future earlier in the try_join! list to ensure that it is always polled, and will not be delayed due to the stream future taking a long time to return Poll::Pending. §示例 具有两个分支的基础 try_join。 async fn do_stuff_async() -> Result<(), &'static str> { // async work } async fn more_async_work() -> Result<(), &'static str> { // more here } let res = tokio::try_join!( do_stuff_async(), more_async_work()); match res { Ok((first, second)) => { // do something with the values } Err(err) => { println!(\"processing failed; error = {}\", err); } } 将 try_join! 与生成的任务一起使用。 use tokio::task::JoinHandle; async fn do_stuff_async() -> Result<(), &'static str> { // async work } async fn more_async_work() -> Result<(), &'static str> { // more here } async fn flatten<T>(handle: JoinHandle<Result<T, &'static str>>) -> Result<T, &'static str> { match handle.await { Ok(Ok(result)) => Ok(result), Ok(Err(err)) => Err(err), Err(err) => Err(\"handling failed\"), } } let handle1 = tokio::spawn(do_stuff_async()); let handle2 = tokio::spawn(more_async_work()); match tokio::try_join!(flatten(handle1), flatten(handle2)) { Ok(val) => { // do something with the values } Err(err) => { println!(\"Failed with {}.\",","module_doc":"等待多个并发分支，当所有分支以 Ok(_) 完成时返回， 或在第一个 Err(_) 时返回。 try_join! 宏必须在 async 函数、闭包和块内使用。 与 join! 类似，try_join! 宏接受一组 异步表达式，并在同一任务上并发求值它们。每个异步 表达式求值为一个 future，来自每个表达式的 future 在当前任务上多路复用。try_join! 宏在所有分支 返回 Ok 时或第一个分支返回 Err 时返回。 所提供的 future 内联存储，无需分配 Vec。 由于所有异步表达式都在当前任务上运行，这些表达式 能够并发运行，但不能并行运行。这意味着所有 表达式都在同一线程上运行，如果某个分支阻塞了线程， 所有其他表达式都将无法继续。如果需要并行执行， 请使用 tokio::spawn 生成每个异步表达式， 然后将其 join handle 传递给 try_join!。 默认情况下，每次唤醒时，try_join! 生成的 future 会轮换首先 轮询哪个包含的 future。 可以通过在宏使用的开头添加 biased; 来覆盖此行为。 有关详细信息，请参阅示例。这","section_headers":[]},{"url":"tokio/fs/fn.canonicalize.html","crate":"tokio","title":"canonicalize","section":"fn","desc":"返回路径的规范化绝对形式，所有中间组件均被规范化，且符号链接被解析。 这是 std::fs::canonicalize 的异步版本。 §Platform-specific behavior 此函数当前对应于 Unix 上的 realpath 函数， 以及 Windows 上的 CreateFile 和 GetFinalPathNameByHandle 函数。 请注意，未来可能会发生变化。 在 Windows 上，这会将路径转换为使用扩展长度路径 语法，这允许你的程序使用更长的路径名，但这意味着你 只能将反斜杠分隔的路径连接到它，并且它可能与 其他应用程序不兼容（如果通过命令行传递给应用程序， 或者写入其他应用程序可能读取的文件中）。 §错误 此函数将在以下情况下返回错误，但不限于 这些情况： path does not exist. A non-final component in path is not a directory. §示例 use tokio::fs; use std::io; #[tokio::main] async fn main() -> io::Result<()> { let path = fs::canonicalize(\"../a/../foo.txt\").await?; Ok(()) }","module_doc":"返回路径的规范化绝对形式，所有中间组件均被规范化，且符号链接被解析。 这是 std::fs::canonicalize 的异步版本。 此函数当前对应于 Unix 上的 realpath 函数， 以及 Windows 上的 CreateFile 和 GetFinalPathNameByHandle 函数。 请注意，未来可能会发生变化。 在 Windows 上，这会将路径转换为使用扩展长度路径 语法，这允许你的程序使用更长的路径名，但这意味着你 只能将反斜杠分隔的路径连接到它，并且它可能与 其他应用程序不兼容（如果通过命令行传递给应用程序， 或者写入其他应用程序可能读取的文件中）。 此函数将在以下情况下返回错误，但不限于 这些情况：","section_headers":[]},{"url":"tokio/fs/fn.copy.html","crate":"tokio","title":"copy","section":"fn","desc":"将一个文件的内容复制到另一个文件。此函数还会将原始文件的权限位 复制到目标文件。 此函数将覆盖 to 的内容。 这是 std::fs::copy 的异步等效版本。 §示例 use tokio::fs; fs::copy(\"foo.txt\", \"bar.txt\").await?;","module_doc":"将一个文件的内容复制到另一个文件。此函数还会将原始文件的权限位 复制到目标文件。 此函数将覆盖 to 的内容。 这是 std::fs::copy 的异步等效版本。","section_headers":[]},{"url":"tokio/fs/fn.create_dir.html","crate":"tokio","title":"create_dir","section":"fn","desc":"在指定路径创建一个新的空目录。 这是 std::fs::create_dir 的异步版本。 §Platform-specific behavior 此函数当前对应于 Unix 上的 mkdir 函数， 以及 Windows 上的 CreateDirectory 函数。 请注意，未来可能会发生变化。 注意：如果给定路径的父目录不存在，此函数将 返回错误。要同时创建目录及其所有缺失的父目录， 请使用 create_dir_all 函数。 §错误 此函数将在以下情况下返回错误，但不限于 这些情况： User lacks permissions to create directory at path. A parent of the given path doesn’t exist. (To create a directory and all its missing parents at the same time, use the create_dir_all function.) path already exists. §示例 use tokio::fs; use std::io; #[tokio::main] async fn main() -> io::Result<()> { fs::create_dir(\"/some/dir\").await?; Ok(()) }","module_doc":"在指定路径创建一个新的空目录。 这是 std::fs::create_dir 的异步版本。 此函数当前对应于 Unix 上的 mkdir 函数， 以及 Windows 上的 CreateDirectory 函数。 请注意，未来可能会发生变化。 注意：如果给定路径的父目录不存在，此函数将 返回错误。要同时创建目录及其所有缺失的父目录， 请使用 create_dir_all 函数。 此函数将在以下情况下返回错误，但不限于 这些情况：","section_headers":[]},{"url":"tokio/fs/fn.create_dir_all.html","crate":"tokio","title":"create_dir_all","section":"fn","desc":"递归地创建一个目录及其所有父组件（如果它们 缺失）。 这是 std::fs::create_dir_all 的异步版本。 §Platform-specific behavior 此函数当前对应于 Unix 上的 mkdir 函数， 以及 Windows 上的 CreateDirectory 函数。 请注意，未来可能会发生变化。 §错误 此函数将在以下情况下返回错误，但不限于 这些情况： If any directory in the path specified by path does not already exist and it could not be created otherwise. The specific error conditions for when a directory is being created (after it is determined to not exist) are outlined by fs::create_dir. 对于 path 中指定的任何目录因同时创建而无法创建的情况， 会做出值得注意的例外处理。 此类情况被视为成功。也就是说，从多个线程或进程 并发调用 create_dir_all 保证不会因自身的竞态条件 而失败。 §示例 use tokio::fs; #[tokio::main] async fn main() -> std::io::Result<()> { fs::create_dir_all(\"/some/dir\").await?; Ok(()) }","module_doc":"递归地创建一个目录及其所有父组件（如果它们 缺失）。 这是 std::fs::create_dir_all 的异步版本。 此函数当前对应于 Unix 上的 mkdir 函数， 以及 Windows 上的 CreateDirectory 函数。 请注意，未来可能会发生变化。 此函数将在以下情况下返回错误，但不限于 这些情况： 对于 path 中指定的任何目录因同时创建而无法创建的情况， 会做出值得注意的例外处理。 此类情况被视为成功。也就是说，从多个线程或进程 并发调用 create_dir_all 保证不会因自身的竞态条件 而失败。","section_headers":[]},{"url":"tokio/fs/fn.hard_link.html","crate":"tokio","title":"hard_link","section":"fn","desc":"在文件系统上创建一个新的硬链接。 这是 std::fs::hard_link 的异步版本。 link 路径将是指向 original 路径的链接。 请注意，系统通常要求这两个路径都位于同一文件系统上。 §Platform-specific behavior 此函数当前对应于 Unix 上的 link 函数， 以及 Windows 上的 CreateHardLink 函数。 请注意，未来可能会发生变化。 §错误 此函数将在以下情况下返回错误，但不限于 这些情况： The original path is not a file or doesn’t exist. §示例 use tokio::fs; #[tokio::main] async fn main() -> std::io::Result<()> { fs::hard_link(\"a.txt\", \"b.txt\").await?; // Hard link a.txt to b.txt Ok(()) }","module_doc":"在文件系统上创建一个新的硬链接。 这是 std::fs::hard_link 的异步版本。 link 路径将是指向 original 路径的链接。 请注意，系统通常要求这两个路径都位于同一文件系统上。 此函数当前对应于 Unix 上的 link 函数， 以及 Windows 上的 CreateHardLink 函数。 请注意，未来可能会发生变化。 此函数将在以下情况下返回错误，但不限于 这些情况：","section_headers":[]},{"url":"tokio/fs/fn.metadata.html","crate":"tokio","title":"metadata","section":"fn","desc":"给定路径，查询文件系统以获取有关文件、 目录等的信息。 这是 std::fs::metadata 的异步版本。 此函数会遍历符号链接以查询有关 目标文件的信息。 §Platform-specific behavior 此函数当前对应于 Unix 上的 stat 函数以及 Windows 上的 GetFileAttributesEx 函数。请注意，未来 可能会发生变化。 §错误 此函数将在以下情况下返回错误，但不限于 这些情况： The user lacks permissions to perform metadata call on path. path does not exist. §示例 use tokio::fs; #[tokio::main] async fn main() -> std::io::Result<()> { let attr = fs::metadata(\"/some/file/path.txt\").await?; // inspect attr ... Ok(()) }","module_doc":"给定路径，查询文件系统以获取有关文件、 目录等的信息。 这是 std::fs::metadata 的异步版本。 此函数会遍历符号链接以查询有关 目标文件的信息。 此函数当前对应于 Unix 上的 stat 函数以及 Windows 上的 GetFileAttributesEx 函数。请注意，未来 可能会发生变化。 此函数将在以下情况下返回错误，但不限于 这些情况：","section_headers":[]},{"url":"tokio/fs/fn.read.html","crate":"tokio","title":"read","section":"fn","desc":"将文件的全部内容读取到字节向量中。 这是 std::fs::read 的异步版本。 这是一个便捷函数，用于结合使用 File::open 和 read_to_end， 使用更少的导入且无需中间变量。如果可用，它会 根据文件大小预分配缓冲区，因此通常比 读取到使用 Vec::new() 创建的向量中更快。 此操作通过使用 spawn_blocking 在 单独的线程池上运行等效的阻塞操作来实现。 §错误 如果 path 不存在，此函数将返回错误。 根据 OpenOptions::open，也可能会返回其他错误。 如果在读取时遇到除 ErrorKind::Interrupted 以外的错误，它也会返回错误。 §io_uring support 在 Linux 上，你还可以使用 io_uring 来执行系统调用。要启用 io_uring，需要在编译时指定 --cfg tokio_unstable 标志， 启用 io-uring cargo feature，并设置 Builder::enable_io_uring 运行时选项。 目前对 io_uring 的支持是实验性的，因此其行为可能 发生变化或在未来版本中被移除。 §示例 use tokio::fs; use std::net::SocketAddr; #[tokio::main] async fn main() -> Result<(), Box<dyn std::error::Error + 'static>> { let contents = fs::read(\"address.txt\").await?; let foo: SocketAddr = String::from_utf8_lossy(&contents).parse()?; Ok(()) }","module_doc":"将文件的全部内容读取到字节向量中。 这是 std::fs::read 的异步版本。 这是一个便捷函数，用于结合使用 File::open 和 read_to_end， 使用更少的导入且无需中间变量。如果可用，它会 根据文件大小预分配缓冲区，因此通常比 读取到使用 Vec::new() 创建的向量中更快。 此操作通过使用 spawn_blocking 在 单独的线程池上运行等效的阻塞操作来实现。 如果 path 不存在，此函数将返回错误。 根据 OpenOptions::open，也可能会返回其他错误。 如果在读取时遇到除 ErrorKind::Interrupted 以外的错误，它也会返回错误。 在 Linux 上，你还可以使用 io_uring 来执行系统调用。要启用 io_uring，需要在编译时指定 --cfg tokio_unstable 标志， 启用 io-uring cargo feature，并设置 Builder::enable_io_uring 运行时选项。 目前对 io_uring 的支持是实验性的，因此其行为可能 发生变化或在未来版本中被移除。","section_headers":[]},{"url":"tokio/fs/fn.read_dir.html","crate":"tokio","title":"read_dir","section":"fn","desc":"返回一个目录中条目的流。 这是 std::fs::read_dir 的异步版本。 此操作通过使用 spawn_blocking 在 单独的线程池上运行等效的阻塞操作来实现。","module_doc":"返回一个目录中条目的流。 这是 std::fs::read_dir 的异步版本。 此操作通过使用 spawn_blocking 在 单独的线程池上运行等效的阻塞操作来实现。","section_headers":[]},{"url":"tokio/fs/fn.read_link.html","crate":"tokio","title":"read_link","section":"fn","desc":"读取符号链接，返回链接指向的文件。 这是 std::fs::read_link 的异步版本。","module_doc":"读取符号链接，返回链接指向的文件。 这是 std::fs::read_link 的异步版本。","section_headers":[]},{"url":"tokio/fs/fn.read_to_string.html","crate":"tokio","title":"read_to_string","section":"fn","desc":"创建一个 future，它将打开一个文件进行读取， 并将全部内容读取到一个字符串中，然后返回该字符串。 这是 std::fs::read_to_string 的异步等价方法。 此操作通过使用 spawn_blocking 在 单独的线程池上运行等效的阻塞操作来实现。 §示例 use tokio::fs; let contents = fs::read_to_string(\"foo.txt\").await?; println!(\"foo.txt contains {} bytes\", contents.len());","module_doc":"创建一个 future，它将打开一个文件进行读取， 并将全部内容读取到一个字符串中，然后返回该字符串。 这是 std::fs::read_to_string 的异步等价方法。 此操作通过使用 spawn_blocking 在 单独的线程池上运行等效的阻塞操作来实现。","section_headers":[]},{"url":"tokio/fs/fn.remove_dir.html","crate":"tokio","title":"remove_dir","section":"fn","desc":"删除一个已存在的空目录。 这是 std::fs::remove_dir 的异步版本。","module_doc":"删除一个已存在的空目录。 这是 std::fs::remove_dir 的异步版本。","section_headers":[]},{"url":"tokio/fs/fn.remove_dir_all.html","crate":"tokio","title":"remove_dir_all","section":"fn","desc":"删除该路径的目录，包括其全部内容。 Use carefully! 这是 std::fs::remove_dir_all 的异步版本","module_doc":"删除该路径的目录，包括其全部内容。 Use carefully! 这是 std::fs::remove_dir_all 的异步版本","section_headers":[]},{"url":"tokio/fs/fn.remove_file.html","crate":"tokio","title":"remove_file","section":"fn","desc":"从文件系统删除一个文件。 请注意，无法保证文件会被立即删除（例如， 取决于平台，其他打开的文件描述符可能会阻止立即 删除）。 这是 std::fs::remove_file 的异步版本。","module_doc":"从文件系统删除一个文件。 请注意，无法保证文件会被立即删除（例如， 取决于平台，其他打开的文件描述符可能会阻止立即 删除）。 这是 std::fs::remove_file 的异步版本。","section_headers":[]},{"url":"tokio/fs/fn.rename.html","crate":"tokio","title":"rename","section":"fn","desc":"将文件或目录重命名为新名称，如果 to 已存在则替换原始文件。 若新名称在不同的挂载点上，此操作将失败。 这是 std::fs::rename 的异步版本。","module_doc":"将文件或目录重命名为新名称，如果 to 已存在则替换原始文件。 若新名称在不同的挂载点上，此操作将失败。 这是 std::fs::rename 的异步版本。","section_headers":[]},{"url":"tokio/fs/fn.set_permissions.html","crate":"tokio","title":"set_permissions","section":"fn","desc":"更改文件或目录的权限。 这是 std::fs::set_permissions 的异步版本","module_doc":"更改文件或目录的权限。 这是 std::fs::set_permissions 的异步版本","section_headers":[]},{"url":"tokio/fs/fn.symlink_dir.html","crate":"tokio","title":"symlink_dir","section":"fn","desc":"在文件系统上创建一个新的目录符号链接。 link 路径将是指向 original 路径的 目录符号链接。 这是 std::os::windows::fs::symlink_dir 的异步版本","module_doc":"在文件系统上创建一个新的目录符号链接。 link 路径将是指向 original 路径的 目录符号链接。 这是 std::os::windows::fs::symlink_dir 的异步版本","section_headers":[]},{"url":"tokio/fs/fn.symlink_file.html","crate":"tokio","title":"symlink_file","section":"fn","desc":"在文件系统上创建一个新的文件符号链接。 link 路径将是指向 original 路径的 文件符号链接。 这是 std::os::windows::fs::symlink_file 的异步版本","module_doc":"在文件系统上创建一个新的文件符号链接。 link 路径将是指向 original 路径的 文件符号链接。 这是 std::os::windows::fs::symlink_file 的异步版本","section_headers":[]},{"url":"tokio/fs/fn.symlink_metadata.html","crate":"tokio","title":"symlink_metadata","section":"fn","desc":"查询路径的文件系统元数据。 这是 std::fs::symlink_metadata 的异步版本","module_doc":"查询路径的文件系统元数据。 这是 std::fs::symlink_metadata 的异步版本","section_headers":[]},{"url":"tokio/fs/fn.try_exists.html","crate":"tokio","title":"try_exists","section":"fn","desc":"如果路径指向已存在的实体，则返回 Ok(true)。 此函数会遍历符号链接以查询有关 目标文件的信息。对于损坏的符号链接，将返回 Ok(false)。 这是 std::path::Path::try_exists 的异步等价方法。 §示例 use tokio::fs; fs::try_exists(\"foo.txt\").await?;","module_doc":"如果路径指向已存在的实体，则返回 Ok(true)。 此函数会遍历符号链接以查询有关 目标文件的信息。对于损坏的符号链接，将返回 Ok(false)。 这是 std::path::Path::try_exists 的异步等价方法。","section_headers":[]},{"url":"tokio/fs/fn.write.html","crate":"tokio","title":"write","section":"fn","desc":"创建一个 future，它将打开一个文件用于写入，并将 contents 的全部内容写入其中。 这是 std::fs::write 的异步等价方法。 此操作通过使用 spawn_blocking 在 单独的线程池上运行等效的阻塞操作来实现。 §示例 use tokio::fs; fs::write(\"foo.txt\", b\"Hello world!\").await?;","module_doc":"创建一个 future，它将打开一个文件用于写入，并将 contents 的全部内容写入其中。 这是 std::fs::write 的异步等价方法。 此操作通过使用 spawn_blocking 在 单独的线程池上运行等效的阻塞操作来实现。","section_headers":[]},{"url":"tokio/fs/struct.DirBuilder.html","crate":"tokio","title":"DirBuilder","section":"struct","desc":"用于以各种方式创建目录的构建器。 这是 std::fs::DirBuilder 的特化版本，用于 Tokio 运行时。 创建一组对所有平台使用默认模式/安全设置，且非递归的新选项。 这是 std::fs::DirBuilder::new 的异步版本。 §示例 use tokio::fs::DirBuilder; let builder = DirBuilder::new(); 指示是否递归创建目录（包括所有父目录）。不存在的父目录将使用相同的安全与权限设置创建。 此选项默认为 false。 这是 std::fs::DirBuilder::recursive 的异步版本。 §示例 use tokio::fs::DirBuilder; let mut builder = DirBuilder::new(); builder.recursive(true); 使用配置的选项创建指定目录。 若目录已存在，则视为错误；除非启用了递归模式。 这是 std::fs::DirBuilder::create 的异步版本。 §错误 在以下情况下将返回错误： Path already points to an existing file. Path already points to an existing directory and the mode is non-recursive. The calling process doesn’t have permissions to create the directory or its missing parents. Other I/O error occurred. §示例 use tokio::fs::DirBuilder; use std::io; #[tokio::main] async fn main() -> io::Result<()> { DirBuilder::new() .recursive(true) .create(\"/tmp/foo/bar/baz\") .await?; Ok(()) } 使用给定的格式化器格式化此值。 更多信息 Returns the “default value” for a type. 更多信息 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 原样返回传入的参数。 调用 U::from(self)。 That is, this conversion is whatever the implementation of From<T> for U 的实现方式。 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"用于以各种方式创建目录的构建器。 这是 std::fs::DirBuilder 的特化版本，用于 Tokio 运行时。","section_headers":[]},{"url":"tokio/fs/struct.DirEntry.html","crate":"tokio","title":"DirEntry","section":"struct","desc":"由 ReadDir 流返回的条目。 这是 std::fs::DirEntry 的特化版本，用于 Tokio 运行时。 DirEntry 的实例表示文件系统上某个目录中的条目。 每个条目都可以通过方法检查，以了解完整 路径或可能的其他元数据（通过各平台的扩展 trait）。 返回此条目所代表的文件的完整路径。 完整路径由原始 read_dir 路径与此条目的文件名拼接而成。 §示例 use tokio::fs; let mut entries = fs::read_dir(\".\").await?; while let Some(entry) = entries.next_entry().await? { println!(\"{:?}\", entry.path()); } 输出类似： \"./whatever.txt\" \"./foo.html\" \"./hello_world.rs\" 当然，确切的文本取决于 . 中有哪些文件。 返回此目录条目的裸文件名（不含任何其他前导路径组件）。 §示例 use tokio::fs; let mut entries = fs::read_dir(\".\").await?; while let Some(entry) = entries.next_entry().await? { println!(\"{:?}\", entry.file_name()); } 返回此条目所指向文件的元数据。 若此条目指向符号链接，此函数不会遍历该符号链接。 §Platform-specific behavior 在 Windows 上，此函数调用开销很小（无需额外的系统调用）；但在 Unix 平台上，此函数等同于对路径调用 symlink_metadata。 §示例 use tokio::fs; let mut entries = fs::read_dir(\".\").await?; while let Some(entry) = entries.next_entry().await? { if let Ok(metadata) = entry.metadata().await { // Now let's show our entry's permissions! println!(\"{:?}: {:?}\", entry.path(), metadata.permissions()); } else { println!(\"Couldn't get file type for {:?}\", entry.path()); } } 返回此条目所指向文件的文件类型。 若此条目指向符号链接，此函数不会遍历该符号链接。 §Platform-specific behavior 在 Windows 与大多数 Unix 平台上，此函数是免费的（无需额外的系统调用），但部分 Unix 平台可能需要等效调用 symlink_metadata 来了解目标文件的类型。 §示例 use tokio::fs; let mut entries = fs::read_dir(\".\").await?; while let Some(entry) = entries.next_entry().await? { if let Ok(file_type) = entry.file_type().await { // Now let's show our entry's file type! println!(\"{:?}: {:?}\", entry.path(), file_type); } else { println!(\"Couldn't get file type for {:?}\", entry.path()); } } 使用给定的格式化器格式化此值。 更多信息 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 原样返回传入的参数。 调用 U::from(self)。 That is, this conversion is whatever the implementation of From<T> for U 的实现方式。 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"由 ReadDir 流返回的条目。 这是 std::fs::DirEntry 的特化版本，用于 Tokio 运行时。 DirEntry 的实例表示文件系统上某个目录中的条目。 每个条目都可以通过方法检查，以了解完整 路径或可能的其他元数据（通过各平台的扩展 trait）。","section_headers":[]},{"url":"tokio/fs/struct.File.html","crate":"tokio","title":"File","section":"struct","desc":"对文件系统上打开文件的引用。 这是 std::fs::File 的特化版本，用于 Tokio 运行时。 可以根据打开时使用的选项对 File 实例进行读取 和/或写入。文件还实现了 AsyncSeek，用于改变 文件内部包含的逻辑游标。 如果存在尚未完成的 IO 操作，文件在超出作用域时不会 立即关闭。为确保文件在丢弃时立即关闭， 应在丢弃之前调用 flush。 请注意，这不能确保文件已完全 写入磁盘；操作系统可能会将更改保留在 内存缓冲区中。有关告知操作系统将数据写入磁盘的信息， 请参阅 sync_all 方法。 对 File 的读写通常使用 AsyncReadExt 和 AsyncWriteExt trait 中的 便捷方法来完成。 §示例 创建一个新文件并以异步方式向其写入字节： use tokio::fs::File; use tokio::io::AsyncWriteExt; // for write_all() let mut file = File::create(\"foo.txt\").await?; file.write_all(b\"hello, world!\").await?; 将文件的内容读入缓冲区： use tokio::fs::File; use tokio::io::AsyncReadExt; // for read_to_end() let mut file = File::open(\"foo.txt\").await?; let mut contents = vec![]; file.read_to_end(&mut contents).await?; println!(\"len = {}\", contents.len()); 尝试以只读模式打开文件。 有关更多详细信息，请参阅 OpenOptions。 §错误 如果在 Tokio 运行时 外部调用，或者路径不存在，此函数将返回错误。 根据 OpenOptions::open，也可能会返回其他错误。 §示例 use tokio::fs::File; use tokio::io::AsyncReadExt; let mut file = File::open(\"foo.txt\").await?; let mut contents = vec![]; file.read_to_end(&mut contents).await?; println!(\"len = {}\", contents.len()); read_to_end 方法定义在 AsyncReadExt trait 上。 以只写模式打开文件。 若文件不存在，此函数将创建它；若已存在，则将其截断。 有关更多详细信息，请参阅 OpenOptions。 §错误 如果在 Tokio 运行时外部调用，或者 底层的 create 调用导致错误，则会导致错误。 §示例 use tokio::fs::File; use tokio::io::AsyncWriteExt; let mut file = File::create(\"foo.txt\").await?; file.write_all(b\"hello, world!\").await?; write_all 方法定义在 AsyncWriteExt trait 上。 以读写模式打开文件。 若文件不存在，此函数将创建它，否则返回错误 若文件已存在。 This way, if the call succeeds, the file returned is guaranteed to be new. 此选项很有用，因为它是原子操作。否则， 在检查文件是否存在和创建新文件之间， 文件可能已被其他进程创建（TOCTOU 竞态条件/攻击）。 这也可以用 File::options().read(true).write(true).create_new(true).open(...) 来表达。 有关更多详细信息，请参阅 OpenOptions。 §示例 use tokio::fs::File; use tokio::io::AsyncWriteExt; let mut file = File::create_new(\"foo.txt\").await?; file.write_all(b\"hello, world!\").await?; write_all 方法定义在 AsyncWriteExt trait 上。 返回一个新的 OpenOptions 对象。 此函数返回一个新的 OpenOptions 对象，如果 open() 或 create() 不合适，你可以使用它来 以特定选项打开或创建文件。 它等价于 OpenOptions::new()，但允许你编写更 可读的代码","module_doc":"对文件系统上打开文件的引用。 这是 std::fs::File 的特化版本，用于 Tokio 运行时。 可以根据打开时使用的选项对 File 实例进行读取 和/或写入。文件还实现了 AsyncSeek，用于改变 文件内部包含的逻辑游标。 如果存在尚未完成的 IO 操作，文件在超出作用域时不会 立即关闭。为确保文件在丢弃时立即关闭， 应在丢弃之前调用 flush。 请注意，这不能确保文件已完全 写入磁盘；操作系统可能会将更改保留在 内存缓冲区中。有关告知操作系统将数据写入磁盘的信息， 请参阅 sync_all 方法。 对 File 的读写通常使用 AsyncReadExt 和 AsyncWriteExt trait 中的 便捷方法来完成。 创建一个新文件并以异步方式向其写入字节：","section_headers":[]},{"url":"tokio/fs/struct.OpenOptions.html","crate":"tokio","title":"OpenOptions","section":"struct","desc":"用于配置文件打开方式的可选项与标志。 此构建器提供配置 File 的打开方式以及 允许在打开的文件上执行哪些操作的能力。 File::open 和 File::create 方法使用此构建器作为常用选项的别名。 一般来说，使用 OpenOptions 时，你将首先调用 new， 然后链接方法调用以设置每个选项，然后调用 open， 传入要打开的文件的路径。这将 为你提供一个 io::Result，其中 包含一个 File，你可以对其进行进一步 操作。 这是 std::fs::OpenOptions 的特化版本，用于 Tokio 运行时。 为比此处提供的方法更高级的配置 实现了 From<std::fs::OpenOptions>。 §示例 打开文件以进行读取： use tokio::fs::OpenOptions; use std::io; #[tokio::main] async fn main() -> io::Result<()> { let file = OpenOptions::new() .read(true) .open(\"foo.txt\") .await?; Ok(()) } 打开一个文件以进行读写，并在文件 不存在时创建它： use tokio::fs::OpenOptions; use std::io; #[tokio::main] async fn main() -> io::Result<()> { let file = OpenOptions::new() .read(true) .write(true) .create(true) .open(\"foo.txt\") .await?; Ok(()) } 创建一组待配置的空白选项。 所有选项最初都设置为 false。 这是 std::fs::OpenOptions::new 的异步版本 §示例 use tokio::fs::OpenOptions; let mut options = OpenOptions::new(); let future = options.read(true).open(\"foo.txt\"); 设置读访问选项。 当此选项为 true 时，将 指示文件在打开时应该是可 read（读取）的。 这是 std::fs::OpenOptions::read 的异步版本 §示例 use tokio::fs::OpenOptions; use std::io; #[tokio::main] async fn main() -> io::Result<()> { let file = OpenOptions::new() .read(true) .open(\"foo.txt\") .await?; Ok(()) } 设置写访问选项。 当此选项为 true 时，将 指示文件在打开时应该是可 write（写入）的。 这是 std::fs::OpenOptions::write 的异步版本 §示例 use tokio::fs::OpenOptions; use std::io; #[tokio::main] async fn main() -> io::Result<()> { let file = OpenOptions::new() .write(true) .open(\"foo.txt\") .await?; Ok(()) } 设置追加模式选项。 当此选项为 true 时，表示写入将 追加到文件末尾而不是覆盖 之前的内容。请注意，设置 .write(true).append(true) 与仅设置 .append(true) 的效果相同。 对于大多数文件系统，操作系统保证所有 写入都是原子的：不会因另一个进程同时 写入而损坏写入。 使用追加模式时，也许一个明显的注意事项是： 确保所有属于一起的数据 在一次操作中写入文件。这可以通过在 将字符串传递给 write() 之前连接字符串来实现， 或者使用缓冲写入器（具有足够大小的缓冲区）， 并在消息完成时调用 flush()。 如果文件同时以读取和追加访问权限打开，请注意 在打开之后以及每次写入之后，读取 位置可能会被设置在 文件末尾。因此，在写入之前，请保存当前位置 （使用 seek(SeekFrom::Current(0))）， 并在下次读取之前恢复它。 这是 std::fs::OpenOptions::append 的异步版本 §Note 如果文件不存在，此函数不会创建该文件。 请使用 create 方法来 创建文件。 §示例 use tokio::fs::OpenOptions; use std::io; #[tokio::main] async fn main() -> io::Result<()","module_doc":"用于配置文件打开方式的可选项与标志。 此构建器提供配置 File 的打开方式以及 允许在打开的文件上执行哪些操作的能力。 File::open 和 File::create 方法使用此构建器作为常用选项的别名。 一般来说，使用 OpenOptions 时，你将首先调用 new， 然后链接方法调用以设置每个选项，然后调用 open， 传入要打开的文件的路径。这将 为你提供一个 io::Result，其中 包含一个 File，你可以对其进行进一步 操作。 这是 std::fs::OpenOptions 的特化版本，用于 Tokio 运行时。 为比此处提供的方法更高级的配置 实现了 From<std::fs::OpenOptions>。 打开文件以进行读取：","section_headers":[]},{"url":"tokio/fs/struct.ReadDir.html","crate":"tokio","title":"ReadDir","section":"struct","desc":"读取目录中的条目。 此结构体由本模块的 read_dir 函数返回， 并将产生 DirEntry 实例。 通过 DirEntry，可以了解诸如 条目路径以及可能的元数据等信息。 可以使用 ReadDirStream 将 ReadDir 转换为 Stream。 §错误 如果在迭代过程中出现某种间歇性 IO 错误， 此流将返回 Err。 返回目录流中的下一个条目。 §Cancel safety 此方法可安全取消。 轮询流中的下一个目录条目。 此方法返回： Poll::Pending if the next directory entry is not yet available. Poll::Ready(Ok(Some(entry))) if the next directory entry is available. Poll::Ready(Ok(None)) if there are no more directory entries in this stream. Poll::Ready(Err(err)) if an IO error occurred while reading the next directory entry. 当该方法返回 Poll::Pending 时，提供的 Context 中的 Waker 被安排为在下一个目录条目 在底层 IO 资源上可用时接收唤醒。 请注意，对于多次调用 poll_next_entry， 只有传递给最近一次调用的 Context 中的 Waker 才会 被安排接收唤醒。 使用给定的格式化器格式化此值。 更多信息 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 原样返回传入的参数。 调用 U::from(self)。 That is, this conversion is whatever the implementation of From<T> for U 的实现方式。 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"读取目录中的条目。 此结构体由本模块的 read_dir 函数返回， 并将产生 DirEntry 实例。 通过 DirEntry，可以了解诸如 条目路径以及可能的元数据等信息。 可以使用 ReadDirStream 将 ReadDir 转换为 Stream。 如果在迭代过程中出现某种间歇性 IO 错误， 此流将返回 Err。","section_headers":[]},{"url":"tokio/io/fn.copy.html","crate":"tokio","title":"copy","section":"fn","desc":"异步将一个读取器的全部内容复制到写入器。 此函数返回一个 future，它将以流式方式持续从 reader 读取数据，然后将其写入 writer，直到 reader 返回 EOF 或失败。 成功时返回从 reader 复制到 writer 的总字节数。 这是 std::io::copy 的异步版本。 会创建一个 8 KB 的堆分配复制缓冲区以将数据从读取器 传送到写入器，如果想要 AsyncBufRead 的替代方案， 请查看 copy_buf。你可以将 copy_buf 与 BufReader 一起使用以更改缓冲区容量。 §When to use async alternatives instead of SyncIoBridge 如果你希望将 std::io::copy 与同步消费者 （如 hasher 或压缩器）一起使用，请考虑使用异步替代方案， 而不是使用 SyncIoBridge 包装读取器。 有关详细示例和指南，请参阅 SyncIoBridge 文档。 §错误 如果对 poll_read 或 poll_write 的任何调用返回错误， 则返回的 future 将立即返回错误。 §示例 use tokio::io; let mut reader: &[u8] = b\"hello\"; let mut writer: Vec<u8> = vec![]; io::copy(&mut reader, &mut writer).await?; assert_eq!(&b\"hello\"[..], &writer[..]);","module_doc":"异步将一个读取器的全部内容复制到写入器。 此函数返回一个 future，它将以流式方式持续从 reader 读取数据，然后将其写入 writer，直到 reader 返回 EOF 或失败。 成功时返回从 reader 复制到 writer 的总字节数。 这是 std::io::copy 的异步版本。 会创建一个 8 KB 的堆分配复制缓冲区以将数据从读取器 传送到写入器，如果想要 AsyncBufRead 的替代方案， 请查看 copy_buf。你可以将 copy_buf 与 BufReader 一起使用以更改缓冲区容量。 如果你希望将 std::io::copy 与同步消费者 （如 hasher 或压缩器）一起使用，请考虑使用异步替代方案， 而不是使用 SyncIoBridge 包装读取器。 有关详细示例和指南，请参阅 SyncIoBridge 文档。 如果对 poll_read 或 poll_write 的任何调用返回错误， 则返回的 future 将立即返回错误。","section_headers":[]},{"url":"tokio/io/fn.copy_bidirectional.html","crate":"tokio","title":"copy_bidirectional","section":"fn","desc":"在 a 与 b 之间双向复制数据。 此函数返回一个 future，它将从两个流读取数据， 并将读取的任何数据写入对端流。 这在两个方向上并发进行。 如果在一个流上观察到 EOF，将 在另一个流上调用 shutdown()， 并停止从该流读取。另一个方向上的数据 复制将继续。 当通信的两个方向都已关闭时，future 将 成功完成。当读取器报告 EOF 时， 该方向将被关闭，此时 在相应的写入器上调用 shutdown()。完成后， 它将按顺序返回一个元组，分别是从 a 复制到 b 的字节数和 从 b 复制到 a 的字节数。 默认情况下，它使用两个 8 KB 缓冲区在 a 和 b 之间传输字节。要设置自己的缓冲区大小， 请使用 copy_bidirectional_with_sizes()。 §错误 如果对 a 或 b 的任何 IO 操作返回错误， future 将立即返回错误。在这种情况下， 从任一流读取的部分数据可能会丢失 （未写入对端流）。 §Return value 返回一个元组，分别为从 a 复制到 b 的字节数和从 b 复制到 a 的字节数。","module_doc":"在 a 与 b 之间双向复制数据。 此函数返回一个 future，它将从两个流读取数据， 并将读取的任何数据写入对端流。 这在两个方向上并发进行。 如果在一个流上观察到 EOF，将 在另一个流上调用 shutdown()， 并停止从该流读取。另一个方向上的数据 复制将继续。 当通信的两个方向都已关闭时，future 将 成功完成。当读取器报告 EOF 时， 该方向将被关闭，此时 在相应的写入器上调用 shutdown()。完成后， 它将按顺序返回一个元组，分别是从 a 复制到 b 的字节数和 从 b 复制到 a 的字节数。 默认情况下，它使用两个 8 KB 缓冲区在 a 和 b 之间传输字节。要设置自己的缓冲区大小， 请使用 copy_bidirectional_with_sizes()。 如果对 a 或 b 的任何 IO 操作返回错误， future 将立即返回错误。在这种情况下， 从任一流读取的部分数据可能会丢失 （未写入对端流）。 返回一个元组，分别为从 a 复制到 b 的字节数和从 b 复制到 a 的字节数。","section_headers":[]},{"url":"tokio/io/fn.copy_bidirectional_with_sizes.html","crate":"tokio","title":"copy_bidirectional_with_sizes","section":"fn","desc":"使用指定大小的缓冲区在 a 和 b 之间双向复制数据。 此方法与 copy_bidirectional() 相同，只是它允许你设置 复制数据时使用的内部缓冲区的大小。","module_doc":"使用指定大小的缓冲区在 a 和 b 之间双向复制数据。 此方法与 copy_bidirectional() 相同，只是它允许你设置 复制数据时使用的内部缓冲区的大小。","section_headers":[]},{"url":"tokio/io/fn.copy_buf.html","crate":"tokio","title":"copy_buf","section":"fn","desc":"异步将一个读取器的全部内容复制到写入器。 此函数返回一个 future，它将以流式方式持续从 reader 读取数据，然后将其写入 writer，直到 reader 返回 EOF 或失败。 成功时返回从 reader 复制到 writer 的总字节数。 这是针对 AsyncBufRead 读取器的 tokio::io::copy 替代方案，且无需额外的 缓冲区分配，因为 AsyncBufRead 允许 访问读取器的内部缓冲区。 §When to use async alternatives instead of SyncIoBridge 如果你希望将 std::io::copy 与同步消费者 （如 hasher 或压缩器）一起使用，请考虑使用异步替代方案， 而不是使用 SyncIoBridge 包装读取器。有关哈希、压缩 和数据解析的详细示例和指南，请参阅 SyncIoBridge 文档。 §错误 如果对 poll_fill_buf 或 poll_write 的任何调用返回错误， 则返回的 future 将立即返回错误。 §示例 use tokio::io; let mut reader: &[u8] = b\"hello\"; let mut writer: Vec<u8> = vec![]; io::copy_buf(&mut reader, &mut writer).await?; assert_eq!(b\"hello\", &writer[..]);","module_doc":"异步将一个读取器的全部内容复制到写入器。 此函数返回一个 future，它将以流式方式持续从 reader 读取数据，然后将其写入 writer，直到 reader 返回 EOF 或失败。 成功时返回从 reader 复制到 writer 的总字节数。 这是针对 AsyncBufRead 读取器的 tokio::io::copy 替代方案，且无需额外的 缓冲区分配，因为 AsyncBufRead 允许 访问读取器的内部缓冲区。 如果你希望将 std::io::copy 与同步消费者 （如 hasher 或压缩器）一起使用，请考虑使用异步替代方案， 而不是使用 SyncIoBridge 包装读取器。有关哈希、压缩 和数据解析的详细示例和指南，请参阅 SyncIoBridge 文档。 如果对 poll_fill_buf 或 poll_write 的任何调用返回错误， 则返回的 future 将立即返回错误。","section_headers":[]},{"url":"tokio/io/fn.duplex.html","crate":"tokio","title":"duplex","section":"fn","desc":"创建一个新的 DuplexStream 对，其行为类似于一对已连接的 socket。 max_buf_size 参数是写入返回 Poll::Pending 之前可写入一端的最大字节数。","module_doc":"创建一个新的 DuplexStream 对，其行为类似于一对已连接的 socket。 max_buf_size 参数是写入返回 Poll::Pending 之前可写入一端的最大字节数。","section_headers":[]},{"url":"tokio/io/fn.empty.html","crate":"tokio","title":"empty","section":"fn","desc":"创建一个读取时始终处于 EOF、写入时忽略所有数据的值。 对返回实例的所有写入都将返回 Poll::Ready(Ok(buf.len()))， 并且不会检查缓冲区的内容。 从返回实例的所有读取都将返回 Poll::Ready(Ok(0))。 这是 std::io::empty 的异步版本。 §示例 一个不太实用的\"不向缓冲区读取任何内容\"的示例： use tokio::io::{self, AsyncReadExt}; let mut buffer = String::new(); io::empty().read_to_string(&mut buffer).await.unwrap(); assert!(buffer.is_empty()); 一种获取缓冲区长度的迂回方式： use tokio::io::{self, AsyncWriteExt}; let buffer = vec![1, 2, 3, 5, 8]; let num_bytes = io::empty().write(&buffer).await.unwrap(); assert_eq!(num_bytes, 5);","module_doc":"创建一个读取时始终处于 EOF、写入时忽略所有数据的值。 对返回实例的所有写入都将返回 Poll::Ready(Ok(buf.len()))， 并且不会检查缓冲区的内容。 从返回实例的所有读取都将返回 Poll::Ready(Ok(0))。 这是 std::io::empty 的异步版本。 一个不太实用的\"不向缓冲区读取任何内容\"的示例：","section_headers":[]},{"url":"tokio/io/fn.join.html","crate":"tokio","title":"join","section":"fn","desc":"将实现 AsyncRead 和 AsyncWrite 的两个值合并为 单个句柄。","module_doc":"将实现 AsyncRead 和 AsyncWrite 的两个值合并为 单个句柄。","section_headers":[]},{"url":"tokio/io/fn.repeat.html","crate":"tokio","title":"repeat","section":"fn","desc":"创建一个无限重复单个字节的异步读取器实例。 从此读取器的所有读取都将通过使用 给定字节填充指定的缓冲区来成功。 这是 std::io::repeat 的异步版本。 §示例 use tokio::io::{self, AsyncReadExt}; let mut buffer = [0; 3]; io::repeat(0b101).read_exact(&mut buffer).await.unwrap(); assert_eq!(buffer, [0b101, 0b101, 0b101]);","module_doc":"创建一个无限重复单个字节的异步读取器实例。 从此读取器的所有读取都将通过使用 给定字节填充指定的缓冲区来成功。 这是 std::io::repeat 的异步版本。","section_headers":[]},{"url":"tokio/io/fn.simplex.html","crate":"tokio","title":"simplex","section":"fn","desc":"创建一个单向缓冲区，其行为类似于内存管道。 max_buf_size 参数是在返回 Poll::Pending 之前可写入缓冲区的最大字节数。 §Unify reader and writer 读端和写端可以统一为支持 同时读写的单个 SimplexStream 结构， 或者可以使用 SimplexStream::new_unsplit() 将 SimplexStream 创建为统一的结构。 let (reader, writer) = tokio::io::simplex(64); let mut simplex_stream = reader.unsplit(writer); simplex_stream.write_all(b\"hello\").await?; let mut buf = [0u8; 5]; simplex_stream.read_exact(&mut buf).await?; assert_eq!(&buf, b\"hello\");","module_doc":"创建一个单向缓冲区，其行为类似于内存管道。 max_buf_size 参数是在返回 Poll::Pending 之前可写入缓冲区的最大字节数。 读端和写端可以统一为支持 同时读写的单个 SimplexStream 结构， 或者可以使用 SimplexStream::new_unsplit() 将 SimplexStream 创建为统一的结构。","section_headers":[]},{"url":"tokio/io/fn.sink.html","crate":"tokio","title":"sink","section":"fn","desc":"创建一个异步写入器实例，它将成功消费所有 数据。 对返回实例的所有 poll_write 调用都将返回 Poll::Ready(Ok(buf.len()))，并且 不会检查缓冲区的内容。 这是 std::io::sink 的异步版本。 §示例 use tokio::io::{self, AsyncWriteExt}; let buffer = vec![1, 2, 3, 5, 8]; let num_bytes = io::sink().write(&buffer).await?; assert_eq!(num_bytes, 5); Ok(())","module_doc":"创建一个异步写入器实例，它将成功消费所有 数据。 对返回实例的所有 poll_write 调用都将返回 Poll::Ready(Ok(buf.len()))，并且 不会检查缓冲区的内容。 这是 std::io::sink 的异步版本。","section_headers":[]},{"url":"tokio/io/fn.split.html","crate":"tokio","title":"split","section":"fn","desc":"将实现 AsyncRead + AsyncWrite 的单个值拆分为 独立的 AsyncRead 和 AsyncWrite 句柄。 要从其 ReadHalf 和 WriteHalf 恢复此读/写对象，请使用 unsplit。","module_doc":"将实现 AsyncRead + AsyncWrite 的单个值拆分为 独立的 AsyncRead 和 AsyncWrite 句柄。 要从其 ReadHalf 和 WriteHalf 恢复此读/写对象，请使用 unsplit。","section_headers":[]},{"url":"tokio/io/fn.stderr.html","crate":"tokio","title":"stderr","section":"fn","desc":"构造一个到当前进程标准错误的新句柄。 返回的句柄允许在 Tokio 运行时内写入标准错误。 对 stderr 的并发写入必须谨慎执行：只有对此 AsyncWrite 的单个写入才能保证完整。特别是请注意，使用 write_all 进行 写入时不能保证作为单次写入完成，因此多个线程使用 write_all 写入数据 可能会导致输出交错。 请注意，与 std::io::stderr 不同，对 stderr() 的每次调用都会产生一个新的 writer，例如，此程序不会 刷新 stderr： tokio::io::stderr().write_all(b\"aa\").await?; tokio::io::stderr().flush().await?;§示例 use tokio::io::{self, AsyncWriteExt}; #[tokio::main] async fn main() -> io::Result<()> { let mut stderr = io::stderr(); stderr.write_all(b\"Print some error here.\").await?; Ok(()) }","module_doc":"构造一个到当前进程标准错误的新句柄。 返回的句柄允许在 Tokio 运行时内写入标准错误。 对 stderr 的并发写入必须谨慎执行：只有对此 AsyncWrite 的单个写入才能保证完整。特别是请注意，使用 write_all 进行 写入时不能保证作为单次写入完成，因此多个线程使用 write_all 写入数据 可能会导致输出交错。 请注意，与 std::io::stderr 不同，对 stderr() 的每次调用都会产生一个新的 writer，例如，此程序不会 刷新 stderr：","section_headers":[]},{"url":"tokio/io/fn.stdin.html","crate":"tokio","title":"stdin","section":"fn","desc":"构造一个到当前进程标准输入的新句柄。 此句柄最适合用于非交互式用途，例如当文件 通过管道传入应用程序时。出于技术原因，stdin 通过在单独的线程上使用普通的阻塞读取实现， 并且无法取消该读取。这可能导致运行时 关闭挂起，直到用户按下回车键。 对于交互场景，建议派生一个专用于用户输入的线程，并在该线程中直接使用阻塞 IO。","module_doc":"构造一个到当前进程标准输入的新句柄。 此句柄最适合用于非交互式用途，例如当文件 通过管道传入应用程序时。出于技术原因，stdin 通过在单独的线程上使用普通的阻塞读取实现， 并且无法取消该读取。这可能导致运行时 关闭挂起，直到用户按下回车键。 对于交互场景，建议派生一个专用于用户输入的线程，并在该线程中直接使用阻塞 IO。","section_headers":[]},{"url":"tokio/io/fn.stdout.html","crate":"tokio","title":"stdout","section":"fn","desc":"构造一个到当前进程标准输出的新句柄。 返回的句柄允许在 Tokio 运行时内写入标准输出。 对 stdout 的并发写入必须谨慎执行：只有对此 AsyncWrite 的单个写入才能保证完整。特别是请注意，使用 write_all 进行 写入时不能保证作为单次写入完成，因此多个线程使用 write_all 写入数据 可能会导致输出交错。 请注意，与 std::io::stdout 不同，对 stdout() 的每次调用都会产生一个新的 writer，例如，此程序不会 刷新 stdout： tokio::io::stdout().write_all(b\"aa\").await?; tokio::io::stdout().flush().await?;§示例 use tokio::io::{self, AsyncWriteExt}; #[tokio::main] async fn main() -> io::Result<()> { let mut stdout = io::stdout(); stdout.write_all(b\"Hello world!\").await?; Ok(()) } 以下是使用循环使用 stdio 的示例。 use tokio::io::{self, AsyncWriteExt}; #[tokio::main] async fn main() { let messages = vec![\"hello\", \" world\\n\"]; // When you use `stdio` in a loop, it is recommended to create // a single `stdio` instance outside the loop and call a write // operation against that instance on each loop. // // Repeatedly creating `stdout` instances inside the loop and // writing to that handle could result in mangled output since // each write operation is handled by a different blocking thread. let mut stdout = io::stdout(); for message in &messages { stdout.write_all(message.as_bytes()).await.unwrap(); stdout.flush().await.unwrap(); } }","module_doc":"构造一个到当前进程标准输出的新句柄。 返回的句柄允许在 Tokio 运行时内写入标准输出。 对 stdout 的并发写入必须谨慎执行：只有对此 AsyncWrite 的单个写入才能保证完整。特别是请注意，使用 write_all 进行 写入时不能保证作为单次写入完成，因此多个线程使用 write_all 写入数据 可能会导致输出交错。 请注意，与 std::io::stdout 不同，对 stdout() 的每次调用都会产生一个新的 writer，例如，此程序不会 刷新 stdout：","section_headers":[]},{"url":"tokio/io/struct.BufReader.html","crate":"tokio","title":"BufReader","section":"struct","desc":"BufReader 结构体为任何读取器添加缓冲。 直接使用 AsyncRead 实例 可能效率极低。BufReader 对底层 AsyncRead 执行少量大块读取， 并在内存中维护结果的缓冲区。 BufReader 可以提升对同一文件或网络 socket 进行小型重复读取调用的程序的速度。 它在一次读取大量数据时没有帮助， 或者只读取一两次时也没有帮助。 当从内存中已存在的数据源（如 Vec<u8>） 读取时，它也没有优势。 当 BufReader 被丢弃时，其缓冲区的内容将被 丢弃。在同一 流上创建多个 BufReader 实例可能会导致数据丢失。 使用默认缓冲区容量创建一个新的 BufReader。默认容量目前为 8 KB，未来可能会更改。 使用指定的缓冲区容量创建一个新的 BufReader。 获取底层 reader 的引用。 不建议直接读取底层读取器。 获取底层 reader 的可变引用。 不建议直接读取底层读取器。 获取底层读取器的 pinned 可变引用。 不建议直接读取底层读取器。 消费此 BufReader，返回底层读取器。 请注意，内部缓冲中的所有剩余数据都将丢失。 返回内部缓冲数据的引用。 与 fill_buf 不同，若缓冲为空，此方法不会尝试填充缓冲。 Attempts to return the contents of the internal buffer, filling it with more data from the inner reader if it is empty. 更多信息 Tells this buffer that amt bytes have been consumed from the buffer, so they should no longer be returned in calls to poll_read. 更多信息 Attempts to read from the AsyncRead into buf. 更多信息 在底层读取器中按字节偏移定位。 使用 SeekFrom::Current(_) 进行 seek 所使用的位置 是底层读取器在没有 BufReader 内部缓冲区时所处的位置。 seek 始终会丢弃内部缓冲区，即使 seek 位置 原本落在该缓冲区内。这保证了在 seek 之后立即调用 .into_inner() 会返回处于 相同位置的底层读取器。 有关更多详细信息，请参阅 AsyncSeek。 注意：在你使用 SeekFrom::Current(n) 进行 seek 且 n 减去内部缓冲区长度会溢出 i64 的极端情况下， 将执行两次 seek 而不是一次。如果第二次 seek 返回 Err，则底层读取器将保持在 你使用 SeekFrom::Current(0) 调用 seek 时所处的位置。 Attempts to seek to an offset, in bytes, in a stream. 更多信息 Waits for a seek operation to complete. 更多信息 Attempt to write bytes from buf into the object. 更多信息 Like poll_write, except that it writes from a slice of buffers. 更多信息 Determines if this writer has an efficient poll_write_vectored implementation. 更多信息 Attempts to flush the object, ensuring that any buffered data reach their destination. 更多信息 Initiates or attempts to shut down this writer, returning success when the I/O connection has completely shut down. 更多信息 使用给定的格式化器格式化此值。 更多信息 从输入类型转换为此类型。 Gets the TypeId of self. 更多信息 读取所有字节到 buf，直到遇到分隔字节 byte 或 EOF。 更多信息 读取所有字节直到遇到换行符（0xA 字节），并将它们追加到提供的缓冲。 更多信息 Returns a stream of the contents of this reader split on the byte byte. 更多信息 Returns the contents of the internal buffer, fi","module_doc":"BufReader 结构体为任何读取器添加缓冲。 直接使用 AsyncRead 实例 可能效率极低。BufReader 对底层 AsyncRead 执行少量大块读取， 并在内存中维护结果的缓冲区。 BufReader 可以提升对同一文件或网络 socket 进行小型重复读取调用的程序的速度。 它在一次读取大量数据时没有帮助， 或者只读取一两次时也没有帮助。 当从内存中已存在的数据源（如 Vec<u8>） 读取时，它也没有优势。 当 BufReader 被丢弃时，其缓冲区的内容将被 丢弃。在同一 流上创建多个 BufReader 实例可能会导致数据丢失。","section_headers":[]},{"url":"tokio/io/struct.BufStream.html","crate":"tokio","title":"BufStream","section":"struct","desc":"封装一个同时实现 AsyncWrite 和 AsyncRead 的类型， 并缓冲其输入和输出。 直接使用实现 AsyncWrite 和 AsyncRead 的东西可能效率极低。 例如，每次 write（无论多小）都必须穿越 syscall 接口， 每次读取也是如此。BufWriter 和 BufReader 类型分别有助于解决这些问题，但只在单个方向上起作用。 BufStream 将一个包装在另一个内，从而双向都得到缓冲。 有关细节，请参阅其文档。 使用 BufWriter 和 BufReader 共同封装一个类型。 有关细节，请参阅这些类型以及 BufStream 的文档。 使用指定的 BufReader 容量和 BufWriter 容量创建一个 BufStream。 有关细节，请参阅这些类型以及 BufStream 的文档。 获取底层 I/O 对象的引用。 不建议直接读取底层 I/O 对象。 获取底层 I/O 对象的可变引用。 不建议直接读取底层 I/O 对象。 获取底层 I/O 对象的 pinned 可变引用。 不建议直接读取底层 I/O 对象。 消费此 BufStream，返回底层的 I/O 对象。 请注意，内部缓冲中的所有剩余数据都将丢失。 Attempts to return the contents of the internal buffer, filling it with more data from the inner reader if it is empty. 更多信息 Tells this buffer that amt bytes have been consumed from the buffer, so they should no longer be returned in calls to poll_read. 更多信息 Attempts to read from the AsyncRead into buf. 更多信息 在底层流中按字节偏移定位。 使用 SeekFrom::Current(_) 进行 seek 所使用的位置 是底层流在没有 BufStream 内部缓冲区时所处的位置。 seek 始终会丢弃内部缓冲区，即使 seek 位置 原本落在该缓冲区内。这保证了在 seek 之后立即调用 .into_inner() 会返回处于 相同位置的底层读取器。 有关更多详细信息，请参阅 AsyncSeek。 注意：在你使用 SeekFrom::Current(n) 进行 seek 且 n 减去内部缓冲区长度会溢出 i64 的极端情况下， 将执行两次 seek 而不是一次。如果第二次 seek 返回 Err，则底层读取器将保持在 你使用 SeekFrom::Current(0) 调用 seek 时所处的位置。 Attempts to seek to an offset, in bytes, in a stream. 更多信息 Waits for a seek operation to complete. 更多信息 Attempt to write bytes from buf into the object. 更多信息 Like poll_write, except that it writes from a slice of buffers. 更多信息 Determines if this writer has an efficient poll_write_vectored implementation. 更多信息 Attempts to flush the object, ensuring that any buffered data reach their destination. 更多信息 Initiates or attempts to shut down this writer, returning success when the I/O connection has completely shut down. 更多信息 使用给定的格式化器格式化此值。 更多信息 从输入类型转换为此类型。 从输入类型转换为此类型。 Gets the TypeId of self. 更多信息 读取所有字节到 buf，直到遇到分隔字节 byte 或 EOF。 更多信息 读取所有字节直到遇到换行符（0xA 字节），并将它们追加到提供的缓冲。 更多信息 Returns a stream of the contents of this reader split on the byte byte. 更多信息 Returns the contents of the internal buffer, fillin","module_doc":"封装一个同时实现 AsyncWrite 和 AsyncRead 的类型， 并缓冲其输入和输出。 直接使用实现 AsyncWrite 和 AsyncRead 的东西可能效率极低。 例如，每次 write（无论多小）都必须穿越 syscall 接口， 每次读取也是如此。BufWriter 和 BufReader 类型分别有助于解决这些问题，但只在单个方向上起作用。 BufStream 将一个包装在另一个内，从而双向都得到缓冲。 有关细节，请参阅其文档。","section_headers":[]},{"url":"tokio/io/struct.BufWriter.html","crate":"tokio","title":"BufWriter","section":"struct","desc":"包装一个 writer 并缓冲其输出。 直接使用实现 AsyncWrite 的东西可能效率极低。BufWriter 在内存中维护数据缓冲区， 并以少量大块的形式将数据写入底层 writer。 BufWriter 可以提升对同一文件或网络 socket 进行小型重复写入调用的程序的速度。 它在一次写入大量数据时没有帮助， 或者只写入一两次时也没有帮助。 当写入内存中的目标（如 Vec<u8>） 时，它也没有优势。 当 BufWriter 被丢弃时，其缓冲区的内容将被 丢弃。在同一 流上创建多个 BufWriter 实例可能会导致数据丢失。 如果需要写出其缓冲区的内容，必须在 writer 丢弃之前 手动调用 flush。 使用默认缓冲区容量创建一个新的 BufWriter。默认容量目前为 8 KB，未来可能会更改。 使用指定的缓冲区容量创建一个新的 BufWriter。 获取底层 writer 的引用。 获取底层写入器的可变引用。 不建议直接写入底层写入器。 获取底层写入器的 pinned 可变引用。 不建议直接写入底层写入器。 消费此 BufWriter，返回底层 writer。 请注意，内部缓冲中的所有剩余数据都将丢失。 返回内部缓冲数据的引用。 Attempts to return the contents of the internal buffer, filling it with more data from the inner reader if it is empty. 更多信息 Tells this buffer that amt bytes have been consumed from the buffer, so they should no longer be returned in calls to poll_read. 更多信息 Attempts to read from the AsyncRead into buf. 更多信息 在底层写入器中按字节偏移定位。 定位前总是会先写出内部缓冲。 Attempts to seek to an offset, in bytes, in a stream. 更多信息 Waits for a seek operation to complete. 更多信息 Attempt to write bytes from buf into the object. 更多信息 Like poll_write, except that it writes from a slice of buffers. 更多信息 Determines if this writer has an efficient poll_write_vectored implementation. 更多信息 Attempts to flush the object, ensuring that any buffered data reach their destination. 更多信息 Initiates or attempts to shut down this writer, returning success when the I/O connection has completely shut down. 更多信息 使用给定的格式化器格式化此值。 更多信息 从输入类型转换为此类型。 Gets the TypeId of self. 更多信息 读取所有字节到 buf，直到遇到分隔字节 byte 或 EOF。 更多信息 读取所有字节直到遇到换行符（0xA 字节），并将它们追加到提供的缓冲。 更多信息 Returns a stream of the contents of this reader split on the byte byte. 更多信息 Returns the contents of the internal buffer, filling it with more data from the inner reader if it is empty. 更多信息 Tells this buffer that amt bytes have been consumed from the buffer, so they should no longer be returned in calls to read. 更多信息 返回此读取器各行的流。 This method is the async equivalent to BufRead::lines. 更多信息 创建一个新的 AsyncRead 实例，将此流与 next 链接起来。 更多信息 从此源拉取一些字节到指定的缓冲中，返回读取的字节数。","module_doc":"包装一个 writer 并缓冲其输出。 直接使用实现 AsyncWrite 的东西可能效率极低。BufWriter 在内存中维护数据缓冲区， 并以少量大块的形式将数据写入底层 writer。 BufWriter 可以提升对同一文件或网络 socket 进行小型重复写入调用的程序的速度。 它在一次写入大量数据时没有帮助， 或者只写入一两次时也没有帮助。 当写入内存中的目标（如 Vec<u8>） 时，它也没有优势。 当 BufWriter 被丢弃时，其缓冲区的内容将被 丢弃。在同一 流上创建多个 BufWriter 实例可能会导致数据丢失。 如果需要写出其缓冲区的内容，必须在 writer 丢弃之前 手动调用 flush。","section_headers":[]},{"url":"tokio/io/struct.Chain.html","crate":"tokio","title":"Chain","section":"struct","desc":"用于 chain 方法的流。 获取此 Chain 中底层 reader 的引用。 获取此 Chain 中底层读取器的可变引用。 请注意避免修改底层读取器的内部 I/O 状态，否则可能损坏此 Chain 的内部状态。 获取此 Chain 中底层读取器的 pinned 可变引用。 请注意避免修改底层读取器的内部 I/O 状态，否则可能损坏此 Chain 的内部状态。 消费此 Chain，返回被包装的 reader。 Attempts to return the contents of the internal buffer, filling it with more data from the inner reader if it is empty. 更多信息 Tells this buffer that amt bytes have been consumed from the buffer, so they should no longer be returned in calls to poll_read. 更多信息 Attempts to read from the AsyncRead into buf. 更多信息 使用给定的格式化器格式化此值。 更多信息 Gets the TypeId of self. 更多信息 读取所有字节到 buf，直到遇到分隔字节 byte 或 EOF。 更多信息 读取所有字节直到遇到换行符（0xA 字节），并将它们追加到提供的缓冲。 更多信息 Returns a stream of the contents of this reader split on the byte byte. 更多信息 Returns the contents of the internal buffer, filling it with more data from the inner reader if it is empty. 更多信息 Tells this buffer that amt bytes have been consumed from the buffer, so they should no longer be returned in calls to read. 更多信息 返回此读取器各行的流。 This method is the async equivalent to BufRead::lines. 更多信息 创建一个新的 AsyncRead 实例，将此流与 next 链接起来。 更多信息 从此源拉取一些字节到指定的缓冲中，返回读取的字节数。 更多信息 从此源拉取一些字节到指定的缓冲中，前进缓冲的内部游标。 更多信息 Reads the exact number of bytes required to fill buf. 更多信息 Reads an unsigned 8 bit integer from the underlying reader. 更多信息 Reads a signed 8 bit integer from the underlying reader. 更多信息 Reads an unsigned 16-bit integer in big-endian order from the underlying reader. 更多信息 Reads a signed 16-bit integer in big-endian order from the underlying reader. 更多信息 Reads an unsigned 32-bit integer in big-endian order from the underlying reader. 更多信息 Reads a signed 32-bit integer in big-endian order from the underlying reader. 更多信息 Reads an unsigned 64-bit integer in big-endian order from the underlying reader. 更多信息 Reads an signed 64-bit integer in big-endian order from the underlying reader. 更多信息 Reads an unsigned 128-bit integer in big-endian order from the underlying reader. 更多信息 Reads an signed 128-bit integer in big-endian order from the underlying r","module_doc":"用于 chain 方法的流。","section_headers":[]},{"url":"tokio/io/struct.DuplexStream.html","crate":"tokio","title":"DuplexStream","section":"struct","desc":"用于在内存中读写字节的双向管道。 一对 DuplexStream 同时创建，它们充当â信道â， 可用作内存中的 IO 类型。写入对中的一端 将允许从另一端读取该数据，反之亦然。 §Closing a DuplexStream 如果 DuplexStream 信道的一端被丢弃， 另一端的任何挂起读取将继续读取数据直到缓冲区被排空， 然后它们将通过返回 0 字节来表示 EOF。 对另一端的任何写入，包括 挂起的写入（正在等待缓冲区中的空闲空间）， 将立即返回 Err(BrokenPipe)。 §Example let (mut client, mut server) = tokio::io::duplex(64); client.write_all(b\"ping\").await?; let mut buf = [0u8; 4]; server.read_exact(&mut buf).await?; assert_eq!(&buf, b\"ping\"); server.write_all(b\"pong\").await?; client.read_exact(&mut buf).await?; assert_eq!(&buf, b\"pong\"); Attempts to read from the AsyncRead into buf. 更多信息 Attempt to write bytes from buf into the object. 更多信息 Like poll_write, except that it writes from a slice of buffers. 更多信息 Determines if this writer has an efficient poll_write_vectored implementation. 更多信息 Attempts to flush the object, ensuring that any buffered data reach their destination. 更多信息 Initiates or attempts to shut down this writer, returning success when the I/O connection has completely shut down. 更多信息 使用给定的格式化器格式化此值。 更多信息 执行此类型的析构函数。 更多信息 Gets the TypeId of self. 更多信息 创建一个新的 AsyncRead 实例，将此流与 next 链接起来。 更多信息 从此源拉取一些字节到指定的缓冲中，返回读取的字节数。 更多信息 从此源拉取一些字节到指定的缓冲中，前进缓冲的内部游标。 更多信息 Reads the exact number of bytes required to fill buf. 更多信息 Reads an unsigned 8 bit integer from the underlying reader. 更多信息 Reads a signed 8 bit integer from the underlying reader. 更多信息 Reads an unsigned 16-bit integer in big-endian order from the underlying reader. 更多信息 Reads a signed 16-bit integer in big-endian order from the underlying reader. 更多信息 Reads an unsigned 32-bit integer in big-endian order from the underlying reader. 更多信息 Reads a signed 32-bit integer in big-endian order from the underlying reader. 更多信息 Reads an unsigned 64-bit integer in big-endian order from the underlying reader. 更多信息 Reads an signed 64-bit integer in big-endian order from the underlying reader. 更多信息 Reads an unsigned 128-bit integer in big-endian order from the underlying reader. 更多信息 Reads an signed 128-bit integer in big-end","module_doc":"用于在内存中读写字节的双向管道。 一对 DuplexStream 同时创建，它们充当â信道â， 可用作内存中的 IO 类型。写入对中的一端 将允许从另一端读取该数据，反之亦然。 如果 DuplexStream 信道的一端被丢弃， 另一端的任何挂起读取将继续读取数据直到缓冲区被排空， 然后它们将通过返回 0 字节来表示 EOF。 对另一端的任何写入，包括 挂起的写入（正在等待缓冲区中的空闲空间）， 将立即返回 Err(BrokenPipe)。","section_headers":[]},{"url":"tokio/io/struct.Empty.html","crate":"tokio","title":"Empty","section":"struct","desc":"Empty 忽略通过 AsyncWrite 写入的任何数据， 并且在通过 AsyncRead 读取时始终为空 （返回零字节）。 此结构体通常通过调用 empty 创建。有关更多详细信息， 请参阅 empty() 的文档。 这是 std::io::empty 的异步版本。 Attempts to return the contents of the internal buffer, filling it with more data from the inner reader if it is empty. 更多信息 Tells this buffer that amt bytes have been consumed from the buffer, so they should no longer be returned in calls to poll_read. 更多信息 Attempts to read from the AsyncRead into buf. 更多信息 Attempts to seek to an offset, in bytes, in a stream. 更多信息 Waits for a seek operation to complete. 更多信息 Attempt to write bytes from buf into the object. 更多信息 Attempts to flush the object, ensuring that any buffered data reach their destination. 更多信息 Initiates or attempts to shut down this writer, returning success when the I/O connection has completely shut down. 更多信息 Determines if this writer has an efficient poll_write_vectored implementation. 更多信息 Like poll_write, except that it writes from a slice of buffers. 更多信息 使用给定的格式化器格式化此值。 更多信息 Gets the TypeId of self. 更多信息 读取所有字节到 buf，直到遇到分隔字节 byte 或 EOF。 更多信息 读取所有字节直到遇到换行符（0xA 字节），并将它们追加到提供的缓冲。 更多信息 Returns a stream of the contents of this reader split on the byte byte. 更多信息 Returns the contents of the internal buffer, filling it with more data from the inner reader if it is empty. 更多信息 Tells this buffer that amt bytes have been consumed from the buffer, so they should no longer be returned in calls to read. 更多信息 返回此读取器各行的流。 This method is the async equivalent to BufRead::lines. 更多信息 创建一个新的 AsyncRead 实例，将此流与 next 链接起来。 更多信息 从此源拉取一些字节到指定的缓冲中，返回读取的字节数。 更多信息 从此源拉取一些字节到指定的缓冲中，前进缓冲的内部游标。 更多信息 Reads the exact number of bytes required to fill buf. 更多信息 Reads an unsigned 8 bit integer from the underlying reader. 更多信息 Reads a signed 8 bit integer from the underlying reader. 更多信息 Reads an unsigned 16-bit integer in big-endian order from the underlying reader. 更多信息 Reads a signed 16-bit integer in big-endian order from the underlying reader. 更多信息 Reads an unsigned 32-bit integer in big-endian ","module_doc":"Empty 忽略通过 AsyncWrite 写入的任何数据， 并且在通过 AsyncRead 读取时始终为空 （返回零字节）。 此结构体通常通过调用 empty 创建。有关更多详细信息， 请参阅 empty() 的文档。 这是 std::io::empty 的异步版本。","section_headers":[]},{"url":"tokio/io/struct.Interest.html","crate":"tokio","title":"Interest","section":"struct","desc":"就绪事件兴趣。 指定调用者在等待 I/O 资源就绪状态时所感兴趣的就绪事件。 对所有可读事件感兴趣。 可读兴趣包括读取关闭事件。 对所有可写事件感兴趣。 可写兴趣包括写入关闭事件。 对错误事件感兴趣。 将错误兴趣传递给底层 OS 选择器。行为是平台特定的，请阅读你所在平台的文档。 如果该值包括可读兴趣，则返回 true。 §示例 use tokio::io::Interest; assert!(Interest::READABLE.is_readable()); assert!(!Interest::WRITABLE.is_readable()); let both = Interest::READABLE | Interest::WRITABLE; assert!(both.is_readable()); 如果该值包括可写兴趣，则返回 true。 §示例 use tokio::io::Interest; assert!(!Interest::READABLE.is_writable()); assert!(Interest::WRITABLE.is_writable()); let both = Interest::READABLE | Interest::WRITABLE; assert!(both.is_writable()); 如果该值包括错误兴趣，则返回 true。 §示例 use tokio::io::Interest; assert!(Interest::ERROR.is_error()); assert!(!Interest::WRITABLE.is_error()); let combined = Interest::READABLE | Interest::ERROR; assert!(combined.is_error()); 将两个 Interest 值相加。 此函数可在 const 上下文中工作。 §示例 use tokio::io::Interest; const BOTH: Interest = Interest::READABLE.add(Interest::WRITABLE); assert!(BOTH.is_readable()); assert!(BOTH.is_writable()); 从 self 中移除 Interest。 出现在 other 中但不在 self 中的兴趣将被忽略。 如果在移除 Interest 后集合为空，则返回 None。 §示例 use tokio::io::Interest; const RW_INTEREST: Interest = Interest::READABLE.add(Interest::WRITABLE); let w_interest = RW_INTEREST.remove(Interest::READABLE).unwrap(); assert!(!w_interest.is_readable()); assert!(w_interest.is_writable()); // 从集合中移除所有关注会返回 `None`。 assert_eq!(w_interest.remove(Interest::WRITABLE), None); // 一次性移除所有关注。 assert_eq!(RW_INTEREST.remove(RW_INTEREST), None); 应用 | 运算符后得到的类型。 Performs the | operation. 更多信息 Performs the |= operation. 更多信息 返回值的副本。 更多信息 Performs copy-assignment from source. 更多信息 使用给定的格式化器格式化此值。 更多信息 测试 self 与 other 值是否相等，供 == 运算符使用。 测试 != 运算符。默认实现几乎总是够用，除非有非常充分的理由，否则不应被覆盖。 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 Performs copy-assignment from self to dest. 更多信息 原样返回参数。 调用 U::from(self)。 也就是说，此转换是 From<T> for U 实现选择执行的操作。 获得所有权后的类型。 Creates owned data from borrowed data, usually by cloning. 更多信息 Uses borrowed data ","module_doc":"就绪事件兴趣。 指定调用者在等待 I/O 资源就绪状态时所感兴趣的就绪事件。","section_headers":[]},{"url":"tokio/io/struct.Join.html","crate":"tokio","title":"Join","section":"struct","desc":"将实现 AsyncRead 和 AsyncWrite 的两个值合并为 单个句柄。 将此 Join 拆分为其 AsyncRead 和 AsyncWrite 组件。 返回内部 reader 的引用。 返回内部 writer 的引用。 返回内部读取器的可变引用。 返回内部 writer 的可变引用。 返回内部读取器的 pinned 可变引用。 返回内部 writer 的固定可变引用。 Attempts to return the contents of the internal buffer, filling it with more data from the inner reader if it is empty. 更多信息 Tells this buffer that amt bytes have been consumed from the buffer, so they should no longer be returned in calls to poll_read. 更多信息 Attempts to read from the AsyncRead into buf. 更多信息 Attempt to write bytes from buf into the object. 更多信息 Attempts to flush the object, ensuring that any buffered data reach their destination. 更多信息 Initiates or attempts to shut down this writer, returning success when the I/O connection has completely shut down. 更多信息 Like poll_write, except that it writes from a slice of buffers. 更多信息 Determines if this writer has an efficient poll_write_vectored implementation. 更多信息 使用给定的格式化器格式化此值。 更多信息 Gets the TypeId of self. 更多信息 读取所有字节到 buf，直到遇到分隔字节 byte 或 EOF。 更多信息 读取所有字节直到遇到换行符（0xA 字节），并将它们追加到提供的缓冲。 更多信息 Returns a stream of the contents of this reader split on the byte byte. 更多信息 Returns the contents of the internal buffer, filling it with more data from the inner reader if it is empty. 更多信息 Tells this buffer that amt bytes have been consumed from the buffer, so they should no longer be returned in calls to read. 更多信息 返回此读取器各行的流。 This method is the async equivalent to BufRead::lines. 更多信息 创建一个新的 AsyncRead 实例，将此流与 next 链接起来。 更多信息 从此源拉取一些字节到指定的缓冲中，返回读取的字节数。 更多信息 从此源拉取一些字节到指定的缓冲中，前进缓冲的内部游标。 更多信息 Reads the exact number of bytes required to fill buf. 更多信息 Reads an unsigned 8 bit integer from the underlying reader. 更多信息 Reads a signed 8 bit integer from the underlying reader. 更多信息 Reads an unsigned 16-bit integer in big-endian order from the underlying reader. 更多信息 Reads a signed 16-bit integer in big-endian order from the underlying reader. 更多信息 Reads an unsigned 32-bit integer in big-endian order from the underlying reader. 更多信息 Reads a si","module_doc":"将实现 AsyncRead 和 AsyncWrite 的两个值合并为 单个句柄。","section_headers":[]},{"url":"tokio/io/struct.Lines.html","crate":"tokio","title":"Lines","section":"struct","desc":"从 AsyncBufRead 读取行。 可以使用 LinesStream 将 Lines 转换为 Stream。 此类型通常使用 lines 方法创建。 返回流中的下一行。 §Cancel safety 此方法可安全取消。 §示例 use tokio::io::AsyncBufReadExt; let mut lines = my_buf_read.lines(); while let Some(line) = lines.next_line().await? { println!(\"length = {}\", line.len()) } 获取底层读取器的可变引用。 获取底层读取器的引用。 解包此 Lines<R>，返回底层读取器。 请注意，内部缓冲中的所有剩余数据都将丢失。 Therefore, a following read from the underlying reader may lead to data loss. 轮询流中的下一行。 此方法返回： Poll::Pending if the next line is not yet available. Poll::Ready(Ok(Some(line))) if the next line is available. Poll::Ready(Ok(None)) if there are no more lines in this stream. Poll::Ready(Err(err)) if an IO error occurred while reading the next line. 当该方法返回 Poll::Pending 时，提供的 Context 中的 Waker 被安排为在底层 IO 资源上有更多 字节可用时接收唤醒。请注意，对于多次调用 poll_next_line，只有传递给最近一次调用的 Context 中的 Waker 才会 被安排接收唤醒。 使用给定的格式化器格式化此值。 更多信息 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 原样返回传入的参数。 调用 U::from(self)。 That is, this conversion is whatever the implementation of From<T> for U 的实现方式。 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"从 AsyncBufRead 读取行。 可以使用 LinesStream 将 Lines 转换为 Stream。 此类型通常使用 lines 方法创建。","section_headers":[]},{"url":"tokio/io/struct.ReadBuf.html","crate":"tokio","title":"ReadBuf","section":"struct","desc":"一个字节缓冲区的包装器，可增量填充和初始化。 此类型是一种“双游标”。它跟踪缓冲区中的三个区域：缓冲区开头已逻辑填充数据的区域、已在某个时刻初始化但尚未逻辑填充的区域，以及末尾可能未初始化的区域。填充区域保证是初始化区域的子集。 总之，缓冲区的内容可以可视化为： [ capacity ] [ filled | unfilled ] [ initialized | uninitialized ] 从未初始化区域取消初始化任何字节属于未定义行为，因为这仅仅是未知此区域是否未初始化，并且如果其一部分实际上是已初始化的，则它必须保持已初始化。 从完全初始化的缓冲区创建一个新的 ReadBuf。 从可能未初始化的缓冲区创建一个新的 ReadBuf。 内部游标会将整个缓冲区标记为未初始化。如果已知缓冲区已部分初始化，则使用 assume_init 移动内部游标。 返回缓冲区的总容量。 返回对缓冲区已填充部分的共享引用。 返回对缓冲区已填充部分的可变引用。 返回一个新的 ReadBuf，由最多 n 的未填充部分组成。 返回对缓冲区已初始化部分的共享引用。 这包括已填充的部分。 返回对缓冲区已初始化部分的可变引用。 这包括已填充的部分。 返回对整个缓冲区的可变引用，而不确保它已完全初始化。 0 到 self.filled().len() 之间的元素是已填充的，而 0 到 self.initialized().len() 之间的元素是已初始化的（因此可以转换为 &mut [u8]）。 此方法的调用者必须确保这些不变量得到遵守。例如，如果调用者初始化了缓冲区未初始化部分的某些内容，则必须使用已初始化的字节数调用 assume_init。 §安全性 调用者不得反初始化缓冲区中已经初始化的部分。这包括 ReadBuf 标记为未初始化的区域中的任何字节。 返回对缓冲区未填充部分的可变引用，而不确保它已完全初始化。 §安全性 调用者不得反初始化缓冲区中已经初始化的部分。这包括 ReadBuf 标记为未初始化的区域中的任何字节。 返回对缓冲区未填充部分的可变引用，确保它已完全初始化。 由于 ReadBuf 跟踪缓冲区中已初始化的区域，因此在首次使用之后，这实际上是“免费”的。 返回对缓冲区未填充部分前 n 个字节的可变引用，确保它已完全初始化。 §恐慌 如果 self.remaining() 小于 n，则会发生 panic。 返回切片末尾尚未填充的字节数。 清空缓冲区，将填充区域重置为空。 已初始化的字节数不变，缓冲区的内容也不会被修改。 推进缓冲区填充区域的大小。 已初始化的字节数不会改变。 §恐慌 如果缓冲区的填充区域变得大于初始化区域，则会发生 panic。 设置缓冲区填充区域的大小。 已初始化的字节数不会改变。 请注意，除了可以扩大缓冲区填充区域之外，这还可用于缩小它（例如，由就地压缩数据的 AsyncRead 实现使用）。 §恐慌 如果缓冲区的填充区域变得大于初始化区域，则会发生 panic。 断言缓冲区的前 n 个未填充字节已初始化。 ReadBuf 假定字节永远不会被取消初始化，因此当使用少于已知已初始化的字节数调用此方法时，它不会执行任何操作。 §安全性 调用者必须确保缓冲区的 n 个未填充字节已初始化。 将数据追加到缓冲区，推进写入位置，可能也会推进初始化位置。 §恐慌 如果 self.remaining() 小于 buf.len()，则会发生 panic。 使用给定的格式化器格式化此值。 更多信息 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 原样返回参数。 调用 U::from(self)。 也就是说，此转换是 From<T> for U 实现选择执行的操作。 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"一个字节缓冲区的包装器，可增量填充和初始化。 此类型是一种“双游标”。它跟踪缓冲区中的三个区域：缓冲区开头已逻辑填充数据的区域、已在某个时刻初始化但尚未逻辑填充的区域，以及末尾可能未初始化的区域。填充区域保证是初始化区域的子集。 总之，缓冲区的内容可以可视化为：","section_headers":[]},{"url":"tokio/io/struct.ReadHalf.html","crate":"tokio","title":"ReadHalf","section":"struct","desc":"由 split 返回的值的可读半部分。 检查此 ReadHalf 与某个 WriteHalf 是否从同一个流拆分而来。 与之前拆分的 WriteHalf 重新合并。 §Panics 若此 ReadHalf 与给定的 WriteHalf 不是来自同一次 split 操作，此方法将 panic。 可通过提前调用 is_pair_of() 进行检查。 Attempts to read from the AsyncRead into buf. 更多信息 使用给定的格式化器格式化此值。 更多信息 Gets the TypeId of self. 更多信息 创建一个新的 AsyncRead 实例，将此流与 next 链接起来。 更多信息 从此源拉取一些字节到指定的缓冲中，返回读取的字节数。 更多信息 从此源拉取一些字节到指定的缓冲中，前进缓冲的内部游标。 更多信息 Reads the exact number of bytes required to fill buf. 更多信息 Reads an unsigned 8 bit integer from the underlying reader. 更多信息 Reads a signed 8 bit integer from the underlying reader. 更多信息 Reads an unsigned 16-bit integer in big-endian order from the underlying reader. 更多信息 Reads a signed 16-bit integer in big-endian order from the underlying reader. 更多信息 Reads an unsigned 32-bit integer in big-endian order from the underlying reader. 更多信息 Reads a signed 32-bit integer in big-endian order from the underlying reader. 更多信息 Reads an unsigned 64-bit integer in big-endian order from the underlying reader. 更多信息 Reads an signed 64-bit integer in big-endian order from the underlying reader. 更多信息 Reads an unsigned 128-bit integer in big-endian order from the underlying reader. 更多信息 Reads an signed 128-bit integer in big-endian order from the underlying reader. 更多信息 Reads an 32-bit floating point type in big-endian order from the underlying reader. 更多信息 Reads an 64-bit floating point type in big-endian order from the underlying reader. 更多信息 Reads an unsigned 16-bit integer in little-endian order from the underlying reader. 更多信息 Reads a signed 16-bit integer in little-endian order from the underlying reader. 更多信息 Reads an unsigned 32-bit integer in little-endian order from the underlying reader. 更多信息 Reads a signed 32-bit integer in little-endian order from the underlying reader. 更多信息 Reads an unsigned 64-bit integer in little-endian order from the underlying reader. 更多信息 Reads an signed 64-bit integer in little-endian order from the underlying reader. 更多信息 Reads an unsigned 128-bit integer in little-endi","module_doc":"由 split 返回的值的可读半部分。","section_headers":[]},{"url":"tokio/io/struct.Ready.html","crate":"tokio","title":"Ready","section":"struct","desc":"描述 I/O 资源的就绪状态。 Ready 跟踪 I/O 资源准备好执行的操作。 返回空的 Ready 集合。 返回表示可读就绪状态的 Ready。 返回表示可写就绪状态的 Ready。 返回表示读取关闭就绪状态的 Ready。 返回表示写入关闭就绪状态的 Ready。 返回表示错误就绪状态的 Ready。 返回表示所有操作就绪状态的 Ready。 如果 Ready 是空集合，则返回 true。 §示例 use tokio::io::Ready; assert!(Ready::EMPTY.is_empty()); assert!(!Ready::READABLE.is_empty()); 如果该值包括 readable（可读），则返回 true。 §示例 use tokio::io::Ready; assert!(!Ready::EMPTY.is_readable()); assert!(Ready::READABLE.is_readable()); assert!(Ready::READ_CLOSED.is_readable()); assert!(!Ready::WRITABLE.is_readable()); 如果该值包括可写 readiness（就绪），则返回 true。 §示例 use tokio::io::Ready; assert!(!Ready::EMPTY.is_writable()); assert!(!Ready::READABLE.is_writable()); assert!(Ready::WRITABLE.is_writable()); assert!(Ready::WRITE_CLOSED.is_writable()); 如果该值包括读取关闭 readiness（就绪），则返回 true。 §示例 use tokio::io::Ready; assert!(!Ready::EMPTY.is_read_closed()); assert!(!Ready::READABLE.is_read_closed()); assert!(Ready::READ_CLOSED.is_read_closed()); 如果该值包括写入关闭 readiness（就绪），则返回 true。 §示例 use tokio::io::Ready; assert!(!Ready::EMPTY.is_write_closed()); assert!(!Ready::WRITABLE.is_write_closed()); assert!(Ready::WRITE_CLOSED.is_write_closed()); 如果该值包括错误 readiness（就绪），则返回 true。 §示例 use tokio::io::Ready; assert!(!Ready::EMPTY.is_error()); assert!(!Ready::WRITABLE.is_error()); assert!(Ready::ERROR.is_error()); 应用 & 运算符后得到的类型。 Performs the & operation. 更多信息 应用 | 运算符后得到的类型。 Performs the | operation. 更多信息 Performs the |= operation. 更多信息 返回值的副本。 更多信息 Performs copy-assignment from source. 更多信息 使用给定的格式化器格式化此值。 更多信息 This method returns an Ordering between self and other. 更多信息 Compares and returns the maximum of two values. 更多信息 Compares and returns the minimum of two values. 更多信息 Restrict a value to a certain interval. 更多信息 测试 self 与 other 值是否相等，供 == 运算符使用。 测试 != 运算符。默认实现几乎总是够用，除非有非常充分的理由，否则不应被覆盖。 若存在，此方法返回 self 与 other 值之间的排序关系。 更多信息 测试小于（针对 self 与 other），供 < 运算符使用。 更多信息 Tests less than or equal to (for self and other) and is used by the <= operator. 更多信息 Tests greater than (for self and other) and is used by t","module_doc":"描述 I/O 资源的就绪状态。 Ready 跟踪 I/O 资源准备好执行的操作。","section_headers":[]},{"url":"tokio/io/struct.Repeat.html","crate":"tokio","title":"Repeat","section":"struct","desc":"一个反复产生一个字节又又一个字节 又又一个字节的异步读取器â¦ 此结构体通常通过调用 repeat 创建。 有关更多详细信息，请参阅 repeat() 的文档。 这是 std::io::Repeat 的异步版本。 Attempts to read from the AsyncRead into buf. 更多信息 使用给定的格式化器格式化此值。 更多信息 Gets the TypeId of self. 更多信息 创建一个新的 AsyncRead 实例，将此流与 next 链接起来。 更多信息 从此源拉取一些字节到指定的缓冲中，返回读取的字节数。 更多信息 从此源拉取一些字节到指定的缓冲中，前进缓冲的内部游标。 更多信息 Reads the exact number of bytes required to fill buf. 更多信息 Reads an unsigned 8 bit integer from the underlying reader. 更多信息 Reads a signed 8 bit integer from the underlying reader. 更多信息 Reads an unsigned 16-bit integer in big-endian order from the underlying reader. 更多信息 Reads a signed 16-bit integer in big-endian order from the underlying reader. 更多信息 Reads an unsigned 32-bit integer in big-endian order from the underlying reader. 更多信息 Reads a signed 32-bit integer in big-endian order from the underlying reader. 更多信息 Reads an unsigned 64-bit integer in big-endian order from the underlying reader. 更多信息 Reads an signed 64-bit integer in big-endian order from the underlying reader. 更多信息 Reads an unsigned 128-bit integer in big-endian order from the underlying reader. 更多信息 Reads an signed 128-bit integer in big-endian order from the underlying reader. 更多信息 Reads an 32-bit floating point type in big-endian order from the underlying reader. 更多信息 Reads an 64-bit floating point type in big-endian order from the underlying reader. 更多信息 Reads an unsigned 16-bit integer in little-endian order from the underlying reader. 更多信息 Reads a signed 16-bit integer in little-endian order from the underlying reader. 更多信息 Reads an unsigned 32-bit integer in little-endian order from the underlying reader. 更多信息 Reads a signed 32-bit integer in little-endian order from the underlying reader. 更多信息 Reads an unsigned 64-bit integer in little-endian order from the underlying reader. 更多信息 Reads an signed 64-bit integer in little-endian order from the underlying reader. 更多信息 Reads an unsigned 128-bit integer in little-endian order from the underlying reader. 更多信息 Reads an signed 128-bit ","module_doc":"一个反复产生一个字节又又一个字节 又又一个字节的异步读取器â¦ 此结构体通常通过调用 repeat 创建。 有关更多详细信息，请参阅 repeat() 的文档。 这是 std::io::Repeat 的异步版本。","section_headers":[]},{"url":"tokio/io/struct.SimplexStream.html","crate":"tokio","title":"SimplexStream","section":"struct","desc":"一个单向管道，用于在内存中读写字节。 它可以通过 simplex 函数构造（该函数会创建一对读端和写端）， 也可以通过调用 SimplexStream::new_unsplit 来构造， 后者会同时创建用于读和写的句柄。 §Example let (mut receiver, mut sender) = tokio::io::simplex(64); sender.write_all(b\"ping\").await?; let mut buf = [0u8; 4]; receiver.read_exact(&mut buf).await?; assert_eq!(&buf, b\"ping\"); 创建一个像内存管道一样的单向缓冲。要创建读写分离的拆分版本， version with separate reader and writer you can use simplex function. max_buf_size 参数指定在返回 Poll::Pending 之前可写入缓冲区的最大字节数。 Attempts to read from the AsyncRead into buf. 更多信息 Attempt to write bytes from buf into the object. 更多信息 Like poll_write, except that it writes from a slice of buffers. 更多信息 Determines if this writer has an efficient poll_write_vectored implementation. 更多信息 Attempts to flush the object, ensuring that any buffered data reach their destination. 更多信息 Initiates or attempts to shut down this writer, returning success when the I/O connection has completely shut down. 更多信息 使用给定的格式化器格式化此值。 更多信息 Gets the TypeId of self. 更多信息 创建一个新的 AsyncRead 实例，将此流与 next 链接起来。 更多信息 从此源拉取一些字节到指定的缓冲中，返回读取的字节数。 更多信息 从此源拉取一些字节到指定的缓冲中，前进缓冲的内部游标。 更多信息 Reads the exact number of bytes required to fill buf. 更多信息 Reads an unsigned 8 bit integer from the underlying reader. 更多信息 Reads a signed 8 bit integer from the underlying reader. 更多信息 Reads an unsigned 16-bit integer in big-endian order from the underlying reader. 更多信息 Reads a signed 16-bit integer in big-endian order from the underlying reader. 更多信息 Reads an unsigned 32-bit integer in big-endian order from the underlying reader. 更多信息 Reads a signed 32-bit integer in big-endian order from the underlying reader. 更多信息 Reads an unsigned 64-bit integer in big-endian order from the underlying reader. 更多信息 Reads an signed 64-bit integer in big-endian order from the underlying reader. 更多信息 Reads an unsigned 128-bit integer in big-endian order from the underlying reader. 更多信息 Reads an signed 128-bit integer in big-endian order from the underlying reader. 更多信息 Reads an 32-bit floating point type in big-endian orde","module_doc":"一个单向管道，用于在内存中读写字节。 它可以通过 simplex 函数构造（该函数会创建一对读端和写端）， 也可以通过调用 SimplexStream::new_unsplit 来构造， 后者会同时创建用于读和写的句柄。","section_headers":[]},{"url":"tokio/io/struct.Sink.html","crate":"tokio","title":"Sink","section":"struct","desc":"一个将数据移入虚无的异步 writer。 此结构体通常通过调用 sink 创建。 有关更多详细信息，请参阅 sink() 的文档。 这是 std::io::Sink 的异步版本。 Attempt to write bytes from buf into the object. 更多信息 Attempts to flush the object, ensuring that any buffered data reach their destination. 更多信息 Initiates or attempts to shut down this writer, returning success when the I/O connection has completely shut down. 更多信息 Like poll_write, except that it writes from a slice of buffers. 更多信息 Determines if this writer has an efficient poll_write_vectored implementation. 更多信息 使用给定的格式化器格式化此值。 更多信息 Gets the TypeId of self. 更多信息 将一个缓冲写入此写入器，返回写入的字节数。 更多信息 Like write, except that it writes from a slice of buffers. 更多信息 将一个缓冲写入此写入器，前进缓冲的内部游标。 更多信息 尝试将整个缓冲写入此写入器。 更多信息 尝试将整个缓冲写入此写入器。 更多信息 Writes an unsigned 8-bit integer to the underlying writer. 更多信息 Writes a signed 8-bit integer to the underlying writer. 更多信息 Writes an unsigned 16-bit integer in big-endian order to the underlying writer. 更多信息 Writes a signed 16-bit integer in big-endian order to the underlying writer. 更多信息 Writes an unsigned 32-bit integer in big-endian order to the underlying writer. 更多信息 Writes a signed 32-bit integer in big-endian order to the underlying writer. 更多信息 Writes an unsigned 64-bit integer in big-endian order to the underlying writer. 更多信息 Writes an signed 64-bit integer in big-endian order to the underlying writer. 更多信息 Writes an unsigned 128-bit integer in big-endian order to the underlying writer. 更多信息 Writes an signed 128-bit integer in big-endian order to the underlying writer. 更多信息 Writes an 32-bit floating point type in big-endian order to the underlying writer. 更多信息 Writes an 64-bit floating point type in big-endian order to the underlying writer. 更多信息 Writes an unsigned 16-bit integer in little-endian order to the underlying writer. 更多信息 Writes a signed 16-bit integer in little-endian order to the underlying writer. 更多信息 Writes an unsigned 32-bit integer in little-endian order to the underlying writer. 更多信息 Writes a signed 32-bit integer in little-endia","module_doc":"一个将数据移入虚无的异步 writer。 此结构体通常通过调用 sink 创建。 有关更多详细信息，请参阅 sink() 的文档。 这是 std::io::Sink 的异步版本。","section_headers":[]},{"url":"tokio/io/struct.Split.html","crate":"tokio","title":"Split","section":"struct","desc":"用于 split 方法的分割器。 可以使用 SplitStream 将 Split 转换为 Stream。 返回流中的下一段。 §示例 use tokio::io::AsyncBufReadExt; let mut segments = my_buf_read.split(b'f'); while let Some(segment) = segments.next_segment().await? { println!(\"length = {}\", segment.len()) } 轮询流中的下一段。 此方法返回： Poll::Pending if the next segment is not yet available. Poll::Ready(Ok(Some(segment))) if the next segment is available. Poll::Ready(Ok(None)) if there are no more segments in this stream. Poll::Ready(Err(err)) if an IO error occurred while reading the next segment. 当该方法返回 Poll::Pending 时，提供的 Context 中的 Waker 被安排为在底层 IO 资源上有更多字节可用时接收唤醒。 请注意，对于多次调用 poll_next_segment， 只有传递给最近一次调用的 Context 中的 Waker 才会 被安排接收唤醒。 使用给定的格式化器格式化此值。 更多信息 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 原样返回传入的参数。 调用 U::from(self)。 That is, this conversion is whatever the implementation of From<T> for U 的实现方式。 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"用于 split 方法的分割器。 可以使用 SplitStream 将 Split 转换为 Stream。","section_headers":[]},{"url":"tokio/io/struct.Stderr.html","crate":"tokio","title":"Stderr","section":"struct","desc":"进程标准错误流的句柄。 对 stderr 的并发写入必须谨慎执行：只有对此 AsyncWrite 的单个写入才能保证完整。特别是请注意，使用 write_all 进行 写入时不能保证作为单次写入完成，因此多个线程使用 write_all 写入数据 可能会导致输出交错。 由 stderr 函数创建。 §示例 use tokio::io::{self, AsyncWriteExt}; #[tokio::main] async fn main() -> io::Result<()> { let mut stderr = io::stderr(); stderr.write_all(b\"Print some error here.\").await?; Ok(()) } Borrows the handle. 更多信息 Extracts the raw handle. 更多信息 Attempt to write bytes from buf into the object. 更多信息 Attempts to flush the object, ensuring that any buffered data reach their destination. 更多信息 Initiates or attempts to shut down this writer, returning success when the I/O connection has completely shut down. 更多信息 Like poll_write, except that it writes from a slice of buffers. 更多信息 Determines if this writer has an efficient poll_write_vectored implementation. 更多信息 使用给定的格式化器格式化此值。 更多信息 Gets the TypeId of self. 更多信息 将一个缓冲写入此写入器，返回写入的字节数。 更多信息 Like write, except that it writes from a slice of buffers. 更多信息 将一个缓冲写入此写入器，前进缓冲的内部游标。 更多信息 尝试将整个缓冲写入此写入器。 更多信息 尝试将整个缓冲写入此写入器。 更多信息 Writes an unsigned 8-bit integer to the underlying writer. 更多信息 Writes a signed 8-bit integer to the underlying writer. 更多信息 Writes an unsigned 16-bit integer in big-endian order to the underlying writer. 更多信息 Writes a signed 16-bit integer in big-endian order to the underlying writer. 更多信息 Writes an unsigned 32-bit integer in big-endian order to the underlying writer. 更多信息 Writes a signed 32-bit integer in big-endian order to the underlying writer. 更多信息 Writes an unsigned 64-bit integer in big-endian order to the underlying writer. 更多信息 Writes an signed 64-bit integer in big-endian order to the underlying writer. 更多信息 Writes an unsigned 128-bit integer in big-endian order to the underlying writer. 更多信息 Writes an signed 128-bit integer in big-endian order to the underlying writer. 更多信息 Writes an 32-bit floating point type in big-endian order to the underlying writer. 更多信息 Writes an 64-bit floating point type in big-endian order to the underlying writer. 更多信息 Writes ","module_doc":"进程标准错误流的句柄。 对 stderr 的并发写入必须谨慎执行：只有对此 AsyncWrite 的单个写入才能保证完整。特别是请注意，使用 write_all 进行 写入时不能保证作为单次写入完成，因此多个线程使用 write_all 写入数据 可能会导致输出交错。 由 stderr 函数创建。","section_headers":[]},{"url":"tokio/io/struct.Stdin.html","crate":"tokio","title":"Stdin","section":"struct","desc":"进程标准输入流的句柄。 该句柄实现 AsyncRead trait，但请注意 必须谨慎执行对 Stdin 的并发读取。 此句柄最适合用于非交互式用途，例如当文件 通过管道传入应用程序时。出于技术原因，stdin 通过在单独的线程上使用普通的阻塞读取实现， 并且无法取消该读取。这可能导致运行时 关闭挂起，直到用户按下回车键。 对于交互场景，建议派生一个专用于用户输入的线程，并在该线程中直接使用阻塞 IO。 由 stdin 函数创建。 Borrows the handle. 更多信息 Extracts the raw handle. 更多信息 Attempts to read from the AsyncRead into buf. 更多信息 使用给定的格式化器格式化此值。 更多信息 Gets the TypeId of self. 更多信息 创建一个新的 AsyncRead 实例，将此流与 next 链接起来。 更多信息 从此源拉取一些字节到指定的缓冲中，返回读取的字节数。 更多信息 从此源拉取一些字节到指定的缓冲中，前进缓冲的内部游标。 更多信息 Reads the exact number of bytes required to fill buf. 更多信息 Reads an unsigned 8 bit integer from the underlying reader. 更多信息 Reads a signed 8 bit integer from the underlying reader. 更多信息 Reads an unsigned 16-bit integer in big-endian order from the underlying reader. 更多信息 Reads a signed 16-bit integer in big-endian order from the underlying reader. 更多信息 Reads an unsigned 32-bit integer in big-endian order from the underlying reader. 更多信息 Reads a signed 32-bit integer in big-endian order from the underlying reader. 更多信息 Reads an unsigned 64-bit integer in big-endian order from the underlying reader. 更多信息 Reads an signed 64-bit integer in big-endian order from the underlying reader. 更多信息 Reads an unsigned 128-bit integer in big-endian order from the underlying reader. 更多信息 Reads an signed 128-bit integer in big-endian order from the underlying reader. 更多信息 Reads an 32-bit floating point type in big-endian order from the underlying reader. 更多信息 Reads an 64-bit floating point type in big-endian order from the underlying reader. 更多信息 Reads an unsigned 16-bit integer in little-endian order from the underlying reader. 更多信息 Reads a signed 16-bit integer in little-endian order from the underlying reader. 更多信息 Reads an unsigned 32-bit integer in little-endian order from the underlying reader. 更多信息 Reads a signed 32-bit integer in little-endian order from the underlying reader. 更多信息 Reads an unsigned 64-bit integer in little-endian order from the underlying reader. 更多信息 Reads an signed 64-bit integer in li","module_doc":"进程标准输入流的句柄。 该句柄实现 AsyncRead trait，但请注意 必须谨慎执行对 Stdin 的并发读取。 此句柄最适合用于非交互式用途，例如当文件 通过管道传入应用程序时。出于技术原因，stdin 通过在单独的线程上使用普通的阻塞读取实现， 并且无法取消该读取。这可能导致运行时 关闭挂起，直到用户按下回车键。 对于交互场景，建议派生一个专用于用户输入的线程，并在该线程中直接使用阻塞 IO。 由 stdin 函数创建。","section_headers":[]},{"url":"tokio/io/struct.Stdout.html","crate":"tokio","title":"Stdout","section":"struct","desc":"进程标准输出流的句柄。 对 stdout 的并发写入必须谨慎执行：只有对此 AsyncWrite 的单个写入才能保证完整。特别是请注意，使用 write_all 进行 写入时不能保证作为单次写入完成，因此多个线程使用 write_all 写入数据 可能会导致输出交错。 由 stdout 函数创建。 §示例 use tokio::io::{self, AsyncWriteExt}; #[tokio::main] async fn main() -> io::Result<()> { let mut stdout = io::stdout(); stdout.write_all(b\"Hello world!\").await?; Ok(()) } 以下是使用循环使用 stdio 的示例。 use tokio::io::{self, AsyncWriteExt}; #[tokio::main] async fn main() { let messages = vec![\"hello\", \" world\\n\"]; // When you use `stdio` in a loop, it is recommended to create // a single `stdio` instance outside the loop and call a write // operation against that instance on each loop. // // Repeatedly creating `stdout` instances inside the loop and // writing to that handle could result in mangled output since // each write operation is handled by a different blocking thread. let mut stdout = io::stdout(); for message in &messages { stdout.write_all(message.as_bytes()).await.unwrap(); stdout.flush().await.unwrap(); } } Borrows the handle. 更多信息 Extracts the raw handle. 更多信息 Attempt to write bytes from buf into the object. 更多信息 Attempts to flush the object, ensuring that any buffered data reach their destination. 更多信息 Initiates or attempts to shut down this writer, returning success when the I/O connection has completely shut down. 更多信息 Like poll_write, except that it writes from a slice of buffers. 更多信息 Determines if this writer has an efficient poll_write_vectored implementation. 更多信息 使用给定的格式化器格式化此值。 更多信息 Gets the TypeId of self. 更多信息 将一个缓冲写入此写入器，返回写入的字节数。 更多信息 Like write, except that it writes from a slice of buffers. 更多信息 将一个缓冲写入此写入器，前进缓冲的内部游标。 更多信息 尝试将整个缓冲写入此写入器。 更多信息 尝试将整个缓冲写入此写入器。 更多信息 Writes an unsigned 8-bit integer to the underlying writer. 更多信息 Writes a signed 8-bit integer to the underlying writer. 更多信息 Writes an unsigned 16-bit integer in big-endian order to the underlying writer. 更多信息 Writes a signed 16-bit integer in big-endian order to the underlying writer. 更多信息 Writes an unsigned 32-bit integer in big-endian ","module_doc":"进程标准输出流的句柄。 对 stdout 的并发写入必须谨慎执行：只有对此 AsyncWrite 的单个写入才能保证完整。特别是请注意，使用 write_all 进行 写入时不能保证作为单次写入完成，因此多个线程使用 write_all 写入数据 可能会导致输出交错。 由 stdout 函数创建。","section_headers":[]},{"url":"tokio/io/struct.Take.html","crate":"tokio","title":"Take","section":"struct","desc":"用于 take 方法的流。 返回此实例在返回 EOF 之前可以读取的剩余字节数。 §Note 如果底层的 AsyncRead 实例到达 EOF，则此实例可能在读取少于 此方法指示的字节数之后到达 EOF。 设置此实例返回 EOF 之前可读取的字节数。 return EOF. This is the same as constructing a new Take instance, so the amount of bytes read and the previous limit value don’t matter when calling this method. 获取底层 reader 的引用。 获取底层 reader 的可变引用。 请注意避免修改底层 I/O 状态， underlying reader as doing so may corrupt the internal limit of this Take. 获取底层读取器的 pinned 可变引用。 请注意避免修改底层 I/O 状态， underlying reader as doing so may corrupt the internal limit of this Take. 消费此 Take，返回被包装的 reader。 Attempts to return the contents of the internal buffer, filling it with more data from the inner reader if it is empty. 更多信息 Tells this buffer that amt bytes have been consumed from the buffer, so they should no longer be returned in calls to poll_read. 更多信息 Attempts to read from the AsyncRead into buf. 更多信息 使用给定的格式化器格式化此值。 更多信息 Gets the TypeId of self. 更多信息 读取所有字节到 buf，直到遇到分隔字节 byte 或 EOF。 更多信息 读取所有字节直到遇到换行符（0xA 字节），并将它们追加到提供的缓冲。 更多信息 Returns a stream of the contents of this reader split on the byte byte. 更多信息 Returns the contents of the internal buffer, filling it with more data from the inner reader if it is empty. 更多信息 Tells this buffer that amt bytes have been consumed from the buffer, so they should no longer be returned in calls to read. 更多信息 返回此读取器各行的流。 This method is the async equivalent to BufRead::lines. 更多信息 创建一个新的 AsyncRead 实例，将此流与 next 链接起来。 更多信息 从此源拉取一些字节到指定的缓冲中，返回读取的字节数。 更多信息 从此源拉取一些字节到指定的缓冲中，前进缓冲的内部游标。 更多信息 Reads the exact number of bytes required to fill buf. 更多信息 Reads an unsigned 8 bit integer from the underlying reader. 更多信息 Reads a signed 8 bit integer from the underlying reader. 更多信息 Reads an unsigned 16-bit integer in big-endian order from the underlying reader. 更多信息 Reads a signed 16-bit integer in big-endian order from the underlying reader. 更多信息 Reads an unsigned 32-bit integer in big-endian order from the underlying reader. 更多信息 Reads a signed 32-bit integer in big-endian order from the underl","module_doc":"用于 take 方法的流。","section_headers":[]},{"url":"tokio/io/struct.WriteHalf.html","crate":"tokio","title":"WriteHalf","section":"struct","desc":"由 split 返回的值的可写半部分。 检查此 WriteHalf 与某个 ReadHalf 是否从同一个流拆分而来。 Attempt to write bytes from buf into the object. 更多信息 Attempts to flush the object, ensuring that any buffered data reach their destination. 更多信息 Initiates or attempts to shut down this writer, returning success when the I/O connection has completely shut down. 更多信息 Like poll_write, except that it writes from a slice of buffers. 更多信息 Determines if this writer has an efficient poll_write_vectored implementation. 更多信息 使用给定的格式化器格式化此值。 更多信息 Gets the TypeId of self. 更多信息 将一个缓冲写入此写入器，返回写入的字节数。 更多信息 Like write, except that it writes from a slice of buffers. 更多信息 将一个缓冲写入此写入器，前进缓冲的内部游标。 更多信息 尝试将整个缓冲写入此写入器。 更多信息 尝试将整个缓冲写入此写入器。 更多信息 Writes an unsigned 8-bit integer to the underlying writer. 更多信息 Writes a signed 8-bit integer to the underlying writer. 更多信息 Writes an unsigned 16-bit integer in big-endian order to the underlying writer. 更多信息 Writes a signed 16-bit integer in big-endian order to the underlying writer. 更多信息 Writes an unsigned 32-bit integer in big-endian order to the underlying writer. 更多信息 Writes a signed 32-bit integer in big-endian order to the underlying writer. 更多信息 Writes an unsigned 64-bit integer in big-endian order to the underlying writer. 更多信息 Writes an signed 64-bit integer in big-endian order to the underlying writer. 更多信息 Writes an unsigned 128-bit integer in big-endian order to the underlying writer. 更多信息 Writes an signed 128-bit integer in big-endian order to the underlying writer. 更多信息 Writes an 32-bit floating point type in big-endian order to the underlying writer. 更多信息 Writes an 64-bit floating point type in big-endian order to the underlying writer. 更多信息 Writes an unsigned 16-bit integer in little-endian order to the underlying writer. 更多信息 Writes a signed 16-bit integer in little-endian order to the underlying writer. 更多信息 Writes an unsigned 32-bit integer in little-endian order to the underlying writer. 更多信息 Writes a signed 32-bit integer in little-endian order to the underlying writ","module_doc":"由 split 返回的值的可写半部分。","section_headers":[]},{"url":"tokio/io/trait.AsyncBufRead.html","crate":"tokio","title":"AsyncBufRead","section":"trait","desc":"异步读取字节。 此 trait 类似于 std::io::BufRead，但与异步任务系统集成。特别地，与 BufRead::fill_buf 不同，poll_fill_buf 方法将在数据尚未可用时自动将当前任务排队等待唤醒并返回，而不是阻塞调用线程。 用于处理 AsyncBufRead 值的实用工具由 AsyncBufReadExt 提供。 尝试返回内部缓冲区的内容，如果为空，则使用内部 reader 中的更多数据填充它。 成功时，返回 Poll::Ready(Ok(buf))。 如果没有可读取的数据，该方法将返回 Poll::Pending，并安排当前任务（通过 cx.waker().wake_by_ref()）在对象变为可读或关闭时接收通知。 此函数是较低级别的调用。它需要与 consume 方法配对才能正常工作。调用此方法时，不会\"读取\"任何内容，因为稍后调用 poll_read 可能会返回相同的内容。因此，必须使用从此缓冲区消费的字节数调用 consume，以确保字节永远不会被返回两次。 返回空缓冲区表示流已到达 EOF。 告诉此缓冲区已从缓冲区中消费了 amt 字节，因此它们不应再在对 poll_read 的调用中返回。 此函数是较低级别的调用。它需要与 poll_fill_buf 方法配对才能正常工作。此函数不执行任何 I/O，它只是通知此对象，其从 poll_fill_buf 返回的缓冲区中已消费了某些数量，并且不应再返回。因此，如果在调用之前没有调用 poll_fill_buf，则此函数可能会做奇怪的事情。 amt 必须 <= 由 poll_fill_buf 返回的缓冲区中的字节数。","module_doc":"异步读取字节。 此 trait 类似于 std::io::BufRead，但与异步任务系统集成。特别地，与 BufRead::fill_buf 不同，poll_fill_buf 方法将在数据尚未可用时自动将当前任务排队等待唤醒并返回，而不是阻塞调用线程。 用于处理 AsyncBufRead 值的实用工具由 AsyncBufReadExt 提供。","section_headers":[]},{"url":"tokio/io/trait.AsyncBufReadExt.html","crate":"tokio","title":"AsyncBufReadExt","section":"trait","desc":"一个扩展 trait，为 AsyncBufRead 类型添加实用方法。 读取所有字节到 buf，直到遇到分隔字节 byte 或 EOF。 Equivalent to: ⓘasync fn read_until(&mut self, byte: u8, buf: &mut Vec<u8>) -> io::Result<usize>; 此函数将从底层流读取字节，直到找到分隔字节或 EOF。找到后，到此为止的所有字节（包括分隔字节本身，若找到的话）将被追加到 buf。 若成功，此函数将返回读取的总字节数。 如果此函数返回 Ok(0)，则流已达到 EOF。 §错误 此函数会忽略所有 ErrorKind::Interrupted 错误， 并返回 fill_buf 所返回的其他任何错误。 如果遇到 I/O 错误，则到目前为止已读取的所有字节都将保留在 buf 中， 并且其长度会相应调整。 §Cancel safety 如果该方法在 tokio::select! 语句中作为事件使用，且另一个分支先完成，则部分数据可能已被读取。 已部分读取的字节会附加到 buf，可以再次调用该方法以继续读取， 直到遇到 byte。 此方法返回已读取的总字节数。如果取消对 read_until 的调用， 然后再次调用以继续读取，则计数器会重置。 §示例 std::io::Cursor 是实现 BufRead 的类型。 在此示例中，我们使用 Cursor 按连字符分隔的方式读取字节切片中的所有字节： use tokio::io::AsyncBufReadExt; use std::io::Cursor; let mut cursor = Cursor::new(b\"lorem-ipsum\"); let mut buf = vec![]; // cursor is at 'l' let num_bytes = cursor.read_until(b'-', &mut buf) .await .expect(\"reading from cursor won't fail\"); assert_eq!(num_bytes, 6); assert_eq!(buf, b\"lorem-\"); buf.clear(); // cursor is at 'i' let num_bytes = cursor.read_until(b'-', &mut buf) .await .expect(\"reading from cursor won't fail\"); assert_eq!(num_bytes, 5); assert_eq!(buf, b\"ipsum\"); buf.clear(); // cursor is at EOF let num_bytes = cursor.read_until(b'-', &mut buf) .await .expect(\"reading from cursor won't fail\"); assert_eq!(num_bytes, 0); assert_eq!(buf, b\"\"); 读取所有字节直到遇到换行符（0xA 字节），并将它们追加到提供的缓冲区中。 Equivalent to: ⓘasync fn read_line(&mut self, buf: &mut String) -> io::Result<usize>; 此函数会从底层流读取字节，直到找到换行分隔符（0xA 字节）或 EOF。 找到后，所有字节（如果找到分隔符，则包括分隔符）将附加到 buf。 若成功，此函数将返回读取的总字节数。 如果此函数返回 Ok(0)，则流已达到 EOF。 §错误 此函数的错误语义与 read_until 相同， 并且如果读取的字节不是有效的 UTF-8，也会返回错误。 如果遇到 I/O 错误，则在所有已读取数据均为有效 UTF-8 的情况下， buf 中可能已包含部分已读取的字节。 §Cancel safety 此方法不是可取消安全的。如果该方法在 tokio::select! 语句中作为事件使用，且另一个分支先完成，则部分数据可能已被读取， 并且该部分数据会丢失。取消调用时，buf 的内容不提供任何保证。 当前实现会将 buf 替换为空字符串，但未来可能会有所变化。 由于字符串只能包含有效 UTF-8 的要求，此函数的行为与 read_until 不相同。 如果你需要一个可取消安全的 read_line，有三种选择： Call read_until with a newline character and manually perform the utf-8 check. The stream returned by lines has a can","module_doc":"一个扩展 trait，为 AsyncBufRead 类型添加实用方法。","section_headers":[]},{"url":"tokio/io/trait.AsyncRead.html","crate":"tokio","title":"AsyncRead","section":"trait","desc":"从源读取字节。 此 trait 类似于 std::io::Read trait，但与异步任务系统集成。特别地，与 Read::read 不同，poll_read 方法将在数据尚未可用时自动将当前任务排队等待唤醒并返回，而不是阻塞调用线程。 具体来说，这意味着 poll_read 函数将返回以下之一： Poll::Ready(Ok(())) 表示已立即读取数据并将其放入输出缓冲区中。读取的数据量可以通过 ReadBuf::filled 返回的切片长度的增加来确定。如果差值为 0，则表示已到达 EOF，或者输出缓冲区的容量为零（即 buf.remaining() == 0）。 Poll::Pending 表示没有数据被读入所提供的缓冲区。I/O 对象当前不可读，但将来可能变为可读。最重要的是，当前 future 的任务被安排在对象可读时获得 unpark。这意味着与 Future::poll 一样，当 I/O 对象再次可读时，你将收到通知。 Poll::Ready(Err(e)) 表示其他错误，这些是来自底层对象的标准 I/O 错误。 此 trait 重要的是意味着 read 方法只能在 future 任务的上下文中工作。如果在任务外使用，该对象可能会发生 panic。 用于处理 AsyncRead 值的实用工具由 AsyncReadExt 提供。 尝试从 AsyncRead 读取到 buf 中。 成功时，返回 Poll::Ready(Ok(())) 并将数据放入 buf 的未填充部分。如果没有读取数据（buf.filled().len() 未变化），则表示已到达 EOF，或者输出缓冲区的容量为零（即 buf.remaining() == 0）。 如果没有可读取的数据，该方法将返回 Poll::Pending，并安排当前任务（通过 cx.waker()）在对象变为可读或关闭时接收通知。","module_doc":"从源读取字节。 此 trait 类似于 std::io::Read trait，但与异步任务系统集成。特别地，与 Read::read 不同，poll_read 方法将在数据尚未可用时自动将当前任务排队等待唤醒并返回，而不是阻塞调用线程。 具体来说，这意味着 poll_read 函数将返回以下之一： Poll::Ready(Ok(())) 表示已立即读取数据并将其放入输出缓冲区中。读取的数据量可以通过 ReadBuf::filled 返回的切片长度的增加来确定。如果差值为 0，则表示已到达 EOF，或者输出缓冲区的容量为零（即 buf.remaining() == 0）。 Poll::Pending 表示没有数据被读入所提供的缓冲区。I/O 对象当前不可读，但将来可能变为可读。最重要的是，当前 future 的任务被安排在对象可读时获得 unpark。这意味着与 Future::poll 一样，当 I/O 对象再次可读时，你将收到通知。 Poll::Ready(Err(e)) 表示其他错误，这些是来自底层对象的标准 I/O 错误。 此 trait 重要的是意味着 read 方法只能在 ","section_headers":[]},{"url":"tokio/io/trait.AsyncReadExt.html","crate":"tokio","title":"AsyncReadExt","section":"trait","desc":"从源读取字节。 作为扩展 trait 实现，向所有 AsyncRead 类型添加实用方法。 调用者通常会导入此 trait 而不是 AsyncRead。 use tokio::fs::File; use tokio::io::{self, AsyncReadExt}; #[tokio::main] async fn main() -> io::Result<()> { let mut f = File::open(\"foo.txt\").await?; let mut buffer = [0; 10]; // The `read` method is defined by this trait. let n = f.read(&mut buffer[..]).await?; Ok(()) } 有关更多详细信息，请参阅 模块 文档。 创建一个新的 AsyncRead 实例，将此流与 next 链接起来。 返回的 AsyncRead 实例将首先从此对象读取所有字节直到 EOF。之后的输出等同于 next 的输出。 §示例 File 实现 AsyncRead： use tokio::fs::File; use tokio::io::{self, AsyncReadExt}; #[tokio::main] async fn main() -> io::Result<()> { let f1 = File::open(\"foo.txt\").await?; let f2 = File::open(\"bar.txt\").await?; let mut handle = f1.chain(f2); let mut buffer = String::new(); // read the value into a String. We could use any AsyncRead // method here, this is just one example. handle.read_to_string(&mut buffer).await?; Ok(()) } 从此源拉取一些字节到指定的缓冲中，返回读取的字节数。 等价于： ⓘasync fn read(&mut self, buf: &mut [u8]) -> io::Result<usize>; 此方法不保证是立即完成还是异步完成。 §Return 若此方法的返回值为 Ok(n)，则必须保证 0 <= n <= buf.len()。非零的 n 值表示缓冲 buf 已从该源填充了 n 个字节的数据。若 n 为 0，则可能表示以下两种情况之一： This reader has reached its “end of file” and will likely no longer be able to produce bytes. Note that this does not mean that the reader will always no longer be able to produce bytes. The buffer specified was 0 bytes in length. 调用此函数时，不保证 buf 的内容，实现不能依赖 buf 内容的任何属性。建议实现仅向 buf 写入数据，而不是读取其内容。 相应地，但是，此方法的调用者不能假设实现使用 buf 的方式的任何保证。打算写入缓冲的代码也可能从其中读取。 请你负责确保在调用 read 之前 buf 已初始化。 §错误 若此函数遇到任何形式的 I/O 或其他错误，则返回错误变体。若返回错误，则必须保证尚未读取任何字节。 §Cancel safety 此方法是取消安全的。若你将其作为事件用在 tokio::select! 语句中，且其他分支先完成，则保证尚未读取任何数据。 §示例 File 实现 Read： use tokio::fs::File; use tokio::io::{self, AsyncReadExt}; #[tokio::main] async fn main() -> io::Result<()> { let mut f = File::open(\"foo.txt\").await?; let mut buffer = [0; 10]; // read up to 10 bytes let n = f.read(&mut buffer[..]).await?; println!(\"The bytes: {:?}\", &buffer[..n]); Ok(()) } 从此源拉取一些字节到指定的缓冲中，前进缓冲的内部游标。 等价于： ⓘasync fn read_buf<B: BufMut>(&mut sel","module_doc":"从源读取字节。 作为扩展 trait 实现，向所有 AsyncRead 类型添加实用方法。 调用者通常会导入此 trait 而不是 AsyncRead。","section_headers":[]},{"url":"tokio/io/trait.AsyncSeek.html","crate":"tokio","title":"AsyncSeek","section":"trait","desc":"异步定位字节。 此 trait 类似于 std::io::Seek trait，但与异步任务系统集成。特别地，与 Seek::seek 不同，start_seek 方法不会阻塞调用线程。 用于处理 AsyncSeek 值的实用工具由 AsyncSeekExt 提供。 尝试在流中以字节为单位 seek 到一个偏移量。 允许 seek 超出流的末尾，但行为由实现定义。 如果此函数成功返回，则作业已提交。要了解何时完成，请调用 poll_complete。 §错误 如果还有另一个 seek 正在进行，此函数可能返回 io::ErrorKind::Other。为避免这种情况，建议在调用 start_seek 之前先调用 poll_complete，以确保所有挂起的 seek 都已完成。 等待 seek 操作完成。 如果 seek 操作成功完成，此方法返回从流开头开始的新位置。该位置稍后可与 SeekFrom::Start 一起使用。 此方法返回的位置只能在调用 start_seek 之后立即依赖。如果自调用 start_seek 以来已经通过例如读取或写入更改了位置，则返回的位置是否考虑了该位置变化是未指定的。类似地，如果从未调用过 start_seek，则 poll_complete 返回的是实际位置还是其他占位值（如 0）是未指定的。 §错误 seek 到负偏移量被视为错误。","module_doc":"异步定位字节。 此 trait 类似于 std::io::Seek trait，但与异步任务系统集成。特别地，与 Seek::seek 不同，start_seek 方法不会阻塞调用线程。 用于处理 AsyncSeek 值的实用工具由 AsyncSeekExt 提供。","section_headers":[]},{"url":"tokio/io/trait.AsyncSeekExt.html","crate":"tokio","title":"AsyncSeekExt","section":"trait","desc":"为 AsyncSeek 类型添加实用方法的扩展 trait。 §示例 use std::io::{self, Cursor, SeekFrom}; use tokio::io::{AsyncSeekExt, AsyncReadExt}; let mut cursor = Cursor::new(b\"abcdefg\"); // the `seek` method is defined by this trait cursor.seek(SeekFrom::Start(3)).await?; let mut buf = [0; 1]; let n = cursor.read(&mut buf).await?; assert_eq!(n, 1); assert_eq!(buf, [b'd']); Ok(()) 有关更多详细信息，请参阅 模块 文档。 创建一个将定位 IO 对象，然后产出对象中的新位置以及对象本身的 Future。 等价于： ⓘasync fn seek(&mut self, pos: SeekFrom) -> io::Result<u64>; 若发生错误，缓冲与对象将被丢弃，并产出错误。 §示例 use tokio::fs::File; use tokio::io::{AsyncSeekExt, AsyncReadExt}; use std::io::SeekFrom; let mut file = File::open(\"foo.txt\").await?; file.seek(SeekFrom::Start(6)).await?; let mut contents = vec![0u8; 10]; file.read_exact(&mut contents).await?; 创建一个将倒回到流开头的 Future。 这是便捷方法，等价于 self.seek(SeekFrom::Start(0))。 创建一个 Future，返回从流开头计算的当前定位位置。 这等价于 self.seek(SeekFrom::Current(0))。","module_doc":"为 AsyncSeek 类型添加实用方法的扩展 trait。","section_headers":[]},{"url":"tokio/io/trait.AsyncWrite.html","crate":"tokio","title":"AsyncWrite","section":"trait","desc":"异步写入字节。 此 trait 类似于 std::io::Write trait，但它与异步任务系统集成。具体而言，poll_write 方法与 Write::write 不同，如果数据尚不可用，它会自动将当前任务排队等待唤醒并返回，而不是阻塞调用线程。 具体来说，这意味着 poll_write 函数将返回以下之一： Poll::Ready(Ok(n)) 意味着立即写入了 n 字节的数据。 Poll::Pending 意味着没有从所提供的缓冲区写入任何数据。I/O 对象当前不可写，但将来可能变得可写。最重要的是，当对象可写时，当前 future 的任务会被调度以获得 unpark 通知。这意味着与 Future::poll 类似，当 I/O 对象再次可写时，你将收到通知。 Poll::Ready(Err(e)) 表示其他错误，这些是来自底层对象的标准 I/O 错误。 用于处理 AsyncWrite 值的实用工具由 AsyncWriteExt 提供。大多数用户将通过这些扩展方法与 AsyncWrite 类型进行交互，这些方法提供了符合人体工程学的异步函数，例如 write_all 和 flush。 尝试将字节从 buf 写入对象。 成功时返回 Poll::Ready(Ok(num_bytes_written))。如果成功，则必须保证 n <= buf.len()。返回值 0 通常意味着底层对象不再能够接受字节，并且将来很可能也无法接受，或者所提供的缓冲区为空。 如果对象未准备好写入数据，则方法返回 Poll::Pending，并安排当前任务（通过 cx.waker()）在对象变为可写或被关闭时接收通知。 尝试刷新对象，确保任何已缓冲的数据都到达其目标。 成功时返回 Poll::Ready(Ok(()))。 如果刷新无法立即完成，此方法将返回 Poll::Pending，并安排当前任务（通过 cx.waker()）在对象能够推进刷新时收到通知。 启动或尝试关闭此写入器，在 I/O 连接完全关闭时返回成功。 此方法旨在用于 I/O 连接的异步关闭。例如，这适用于实现 TLS 连接的关闭，或在代理连接上调用 TcpStream::shutdown。协议有时需要刷新最后的数据片段或执行优雅的关闭握手，根据需要读/写更多数据。此方法是此类协议实现优雅关闭逻辑的钩子。 AsyncWrite trait 的实现者必须实现此 shutdown 方法。包装器通常只想将此调用代理到被包装的类型，而基本类型通常在此处实现关闭逻辑或仅返回 Ok(().into())。请注意，如果你正在包装底层的 AsyncWrite，则对 shutdown 的调用意味着整个流已被传递性地关闭。在执行完包装器的关闭逻辑后，你应该关闭底层流。 调用 shutdown 意味着调用了 flush。一旦此方法返回 Ready，就意味着在关闭发生之前已成功执行了刷新。也就是说，调用者无需在调用 shutdown 之前调用 flush。他们可以相信通过调用 shutdown，任何挂起的已缓冲数据都将被写出。 §返回值 此函数返回 Poll<io::Result<()>>，分类如下： Poll::Ready(Ok(())) - 表示连接已成功关闭，现在可以安全地释放/丢弃/关闭与其关联的资源。此方法意味着当前任务将不再收到由此方法导致的任何通知，并且 I/O 对象本身很可能不再可用。 Poll::Pending - 表示关闭已启动但目前无法完成。这可能意味着需要更多 I/O 才能继续此关闭操作。当前任务被调度为在其他条件允许继续关闭操作时收到通知。唤醒时应再次调用此方法。 Poll::Ready(Err(e)) - 表示关闭过程中发生了致命错误，表明关闭操作未成功完成。这通常意味着 I/O 对象不再可用。 §错误 此函数可以通过 Err 返回正常的 I/O 错误，如上所述。此外，此方法还可能导致底层的 Write::write 方法不再可用（例如，将来会返回错误）。建议一旦调用了 shutdown，就不再调用 write 方法。 §恐慌 如果未在 future 任务的上下文中调用，此函数将发生 panic。 与 poll_write 类似，只是它从一个缓冲区切片写入。 数据按顺序从每个缓冲区复制，最后读取的缓冲区可能仅被部分消费。此方法的行为必须与使用连接后的缓冲区对 write 的调用相同。 默认实现使用所提供的第一个非空缓冲区（如果不存在则使用空缓冲区）调用 poll_write。 成功时返回 Poll::Ready(Ok(num_bytes_written))。 如果对象未准备好写入数据，则方法返回 Poll::Pending，并安排当前任务（通过 cx.waker()）在对象","module_doc":"异步写入字节。 此 trait 类似于 std::io::Write trait，但它与异步任务系统集成。具体而言，poll_write 方法与 Write::write 不同，如果数据尚不可用，它会自动将当前任务排队等待唤醒并返回，而不是阻塞调用线程。 具体来说，这意味着 poll_write 函数将返回以下之一： Poll::Ready(Ok(n)) 意味着立即写入了 n 字节的数据。 Poll::Pending 意味着没有从所提供的缓冲区写入任何数据。I/O 对象当前不可写，但将来可能变得可写。最重要的是，当对象可写时，当前 future 的任务会被调度以获得 unpark 通知。这意味着与 Future::poll 类似，当 I/O 对象再次可写时，你将收到通知。 Poll::Ready(Err(e)) 表示其他错误，这些是来自底层对象的标准 I/O 错误。 用于处理 AsyncWrite 值的实用工具由 AsyncWriteExt 提供。大多数用户将通过这些扩展方法与 AsyncWrite 类型进行交互，这些方法提供了符合人体工程学的异步函数，例如 write_all 和 f","section_headers":[]},{"url":"tokio/io/trait.AsyncWriteExt.html","crate":"tokio","title":"AsyncWriteExt","section":"trait","desc":"将字节写入汇。 作为扩展 trait 实现，向所有 AsyncWrite 类型添加实用方法。 调用者通常会导入此 trait 而不是 AsyncWrite。 use tokio::io::{self, AsyncWriteExt}; use tokio::fs::File; #[tokio::main] async fn main() -> io::Result<()> { let data = b\"some bytes\"; let mut pos = 0; let mut buffer = File::create(\"foo.txt\").await?; while pos < data.len() { let bytes_written = buffer.write(&data[pos..]).await?; pos += bytes_written; } Ok(()) } 有关更多详细信息，请参阅 模块 文档。 将一个缓冲写入此写入器，返回写入的字节数。 Equivalent to: ⓘasync fn write(&mut self, buf: &[u8]) -> io::Result<usize>; 此函数将尝试写入 buf 的全部内容，但整个写入可能无法完成， 或者可能产生错误。对 write 的调用表示至多一次对 任何包装对象的写入尝试。 §Return 如果返回值为 Ok(n)，则必须保证 n <= buf.len()。 返回值为 0 通常表示底层对象已无法再接受字节， 将来也不太可能接受，或者所提供的缓冲区为空。 §错误 每次调用 write 都可能生成一个 I/O 错误，表明操作未能完成。若返回错误，则缓冲中的所有字节均未写入此写入器。 如果无法将整个缓冲区写入此 writer，不视为错误。 §Cancel safety 此方法是可取消安全的：如果在 tokio::select! 语句中作为事件使用且另一个分支先完成，则可以保证没有 数据被写入此 AsyncWrite。 §示例 use tokio::io::{self, AsyncWriteExt}; use tokio::fs::File; #[tokio::main] async fn main() -> io::Result<()> { let mut file = File::create(\"foo.txt\").await?; // Writes some prefix of the byte string, not necessarily all of it. file.write(b\"some bytes\").await?; file.flush().await?; Ok(()) } 与 write 类似，但从缓冲区切片中写入数据。 Equivalent to: ⓘasync fn write_vectored(&mut self, bufs: &[IoSlice<'_>]) -> io::Result<usize>; 有关更多详细信息，请参阅 AsyncWrite::poll_write_vectored。 §Cancel safety 此方法是可取消安全的：如果在 tokio::select! 语句中作为事件使用且另一个分支先完成，则可以保证没有 数据被写入此 AsyncWrite。 §示例 use tokio::io::{self, AsyncWriteExt}; use tokio::fs::File; use std::io::IoSlice; #[tokio::main] async fn main() -> io::Result<()> { let mut file = File::create(\"foo.txt\").await?; let bufs: &[_] = &[ IoSlice::new(b\"hello\"), IoSlice::new(b\" \"), IoSlice::new(b\"world\"), ]; file.write_vectored(&bufs).await?; file.flush().await?; Ok(()) } 将一个缓冲写入此写入器，前进缓冲的内部游标。 Equivalent to: ⓘasync fn write_buf<B: Buf>(&mut self, buf: &mut B) -> io::Result<usize>; 此函数将尝试写入 buf 的全部内容，但整个写入可能无法完成， 或者可能产生错误。操作完成后，缓冲区的 内部游标将前进已写入的字节数。后续使用相同 buf 值调用 write_buf 时， 将从第一次调用 write_buf 完成的位置继续。 对 write_buf 的调用表示","module_doc":"将字节写入汇。 作为扩展 trait 实现，向所有 AsyncWrite 类型添加实用方法。 调用者通常会导入此 trait 而不是 AsyncWrite。","section_headers":[]},{"url":"tokio/net/fn.lookup_host.html","crate":"tokio","title":"lookup_host","section":"fn","desc":"执行 DNS 解析。 返回的迭代器可能不会真正产生任何值，这取决于执行的所有解析结果。 此 API 并不打算涵盖所有 DNS 用例。超出基本用例的任何内容都应使用专门的库完成。 §示例 解析 DNS 条目： use tokio::net; use std::io; #[tokio::main] async fn main() -> io::Result<()> { for addr in net::lookup_host(\"localhost:3000\").await? { println!(\"socket address is {}\", addr); } Ok(()) }","module_doc":"执行 DNS 解析。 返回的迭代器可能不会真正产生任何值，这取决于执行的所有解析结果。 此 API 并不打算涵盖所有 DNS 用例。超出基本用例的任何内容都应使用专门的库完成。 解析 DNS 条目：","section_headers":[]},{"url":"tokio/net/struct.TcpListener.html","crate":"tokio","title":"TcpListener","section":"struct","desc":"监听连接的 TCP 套接字服务器。 你可以使用 accept 方法接受新连接。 TcpListener 可以通过 TcpListenerStream 转换为 Stream。 当该值被 drop 时，套接字将被关闭。 §错误 请注意，接受连接可能会导致各种错误，并非所有错误都一定是致命的——例如打开的文件描述符过多，或者对端在等待接受队列时关闭连接。如果不以任何方式处理这些情况，它们将终止流。 §示例 使用 accept： use tokio::net::TcpListener; use std::io; async fn process_socket<T>(socket: T) { // 在此处使用 socket 工作 } #[tokio::main] async fn main() -> io::Result<()> { let listener = TcpListener::bind(\"127.0.0.1:8080\").await?; loop { let (socket, _) = listener.accept().await?; process_socket(socket).await; } } 创建一个新的 TcpListener，它将绑定到指定的地址。 返回的监听器已准备好接受连接。 使用端口号 0 绑定将请求操作系统为此监听器分配一个端口。所分配的端口可以通过 local_addr 方法查询。 地址类型可以是 ToSocketAddrs trait 的任何实现者。如果 addr 产生多个地址，则会依次尝试与每个地址进行绑定，直到其中一个成功并返回监听器。如果没有任何地址成功创建监听器，则返回最后一次尝试（最后一个地址）的错误。 此函数在 Unix 上的套接字上设置 SO_REUSEADDR 选项。 要在绑定之前配置套接字，可以使用 TcpSocket 类型。 §示例 use tokio::net::TcpListener; use std::io; #[tokio::main] async fn main() -> io::Result<()> { let listener = TcpListener::bind(\"127.0.0.1:2345\").await?; // 使用 listener Ok(()) } 从此监听器接受一个新的传入连接。 一旦建立新的 TCP 连接，此函数就会让步。连接建立后，将返回相应的 TcpStream 和远程对等方的地址。 §取消安全性 此方法是可安全取消的。如果在 tokio::select! 语句中将此方法用作事件，并且其他某个分支先完成，则可以保证此方法未接受任何新连接。 §示例 use tokio::net::TcpListener; use std::io; #[tokio::main] async fn main() -> io::Result<()> { let listener = TcpListener::bind(\"127.0.0.1:8080\").await?; match listener.accept().await { Ok((_socket, addr)) => println!(\"new client: {:?}\", addr), Err(e) => println!(\"couldn't get client: {:?}\", e), } Ok(()) } 轮询以接受到此监听器的新传入连接。 如果没有连接可接受，则返回 Poll::Pending，并且当前任务将由 waker 通知。请注意，在多次调用 poll_accept 时，只有传递给最近一次调用的 Context 中的 Waker 会被调度接收唤醒。 从 std::net::TcpListener 创建一个新的 TcpListener。 此函数旨在用于将标准库的 TCP 监听器包装为 Tokio 等价物。 此 API 通常与 socket2 crate 和 Socket 类型配对使用，以便在将监听器交给底层事件循环之前构建并自定义它。这允许配置诸如 SO_REUSEPORT、绑定到多个地址等选项。 §注意 调用者负责确保监听器处于非阻塞模式。否则，对监听器的所有 I/O 操作都会阻塞线程，从而导致意外行为。可以使用 set_nonblocking 设置非阻塞模式。 在阻塞模式下传入监听器始终是错误的，这种情况下行为将来可能会发生变化。例如，可能会发生 panic。 §示例 use std::error::Error; use tokio::net::TcpListener; #[tokio::main] async fn main() -> Result<(), Box<dyn Error>>","module_doc":"监听连接的 TCP 套接字服务器。 你可以使用 accept 方法接受新连接。 TcpListener 可以通过 TcpListenerStream 转换为 Stream。 当该值被 drop 时，套接字将被关闭。 请注意，接受连接可能会导致各种错误，并非所有错误都一定是致命的——例如打开的文件描述符过多，或者对端在等待接受队列时关闭连接。如果不以任何方式处理这些情况，它们将终止流。 使用 accept：","section_headers":[]},{"url":"tokio/net/struct.TcpSocket.html","crate":"tokio","title":"TcpSocket","section":"struct","desc":"一个尚未转换为 TcpStream 或 TcpListener 的 TCP 套接字。 TcpSocket 包装一个操作系统套接字，使调用者能够在建立 TCP 连接或接受入站连接之前配置套接字。调用者可以设置套接字选项并使用套接字地址显式绑定套接字。 当 TcpSocket 值被 drop 时，底层套接字将被关闭。 只有当 TcpStream::connect 和 TcpListener::bind 使用的默认配置不满足所需的使用场景时，才应直接使用 TcpSocket。 调用 TcpStream::connect(\"127.0.0.1:8080\") 等价于： use tokio::net::TcpSocket; use std::io; #[tokio::main] async fn main() -> io::Result<()> { let addr = \"127.0.0.1:8080\".parse().unwrap(); let socket = TcpSocket::new_v4()?; let stream = socket.connect(addr).await?; Ok(()) } 调用 TcpListener::bind(\"127.0.0.1:8080\") 等价于： use tokio::net::TcpSocket; use std::io; #[tokio::main] async fn main() -> io::Result<()> { let addr = \"127.0.0.1:8080\".parse().unwrap(); let socket = TcpSocket::new_v4()?; // 在基于 Berkeley 套接字的平台上，这允许快速 // 重新绑定一个 socket，而无需等待操作系统清理 // 先前的那个。 // // 在 Windows 上，这允许重新绑定正在使用的 socket， // 但这会导致 \"socket hijacking\"，因此我们显式不在此处设置。 // https://docs.microsoft.com/en-us/windows/win32/winsock/using-so-reuseaddr-and-so-exclusiveaddruse socket.set_reuseaddr(true)?; socket.bind(addr)?; // 注意：`TcpListener::bind` 实际使用的 backlog 取决于平台， // 因为 Tokio 依赖 Mio 的默认 backlog 配置。此处的 `1024` 仅作 // 说明，并不反映实际使用的值。 let listener = socket.listen(1024)?; Ok(()) } 要设置 TcpSocket 尚未明确提供的套接字选项，可以通过使用 AsRawFd/AsRawSocket 访问 RawFd/RawSocket，并使用诸如 socket2 的 crate 来设置选项。 创建一个配置为 IPv4 的新套接字。 使用 AF_INET 和 SOCK_STREAM 调用 socket(2)。 §Returns 成功时返回新创建的 TcpSocket。如果遇到错误，则改为返回该错误。 §示例 创建一个新的 IPv4 套接字并开始监听。 use tokio::net::TcpSocket; use std::io; #[tokio::main] async fn main() -> io::Result<()> { let addr = \"127.0.0.1:8080\".parse().unwrap(); let socket = TcpSocket::new_v4()?; socket.bind(addr)?; let listener = socket.listen(128)?; Ok(()) } 创建一个配置为 IPv6 的新套接字。 使用 AF_INET6 和 SOCK_STREAM 调用 socket(2)。 §Returns 成功时返回新创建的 TcpSocket。如果遇到错误，则改为返回该错误。 §示例 创建一个新的 IPv6 套接字并开始监听。 use tokio::net::TcpSocket; use std::io; #[tokio::main] async fn main() -> io::Result<()> { let addr = \"[::1]:8080\".parse().unwrap(); let socket = TcpSocket::new_v6()?; socket.bind(addr)?; let listener = socket.listen(","module_doc":"一个尚未转换为 TcpStream 或 TcpListener 的 TCP 套接字。 TcpSocket 包装一个操作系统套接字，使调用者能够在建立 TCP 连接或接受入站连接之前配置套接字。调用者可以设置套接字选项并使用套接字地址显式绑定套接字。 当 TcpSocket 值被 drop 时，底层套接字将被关闭。 只有当 TcpStream::connect 和 TcpListener::bind 使用的默认配置不满足所需的使用场景时，才应直接使用 TcpSocket。 调用 TcpStream::connect(\"127.0.0.1:8080\") 等价于：","section_headers":[]},{"url":"tokio/net/struct.TcpStream.html","crate":"tokio","title":"TcpStream","section":"struct","desc":"本地套接字和远程套接字之间的 TCP 流。 TCP 流可以通过 connect 方法连接到端点来创建，也可以通过 接受来自 监听器 的连接来创建。也可以通过 TcpSocket 类型创建 TCP 流。 对 TcpStream 的读写通常使用 AsyncReadExt 和 AsyncWriteExt trait 上的便捷方法来完成。 §示例 use tokio::net::TcpStream; use tokio::io::AsyncWriteExt; use std::error::Error; #[tokio::main] async fn main() -> Result<(), Box<dyn Error>> { // 连接到对等方 let mut stream = TcpStream::connect(\"127.0.0.1:8080\").await?; // 写入一些数据。 stream.write_all(b\"hello world!\").await?; Ok(()) } write_all 方法定义在 AsyncWriteExt trait 上。 要在写入方向关闭流，可以调用 shutdown() 方法。这将导致对等方收到长度为 0 的读取，表示不会再发送更多数据。这只会单向关闭流。 打开到远程主机的 TCP 连接。 addr 是远程主机的地址。任何实现了 ToSocketAddrs trait 的类型都可以作为地址提供。如果 addr 产生多个地址，则会依次尝试与每个地址进行连接，直到连接成功。如果没有任何地址导致成功连接，则返回最后一次连接尝试（最后一个地址）的错误。 要在连接之前配置套接字，可以使用 TcpSocket 类型。 §示例 use tokio::net::TcpStream; use tokio::io::AsyncWriteExt; use std::error::Error; #[tokio::main] async fn main() -> Result<(), Box<dyn Error>> { // 连接到对等方 let mut stream = TcpStream::connect(\"127.0.0.1:8080\").await?; // 写入一些数据。 stream.write_all(b\"hello world!\").await?; Ok(()) } write_all 方法定义在 AsyncWriteExt trait 上。 从 std::net::TcpStream 创建一个新的 TcpStream。 此函数旨在用于将标准库的 TCP 流包装为 Tokio 等价物。 §注意 调用者负责确保流处于非阻塞模式。否则，对流的所有 I/O 操作都会阻塞线程，从而导致意外行为。可以使用 set_nonblocking 设置非阻塞模式。 在阻塞模式下传入监听器始终是错误的，这种情况下行为将来可能会发生变化。例如，可能会发生 panic。 §示例 use std::error::Error; use tokio::net::TcpStream; #[tokio::main] async fn main() -> Result<(), Box<dyn Error>> { let std_stream = std::net::TcpStream::connect(\"127.0.0.1:34254\")?; std_stream.set_nonblocking(true)?; let stream = TcpStream::from_std(std_stream)?; Ok(()) }§恐慌 如果不在已启用 I/O 的运行时中调用，此函数会发生 panic。 当从由 tokio 运行时驱动的 future 中调用此函数时，运行时通常是隐式设置的；否则可以通过 Runtime::enter 函数显式设置运行时。 将 tokio::net::TcpStream 转换为 std::net::TcpStream。 返回的 std::net::TcpStream 将 nonblocking 模式设置为 true。如果需要，使用 set_nonblocking 更改阻塞模式。 §示例 use std::error::Error; use std::io::Read; use tokio::net::TcpListener; #[tokio::main] async fn main() -> Result<(), Box<dyn Error>> { let mut data = [0u8; 12]; let listener = TcpListener::bind(\"127.0.0.1:34254\").await","module_doc":"本地套接字和远程套接字之间的 TCP 流。 TCP 流可以通过 connect 方法连接到端点来创建，也可以通过 接受来自 监听器 的连接来创建。也可以通过 TcpSocket 类型创建 TCP 流。 对 TcpStream 的读写通常使用 AsyncReadExt 和 AsyncWriteExt trait 上的便捷方法来完成。","section_headers":[]},{"url":"tokio/net/struct.UdpSocket.html","crate":"tokio","title":"UdpSocket","section":"struct","desc":"一个 UDP 套接字。 UDP 与 TCP 不同，是“无连接”的。这意味着，无论你绑定到什么地址，UdpSocket 都可以自由地与许多不同的远程端通信。在 Tokio 中，基本上有两种主要的使用 UdpSocket 的方式： one to many: bind and use send_to and recv_from to communicate with many different addresses one to one: connect and associate with a single address, using send and recv to communicate only with that remote address 本类型不提供 split 方法，因为可以通过将套接字包装在 Arc 中来实现此功能。请注意，你不需要 Mutex 来共享 UdpSocket——一个 Arc<UdpSocket> 就足够了。这是因为所有方法都接受 &self 而非 &mut self。一旦将其包装在 Arc 中，你就可以对 Arc<UdpSocket> 调用 .clone() 来获得多个共享句柄以访问同一套接字。此类用法的示例可在下方找到。 §流 如果你需要通过 UDP 进行监听并产生一个 Stream，可以参考 UdpFramed。 §示例：一对多（bind） 使用 bind，我们可以创建一个简单的 echo 服务器，它与许多不同的客户端之间相互收发数据： use tokio::net::UdpSocket; use std::io; #[tokio::main] async fn main() -> io::Result<()> { let sock = UdpSocket::bind(\"0.0.0.0:8080\").await?; let mut buf = [0; 1024]; loop { let (len, addr) = sock.recv_from(&mut buf).await?; println!(\"{:?} bytes received from {:?}\", len, addr); let len = sock.send_to(&buf[..len], addr).await?; println!(\"{:?} bytes sent\", len); } }§示例：一对一（connect） 或者使用 connect，我们可以使用 send 和 recv 与单个远程地址进行 echo： use tokio::net::UdpSocket; use std::io; #[tokio::main] async fn main() -> io::Result<()> { let sock = UdpSocket::bind(\"0.0.0.0:8080\").await?; let remote_addr = \"127.0.0.1:59611\"; sock.connect(remote_addr).await?; let mut buf = [0; 1024]; loop { let len = sock.recv(&mut buf).await?; println!(\"{:?} bytes received from {:?}\", len, remote_addr); let len = sock.send(&buf[..len]).await?; println!(\"{:?} bytes sent\", len); } }§示例：使用 Arc 分割 由于 send_to 和 recv_from 接受 &self，因此完全可以使用 Arc<UdpSocket> 并将其引用共享给多个任务。下面是一个类似的 “echo” 示例，它支持并发的发送/接收： use tokio::{net::UdpSocket, sync::mpsc}; use std::{io, net::SocketAddr, sync::Arc}; #[tokio::main] async fn main() -> io::Result<()> { let sock = UdpSocket::bind(\"0.0.0.0:8080\".parse::<SocketAddr>().unwrap()).await?; let r = Arc::new(sock); let s = r.clone(); let (tx, mut rx) = mpsc::channel::<(Vec<u8>, SocketAddr)>(1_000); tokio::spawn(async move { while let Some((bytes, addr)) = rx","module_doc":"一个 UDP 套接字。 UDP 与 TCP 不同，是“无连接”的。这意味着，无论你绑定到什么地址，UdpSocket 都可以自由地与许多不同的远程端通信。在 Tokio 中，基本上有两种主要的使用 UdpSocket 的方式： 本类型不提供 split 方法，因为可以通过将套接字包装在 Arc 中来实现此功能。请注意，你不需要 Mutex 来共享 UdpSocket——一个 Arc<UdpSocket> 就足够了。这是因为所有方法都接受 &self 而非 &mut self。一旦将其包装在 Arc 中，你就可以对 Arc<UdpSocket> 调用 .clone() 来获得多个共享句柄以访问同一套接字。此类用法的示例可在下方找到。 如果你需要通过 UDP 进行监听并产生一个 Stream，可以参考 UdpFramed。 使用 bind，我们可以创建一个简单的 echo 服务器，它与许多不同的客户端之间相互收发数据：","section_headers":[]},{"url":"tokio/net/trait.ToSocketAddrs.html","crate":"tokio","title":"ToSocketAddrs","section":"trait","desc":"在不阻塞的情况下转换或解析为一个或多个 SocketAddr 值。 §DNS 字符串类型的 ToSocketAddrs 实现需要 DNS 查找。 §调用 目前，此 trait 仅用作需要引用目标套接字地址的 Tokio 函数的参数。要直接执行 SocketAddr 转换，请使用 lookup_host()。 此 trait 是封闭的，意图是不透明的。该 trait 的细节将会改变。稳定化有待 Rust 语言的增强。","module_doc":"在不阻塞的情况下转换或解析为一个或多个 SocketAddr 值。 字符串类型的 ToSocketAddrs 实现需要 DNS 查找。 目前，此 trait 仅用作需要引用目标套接字地址的 Tokio 函数的参数。要直接执行 SocketAddr 转换，请使用 lookup_host()。 此 trait 是封闭的，意图是不透明的。该 trait 的细节将会改变。稳定化有待 Rust 语言的增强。","section_headers":[]},{"url":"tokio/net/tcp/struct.OwnedReadHalf.html","crate":"tokio","title":"OwnedReadHalf","section":"struct","desc":"TcpStream 的 owned 读半部，由 into_split 创建。 从 OwnedReadHalf 读取通常使用 AsyncReadExt trait 上提供的便捷方法来完成。 尝试将 TcpStream 的两个半部分重新合并并恢复原始套接字。仅当两个半部分源自同一调用 into_split 时才会成功。 尝试在套接字上接收数据，但不会从队列中移除该数据，如果数据尚不可用，则注册当前任务以进行唤醒。 请注意，在多次调用 poll_peek 或 poll_read 时，只有传递给最近一次调用的 Context 中的 Waker 会被调度以接收唤醒。 有关更多详细信息，请参阅 TcpStream::poll_peek 的方法级文档。 §示例 use tokio::io::{self, ReadBuf}; use tokio::net::TcpStream; use std::future::poll_fn; #[tokio::main] async fn main() -> io::Result<()> { let stream = TcpStream::connect(\"127.0.0.1:8000\").await?; let (mut read_half, _) = stream.into_split(); let mut buf = [0; 10]; let mut buf = ReadBuf::new(&mut buf); poll_fn(|cx| { read_half.poll_peek(cx, &mut buf) }).await?; Ok(()) } 在套接字上从其连接的远程地址接收数据，但不会从队列中移除该数据。成功时返回窥视到的字节数。 有关更多详细信息，请参阅 TcpStream::peek 的方法级文档。 §示例 use tokio::net::TcpStream; use tokio::io::AsyncReadExt; use std::error::Error; #[tokio::main] async fn main() -> Result<(), Box<dyn Error>> { // 连接到对等方 let stream = TcpStream::connect(\"127.0.0.1:8080\").await?; let (mut read_half, _) = stream.into_split(); let mut b1 = [0; 10]; let mut b2 = [0; 10]; // 窥探数据 let n = read_half.peek(&mut b1).await?; // 读取数据 assert_eq!(n, read_half.read(&mut b2[..n]).await?); assert_eq!(&b1[..n], &b2[..n]); Ok(()) } read 方法定义在 AsyncReadExt trait 上。 等待任何所请求的就绪状态。 此函数通常与 try_read() 配合使用。它可以用来代替 readable()，以检查返回的就绪集中的 Ready::READABLE 和 Ready::READ_CLOSED 事件。 该函数可能在套接字尚未就绪时完成。这是一种误报，尝试进行操作将返回 io::ErrorKind::WouldBlock。该函数也可能返回空的 Ready 集合，因此应始终检查返回值，如果请求的状态未设置，可能需要再次等待。 此函数等价于 TcpStream::ready。 §取消安全性 此方法可安全取消。一旦就绪事件发生，该方法将继续立即返回，直到通过尝试读取或写入而消费该就绪事件（失败时返回 WouldBlock 或 Poll::Pending）。 等待套接字变为可读。 此函数等价于 ready(Interest::READABLE)，通常与 try_read() 配对使用。 此函数同样等价于 TcpStream::ready。 §取消安全性 此方法可安全取消。一旦就绪事件发生，该方法将继续立即返回，直到通过尝试读取而消费该就绪事件（失败时返回 WouldBlock 或 Poll::Pending）。 尝试从流中读取数据到提供的缓冲区，返回读取的字节数。 接收套接字上任何待处理的数据，但不会等待新数据到达。成功时返回读取的字节数。由于 try_read() 是非阻塞的，缓冲区不必由异步任务存储，可以完全存在于栈上。 通常，readable() 或 ready() 与此函数配对使用。 §返回值 如果成功读取数据，则返回 Ok(n)，其中 n 是读取的字节数。如果 n 为 0，则可能表示以下两种情况之一： The stream’s","module_doc":"TcpStream 的 owned 读半部，由 into_split 创建。 从 OwnedReadHalf 读取通常使用 AsyncReadExt trait 上提供的便捷方法来完成。","section_headers":[]},{"url":"tokio/net/tcp/struct.OwnedWriteHalf.html","crate":"tokio","title":"OwnedWriteHalf","section":"struct","desc":"TcpStream 的 owned 写半部，由 into_split 创建。 请注意，在此类型的 AsyncWrite 实现中，poll_shutdown 将关闭 TCP 流的写方向。Drop 写半部也会关闭 TCP 流的写半部。 写入 OwnedWriteHalf 通常使用 AsyncWriteExt trait 上提供的便捷方法来完成。 尝试将 TcpStream 的两个半部分重新合并并恢复原始套接字。仅当两个半部分源自同一调用 into_split 时才会成功。 销毁写半部，但在读半部被 drop 之前不会关闭流的写半部。如果读半部已被 drop，则这会关闭流。 等待任何所请求的就绪状态。 此函数通常与 try_write() 配合使用。它可以用来代替 writable()，以检查返回的就绪集中的 Ready::WRITABLE 和 Ready::WRITE_CLOSED 事件。 该函数可能在套接字尚未就绪时完成。这是一种误报，尝试进行操作将返回 io::ErrorKind::WouldBlock。该函数也可能返回空的 Ready 集合，因此应始终检查返回值，如果请求的状态未设置，可能需要再次等待。 此函数等价于 TcpStream::ready。 §取消安全性 此方法可安全取消。一旦就绪事件发生，该方法将继续立即返回，直到通过尝试读取或写入而消费该就绪事件（失败时返回 WouldBlock 或 Poll::Pending）。 等待套接字变为可写。 此函数等价于 ready(Interest::WRITABLE)，通常与 try_write() 配对使用。 §取消安全性 此方法可安全取消。一旦就绪事件发生，该方法将继续立即返回，直到通过尝试写入而消费该就绪事件（失败时返回 WouldBlock 或 Poll::Pending）。 尝试将缓冲区写入流，返回写入的字节数。 该函数将尝试写入 buf 的全部内容，但缓冲区可能只有部分被写入。 此函数通常与 writable() 配对使用。 §返回值 如果成功写入数据，则返回 Ok(n)，其中 n 是写入的字节数。如果流未准备好写入数据，则返回 Err(io::ErrorKind::WouldBlock)。 尝试将多个缓冲区写入流，返回写入的字节数。 按顺序从每个缓冲区写入数据，最后读取的缓冲区可能只被部分使用。此方法的行为等效于使用串联缓冲区对 try_write() 进行单次调用。 此函数通常与 writable() 配对使用。 §返回值 如果成功写入数据，则返回 Ok(n)，其中 n 是写入的字节数。如果流未准备好写入数据，则返回 Err(io::ErrorKind::WouldBlock)。 返回此流连接到的远程地址。 返回此流绑定的本地地址。 将此类型转换为输入类型的共享引用（通常自动推导）。 Attempt to write bytes from buf into the object. 更多信息 Like poll_write, except that it writes from a slice of buffers. 更多信息 Determines if this writer has an efficient poll_write_vectored implementation. 更多信息 Attempts to flush the object, ensuring that any buffered data reach their destination. 更多信息 Initiates or attempts to shut down this writer, returning success when the I/O connection has completely shut down. 更多信息 使用给定的格式化器格式化此值。 更多信息 执行此类型的析构函数。 更多信息 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 原样返回参数。 调用 U::from(self)。 也就是说，此转换是 From<T> for U 实现选择执行的操作。 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"TcpStream 的 owned 写半部，由 into_split 创建。 请注意，在此类型的 AsyncWrite 实现中，poll_shutdown 将关闭 TCP 流的写方向。Drop 写半部也会关闭 TCP 流的写半部。 写入 OwnedWriteHalf 通常使用 AsyncWriteExt trait 上提供的便捷方法来完成。","section_headers":[]},{"url":"tokio/net/tcp/struct.ReadHalf.html","crate":"tokio","title":"ReadHalf","section":"struct","desc":"TcpStream 的借用读半部，由 split 创建。 从 ReadHalf 读取通常使用 AsyncReadExt trait 上提供的便捷方法来完成。 尝试在套接字上接收数据，但不会从队列中移除该数据。如果数据尚未可用，则注册当前任务以接收唤醒。 请注意，在多次调用 poll_peek 或 poll_read 时，只有传递给最近一次调用的 Context 中的 Waker 会被调度以接收唤醒。 有关更多详细信息，请参阅 TcpStream::poll_peek 的方法级文档。 §示例 use tokio::io::{self, ReadBuf}; use tokio::net::TcpStream; use std::future::poll_fn; #[tokio::main] async fn main() -> io::Result<()> { let mut stream = TcpStream::connect(\"127.0.0.1:8000\").await?; let (mut read_half, _) = stream.split(); let mut buf = [0; 10]; let mut buf = ReadBuf::new(&mut buf); poll_fn(|cx| { read_half.poll_peek(cx, &mut buf) }).await?; Ok(()) } 在套接字上从其连接的远程地址接收数据，但不会从队列中移除该数据。成功时返回窥视到的字节数。 有关更多详细信息，请参阅 TcpStream::peek 的方法级文档。 §示例 use tokio::net::TcpStream; use tokio::io::AsyncReadExt; use std::error::Error; #[tokio::main] async fn main() -> Result<(), Box<dyn Error>> { // 连接到对等方 let mut stream = TcpStream::connect(\"127.0.0.1:8080\").await?; let (mut read_half, _) = stream.split(); let mut b1 = [0; 10]; let mut b2 = [0; 10]; // 窥探数据 let n = read_half.peek(&mut b1).await?; // 读取数据 assert_eq!(n, read_half.read(&mut b2[..n]).await?); assert_eq!(&b1[..n], &b2[..n]); Ok(()) } read 方法定义在 AsyncReadExt trait 上。 等待任何所请求的就绪状态。 此函数通常与 try_read() 配合使用。它可以用来代替 readable()，以检查返回的就绪集中的 Ready::READABLE 和 Ready::READ_CLOSED 事件。 该函数可能在套接字尚未就绪时完成。这是一种误报，尝试进行操作将返回 io::ErrorKind::WouldBlock。该函数也可能返回空的 Ready 集合，因此应始终检查返回值，如果请求的状态未设置，可能需要再次等待。 此函数等价于 TcpStream::ready。 §取消安全性 此方法可安全取消。一旦就绪事件发生，该方法将继续立即返回，直到通过尝试读取或写入而消费该就绪事件（失败时返回 WouldBlock 或 Poll::Pending）。 等待套接字变为可读。 此函数等价于 ready(Interest::READABLE)，通常与 try_read() 配对使用。 此函数同样等价于 TcpStream::ready。 §取消安全性 此方法可安全取消。一旦就绪事件发生，该方法将继续立即返回，直到通过尝试读取而消费该就绪事件（失败时返回 WouldBlock 或 Poll::Pending）。 尝试从流中读取数据到提供的缓冲区，返回读取的字节数。 接收套接字上任何待处理的数据，但不会等待新数据到达。成功时返回读取的字节数。由于 try_read() 是非阻塞的，缓冲区不必由异步任务存储，可以完全存在于栈上。 通常，readable() 或 ready() 与此函数配对使用。 §返回值 如果成功读取数据，则返回 Ok(n)，其中 n 是读取的字节数。如果 n 为 0，则可能表示以下两种情况之一： The stream’s read half is closed and will no longer yield data. The specified buffer was 0 byt","module_doc":"TcpStream 的借用读半部，由 split 创建。 从 ReadHalf 读取通常使用 AsyncReadExt trait 上提供的便捷方法来完成。","section_headers":[]},{"url":"tokio/net/tcp/struct.ReuniteError.html","crate":"tokio","title":"ReuniteError","section":"struct","desc":"指示两个半部分不是来自同一个套接字，因此无法重新合并的错误。 使用给定的格式化器格式化此值。 更多信息 使用给定的格式化器格式化此值。 更多信息 返回此错误的更底层来源（若有）。 更多信息 Provides type-based access to context intended for error reports. 更多信息 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 原样返回参数。 调用 U::from(self)。 也就是说，此转换是 From<T> for U 实现选择执行的操作。 Converts the given value to a String. 更多信息 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"指示两个半部分不是来自同一个套接字，因此无法重新合并的错误。","section_headers":["元组字段"]},{"url":"tokio/net/tcp/struct.WriteHalf.html","crate":"tokio","title":"WriteHalf","section":"struct","desc":"TcpStream 的借用写半部，由 split 创建。 请注意，在此类型的 AsyncWrite 实现中，poll_shutdown 将关闭 TCP 流的写方向。 写入 WriteHalf 通常使用 AsyncWriteExt trait 上提供的便捷方法来完成。 等待任何所请求的就绪状态。 此函数通常与 try_write() 配合使用。它可以用来代替 writable()，以检查返回的就绪集中的 Ready::WRITABLE 和 Ready::WRITE_CLOSED 事件。 该函数可能在套接字尚未就绪时完成。这是一种误报，尝试进行操作将返回 io::ErrorKind::WouldBlock。该函数也可能返回空的 Ready 集合，因此应始终检查返回值，如果请求的状态未设置，可能需要再次等待。 此函数等价于 TcpStream::ready。 §取消安全性 此方法可安全取消。一旦就绪事件发生，该方法将继续立即返回，直到通过尝试读取或写入而消费该就绪事件（失败时返回 WouldBlock 或 Poll::Pending）。 等待套接字变为可写。 此函数等价于 ready(Interest::WRITABLE)，通常与 try_write() 配对使用。 §取消安全性 此方法可安全取消。一旦就绪事件发生，该方法将继续立即返回，直到通过尝试写入而消费该就绪事件（失败时返回 WouldBlock 或 Poll::Pending）。 尝试将缓冲区写入流，返回写入的字节数。 该函数将尝试写入 buf 的全部内容，但缓冲区可能只有部分被写入。 此函数通常与 writable() 配对使用。 §返回值 如果成功写入数据，则返回 Ok(n)，其中 n 是写入的字节数。如果流未准备好写入数据，则返回 Err(io::ErrorKind::WouldBlock)。 尝试将多个缓冲区写入流，返回写入的字节数。 按顺序从每个缓冲区写入数据，最后读取的缓冲区可能只被部分使用。此方法的行为等效于使用串联缓冲区对 try_write() 进行单次调用。 此函数通常与 writable() 配对使用。 §返回值 如果成功写入数据，则返回 Ok(n)，其中 n 是写入的字节数。如果流未准备好写入数据，则返回 Err(io::ErrorKind::WouldBlock)。 返回此流连接到的远程地址。 返回此流绑定的本地地址。 将此类型转换为输入类型的共享引用（通常自动推导）。 Attempt to write bytes from buf into the object. 更多信息 Like poll_write, except that it writes from a slice of buffers. 更多信息 Determines if this writer has an efficient poll_write_vectored implementation. 更多信息 Attempts to flush the object, ensuring that any buffered data reach their destination. 更多信息 Initiates or attempts to shut down this writer, returning success when the I/O connection has completely shut down. 更多信息 使用给定的格式化器格式化此值。 更多信息 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 原样返回参数。 调用 U::from(self)。 也就是说，此转换是 From<T> for U 实现选择执行的操作。 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"TcpStream 的借用写半部，由 split 创建。 请注意，在此类型的 AsyncWrite 实现中，poll_shutdown 将关闭 TCP 流的写方向。 写入 WriteHalf 通常使用 AsyncWriteExt trait 上提供的便捷方法来完成。","section_headers":[]},{"url":"tokio/net/windows/named_pipe/enum.PipeEnd.html","crate":"tokio","title":"PipeEnd","section":"enum","desc":"表示命名管道的结束。 非穷尽枚举未来可能添加新的变体。因此，在对非穷尽枚举的变体进行模式匹配时，必须额外增加一个通配符分支以涵盖未来的新变体。 命名管道指的是命名管道实例的客户端。 对应 PIPE_CLIENT_END。 命名管道指的是命名管道实例的服务器端。 对应 PIPE_SERVER_END。 返回值的副本。 更多信息 Performs copy-assignment from source. 更多信息 使用给定的格式化器格式化此值。 更多信息 Feeds this value into the given Hasher. 更多信息 Feeds a slice of this type into the given Hasher. 更多信息 测试 self 与 other 值是否相等，供 == 运算符使用。 测试 != 运算符。默认实现几乎总是够用，除非有非常充分的理由，否则不应被覆盖。 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 Performs copy-assignment from self to dest. 更多信息 原样返回参数。 调用 U::from(self)。 也就是说，此转换是 From<T> for U 实现选择执行的操作。 获得所有权后的类型。 Creates owned data from borrowed data, usually by cloning. 更多信息 Uses borrowed data to replace owned data, usually by cloning. 更多信息 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"表示命名管道的结束。","section_headers":["变体 (Non-exhaustive)"]},{"url":"tokio/net/windows/named_pipe/enum.PipeMode.html","crate":"tokio","title":"PipeMode","section":"enum","desc":"命名管道的管道模式。 通过 ServerOptions::pipe_mode 设置。 非穷尽枚举未来可能添加新的变体。因此，在对非穷尽枚举的变体进行模式匹配时，必须额外增加一个通配符分支以涵盖未来的新变体。 数据作为字节流写入管道。管道不区分不同写入操作期间写入的字节。 对应 PIPE_TYPE_BYTE。 数据作为消息流写入管道。管道将每次写入操作期间写入的字节视为一个消息单元。在命名管道上的任何读取操作，当消息未被完全读取时，会返回 ERROR_MORE_DATA。 对应 PIPE_TYPE_MESSAGE。 返回值的副本。 更多信息 Performs copy-assignment from source. 更多信息 使用给定的格式化器格式化此值。 更多信息 Feeds this value into the given Hasher. 更多信息 Feeds a slice of this type into the given Hasher. 更多信息 测试 self 与 other 值是否相等，供 == 运算符使用。 测试 != 运算符。默认实现几乎总是够用，除非有非常充分的理由，否则不应被覆盖。 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 Performs copy-assignment from self to dest. 更多信息 原样返回参数。 调用 U::from(self)。 也就是说，此转换是 From<T> for U 实现选择执行的操作。 获得所有权后的类型。 Creates owned data from borrowed data, usually by cloning. 更多信息 Uses borrowed data to replace owned data, usually by cloning. 更多信息 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"命名管道的管道模式。 通过 ServerOptions::pipe_mode 设置。","section_headers":["变体 (Non-exhaustive)"]},{"url":"tokio/net/windows/named_pipe/struct.ClientOptions.html","crate":"tokio","title":"ClientOptions","section":"struct","desc":"适用于从客户端构建和与命名管道交互的 builder。 参见 ClientOptions::open。 使用默认设置创建一个新的命名管道 builder。 use tokio::net::windows::named_pipe::{ServerOptions, ClientOptions}; const PIPE_NAME: &str = r\"\\\\.\\pipe\\tokio-named-pipe-client-new\"; // 必须先创建 server，客户端创建才能成功。 let server = ServerOptions::new().create(PIPE_NAME)?; let client = ClientOptions::new().open(PIPE_NAME)?; 如果客户端支持读取数据。默认情况下这是启用的。 这对应于在调用 CreateFile 时设置 GENERIC_READ。 如果创建的管道支持写入数据。默认情况下这是启用的。 这对应于在调用 CreateFile 时设置 GENERIC_WRITE。 设置 qos 标志，这些标志会与 CreateFile 调用中的其他标志和属性组合在一起。 默认情况下，security_qos_flags 被设置为 SECURITY_IDENTIFICATION，调用此函数将完全使用指定的参数覆盖该值。 当 security_qos_flags 未设置时，如果允许打开用户指定的路径，则恶意程序可以通过欺骗特权 Rust 进程打开命名管道来获得该进程的提升特权。因此，在打开任意路径时，也应该设置 security_qos_flags。但是，这些位可能与其他标志冲突，特别是 FILE_FLAG_OPEN_NO_RECALL。 有关可能的值的信息，请参阅 Windows 开发人员中心网站上的模拟级别。使用此方法时会自动设置 SECURITY_SQOS_PRESENT 标志。 管道模式。 默认管道模式为 PipeMode::Byte。关于每种模式的含义，请参阅 PipeMode 的文档。 打开由 addr 标识的命名管道。 这使用 CreateFile 打开客户端，并将 dwCreationDisposition 选项设置为 OPEN_EXISTING。 §错误 如果在 Tokio 运行时 之外调用、在未启用 I/O 的运行时中调用，或发生任何操作系统特定的 I/O 错误，则返回错误。 在客户端创建命名管道时，需要考虑一些错误： std::io::ErrorKind::NotFound - This indicates that the named pipe does not exist. Presumably the server is not up. ERROR_PIPE_BUSY - This error is raised when the named pipe exists, but the server is not currently waiting for a connection. Please see the examples for how to check for this error. 一个等待管道可用的连接循环如下所示： use std::time::Duration; use tokio::net::windows::named_pipe::ClientOptions; use tokio::time; use windows_sys::Win32::Foundation::ERROR_PIPE_BUSY; const PIPE_NAME: &str = r\"\\\\.\\pipe\\mynamedpipe\"; let client = loop { match ClientOptions::new().open(PIPE_NAME) { Ok(client) => break client, Err(e) if e.raw_os_error() == Some(ERROR_PIPE_BUSY as i32) => (), Err(e) => return Err(e), } time::sleep(Duration::from_millis(50)).await; }; // 使用已连接的客户端。 打开由 addr 标识的命名管道。 这与 open 相同，只是它支持提供指向 SECURITY_ATTRIBUTES 结构的原始指针，该指针将作为 lpSecurityAttributes 参数传递给 CreateFile。 §安全性 attrs 参数必须为 null 或指向 SECURITY_ATTRIBUTES 结构体的有效实例。如果该参数为 null，","module_doc":"适用于从客户端构建和与命名管道交互的 builder。 参见 ClientOptions::open。","section_headers":[]},{"url":"tokio/net/windows/named_pipe/struct.NamedPipeClient.html","crate":"tokio","title":"NamedPipeClient","section":"struct","desc":"Windows 命名管道客户端。 使用 ClientOptions::open 构造。 正确连接客户端涉及几个步骤。当通过 ClientOptions::open 连接时，它可能会出错，表示以下两种情况之一： std::io::ErrorKind::NotFound - There is no server available. ERROR_PIPE_BUSY - There is a server available, but it is busy. Sleep for a while and try again. 因此，正确实现的客户端如下所示： use std::time::Duration; use tokio::net::windows::named_pipe::ClientOptions; use tokio::time; use windows_sys::Win32::Foundation::ERROR_PIPE_BUSY; const PIPE_NAME: &str = r\"\\\\.\\pipe\\named-pipe-idiomatic-client\"; let client = loop { match ClientOptions::new().open(PIPE_NAME) { Ok(client) => break client, Err(e) if e.raw_os_error() == Some(ERROR_PIPE_BUSY as i32) => (), Err(e) => return Err(e), } time::sleep(Duration::from_millis(50)).await; }; /* use the connected client */ 从指定的原始句柄构造一个新的命名管道客户端。 此函数将取得所提供句柄的所有权，将关闭句柄的责任移交给返回的对象。 此函数同样是不安全的，因为当前返回的原语带有以下约定：它们是其包装的文件描述符的唯一所有者。使用此函数可能会意外违反该约定，从而在依赖该约定的代码中导致内存不安全。 §错误 如果在 Tokio 运行时 之外调用、在未启用 I/O 的运行时中调用，或发生任何操作系统特定的 I/O 错误，则返回错误。 检索与客户端关联的命名管道的信息。 use tokio::net::windows::named_pipe::{ClientOptions, PipeEnd, PipeMode}; const PIPE_NAME: &str = r\"\\\\.\\pipe\\tokio-named-pipe-client-info\"; let client = ClientOptions::new() .open(PIPE_NAME)?; let client_info = client.info()?; assert_eq!(client_info.end, PipeEnd::Client); assert_eq!(client_info.mode, PipeMode::Message); assert_eq!(client_info.max_instances, 5); 等待任何所请求的就绪状态。 此函数通常与 try_read() 或 try_write() 配对使用。它可用于在单个任务上同时对同一管道进行读/写，而无需拆分管道。 该函数可能在管道尚未就绪时完成。这是一种误报，尝试进行操作将返回 io::ErrorKind::WouldBlock。该函数也可能返回空的 Ready 集合，因此应始终检查返回值，如果请求的状态未设置，可能需要再次等待。 §示例 在同一任务上并发地对管道进行读写，而无需拆分。 use tokio::io::Interest; use tokio::net::windows::named_pipe; use std::error::Error; use std::io; const PIPE_NAME: &str = r\"\\\\.\\pipe\\tokio-named-pipe-client-ready\"; #[tokio::main] async fn main() -> Result<(), Box<dyn Error>> { let client = named_pipe::ClientOptions::new().open(PIPE_NAME)?; loop { let ready = client.ready(Interest::READABLE | Interest::WRITABLE).await?; if ready.is_readable() { let mut data = vec![0; 1024]; // 尝试","module_doc":"Windows 命名管道客户端。 使用 ClientOptions::open 构造。 正确连接客户端涉及几个步骤。当通过 ClientOptions::open 连接时，它可能会出错，表示以下两种情况之一： 因此，正确实现的客户端如下所示：","section_headers":[]},{"url":"tokio/net/windows/named_pipe/struct.NamedPipeServer.html","crate":"tokio","title":"NamedPipeServer","section":"struct","desc":"Windows 命名管道 服务器。 接受客户端连接涉及使用 ServerOptions::create 创建服务器，并使用 NamedPipeServer::connect 等待客户端连接。 为避免客户端在连接服务器时偶尔因 std::io::ErrorKind::NotFound 而失败，我们必须确保始终至少有一个服务器实例可用。这意味着服务器的典型监听循环有点复杂，因为我们必须确保在客户端可能连接时永远不会意外地 drop 服务器。 因此，正确实现的服务器如下所示： use std::io; use tokio::net::windows::named_pipe::ServerOptions; const PIPE_NAME: &str = r\"\\\\.\\pipe\\named-pipe-idiomatic-server\"; // 需要尽早构造第一个 server，以便客户端 // 能正确连接。否则调用 .wait 会导致客户端 // 错误。 // // 这里还使用了 `first_pipe_instance`，以确保 // 当前没有其它 server 已在运行。 let mut server = ServerOptions::new() .first_pipe_instance(true) .create(PIPE_NAME)?; // 派生 server 循环。 let server = tokio::spawn(async move { loop { // 等待客户端连接。 server.connect().await?; let connected_client = server; // 在将已有的 server 发送到任务之前，先构造下一个 // 待连接的 server。这可确保 server 在任务结束前 // 不会先关闭（任务结束后），新的 server 才可用， // 否则客户端可能会报 // `io::ErrorKind::NotFound` 错误。 server = ServerOptions::new().create(PIPE_NAME)?; let client = tokio::spawn(async move { /* use the connected client */ }); } Ok::<_, io::Error>(()) }); /* do something else not server related here */ 从指定的原始句柄构造一个新的命名管道服务器。 此函数将取得所提供句柄的所有权，将关闭句柄的责任移交给返回的对象。 此函数同样是不安全的，因为当前返回的原语带有以下约定：它们是其包装的文件描述符的唯一所有者。使用此函数可能会意外违反该约定，从而在依赖该约定的代码中导致内存不安全。 §错误 如果在 Tokio 运行时 之外调用、在未启用 I/O 的运行时中调用，或发生任何操作系统特定的 I/O 错误，则返回错误。 检索与服务器关联的命名管道的信息。 use tokio::net::windows::named_pipe::{PipeEnd, PipeMode, ServerOptions}; const PIPE_NAME: &str = r\"\\\\.\\pipe\\tokio-named-pipe-server-info\"; let server = ServerOptions::new() .pipe_mode(PipeMode::Message) .max_instances(5) .create(PIPE_NAME)?; let server_info = server.info()?; assert_eq!(server_info.end, PipeEnd::Server); assert_eq!(server_info.mode, PipeMode::Message); assert_eq!(server_info.max_instances, 5); 使命名管道服务器进程能够等待客户端进程连接到命名管道的实例。客户端进程通过创建具有相同名称的命名管道来连接。 这对应于 ConnectNamedPipe 系统调用。 §取消安全性 在某种意义上此方法是取消安全的：如果在 select! 语句中将其用作事件，并且其他某个分支先完成，则不会丢失任何连接事件。 §示例 use tokio::net::windows::named_pipe::ServerOptions; const PIPE_NAME: &str = r\"\\\\.\\pipe\\mynamedpipe\"; let pipe = ServerOptions::new().create(PIPE_NAME)?; //","module_doc":"Windows 命名管道 服务器。 接受客户端连接涉及使用 ServerOptions::create 创建服务器，并使用 NamedPipeServer::connect 等待客户端连接。 为避免客户端在连接服务器时偶尔因 std::io::ErrorKind::NotFound 而失败，我们必须确保始终至少有一个服务器实例可用。这意味着服务器的典型监听循环有点复杂，因为我们必须确保在客户端可能连接时永远不会意外地 drop 服务器。 因此，正确实现的服务器如下所示：","section_headers":[]},{"url":"tokio/net/windows/named_pipe/struct.PipeInfo.html","crate":"tokio","title":"PipeInfo","section":"struct","desc":"关于命名管道的信息。 通过 NamedPipeServer::info 或 NamedPipeClient::info 构造。 指示命名管道的模式。 表示命名管道的结束。 可为该管道创建的最大实例数。 为输出缓冲区预留的字节数。 为输入缓冲区预留的字节数。 返回值的副本。 更多信息 Performs copy-assignment from source. 更多信息 使用给定的格式化器格式化此值。 更多信息 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 Performs copy-assignment from self to dest. 更多信息 原样返回参数。 调用 U::from(self)。 也就是说，此转换是 From<T> for U 实现选择执行的操作。 获得所有权后的类型。 Creates owned data from borrowed data, usually by cloning. 更多信息 Uses borrowed data to replace owned data, usually by cloning. 更多信息 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"关于命名管道的信息。 通过 NamedPipeServer::info 或 NamedPipeClient::info 构造。","section_headers":["字段 (Non-exhaustive)"]},{"url":"tokio/net/windows/named_pipe/struct.ServerOptions.html","crate":"tokio","title":"ServerOptions","section":"struct","desc":"用于构造具有命名管道特定选项的命名管道的构建器结构。想要修改管道相关选项的命名管道服务器必须使用此结构。 请参阅 ServerOptions::create。 使用默认设置创建一个新的命名管道 builder。 use tokio::net::windows::named_pipe::ServerOptions; const PIPE_NAME: &str = r\"\\\\.\\pipe\\tokio-named-pipe-new\"; let server = ServerOptions::new().create(PIPE_NAME)?; 管道模式。 默认管道模式为 PipeMode::Byte。关于每种模式的含义，请参阅 PipeMode 的文档。 这对应于在 dwPipeMode 中指定 PIPE_TYPE_ 和 PIPE_READMODE_。 管道中的数据流仅从客户端流向服务器。 这对应于设置 PIPE_ACCESS_INBOUND。 §错误 服务器端通过拒绝入站访问来阻止连接；客户端在尝试创建连接时会收到 std::io::ErrorKind::PermissionDenied 错误。 use std::io; use tokio::net::windows::named_pipe::{ClientOptions, ServerOptions}; const PIPE_NAME: &str = r\"\\\\.\\pipe\\tokio-named-pipe-access-inbound-err1\"; let _server = ServerOptions::new() .access_inbound(false) .create(PIPE_NAME)?; let e = ClientOptions::new() .open(PIPE_NAME) .unwrap_err(); assert_eq!(e.kind(), io::ErrorKind::PermissionDenied); 禁用写入允许客户端连接，但如果尝试写入则会收到 std::io::ErrorKind::PermissionDenied 错误。 use std::io; use tokio::io::AsyncWriteExt; use tokio::net::windows::named_pipe::{ClientOptions, ServerOptions}; const PIPE_NAME: &str = r\"\\\\.\\pipe\\tokio-named-pipe-access-inbound-err2\"; let server = ServerOptions::new() .access_inbound(false) .create(PIPE_NAME)?; let mut client = ClientOptions::new() .write(false) .open(PIPE_NAME)?; server.connect().await?; let e = client.write(b\"ping\").await.unwrap_err(); assert_eq!(e.kind(), io::ErrorKind::PermissionDenied);§示例 仅支持服务器到客户端通信的单向命名管道。 use std::io; use tokio::io::{AsyncReadExt, AsyncWriteExt}; use tokio::net::windows::named_pipe::{ClientOptions, ServerOptions}; const PIPE_NAME: &str = r\"\\\\.\\pipe\\tokio-named-pipe-access-inbound\"; let mut server = ServerOptions::new() .access_inbound(false) .create(PIPE_NAME)?; let mut client = ClientOptions::new() .write(false) .open(PIPE_NAME)?; server.connect().await?; let write = server.write_all(b\"ping\"); let mut buf = [0u8; 4]; let read = client.read_exact(&mut buf); let ((), read) = tokio::try_join!(write, read)?; assert_eq!(read, 4); assert_eq!(&buf[..], b\"ping\"); 管道中的数据流仅从服务器流向","module_doc":"用于构造具有命名管道特定选项的命名管道的构建器结构。想要修改管道相关选项的命名管道服务器必须使用此结构。 请参阅 ServerOptions::create。","section_headers":[]},{"url":"tokio/process/struct.Child.html","crate":"tokio","title":"Child","section":"struct","desc":"表示已生成到事件循环上的子进程。 §Caveats 与标准库的行为类似，且与 futures 范式中\"丢弃即取消\"不同， 默认情况下，已生成的进程即使在 Child 句柄被丢弃后仍将继续执行。 Command::kill_on_drop 方法可用于修改此行为， 在 Child 包装器在子进程退出前被丢弃时杀死子进程。 子进程标准输入 (stdin) 的写入 handle（若已捕获）。为避免部分移动 child 导致在使用 stdin 时无法对 child 调用其他函数，你可能需要这样做: let stdin = child.stdin.take().unwrap(); 子进程标准输出 (stdout) 的读取 handle（若已捕获）。 You might find it helpful to do let stdout = child.stdout.take().unwrap(); 这可以避免部分移动 child，从而在使用 stdout 时不会阻碍对 child 函数的调用。 子进程标准错误 (stderr) 的读取 handle（若已捕获）。 You might find it helpful to do let stderr = child.stderr.take().unwrap(); 这可以避免部分移动 child，从而在使用 stderr 时不会阻碍对 child 函数的调用。 返回与此子进程关联的、由操作系统分配的进程标识符（在子进程仍在运行时）。 一旦子进程被轮询到完成，此方法将返回 None。 这样做是为了避免在 Unix 等操作系统上引起混淆——这些平台上 进程完成后，操作系统标识符可能会被回收复用。 在子进程仍在运行时，提取与此子进程关联的原始句柄。 如果子进程已退出，则返回 None。 尝试强制子进程退出，但不等待请求生效。 On Unix platforms, this is the equivalent to sending a SIGKILL. Note 在 Unix 平台上发送 kill 信号后子进程可能成为僵尸进程；为避免这种情况，调用者应确保 either child.wait().await or child.try_wait() is invoked successfully. 强制子进程退出。 这相当于在 unix 平台上发送 SIGKILL， 然后调用 wait。 注意：标准库版本的 Child::kill 不会 wait。 在标准库中，如需等价于 Child::kill 的功能， 请使用 start_kill。 §示例 如果需要远程杀死子进程，可以使用 select! 宏 结合 oneshot 信道来实现。在以下示例中，子进程将运行到完成， 除非在 oneshot 信道上发送消息。 如果发送了消息，则会立即使用 .kill() 方法杀死子进程。 use tokio::process::Command; use tokio::sync::oneshot::channel; #[tokio::main] async fn main() { let (send, recv) = channel::<()>(); let mut child = Command::new(\"sleep\").arg(\"1\").spawn().unwrap(); tokio::spawn(async move { send.send(()) }); tokio::select! { _ = child.wait() => {} _ = recv => child.kill().await.expect(\"kill failed\"), } } 你还可以与子进程的标准 I/O 交互。例如， 可以在等待子进程退出的同时读取其 stdout。 #[tokio::main] async fn main() { let (_tx, rx) = channel::<()>(); let mut child = Command::new(\"echo\") .arg(\"Hello World!\") .stdout(Stdio::piped()) .spawn() .unwrap(); let mut stdout = child.stdout.take().expect(\"stdout is not captured\"); let read_stdout = tokio::spawn(async move { let mut buff = Vec::new(); let _ = stdout.read_to_end(&mut buff).await; buff }); tokio::select! { _ = child.wai","module_doc":"表示已生成到事件循环上的子进程。 与标准库的行为类似，且与 futures 范式中\"丢弃即取消\"不同， 默认情况下，已生成的进程即使在 Child 句柄被丢弃后仍将继续执行。 Command::kill_on_drop 方法可用于修改此行为， 在 Child 包装器在子进程退出前被丢弃时杀死子进程。","section_headers":["字段"]},{"url":"tokio/process/struct.ChildStderr.html","crate":"tokio","title":"ChildStderr","section":"struct","desc":"已生成子进程的标准错误流。 此类型实现 AsyncRead trait，可用于从子进程的 stderr 句柄异步读取数据。 转换为 OwnedHandle。 从同步 ChildStderr 创建一个异步 ChildStderr。 §错误 此方法可能在将管道设置为非阻塞模式，或在向运行时的 IO 驱动注册管道时遇到错误而失败。 Borrows the handle. 更多信息 Extracts the raw handle. 更多信息 Attempts to read from the AsyncRead into buf. 更多信息 使用给定的格式化器格式化此值。 更多信息 转换出错时返回的类型。 Gets the TypeId of self. 更多信息 创建一个新的 AsyncRead 实例，将此流与 next 链接起来。 更多信息 从此源拉取一些字节到指定的缓冲中，返回读取的字节数。 更多信息 从此源拉取一些字节到指定的缓冲中，前进缓冲的内部游标。 更多信息 Reads the exact number of bytes required to fill buf. 更多信息 Reads an unsigned 8 bit integer from the underlying reader. 更多信息 Reads a signed 8 bit integer from the underlying reader. 更多信息 Reads an unsigned 16-bit integer in big-endian order from the underlying reader. 更多信息 Reads a signed 16-bit integer in big-endian order from the underlying reader. 更多信息 Reads an unsigned 32-bit integer in big-endian order from the underlying reader. 更多信息 Reads a signed 32-bit integer in big-endian order from the underlying reader. 更多信息 Reads an unsigned 64-bit integer in big-endian order from the underlying reader. 更多信息 Reads an signed 64-bit integer in big-endian order from the underlying reader. 更多信息 Reads an unsigned 128-bit integer in big-endian order from the underlying reader. 更多信息 Reads an signed 128-bit integer in big-endian order from the underlying reader. 更多信息 Reads an 32-bit floating point type in big-endian order from the underlying reader. 更多信息 Reads an 64-bit floating point type in big-endian order from the underlying reader. 更多信息 Reads an unsigned 16-bit integer in little-endian order from the underlying reader. 更多信息 Reads a signed 16-bit integer in little-endian order from the underlying reader. 更多信息 Reads an unsigned 32-bit integer in little-endian order from the underlying reader. 更多信息 Reads a signed 32-bit integer in little-endian order from the underlying reader. 更多信息 Reads an unsigned 64-bit integer in little-endian order from the underlying reader. 更多信息 Reads an signed 64-bit integer in little-endian order from the underlying read","module_doc":"已生成子进程的标准错误流。 此类型实现 AsyncRead trait，可用于从子进程的 stderr 句柄异步读取数据。","section_headers":[]},{"url":"tokio/process/struct.ChildStdin.html","crate":"tokio","title":"ChildStdin","section":"struct","desc":"已生成子进程的标准输入流。 此类型实现 AsyncWrite trait，可用于向子进程的 stdin 句柄异步传递数据。 转换为 OwnedHandle。 从同步 ChildStdin 创建一个异步 ChildStdin。 §错误 此方法可能在将管道设置为非阻塞模式，或在向运行时的 IO 驱动注册管道时遇到错误而失败。 Borrows the handle. 更多信息 Extracts the raw handle. 更多信息 Attempt to write bytes from buf into the object. 更多信息 Attempts to flush the object, ensuring that any buffered data reach their destination. 更多信息 Initiates or attempts to shut down this writer, returning success when the I/O connection has completely shut down. 更多信息 Like poll_write, except that it writes from a slice of buffers. 更多信息 Determines if this writer has an efficient poll_write_vectored implementation. 更多信息 使用给定的格式化器格式化此值。 更多信息 转换出错时返回的类型。 Gets the TypeId of self. 更多信息 将一个缓冲写入此写入器，返回写入的字节数。 更多信息 Like write, except that it writes from a slice of buffers. 更多信息 将一个缓冲写入此写入器，前进缓冲的内部游标。 更多信息 尝试将整个缓冲写入此写入器。 更多信息 尝试将整个缓冲写入此写入器。 更多信息 Writes an unsigned 8-bit integer to the underlying writer. 更多信息 Writes a signed 8-bit integer to the underlying writer. 更多信息 Writes an unsigned 16-bit integer in big-endian order to the underlying writer. 更多信息 Writes a signed 16-bit integer in big-endian order to the underlying writer. 更多信息 Writes an unsigned 32-bit integer in big-endian order to the underlying writer. 更多信息 Writes a signed 32-bit integer in big-endian order to the underlying writer. 更多信息 Writes an unsigned 64-bit integer in big-endian order to the underlying writer. 更多信息 Writes an signed 64-bit integer in big-endian order to the underlying writer. 更多信息 Writes an unsigned 128-bit integer in big-endian order to the underlying writer. 更多信息 Writes an signed 128-bit integer in big-endian order to the underlying writer. 更多信息 Writes an 32-bit floating point type in big-endian order to the underlying writer. 更多信息 Writes an 64-bit floating point type in big-endian order to the underlying writer. 更多信息 Writes an unsigned 16-bit integer in little-endian order to the underlying writer. 更多信息 Writes a signed 16-bit integer in little-endian order to the underlying writer. ","module_doc":"已生成子进程的标准输入流。 此类型实现 AsyncWrite trait，可用于向子进程的 stdin 句柄异步传递数据。","section_headers":[]},{"url":"tokio/process/struct.ChildStdout.html","crate":"tokio","title":"ChildStdout","section":"struct","desc":"已生成子进程的标准输出流。 此类型实现 AsyncRead trait，可用于从子进程的 stdout 句柄异步读取数据。 转换为 OwnedHandle。 从同步 ChildStdout 创建一个异步 ChildStdout。 §错误 此方法可能在将管道设置为非阻塞模式，或在向运行时的 IO 驱动注册管道时遇到错误而失败。 Borrows the handle. 更多信息 Extracts the raw handle. 更多信息 Attempts to read from the AsyncRead into buf. 更多信息 使用给定的格式化器格式化此值。 更多信息 转换出错时返回的类型。 Gets the TypeId of self. 更多信息 创建一个新的 AsyncRead 实例，将此流与 next 链接起来。 更多信息 从此源拉取一些字节到指定的缓冲中，返回读取的字节数。 更多信息 从此源拉取一些字节到指定的缓冲中，前进缓冲的内部游标。 更多信息 Reads the exact number of bytes required to fill buf. 更多信息 Reads an unsigned 8 bit integer from the underlying reader. 更多信息 Reads a signed 8 bit integer from the underlying reader. 更多信息 Reads an unsigned 16-bit integer in big-endian order from the underlying reader. 更多信息 Reads a signed 16-bit integer in big-endian order from the underlying reader. 更多信息 Reads an unsigned 32-bit integer in big-endian order from the underlying reader. 更多信息 Reads a signed 32-bit integer in big-endian order from the underlying reader. 更多信息 Reads an unsigned 64-bit integer in big-endian order from the underlying reader. 更多信息 Reads an signed 64-bit integer in big-endian order from the underlying reader. 更多信息 Reads an unsigned 128-bit integer in big-endian order from the underlying reader. 更多信息 Reads an signed 128-bit integer in big-endian order from the underlying reader. 更多信息 Reads an 32-bit floating point type in big-endian order from the underlying reader. 更多信息 Reads an 64-bit floating point type in big-endian order from the underlying reader. 更多信息 Reads an unsigned 16-bit integer in little-endian order from the underlying reader. 更多信息 Reads a signed 16-bit integer in little-endian order from the underlying reader. 更多信息 Reads an unsigned 32-bit integer in little-endian order from the underlying reader. 更多信息 Reads a signed 32-bit integer in little-endian order from the underlying reader. 更多信息 Reads an unsigned 64-bit integer in little-endian order from the underlying reader. 更多信息 Reads an signed 64-bit integer in little-endian order from the underlying read","module_doc":"已生成子进程的标准输出流。 此类型实现 AsyncRead trait，可用于从子进程的 stdout 句柄异步读取数据。","section_headers":[]},{"url":"tokio/process/struct.Command.html","crate":"tokio","title":"Command","section":"struct","desc":"此结构体模仿标准库中的 std::process::Command API，但将用于创建进程的函数替换为 异步变体。主要提供的 异步函数为 spawn、status 和 output。 Command 使用某些 std 类型的异步版本（例如 Child）。 构造一个新的 Command，用于在 路径 program 启动程序，使用以下默认配置： No arguments to the program Inherit the current process’s environment Inherit the current process’s working directory Inherit stdin/stdout/stderr for spawn or status, but create pipes for output 提供了 Builder 方法来修改这些默认值并 otherwise configure the process. 如果 program 不是绝对路径，将以 操作系统定义的方式搜索 PATH。 要使用的搜索路径可以通过 在 Command 上设置 PATH 环境变量来控制，但这在 Windows 上有一些 实现限制 （参见 issue rust-lang/rust#37519）。 §示例 基本用法： use tokio::process::Command; let mut command = Command::new(\"sh\"); 廉价地转换为 &std::process::Command，用于期望使用标准库类型的位置。 廉价地转换为 &mut std::process::Command，用于期望使用标准库类型的位置。 廉价地转换为 std::process::Command。 请注意，Tokio 特定选项将丢失。目前，这仅适用于 kill_on_drop。 添加传递给程序的参数。 每次调用只能传递一个参数。因此，请勿这样： let mut command = tokio::process::Command::new(\"sh\"); command.arg(\"-C /path/to/repo\"); 用法如下： let mut command = tokio::process::Command::new(\"sh\"); command.arg(\"-C\"); command.arg(\"/path/to/repo\"); 若要传递多个参数，请参阅 args。 §示例 基本用法： use tokio::process::Command; let output = Command::new(\"ls\") .arg(\"-l\") .arg(\"-a\") .output().await.unwrap(); 添加多个传递给程序的参数。 要传递单个参数，请参阅 arg。 §示例 基本用法： use tokio::process::Command; let output = Command::new(\"ls\") .args(&[\"-l\", \"-a\"]) .output().await.unwrap(); 向命令行追加字面文本，不进行任何引号或转义。 这对于将参数传递给 cmd.exe /c 很有用，因为它不遵循 CommandLineToArgvW 的转义规则。 插入或更新一个环境变量映射。 请注意，在 Windows 上环境变量名称不区分大小写（但保留大小写）， 而在所有其他平台上区分大小写。 §示例 基本用法： use tokio::process::Command; let output = Command::new(\"ls\") .env(\"PATH\", \"/bin\") .output().await.unwrap(); 添加或更新多个环境变量映射。 §示例 基本用法： use tokio::process::Command; use std::process::{Stdio}; use std::env; use std::collections::HashMap; let filtered_env : HashMap<String, String> = env::vars().filter(|&(ref k, _)| k == \"TERM\" || k == \"TZ\" || k == \"LANG\" || k == \"PATH\" ).collect(); let output = Command::new(\"printenv\") .stdin(Stdio::null()) .stdout(Stdio::inherit()) .env_clear() .envs(&filtered_env) .output().await.unwr","module_doc":"此结构体模仿标准库中的 std::process::Command API，但将用于创建进程的函数替换为 异步变体。主要提供的 异步函数为 spawn、status 和 output。 Command 使用某些 std 类型的异步版本（例如 Child）。","section_headers":[]},{"url":"tokio/runtime/enum.RuntimeFlavor.html","crate":"tokio","title":"RuntimeFlavor","section":"enum","desc":"Runtime 的风格。 这是 Handle::runtime_flavor 的返回类型。 非穷尽枚举未来可能添加新的变体。因此，在对非穷尽枚举的变体进行模式匹配时，必须额外增加一个通配符分支以涵盖未来的新变体。 在当前线程上执行所有任务的风格。 跨多个线程执行任务的风格。 使用给定的格式化器格式化此值。 更多信息 测试 self 与 other 值是否相等，供 == 运算符使用。 测试 != 运算符。默认实现几乎总是够用，除非有非常充分的理由，否则不应被覆盖。 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 原样返回参数。 调用 U::from(self)。 也就是说，此转换是 From<T> for U 实现选择执行的操作。 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"Runtime 的风格。 这是 Handle::runtime_flavor 的返回类型。","section_headers":["变体 (Non-exhaustive)"]},{"url":"tokio/runtime/fn.is_rt_shutdown_err.html","crate":"tokio","title":"is_rt_shutdown_err","section":"fn","desc":"检查给定的错误是否由 Tokio 在关闭其运行时时发出。 §示例 use tokio::runtime::Runtime; use tokio::net::TcpListener; fn main() { let rt1 = Runtime::new().unwrap(); let rt2 = Runtime::new().unwrap(); let listener = rt1.block_on(async { TcpListener::bind(\"127.0.0.1:0\").await.unwrap() }); drop(rt1); rt2.block_on(async { let res = listener.accept().await; assert!(res.is_err()); assert!(tokio::runtime::is_rt_shutdown_err(res.as_ref().unwrap_err())); }); }","module_doc":"检查给定的错误是否由 Tokio 在关闭其运行时时发出。","section_headers":[]},{"url":"tokio/runtime/struct.Builder.html","crate":"tokio","title":"Builder","section":"struct","desc":"使用自定义配置值构建 Tokio 运行时。 可以按链式调用各方法来设置配置值。运行时通过调用 build 来构造。 通过 Builder::new_multi_thread 或 Builder::new_current_thread 获取 Builder 的新实例。 有关各种配置选项的详细信息，请参阅函数级文档。 §示例 use tokio::runtime::Builder; fn main() { // 构建运行时 let runtime = Builder::new_multi_thread() .worker_threads(4) .thread_name(\"my-custom-name\") .thread_stack_size(3 * 1024 * 1024) .build() .unwrap(); // 使用运行时 ... } 返回一个选择了当前线程调度器的新构建器。 可以在返回值上链式调用各配置方法。 要在所得到的运行时上派生非 Send 任务，需要将其与 LocalSet 组合使用，或者调用 build_local 来创建 LocalRuntime。 同时启用 I/O 和时间驱动。 这是单独调用 enable_io 和 enable_time 的简写。如果未来 Tokio 中添加了其他组件， enable_all 也会包括这些未来的组件。 §示例 use tokio::runtime; let rt = runtime::Builder::new_multi_thread() .enable_all() .build() .unwrap(); 设置 Runtime 将使用的工作线程数。 该值可以是大于 0 的任何数字，不过建议保持一个较小的值。 这会覆盖从环境变量 TOKIO_WORKER_THREADS 读取的值。 §默认值 默认值是系统可用的 CPU 核心数。 使用 current_thread 运行时，此方法无效。 §示例§具有 4 个线程的多线程运行时 use tokio::runtime; // 这将派生一个具有 4 个工作线程的工作窃取型运行时。 let rt = runtime::Builder::new_multi_thread() .worker_threads(4) .build() .unwrap(); rt.spawn(async move {});§当前线程运行时（仅通过 Runtime::block_on 在当前线程上运行） use tokio::runtime; // 创建一个必须通过调用 // `Runtime::block_on` 来驱动的运行时。 let rt = runtime::Builder::new_current_thread() .build() .unwrap(); // 这将在当前线程上运行运行时和 future rt.block_on(async move {});§恐慌 如果 val 不大于 0，则会发生 panic。 指定运行时派生的额外线程的上限。 这些线程用于阻塞操作，例如通过 spawn_blocking 派生的任务，包括但不限于： fs 操作 通过 ToSocketAddrs 进行 DNS 解析 写入 Stdout 或 Stderr 从 Stdin 读取 与 worker_threads 不同，这些线程并非始终处于活动状态， 如果空闲时间过长会退出。可以使用 thread_keep_alive 更改该超时时长。 建议不要将该上限设置得过低，以避免在需要 spawn_blocking 的操作上发生挂起。 默认值是 512。 §队列行为 当提交一个阻塞任务时，它会被插入一个队列中。如果有空闲线程，则会通知其中一个线程来运行该任务。否则， 如果尚未达到本方法设置的阈值，则会派生一个新的线程。如果没有可用的空闲线程，并且也不再允许派生新线程， 那么该任务将一直留在队列中，直到某个繁忙的线程将其取出。需要注意的是，由于队列不施加任何背压， 因此它可能会无限增长。 §恐慌 如果 val 不大于 0，则会发生 panic。 §从 0.x 升级 在旧版本中，max_threads 同时限制阻塞线程和工作线程的数量， 但当前的 max_blocking_threads 不再将异步工作线程计入该数量中。 设置 Runtime 线程池所派生线程的名称。 默认名称为 “tokio-rt-worker”。 §示例 let rt = runtime::Builder::new_multi_thread() .thread_name(\"my-pool\") .build(); 设置运行时的名称。 §示例 let rt = runtime::Builder::new_multi_","module_doc":"使用自定义配置值构建 Tokio 运行时。 可以按链式调用各方法来设置配置值。运行时通过调用 build 来构造。 通过 Builder::new_multi_thread 或 Builder::new_current_thread 获取 Builder 的新实例。 有关各种配置选项的详细信息，请参阅函数级文档。","section_headers":[]},{"url":"tokio/runtime/struct.EnterGuard.html","crate":"tokio","title":"EnterGuard","section":"struct","desc":"运行时上下文守卫。 由 Runtime::enter 和 Handle::enter 返回，上下文守卫在 drop 时退出运行时上下文。 使用给定的格式化器格式化此值。 更多信息 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 原样返回参数。 调用 U::from(self)。 也就是说，此转换是 From<T> for U 实现选择执行的操作。 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"运行时上下文守卫。 由 Runtime::enter 和 Handle::enter 返回，上下文守卫在 drop 时退出运行时上下文。","section_headers":[]},{"url":"tokio/runtime/struct.Handle.html","crate":"tokio","title":"Handle","section":"struct","desc":"运行时句柄。 该句柄内部是引用计数的，可以自由克隆。可以使用 Runtime::handle 方法获取句柄。 进入运行时上下文。这允许你构造在创建时必须有可用的执行器的类型，例如 Sleep 或 TcpStream。它还允许你调用 tokio::spawn 和 Handle::current 等方法而不会发生 panic。 §恐慌 当多次调用 Handle::enter 时，返回的 guard 必须以获取它们的相反顺序 drop。否则将导致 panic 和可能的内存泄漏。 §示例 use tokio::runtime::Runtime; let rt = Runtime::new().unwrap(); let _guard = rt.enter(); tokio::spawn(async { println!(\"Hello world!\"); }); 不要执行以下操作，这显示了一个会导致 panic 和可能的内存泄漏的场景。 ⓘuse tokio::runtime::Runtime; let rt1 = Runtime::new().unwrap(); let rt2 = Runtime::new().unwrap(); let enter1 = rt1.enter(); let enter2 = rt2.enter(); drop(enter1); drop(enter2); 返回当前正在运行的 Runtime 的 Handle 视图。 §恐慌 如果在 Tokio 运行时的上下文之外调用此函数，则会发生 panic。这意味着你必须在由运行时运行的某个线程上调用它，或者从具有活动 EnterGuard 的线程调用。例如，从 std::thread::spawn 创建的线程中调用会导致 panic，除非该线程具有活动的 EnterGuard。 §示例 这可用于从在该运行时上运行的 async 块或函数获取周围运行时的句柄。 use tokio::runtime::Handle; // 在 async 块或函数内。 let handle = Handle::current(); handle.spawn(async { println!(\"now running in the existing Runtime\"); }); thread::spawn(move || { // 注意，handle 是在该线程外创建然后移入的 handle.spawn(async { /* ... */ }); // 下面这一行会导致 panic，因为我们尚未进入 runtime // 并创建 EnterGuard // let handle2 = Handle::current(); // panic // 因此我们在这里用 Handle::enter() 创建一个 guard； let _guard = handle.enter(); // 现在我们可以调用 Handle::current()； let handle2 = Handle::current(); }); 返回当前正在运行的 Runtime 的 Handle 视图 如果尚未启动 Runtime，则返回错误 与 current 不同，此函数从不发生 panic 将 future 派生到 Tokio 运行时。 这会将给定的 future 派生到运行时的执行器上，通常是线程池。然后由该线程池负责轮询该 future 直到其完成。 当调用 spawn 时，提供的 future 将立即开始在后台运行，即使你未 await 返回的 JoinHandle 也是如此（假设运行时正在运行）。 有关更多详细信息，请参阅 模块级 文档。 §示例 use tokio::runtime::Runtime; // 创建运行时 let rt = Runtime::new().unwrap(); // 从此运行时获取一个句柄 let handle = rt.handle(); // 使用 handle 在 runtime 上派生 future handle.spawn(async { println!(\"now running on a worker thread\"); }); 在专用于阻塞操作的执行器上运行所提供的函数。 §示例 use tokio::runtime::Runtime; // 创建运行时 let rt = Runtime::new().unwrap(); // 从此运行时获取一个句柄 let handle = rt.handle(); // 使用 handle 在 runtime 上派生阻塞函数 handle.spawn_blocking(|| { println!(\"now running o","module_doc":"运行时句柄。 该句柄内部是引用计数的，可以自由克隆。可以使用 Runtime::handle 方法获取句柄。","section_headers":[]},{"url":"tokio/runtime/struct.Id.html","crate":"tokio","title":"Id","section":"struct","desc":"唯一标识一个运行时（相对于所有其他当前正在运行的运行时）的不透明 ID。 §注意 Runtime IDs are unique relative to other currently running runtimes. When a runtime completes, the same ID may be used for another runtime. Runtime IDs are not sequential, and do not indicate the order in which runtimes are started or any other data. The runtime ID of the currently running task can be obtained from the Handle. §示例 use tokio::runtime::Handle; #[tokio::main(flavor = \"multi_thread\", worker_threads = 4)] async fn main() { println!(\"Current runtime id: {}\", Handle::current().id()); } 返回值的副本。 更多信息 Performs copy-assignment from source. 更多信息 使用给定的格式化器格式化此值。 更多信息 使用给定的格式化器格式化此值。 更多信息 Feeds this value into the given Hasher. 更多信息 Feeds a slice of this type into the given Hasher. 更多信息 测试 self 与 other 值是否相等，供 == 运算符使用。 测试 != 运算符。默认实现几乎总是够用，除非有非常充分的理由，否则不应被覆盖。 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 Performs copy-assignment from self to dest. 更多信息 原样返回参数。 调用 U::from(self)。 也就是说，此转换是 From<T> for U 实现选择执行的操作。 获得所有权后的类型。 Creates owned data from borrowed data, usually by cloning. 更多信息 Uses borrowed data to replace owned data, usually by cloning. 更多信息 Converts the given value to a String. 更多信息 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"唯一标识一个运行时（相对于所有其他当前正在运行的运行时）的不透明 ID。","section_headers":[]},{"url":"tokio/runtime/struct.LocalOptions.html","crate":"tokio","title":"LocalOptions","section":"struct","desc":"仅 LocalRuntime 的配置选项 目前，还没有此类选项，但将来可能会添加诸如 !Send + !Sync 钩子之类的东西。 使用 LocalOptions::default() 创建默认选项集。此类型与 Builder::build_local 一起使用。 使用给定的格式化器格式化此值。 更多信息 Returns the “default value” for a type. 更多信息 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 原样返回参数。 调用 U::from(self)。 也就是说，此转换是 From<T> for U 实现选择执行的操作。 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"仅 LocalRuntime 的配置选项 目前，还没有此类选项，但将来可能会添加诸如 !Send + !Sync 钩子之类的东西。 使用 LocalOptions::default() 创建默认选项集。此类型与 Builder::build_local 一起使用。","section_headers":[]},{"url":"tokio/runtime/struct.LocalRuntime.html","crate":"tokio","title":"LocalRuntime","section":"struct","desc":"本地 Tokio 运行时。 此运行时能够在不使用 LocalSet 的情况下驱动不是 Send + Sync 的任务，因此支持 spawn_local 而无需 LocalSet 上下文。 此运行时不能在线程之间移动，也不能从不同线程驱动。 此运行时与 LocalSet 不兼容。你不应尝试在 LocalRuntime 内驱动 LocalSet。 目前，此运行时支持一种风格，其内部与 current_thread 完全相同，只是与 spawn_local 相关的上述差异除外。 有关如何使用运行时的更多一般信息，请参阅 模块 文档。 使用默认配置值创建一个新的本地运行时实例。 这将导致调度器、I/O 驱动和时间驱动被初始化。 当需要更复杂的配置时，可以使用 运行时构建器。 有关更多详细信息，请参阅模块级文档。 §示例 使用默认配置值创建一个新的 LocalRuntime。 use tokio::runtime::LocalRuntime; let rt = LocalRuntime::new() .unwrap(); // 使用 runtime... 返回运行时 spawner 的句柄。 返回的句柄可用于派生在此运行时上运行的任务，并且可以克隆以便将 Handle 移到其他线程。 由于该句柄可以发送到其他线程，因此它只能用于生成 Send 的任务。 在 LocalRuntime 的句柄上调用 Handle::block_on 容易出错。有关更多信息，请参阅 Handle::block_on 的文档。 §示例 use tokio::runtime::LocalRuntime; let rt = LocalRuntime::new() .unwrap(); let handle = rt.handle(); // 使用 handle... 在运行时上生成一个任务。 这类似于标准 Runtime 上的 spawn 方法，但即使任务不是线程安全的也能工作。 §示例 use tokio::runtime::LocalRuntime; // 创建运行时 let rt = LocalRuntime::new().unwrap(); // 在 runtime 上派生 future rt.spawn_local(async { println!(\"now running on a worker thread\"); }); 在专用阻塞线程池中的某个线程上运行所提供的函数。 此函数将在另一个线程上运行。 有关更多信息，请参阅非本地运行时中的文档。 §示例 use tokio::runtime::LocalRuntime; // 创建运行时 let rt = LocalRuntime::new().unwrap(); // 在 runtime 上派生阻塞函数 rt.spawn_blocking(|| { println!(\"now running on a worker thread\"); }); 在 Tokio 运行时上运行 future 直到完成。这是运行时的入口点。 有关更多信息，请参阅Runtime 上等价方法的文档。 §示例 use tokio::runtime::LocalRuntime; // 创建运行时 let rt = LocalRuntime::new().unwrap(); // 执行 future，阻塞当前线程直到完成 rt.block_on(async { println!(\"hello\"); }); 进入运行时上下文。 这允许你构造在创建时必须有执行器可用的类型，例如 Sleep 或 TcpStream。它还允许你调用诸如 tokio::spawn 之类的方法。 如果这是 LocalRuntime 的句柄，并且此函数是从创建运行时的同一线程调用的，那么你也能够调用 tokio::task::spawn_local。 §示例 use tokio::runtime::LocalRuntime; use tokio::task::JoinHandle; fn function_that_spawns(msg: String) -> JoinHandle<()> { // 如果下面没有使用 `rt.enter`，这里会 panic。 tokio::spawn(async move { println!(\"{}\", msg); }) } fn main() { let rt = LocalRuntime::new().unwrap(); let s = \"Hello World!\".to_string(); // 通过进入此上下文，我们将 `tokio::spawn` 绑定到此执行器。 let _guard = rt.enter(); l","module_doc":"本地 Tokio 运行时。 此运行时能够在不使用 LocalSet 的情况下驱动不是 Send + Sync 的任务，因此支持 spawn_local 而无需 LocalSet 上下文。 此运行时不能在线程之间移动，也不能从不同线程驱动。 此运行时与 LocalSet 不兼容。你不应尝试在 LocalRuntime 内驱动 LocalSet。 目前，此运行时支持一种风格，其内部与 current_thread 完全相同，只是与 spawn_local 相关的上述差异除外。 有关如何使用运行时的更多一般信息，请参阅 模块 文档。","section_headers":[]},{"url":"tokio/runtime/struct.Runtime.html","crate":"tokio","title":"Runtime","section":"struct","desc":"Tokio 运行时。 运行时提供了运行异步任务所需的 I/O 驱动、任务调度器、计时器 以及阻塞线程池。 Runtime 实例可以使用 new 或 Builder 创建。不过，大多数用户会在入口点使用 #[tokio::main] 属性注解。 有关更多详细信息，请参阅 模块级 文档。 §关闭 关闭运行时是通过 drop 该值或调用 shutdown_background 或 shutdown_timeout 来完成的。 通过 Runtime::spawn 生成的任务会一直运行，直到它们让步。然后它们会被 drop。它们不保证会运行到完成，但如果它们在完成之前不让步，则可能会运行到完成。 通过 Runtime::spawn_blocking 生成的阻塞函数会一直运行，直到它们返回。 发起关闭的线程会一直阻塞，直到所有生成的工作都已停止。这可能需要无限长的时间。Drop 实现会永远等待这一过程。 如果不想永远等待，可以使用 shutdown_background 和 shutdown_timeout 方法。当达到超时时，未及时停止的已生成工作以及运行它们的线程会被泄漏。工作会继续运行，直到满足停止条件之一，但发起关闭的线程将不再阻塞。 一旦运行时被 drop，绑定到它的所有未完成的 I/O 资源将不再可用。对它们调用任何方法都将导致错误。 §共享 有几种方法可以建立对 Tokio 运行时的共享访问： Using an Arc<Runtime>. Using a Handle. Entering the runtime context. 使用 Arc<Runtime> 或 Handle 允许你对运行时执行各种操作，例如生成新任务或进入运行时上下文。这两种类型都可以克隆，以创建一个允许访问同一运行时的新句柄。通过将克隆传递到不同的任务或线程，你将能够从这些任务或线程访问运行时。 Arc<Runtime> 与 Handle 之间的区别在于，Arc<Runtime> 会阻止运行时关闭，而 Handle 则不会阻止。这是因为运行时的关闭发生在 Runtime 对象的析构函数运行时。 对 shutdown_background 和 shutdown_timeout 的调用需要 Runtime 类型的独占所有权。在使用 Arc<Runtime> 时，当只剩下一个强引用计数时，可以通过 Arc::try_unwrap 来实现这一点。 运行时上下文是通过 Runtime::enter 或 Handle::enter 方法进入的，这些方法使用线程局部变量来存储当前运行时。只要你在运行时上下文中，像 tokio::spawn 这样的方法将使用你所在上下文中的运行时。 返回运行时 spawner 的句柄。 返回的句柄可用于派生在此运行时上运行的任务，并且可以克隆以便将 Handle 移到其他线程。 在 current_thread 运行时的句柄上调用 Handle::block_on 容易出错。有关更多信息，请参阅 Handle::block_on 的文档。 §示例 use tokio::runtime::Runtime; let rt = Runtime::new() .unwrap(); let handle = rt.handle(); // 使用 handle... 将 future 派生到 Tokio 运行时。 这会将给定的 future 派生到运行时的执行器上，通常是线程池。然后由该线程池负责轮询该 future 直到其完成。 当调用 spawn 时，提供的 future 将立即开始在后台运行，即使你未 await 返回的 JoinHandle 也是如此（假设运行时正在运行）。 有关更多详细信息，请参阅 模块级 文档。 §示例 use tokio::runtime::Runtime; // 创建运行时 let rt = Runtime::new().unwrap(); // 在 runtime 上派生 future rt.spawn(async { println!(\"now running on a worker thread\"); }); 在专用于阻塞操作的执行器上运行所提供的函数。 §示例 use tokio::runtime::Runtime; // 创建运行时 let rt = Runtime::new().unwrap(); // 在 runtime 上派生阻塞函数 rt.spawn_blocking(|| { println!(\"now running on a worker thread\"); }); 在 Tokio 运行时上运行 future 直到完成。这是运行时的入口点。 这会在当前线程上运行给定的 future，阻塞直到","module_doc":"Tokio 运行时。 运行时提供了运行异步任务所需的 I/O 驱动、任务调度器、计时器 以及阻塞线程池。 Runtime 实例可以使用 new 或 Builder 创建。不过，大多数用户会在入口点使用 #[tokio::main] 属性注解。 有关更多详细信息，请参阅 模块级 文档。 关闭运行时是通过 drop 该值或调用 shutdown_background 或 shutdown_timeout 来完成的。 通过 Runtime::spawn 生成的任务会一直运行，直到它们让步。然后它们会被 drop。它们不保证会运行到完成，但如果它们在完成之前不让步，则可能会运行到完成。 通过 Runtime::spawn_blocking 生成的阻塞函数会一直运行，直到它们返回。 发起关闭的线程会一直阻塞，直到所有生成的工作都已停止。这可能需要无限长的时间。Drop 实现会永远等待这一过程。 如果不想永远等待，可以使用 shutdown_background 和 shutdown_timeout 方法。当达到超时时，未及时停止的已生成工作以及运行它们的线程会被泄漏。工作会继续运行，直到满足停止","section_headers":[]},{"url":"tokio/runtime/struct.RuntimeMetrics.html","crate":"tokio","title":"RuntimeMetrics","section":"struct","desc":"运行时指标的句柄。 此句柄内部使用引用计数，可以自由克隆。可使用 Runtime::metrics 方法获取 RuntimeMetrics 句柄。 返回运行时使用的工作线程数。 工作线程数通过在 runtime::Builder 上配置 worker_threads 来设置。使用 current_thread 运行时时，返回值始终为 1。 §示例 use tokio::runtime::Handle; let metrics = Handle::current().metrics(); let n = metrics.num_workers(); println!(\"Runtime is using {} workers\", n); 返回运行时中当前存活的任务数。 当任务生成时此计数器增加，当任务退出时此计数器减少。 注意：在使用多线程运行时时，此数字可能不具有强一致性，即可能没有任务在运行，但指标却报告有任务运行。 §示例 use tokio::runtime::Handle; let metrics = Handle::current().metrics(); let n = metrics.num_alive_tasks(); println!(\"Runtime has {} alive tasks\", n); 返回运行时全局队列中当前调度的任务数。 从非运行时线程生成或通知的任务使用运行时的全局队列进行调度。此指标返回全局队列中当前挂起任务的数量。因此，随着新任务的调度和处理，返回值可能会增加或减少。 §示例 use tokio::runtime::Handle; let metrics = Handle::current().metrics(); let n = metrics.global_queue_depth(); println!(\"{} tasks currently pending in the runtime's global queue\", n); 返回给定工作线程处于忙碌状态的时间量。 工作线程忙碌持续时间在运行时创建时从零开始，并在工作线程花费时间处理工作时增加。使用此值可以指示给定工作线程的负载。如果花费大量时间处于忙碌状态，则工作线程处于负载状态，并且将更少地检查入站事件。 计时器单调递增。它永远不会减少或重置为零。 §Arguments worker 是被查询的工作线程的索引。给定值必须介于 0 和 num_workers() 之间。该索引唯一标识一个工作线程，并将在运行时实例的整个生命周期内持续标识该工作线程。 §恐慌 当 worker 表示无效的工作线程时（即大于或等于 num_workers()），该方法会发生 panic。 §示例 use tokio::runtime::Handle; let metrics = Handle::current().metrics(); let n = metrics.worker_total_busy_duration(0); println!(\"worker 0 was busy for a total of {:?}\", n); 返回给定工作线程已 park 的总次数。 工作线程 park 计数在运行时创建时从零开始，每次工作线程 park 线程等待新的入站事件处理时增加一。这通常意味着工作线程已处理完所有挂起的工作，当前处于空闲状态。 计数器单调递增。它永不会递减或重置为零。 §Arguments worker 是被查询的工作线程的索引。给定值必须介于 0 和 num_workers() 之间。该索引唯一标识一个工作线程，并将在运行时实例的整个生命周期内持续标识该工作线程。 §恐慌 当 worker 表示无效的工作线程时（即大于或等于 num_workers()），该方法会发生 panic。 §示例 use tokio::runtime::Handle; let metrics = Handle::current().metrics(); let n = metrics.worker_park_count(0); println!(\"worker 0 parked {} times\", n); 返回给定工作线程已 park 和 unpark 的总次数。 The worker 工作线程 park/unpark 计数在运行时创建时从零开始，每次工作线程 park 线程等待新的入站事件处理时增加一。这通常意味着工作线程已处理完所有挂起的工作，当前处于空闲状态。当新工作变得可用时，工作线程被 unpark，park/unpark 计数再次增加一。 奇数计数表示工作线程当前已 park。偶数计数表示工作线程当前处于活动状态。 计数器单调递增。它永不会递","module_doc":"运行时指标的句柄。 此句柄内部使用引用计数，可以自由克隆。可使用 Runtime::metrics 方法获取 RuntimeMetrics 句柄。","section_headers":[]},{"url":"tokio/runtime/struct.TryCurrentError.html","crate":"tokio","title":"TryCurrentError","section":"struct","desc":"当没有 Runtime 启动时，由 try_current 返回的错误 如果调用失败是因为 Tokio 上下文中当前没有运行时，则返回 true。 如果调用失败是因为 Tokio 上下文线程局部已被销毁，则返回 true。这通常仅在其他线程局部的析构函数中才会发生。 使用给定的格式化器格式化此值。 更多信息 使用给定的格式化器格式化此值。 更多信息 返回此错误的更底层来源（若有）。 更多信息 Provides type-based access to context intended for error reports. 更多信息 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 原样返回参数。 调用 U::from(self)。 也就是说，此转换是 From<T> for U 实现选择执行的操作。 Converts the given value to a String. 更多信息 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"当没有 Runtime 启动时，由 try_current 返回的错误","section_headers":[]},{"url":"tokio/signal/fn.ctrl_c.html","crate":"tokio","title":"ctrl_c","section":"fn","desc":"当向进程发送âctrl-câ通知时完成。 虽然 Unix 和 Windows 处理信号的方式差异很大，但 两个平台都支持在âctrl-câ上接收信号。 此函数提供用于接收此通知的 可移植 API。 返回的 future 一旦被轮询，就会注册一个监听器。 此 future 将在首次接收到 ctrl-c 时完成， 该之后的初始调用为 Future::poll 或 .await。 §Caveats 在 Unix 平台上，第一次为 特定信号类型注册 Signal 实例时， 会安装一个操作系统信号处理程序， 该程序在接收到该信号时将替换 默认的平台行为，持续 整个进程期间。 例如，Unix 系统默认会在收到 终端上 \"CTRL+C\" 生成的信号时终止进程。但是，当创建 ctrl_c 流以监听此信号时，在其到达时， 它将被转换为流事件，并且进程将继续 执行。即使此 Signal 实例被丢弃， 后续的 SIGINT 传递最终也会被 Tokio 捕获，并且默认平台行为 将不会重置。 因此，应用程序应小心以确保 在监听特定信号后预期的信号行为 按预期发生。 §示例 use tokio::signal; #[tokio::main] async fn main() { println!(\"waiting for ctrl-c\"); signal::ctrl_c().await.expect(\"failed to listen for event\"); println!(\"received ctrl-c event\"); } 在后台监听： tokio::spawn(async move { tokio::signal::ctrl_c().await.unwrap(); // Your handler here });","module_doc":"当向进程发送âctrl-câ通知时完成。 虽然 Unix 和 Windows 处理信号的方式差异很大，但 两个平台都支持在âctrl-câ上接收信号。 此函数提供用于接收此通知的 可移植 API。 返回的 future 一旦被轮询，就会注册一个监听器。 此 future 将在首次接收到 ctrl-c 时完成， 该之后的初始调用为 Future::poll 或 .await。 在 Unix 平台上，第一次为 特定信号类型注册 Signal 实例时， 会安装一个操作系统信号处理程序， 该程序在接收到该信号时将替换 默认的平台行为，持续 整个进程期间。 例如，Unix 系统默认会在收到 终端上 \"CTRL+C\" 生成的信号时终止进程。但是，当创建 ctrl_c 流以监听此信号时，在其到达时， 它将被转换为流事件，并且进程将继续 执行。即使此 Signal 实例被丢弃， 后续的 SIGINT 传递最终也会被 Tokio 捕获，并且默认平台行为 将不会重置。 因此，应用程序应小心以确保 在监听特定信号后预期的信号行为 按预期发生。","section_headers":[]},{"url":"tokio/signal/windows/fn.ctrl_break.html","crate":"tokio","title":"ctrl_break","section":"fn","desc":"创建一个新的监听器，用于接收发送到 进程的âctrl-breakâ通知。 §示例 use tokio::signal::windows::ctrl_break; #[tokio::main] async fn main() -> Result<(), Box<dyn std::error::Error>> { // A listener of CTRL-BREAK events. let mut signal = ctrl_break()?; // Print whenever a CTRL-BREAK event is received. loop { signal.recv().await; println!(\"got signal CTRL-BREAK\"); } }","module_doc":"创建一个新的监听器，用于接收发送到 进程的âctrl-breakâ通知。","section_headers":[]},{"url":"tokio/signal/windows/fn.ctrl_c.html","crate":"tokio","title":"ctrl_c","section":"fn","desc":"创建一个新的监听器，用于接收发送到 进程的âctrl-câ通知。 §示例 use tokio::signal::windows::ctrl_c; #[tokio::main] async fn main() -> Result<(), Box<dyn std::error::Error>> { // A listener of CTRL-C events. let mut signal = ctrl_c()?; // Print whenever a CTRL-C event is received. for countdown in (0..3).rev() { signal.recv().await; println!(\"got CTRL-C. {} more to exit\", countdown); } Ok(()) }","module_doc":"创建一个新的监听器，用于接收发送到 进程的âctrl-câ通知。","section_headers":[]},{"url":"tokio/signal/windows/fn.ctrl_close.html","crate":"tokio","title":"ctrl_close","section":"fn","desc":"创建一个新的监听器，用于接收发送到 进程的âctrl-closeâ通知。 §示例 use tokio::signal::windows::ctrl_close; #[tokio::main] async fn main() -> Result<(), Box<dyn std::error::Error>> { // A listener of CTRL-CLOSE events. let mut signal = ctrl_close()?; // Print whenever a CTRL-CLOSE event is received. for countdown in (0..3).rev() { signal.recv().await; println!(\"got CTRL-CLOSE. {} more to exit\", countdown); } Ok(()) }","module_doc":"创建一个新的监听器，用于接收发送到 进程的âctrl-closeâ通知。","section_headers":[]},{"url":"tokio/signal/windows/fn.ctrl_logoff.html","crate":"tokio","title":"ctrl_logoff","section":"fn","desc":"创建一个新的监听器，用于接收发送到 进程的âctrl-logoffâ通知。 §示例 use tokio::signal::windows::ctrl_logoff; #[tokio::main] async fn main() -> Result<(), Box<dyn std::error::Error>> { // A listener of CTRL-LOGOFF events. let mut signal = ctrl_logoff()?; signal.recv().await; println!(\"got CTRL-LOGOFF. Cleaning up before exiting\"); Ok(()) }","module_doc":"创建一个新的监听器，用于接收发送到 进程的âctrl-logoffâ通知。","section_headers":[]},{"url":"tokio/signal/windows/fn.ctrl_shutdown.html","crate":"tokio","title":"ctrl_shutdown","section":"fn","desc":"创建一个新的监听器，用于接收发送到 进程的âctrl-shutdownâ通知。 §示例 use tokio::signal::windows::ctrl_shutdown; #[tokio::main] async fn main() -> Result<(), Box<dyn std::error::Error>> { // A listener of CTRL-SHUTDOWN events. let mut signal = ctrl_shutdown()?; signal.recv().await; println!(\"got CTRL-SHUTDOWN. Cleaning up before exiting\"); Ok(()) }","module_doc":"创建一个新的监听器，用于接收发送到 进程的âctrl-shutdownâ通知。","section_headers":[]},{"url":"tokio/signal/windows/struct.CtrlBreak.html","crate":"tokio","title":"CtrlBreak","section":"struct","desc":"表示一个监听器，它接收通过 SetConsoleCtrlHandler 发送到进程的âctrl-breakâ通知。 可以使用 CtrlBreakStream 将此监听器转换为 Stream。 对此进程的通知会通知此事件的所有接收者。 此外，通知会合并， 如果它们没有被 足够快地处理。这意味着如果连续收到两个通知， 则监听器可能仅收到关于这两个通知的一个条目。 接收下一个信号通知事件。 尽管此函数返回 Option<()>，但实际上永远不会返回 None。 这是意外暴露的，要删除将是一个破坏性变更。 §示例 use tokio::signal::windows::ctrl_break; #[tokio::main] async fn main() -> Result<(), Box<dyn std::error::Error>> { // A listener of CTRL-BREAK events. let mut signal = ctrl_break()?; // Print whenever a CTRL-BREAK event is received. loop { signal.recv().await; println!(\"got signal CTRL-BREAK\"); } } 在 async 上下文之外，轮询以接收下一个信号通知事件。 尽管此函数返回 Option<()>，但实际上永远不会返回 None。 这是意外暴露的，要删除将是一个破坏性变更。 §示例 从手动实现的 Future 轮询 use std::pin::Pin; use std::future::Future; use std::task::{Context, Poll}; use tokio::signal::windows::CtrlBreak; struct MyFuture { ctrl_break: CtrlBreak, } impl Future for MyFuture { type Output = Option<()>; fn poll(mut self: Pin<&mut Self>, cx: &mut Context<'_>) -> Poll<Self::Output> { println!(\"polling MyFuture\"); self.ctrl_break.poll_recv(cx) } } 使用给定的格式化器格式化此值。 更多信息 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 原样返回传入的参数。 调用 U::from(self)。 That is, this conversion is whatever the implementation of From<T> for U 的实现方式。 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"表示一个监听器，它接收通过 SetConsoleCtrlHandler 发送到进程的âctrl-breakâ通知。 可以使用 CtrlBreakStream 将此监听器转换为 Stream。 对此进程的通知会通知此事件的所有接收者。 此外，通知会合并， 如果它们没有被 足够快地处理。这意味着如果连续收到两个通知， 则监听器可能仅收到关于这两个通知的一个条目。","section_headers":[]},{"url":"tokio/signal/windows/struct.CtrlC.html","crate":"tokio","title":"CtrlC","section":"struct","desc":"表示一个监听器，它接收通过 SetConsoleCtrlHandler 发送到进程的âctrl-câ通知。 可以使用 CtrlCStream 将此事件转换为 Stream。 对此进程的通知会通知此事件的所有接收者。 此外，通知会合并， 如果它们没有被 足够快地处理。这意味着如果连续收到两个通知， 则监听器可能仅收到关于这两个通知的一个条目。 接收下一个信号通知事件。 尽管此函数返回 Option<()>，但实际上永远不会返回 None。 这是意外暴露的，要删除将是一个破坏性变更。 §示例 use tokio::signal::windows::ctrl_c; #[tokio::main] async fn main() -> Result<(), Box<dyn std::error::Error>> { let mut signal = ctrl_c()?; // Print whenever a CTRL-C event is received. for countdown in (0..3).rev() { signal.recv().await; println!(\"got CTRL-C. {} more to exit\", countdown); } Ok(()) } 在 async 上下文之外，轮询以接收下一个信号通知事件。 尽管此函数返回 Option<()>，但实际上永远不会返回 None。 这是意外暴露的，要删除将是一个破坏性变更。 §示例 从手动实现的 Future 轮询 use std::pin::Pin; use std::future::Future; use std::task::{Context, Poll}; use tokio::signal::windows::CtrlC; struct MyFuture { ctrl_c: CtrlC, } impl Future for MyFuture { type Output = Option<()>; fn poll(mut self: Pin<&mut Self>, cx: &mut Context<'_>) -> Poll<Self::Output> { println!(\"polling MyFuture\"); self.ctrl_c.poll_recv(cx) } } 使用给定的格式化器格式化此值。 更多信息 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 原样返回传入的参数。 调用 U::from(self)。 That is, this conversion is whatever the implementation of From<T> for U 的实现方式。 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"表示一个监听器，它接收通过 SetConsoleCtrlHandler 发送到进程的âctrl-câ通知。 可以使用 CtrlCStream 将此事件转换为 Stream。 对此进程的通知会通知此事件的所有接收者。 此外，通知会合并， 如果它们没有被 足够快地处理。这意味着如果连续收到两个通知， 则监听器可能仅收到关于这两个通知的一个条目。","section_headers":[]},{"url":"tokio/signal/windows/struct.CtrlClose.html","crate":"tokio","title":"CtrlClose","section":"struct","desc":"表示一个监听器，它接收通过 SetConsoleCtrlHandler 发送到进程的âctrl-closeâ通知。 对此进程的通知会通知正在监听 此事件的所有监听器。 此外，通知会合并， 如果它们没有被 足够快地处理。这意味着如果连续收到两个通知， 则监听器可能仅收到关于这两个通知的一个条目。 接收下一个信号通知事件。 尽管此函数返回 Option<()>，但实际上永远不会返回 None。 这是意外暴露的，要删除将是一个破坏性变更。 §示例 use tokio::signal::windows::ctrl_close; #[tokio::main] async fn main() -> Result<(), Box<dyn std::error::Error>> { // A listener of CTRL-CLOSE events. let mut signal = ctrl_close()?; // Print whenever a CTRL-CLOSE event is received. signal.recv().await; println!(\"got CTRL-CLOSE. Cleaning up before exiting\"); Ok(()) } 在 async 上下文之外，轮询以接收下一个信号通知事件。 尽管此函数返回 Option<()>，但实际上永远不会返回 None。 这是意外暴露的，要删除将是一个破坏性变更。 §示例 从手动实现的 Future 轮询 use std::pin::Pin; use std::future::Future; use std::task::{Context, Poll}; use tokio::signal::windows::CtrlClose; struct MyFuture { ctrl_close: CtrlClose, } impl Future for MyFuture { type Output = Option<()>; fn poll(mut self: Pin<&mut Self>, cx: &mut Context<'_>) -> Poll<Self::Output> { println!(\"polling MyFuture\"); self.ctrl_close.poll_recv(cx) } } 使用给定的格式化器格式化此值。 更多信息 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 原样返回传入的参数。 调用 U::from(self)。 That is, this conversion is whatever the implementation of From<T> for U 的实现方式。 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"表示一个监听器，它接收通过 SetConsoleCtrlHandler 发送到进程的âctrl-closeâ通知。 对此进程的通知会通知正在监听 此事件的所有监听器。 此外，通知会合并， 如果它们没有被 足够快地处理。这意味着如果连续收到两个通知， 则监听器可能仅收到关于这两个通知的一个条目。","section_headers":[]},{"url":"tokio/signal/windows/struct.CtrlLogoff.html","crate":"tokio","title":"CtrlLogoff","section":"struct","desc":"表示一个监听器，它接收通过 SetConsoleCtrlHandler 发送到进程的âctrl-logoffâ通知。 对此进程的通知会通知正在监听 此事件的所有监听器。 此外，通知会合并， 如果它们没有被 足够快地处理。这意味着如果连续收到两个通知， 则监听器可能仅收到关于这两个通知的一个条目。 接收下一个信号通知事件。 尽管此函数返回 Option<()>，但实际上永远不会返回 None。 这是意外暴露的，要删除将是一个破坏性变更。 §示例 use tokio::signal::windows::ctrl_logoff; #[tokio::main] async fn main() -> Result<(), Box<dyn std::error::Error>> { // An listener of CTRL-LOGOFF events. let mut signal = ctrl_logoff()?; // Print whenever a CTRL-LOGOFF event is received. signal.recv().await; println!(\"got CTRL-LOGOFF. Cleaning up before exiting\"); Ok(()) } 在 async 上下文之外，轮询以接收下一个信号通知事件。 尽管此函数返回 Option<()>，但实际上永远不会返回 None。 这是意外暴露的，要删除将是一个破坏性变更。 §示例 从手动实现的 Future 轮询 use std::pin::Pin; use std::future::Future; use std::task::{Context, Poll}; use tokio::signal::windows::CtrlLogoff; struct MyFuture { ctrl_logoff: CtrlLogoff, } impl Future for MyFuture { type Output = Option<()>; fn poll(mut self: Pin<&mut Self>, cx: &mut Context<'_>) -> Poll<Self::Output> { println!(\"polling MyFuture\"); self.ctrl_logoff.poll_recv(cx) } } 使用给定的格式化器格式化此值。 更多信息 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 原样返回传入的参数。 调用 U::from(self)。 That is, this conversion is whatever the implementation of From<T> for U 的实现方式。 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"表示一个监听器，它接收通过 SetConsoleCtrlHandler 发送到进程的âctrl-logoffâ通知。 对此进程的通知会通知正在监听 此事件的所有监听器。 此外，通知会合并， 如果它们没有被 足够快地处理。这意味着如果连续收到两个通知， 则监听器可能仅收到关于这两个通知的一个条目。","section_headers":[]},{"url":"tokio/signal/windows/struct.CtrlShutdown.html","crate":"tokio","title":"CtrlShutdown","section":"struct","desc":"表示一个监听器，它接收通过 SetConsoleCtrlHandler 发送到进程的âctrl-shutdownâ通知。 对此进程的通知会通知正在监听 此事件的所有监听器。 此外，通知会合并， 如果它们没有被 足够快地处理。这意味着如果连续收到两个通知， 则监听器可能仅收到关于这两个通知的一个条目。 接收下一个信号通知事件。 尽管此函数返回 Option<()>，但实际上永远不会返回 None。 这是意外暴露的，要删除将是一个破坏性变更。 §示例 use tokio::signal::windows::ctrl_shutdown; #[tokio::main] async fn main() -> Result<(), Box<dyn std::error::Error>> { // A listener of CTRL-SHUTDOWN events. let mut signal = ctrl_shutdown()?; // Print whenever a CTRL-SHUTDOWN event is received. signal.recv().await; println!(\"got CTRL-SHUTDOWN. Cleaning up before exiting\"); Ok(()) } 在 async 上下文之外，轮询以接收下一个信号通知事件。 尽管此函数返回 Option<()>，但实际上永远不会返回 None。 这是意外暴露的，要删除将是一个破坏性变更。 §示例 从手动实现的 Future 轮询 use std::pin::Pin; use std::future::Future; use std::task::{Context, Poll}; use tokio::signal::windows::CtrlShutdown; struct MyFuture { ctrl_shutdown: CtrlShutdown, } impl Future for MyFuture { type Output = Option<()>; fn poll(mut self: Pin<&mut Self>, cx: &mut Context<'_>) -> Poll<Self::Output> { println!(\"polling MyFuture\"); self.ctrl_shutdown.poll_recv(cx) } } 使用给定的格式化器格式化此值。 更多信息 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 原样返回传入的参数。 调用 U::from(self)。 That is, this conversion is whatever the implementation of From<T> for U 的实现方式。 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"表示一个监听器，它接收通过 SetConsoleCtrlHandler 发送到进程的âctrl-shutdownâ通知。 对此进程的通知会通知正在监听 此事件的所有监听器。 此外，通知会合并， 如果它们没有被 足够快地处理。这意味着如果连续收到两个通知， 则监听器可能仅收到关于这两个通知的一个条目。","section_headers":[]},{"url":"tokio/sync/enum.SetError.html","crate":"tokio","title":"SetError","section":"enum","desc":"可从 OnceCell::set 返回的错误。 调用 OnceCell::set 时，单元格已被初始化。 该单元格当前正在被初始化。 SetError 是否为 SetError::AlreadyInitializedError。 SetError 是否为 SetError::InitializingError 使用给定的格式化器格式化此值。 更多信息 使用给定的格式化器格式化此值。 更多信息 返回此错误的更底层来源（若有）。 更多信息 Provides type-based access to context intended for error reports. 更多信息 测试 self 与 other 值是否相等，供 == 运算符使用。 测试 != 运算符。默认实现几乎总是够用，除非有非常充分的理由，否则不应被覆盖。 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 原样返回参数。 调用 U::from(self)。 也就是说，此转换是 From<T> for U 实现选择执行的操作。 Converts the given value to a String. 更多信息 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"可从 OnceCell::set 返回的错误。","section_headers":["变体"]},{"url":"tokio/sync/enum.TryAcquireError.html","crate":"tokio","title":"TryAcquireError","section":"enum","desc":"从 Semaphore::try_acquire 函数返回的错误。 信号量已关闭，无法发出新的 permit。 信号量没有可用的 permit。 使用给定的格式化器格式化此值。 更多信息 使用给定的格式化器格式化此值。 更多信息 返回此错误的更底层来源（若有）。 更多信息 Provides type-based access to context intended for error reports. 更多信息 测试 self 与 other 值是否相等，供 == 运算符使用。 测试 != 运算符。默认实现几乎总是够用，除非有非常充分的理由，否则不应被覆盖。 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 原样返回参数。 调用 U::from(self)。 也就是说，此转换是 From<T> for U 实现选择执行的操作。 Converts the given value to a String. 更多信息 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"从 Semaphore::try_acquire 函数返回的错误。","section_headers":["变体"]},{"url":"tokio/sync/struct.AcquireError.html","crate":"tokio","title":"AcquireError","section":"struct","desc":"从 Semaphore::acquire 函数返回的错误。 仅当信号量已关闭时，acquire 操作才会失败。 使用给定的格式化器格式化此值。 更多信息 使用给定的格式化器格式化此值。 更多信息 返回此错误的更底层来源（若有）。 更多信息 Provides type-based access to context intended for error reports. 更多信息 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 原样返回参数。 调用 U::from(self)。 也就是说，此转换是 From<T> for U 实现选择执行的操作。 Converts the given value to a String. 更多信息 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"从 Semaphore::acquire 函数返回的错误。 仅当信号量已关闭时，acquire 操作才会失败。","section_headers":[]},{"url":"tokio/sync/struct.Barrier.html","crate":"tokio","title":"Barrier","section":"struct","desc":"barrier 使多个任务能够同步某些计算的开始。 use tokio::sync::Barrier; use std::sync::Arc; let mut handles = Vec::with_capacity(10); let barrier = Arc::new(Barrier::new(10)); for _ in 0..10 { let c = barrier.clone(); // 相同消息会一起打印。 // 不会看到任何交错。 handles.push(tokio::spawn(async move { println!(\"before wait\"); let wait_result = c.wait().await; println!(\"after wait\"); wait_result })); } // 在所有 \"after wait\" 消息打印完毕之前不会解析 let mut num_leaders = 0; for handle in handles { let wait_result = handle.await.unwrap(); if wait_result.is_leader() { num_leaders += 1; } } // 恰好会有一个 barrier 被解析为 \"leader\" assert_eq!(num_leaders, 1); 创建一个新的 barrier，可以阻塞给定数量的任务。 barrier 将阻塞调用 Barrier::wait 的 n-1 个任务，然后在第 n 个任务调用 wait 时立即唤醒所有任务。 在所有任务都在此会合并之前不会解析。 barrier 在所有任务都已合并一次后可重用，并且可以连续使用。 单个（任意的）future 将收到一个 BarrierWaitResult，从此函数返回时该结果从 BarrierWaitResult::is_leader 返回 true，而所有其他任务将收到从 is_leader 返回 false 的结果。 §取消安全性 此方法不是取消安全的。 使用给定的格式化器格式化此值。 更多信息 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 原样返回参数。 调用 U::from(self)。 也就是说，此转换是 From<T> for U 实现选择执行的操作。 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"barrier 使多个任务能够同步某些计算的开始。","section_headers":[]},{"url":"tokio/sync/struct.BarrierWaitResult.html","crate":"tokio","title":"BarrierWaitResult","section":"struct","desc":"当 Barrier 中的所有任务都已汇聚时，wait 返回一个 BarrierWaitResult。 如果来自 wait 的此任务是“领导者任务”，则返回 true。 只有一个任务的结果会返回 true，所有其他任务的结果都会返回 false。 返回值的副本。 更多信息 Performs copy-assignment from source. 更多信息 使用给定的格式化器格式化此值。 更多信息 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 Performs copy-assignment from self to dest. 更多信息 原样返回参数。 调用 U::from(self)。 也就是说，此转换是 From<T> for U 实现选择执行的操作。 获得所有权后的类型。 Creates owned data from borrowed data, usually by cloning. 更多信息 Uses borrowed data to replace owned data, usually by cloning. 更多信息 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"当 Barrier 中的所有任务都已汇聚时，wait 返回一个 BarrierWaitResult。","section_headers":[]},{"url":"tokio/sync/struct.MappedMutexGuard.html","crate":"tokio","title":"MappedMutexGuard","section":"struct","desc":"对持有 Mutex 的句柄，已通过 MutexGuard::map 对其应用了函数。 这可用于保存受保护数据的子字段。 为已锁定数据的某个组件创建一个新的 MappedMutexGuard。 此操作不会失败，因为传入的 MappedMutexGuard 已经锁定了互斥锁。 这是一个关联函数，需要用作 MappedMutexGuard::map(...)。方法会与锁定数据内容上的同名方法冲突。 尝试为已锁定数据的某个组件创建一个新的 MappedMutexGuard。如果闭包返回 None，则返回原始守卫。 此操作不会失败，因为传入的 MappedMutexGuard 已经锁定了互斥锁。 这是一个关联函数，需要用作 MappedMutexGuard::try_map(...)。方法会与锁定数据内容上的同名方法冲突。 使用给定的格式化器格式化此值。 更多信息 解引用后得到的类型。 解引用此值。 以可变方式解引用此值。 使用给定的格式化器格式化此值。 更多信息 执行此类型的析构函数。 更多信息 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 原样返回参数。 调用 U::from(self)。 也就是说，此转换是 From<T> for U 实现选择执行的操作。 Converts the given value to a String. 更多信息 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"对持有 Mutex 的句柄，已通过 MutexGuard::map 对其应用了函数。 这可用于保存受保护数据的子字段。","section_headers":[]},{"url":"tokio/sync/struct.Mutex.html","crate":"tokio","title":"Mutex","section":"struct","desc":"异步的类似 Mutex 的类型。 此类型的行为类似于 std::sync::Mutex，但有两个主要区别：lock 是一个异步方法，因此不会阻塞，并且锁保护器被设计为可以跨 .await 点持有。 Tokio 的 Mutex 在保证 FIFO（先进先出）的基础上运行。这意味着任务调用 lock 方法的顺序正是它们获取锁的顺序。 §应该使用哪种 mutex？ 与普遍的看法相反，在异步代码中使用标准库的普通 Mutex 是可以的，而且通常是首选。 异步互斥锁相对于阻塞互斥锁提供的特性是能够跨 .await 点保持锁定。这使得异步互斥锁比阻塞互斥锁更昂贵，因此在使用阻塞互斥锁可行的场景下应首选阻塞互斥锁。异步互斥锁的主要用例是为 IO 资源（例如数据库连接）提供共享的可变访问。如果互斥锁后面的值只是数据，通常适合使用阻塞互斥锁，例如标准库中的互斥锁或 parking_lot。 请注意，尽管编译器不会阻止 std Mutex 在任务在线程之间不可移动的情况下跨 .await 点持有其保护器，但在实践中这几乎从不会产生正确的并发代码，因为它很容易导致死锁。 一种常见的模式是将 Arc<Mutex<...>> 包装在一个结构体中，该结构体提供用于对其中的数据执行操作的非异步方法，并且仅在这些方法内部锁定互斥锁。mini-redis 示例提供了此模式的说明。 此外，当你确实需要对 IO 资源的共享访问时，通常更好的做法是生成一个任务来管理 IO 资源，并使用消息传递与该任务通信。 §示例： use tokio::sync::Mutex; use std::sync::Arc; let data1 = Arc::new(Mutex::new(0)); let data2 = Arc::clone(&data1); tokio::spawn(async move { let mut lock = data2.lock().await; *lock += 1; }); let mut lock = data1.lock().await; *lock += 1; use tokio::sync::Mutex; use std::sync::Arc; let count = Arc::new(Mutex::new(0)); for i in 0..5 { let my_count = Arc::clone(&count); tokio::spawn(async move { for j in 0..10 { let mut lock = my_count.lock().await; *lock += 1; println!(\"{} {} {}\", i, j, lock); } }); } loop { if *count.lock().await >= 50 { break; } } println!(\"Count hit 50.\"); 在此示例中有几点需要注意。 The mutex is wrapped in an Arc to allow it to be shared across threads. Each spawned task obtains a lock and releases it on every iteration. Mutation of the data protected by the Mutex is done by de-referencing the obtained lock as seen on lines 13 and 20. Tokio 的 Mutex 以简单的 FIFO（先进先出）方式工作，其中所有对 lock 的调用都按其执行的顺序完成。这样，Mutex 在如何将锁分配给内部数据方面是“公平”且可预测的。每次迭代后都会释放并重新获取锁，所以基本上，每个线程在将值递增一次后都会回到队尾。请注意，线程启动的时序存在一些不可预测性，但一旦它们开始运行，它们就会以可预测的方式交替。最后，由于任何给定时间都只有一个有效的锁，因此在变更内部值时不可能发生竞态条件。 请注意，与 std::sync::Mutex 相反，当持有 MutexGuard 的线程发生 panic 时，此实现不会使互斥锁中毒。在这种情况下，互斥锁将被解锁。如果 panic 被捕获，则受互斥锁保护的数据可能会处于不一致的状态。 以未锁定状态创建一个新锁，可直接使用。 §示例 use tokio::sync::Mutex; let lock = Mutex::new(5); 以未锁定状态创建一个新锁，可直接使用。 当使用 tracing 不稳定特性时，使用 const_new 创建的 Mutex 将不会被检测。因此，它将不会出现在 tokio-co","module_doc":"异步的类似 Mutex 的类型。 此类型的行为类似于 std::sync::Mutex，但有两个主要区别：lock 是一个异步方法，因此不会阻塞，并且锁保护器被设计为可以跨 .await 点持有。 Tokio 的 Mutex 在保证 FIFO（先进先出）的基础上运行。这意味着任务调用 lock 方法的顺序正是它们获取锁的顺序。 与普遍的看法相反，在异步代码中使用标准库的普通 Mutex 是可以的，而且通常是首选。 异步互斥锁相对于阻塞互斥锁提供的特性是能够跨 .await 点保持锁定。这使得异步互斥锁比阻塞互斥锁更昂贵，因此在使用阻塞互斥锁可行的场景下应首选阻塞互斥锁。异步互斥锁的主要用例是为 IO 资源（例如数据库连接）提供共享的可变访问。如果互斥锁后面的值只是数据，通常适合使用阻塞互斥锁，例如标准库中的互斥锁或 parking_lot。 请注意，尽管编译器不会阻止 std Mutex 在任务在线程之间不可移动的情况下跨 .await 点持有其保护器，但在实践中这几乎从不会产生正确的并发代码，因为它很容易导致死锁。 一种常见的模式是将 Arc<Mutex<...>> 包装在一个结构体","section_headers":[]},{"url":"tokio/sync/struct.MutexGuard.html","crate":"tokio","title":"MutexGuard","section":"struct","desc":"对持有 Mutex 的句柄。该 guard 可以跨越任何 .await 点，因为它实现了 Send。 只要你拥有此 guard，你就可以独占访问底层的 T。该 guard 在内部借用 Mutex，因此只要 guard 存在，mutex 就不会被 drop。 每当守卫被 drop 时，锁会自动释放，此时 lock 将再次成功。 为已锁定数据的某个组件创建一个新的 MappedMutexGuard。 此操作不会失败，因为传入的 MutexGuard 已经锁定了互斥锁。 这是一个关联函数，需要用作 MutexGuard::map(...)。方法会与锁定数据内容上的同名方法冲突。 §示例 use tokio::sync::{Mutex, MutexGuard}; #[derive(Debug, Clone, Copy, PartialEq, Eq)] struct Foo(u32); let foo = Mutex::new(Foo(1)); { let mut mapped = MutexGuard::map(foo.lock().await, |f| &mut f.0); *mapped = 2; } assert_eq!(Foo(2), *foo.lock().await); 尝试为已锁定数据的某个组件创建一个新的 MappedMutexGuard。如果闭包返回 None，则返回原始守卫。 此操作不会失败，因为传入的 MutexGuard 已经锁定了互斥锁。 这是一个关联函数，需要用作 MutexGuard::try_map(...)。方法会与锁定数据内容上的同名方法冲突。 §示例 use tokio::sync::{Mutex, MutexGuard}; #[derive(Debug, Clone, Copy, PartialEq, Eq)] struct Foo(u32); let foo = Mutex::new(Foo(1)); { let mut mapped = MutexGuard::try_map(foo.lock().await, |f| Some(&mut f.0)) .expect(\"should not fail\"); *mapped = 2; } assert_eq!(Foo(2), *foo.lock().await); 返回对原始 Mutex 的引用。 use tokio::sync::{Mutex, MutexGuard}; async fn unlock_and_relock<'l>(guard: MutexGuard<'l, u32>) -> MutexGuard<'l, u32> { println!(\"1. contains: {:?}\", *guard); let mutex = MutexGuard::mutex(&guard); drop(guard); let guard = mutex.lock().await; println!(\"2. contains: {:?}\", *guard); guard } 使用给定的格式化器格式化此值。 更多信息 解引用后得到的类型。 解引用此值。 以可变方式解引用此值。 使用给定的格式化器格式化此值。 更多信息 执行此类型的析构函数。 更多信息 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 原样返回参数。 调用 U::from(self)。 也就是说，此转换是 From<T> for U 实现选择执行的操作。 Converts the given value to a String. 更多信息 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"对持有 Mutex 的句柄。该 guard 可以跨越任何 .await 点，因为它实现了 Send。 只要你拥有此 guard，你就可以独占访问底层的 T。该 guard 在内部借用 Mutex，因此只要 guard 存在，mutex 就不会被 drop。 每当守卫被 drop 时，锁会自动释放，此时 lock 将再次成功。","section_headers":[]},{"url":"tokio/sync/struct.Notify.html","crate":"tokio","title":"Notify","section":"struct","desc":"通知单个任务唤醒。 Notify 提供了一种用于向单个任务通知事件的基本机制。Notify 本身不携带任何数据。相反，它用于向另一个任务发出执行操作的信号。 可以将 Notify 视为一个以 0 个许可开始的 Semaphore。notified().await 方法等待许可变为可用，而 notify_one() 在当前没有可用许可时设置一个许可。 Notify 的同步细节类似于 std 中的 thread::park 和 Thread::unpark。Notify 值包含单个许可。notified().await 等待许可可用，消费该许可，然后恢复运行。notify_one() 设置许可，如果有挂起的任务则唤醒它。 如果在 notified().await 之前调用 notify_one()，则下一次调用 notified().await 将立即完成，消费该许可。对 notified().await 的任何后续调用都将等待新的许可。 如果在 notified().await 之前多次调用 notify_one()，则仅存储单个许可。下一次调用 notified().await 将立即完成，但再下一次调用将等待新的许可。 §示例 基本用法。 use tokio::sync::Notify; use std::sync::Arc; let notify = Arc::new(Notify::new()); let notify2 = notify.clone(); let handle = tokio::spawn(async move { notify2.notified().await; println!(\"received notification\"); }); println!(\"sending notification\"); notify.notify_one(); // 等待任务接收通知。 handle.await.unwrap(); 无界多生产者单消费者 (mpsc) 通道。 使用此通道时不会丢失任何唤醒，因为对 notify_one() 的调用会将许可存储在 Notify 中，随后的 notified() 调用将消费该许可。 use tokio::sync::Notify; use std::collections::VecDeque; use std::sync::Mutex; struct Channel<T> { values: Mutex<VecDeque<T>>, notify: Notify, } impl<T> Channel<T> { pub fn send(&self, value: T) { self.values.lock().unwrap() .push_back(value); // 通知消费者有值可用 self.notify.notify_one(); } // 这是单消费者通道，因此不允许对 `recv` 进行 // 多次并发调用。 pub async fn recv(&self) -> T { loop { // 排空值 if let Some(value) = self.values.lock().unwrap().pop_front() { return value; } // 等待值可用 self.notify.notified().await; } } } 无界多生产者多消费者 (mpmc) 通道。 调用 enable 很重要，因为否则如果你并行调用两个 recv 和两个 send，则可能会发生以下情况： Both calls to try_recv return None. Both new elements are added to the vector. The notify_one method is called twice, adding only a single permit to the Notify. Both calls to recv reach the Notified future. One of them consumes the permit, and the other sleeps forever. 如果在 try_recv 之前通过调用 enable 将 Notified future 添加到列表中，则第三步中的 notify_one 调用会从列表中移除这些 future 并将它们标记为已通知，而不是向 Notify 添加许可。这确保了两个 future 都会被唤醒。 请注意，只有在存在两个对 recv 的并发调用时才会发生此失败。这就是为什么上面的 mpsc 示例不需要调用 enable。 use tokio::sync::Notif","module_doc":"通知单个任务唤醒。 Notify 提供了一种用于向单个任务通知事件的基本机制。Notify 本身不携带任何数据。相反，它用于向另一个任务发出执行操作的信号。 可以将 Notify 视为一个以 0 个许可开始的 Semaphore。notified().await 方法等待许可变为可用，而 notify_one() 在当前没有可用许可时设置一个许可。 Notify 的同步细节类似于 std 中的 thread::park 和 Thread::unpark。Notify 值包含单个许可。notified().await 等待许可可用，消费该许可，然后恢复运行。notify_one() 设置许可，如果有挂起的任务则唤醒它。 如果在 notified().await 之前调用 notify_one()，则下一次调用 notified().await 将立即完成，消费该许可。对 notified().await 的任何后续调用都将等待新的许可。 如果在 notified().await 之前多次调用 notify_one()，则仅存储单个许可。下一次调用 notified().await 将立即","section_headers":[]},{"url":"tokio/sync/struct.OnceCell.html","crate":"tokio","title":"OnceCell","section":"struct","desc":"只能写入一次的线程安全单元。 OnceCell 通常用于需要在首次使用时初始化一次但不需要进一步更改的全局变量。Tokio 中的 OnceCell 允许异步执行初始化过程。 §示例 use tokio::sync::OnceCell; async fn some_computation() -> u32 { 1 + 1 } static ONCE: OnceCell<u32> = OnceCell::const_new(); let result = ONCE.get_or_init(some_computation).await; assert_eq!(*result, 2); 编写用于访问值的包装方法通常很有用。 use tokio::sync::OnceCell; static ONCE: OnceCell<u32> = OnceCell::const_new(); async fn get_global_integer() -> &'static u32 { ONCE.get_or_init(|| async { 1 + 1 }).await } let result = get_global_integer().await; assert_eq!(*result, 2); 创建一个新的空 OnceCell 实例。 创建一个新的空 OnceCell 实例。 等效于 OnceCell::new，但可以在静态变量中使用。 当使用 tracing 不稳定功能 时，使用 const_new 创建的 OnceCell 不会被检测。因此，它在 tokio-console 中不可见。如果需要，应改用 OnceCell::new 创建被检测的对象。 §示例 use tokio::sync::OnceCell; static ONCE: OnceCell<u32> = OnceCell::const_new(); async fn get_global_integer() -> &'static u32 { ONCE.get_or_init(|| async { 1 + 1 }).await } let result = get_global_integer().await; assert_eq!(*result, 2); 创建一个新的 OnceCell，其中包含所提供的值（如果有）。 如果 Option 为 None，则等效于 OnceCell::new。 创建一个包含所提供值的新 OnceCell。 §示例 当使用 tracing 不稳定功能 时，使用 const_new_with 创建的 OnceCell 不会被检测。因此，它在 tokio-console 中不可见。如果需要，应改用 OnceCell::new_with 创建被检测的对象。 use tokio::sync::OnceCell; static ONCE: OnceCell<u32> = OnceCell::const_new_with(1); async fn get_global_integer() -> &'static u32 { ONCE.get_or_init(|| async { 1 + 1 }).await } let result = get_global_integer().await; assert_eq!(*result, 1); 如果 OnceCell 当前包含值，则返回 true，否则返回 false。 返回当前存储在 OnceCell 中的值的引用，如果 OnceCell 为空则返回 None。 返回当前存储在 OnceCell 中的值的可变引用，如果 OnceCell 为空则返回 None。 由于此调用可变地借用 OnceCell，因此可以安全地变更 OnceCell 中的值——可变借用在静态上保证不存在其他引用。 如果 OnceCell 为空，则将其值设置为给定值。 如果 OnceCell 已经有值，则此调用将失败并返回 SetError::AlreadyInitializedError。 如果 OnceCell 为空，但其他某个任务当前正在尝试设置值，则此调用将失败并返回 SetError::InitializingError。 获取 OnceCell 中当前的值，或使用给定的异步操作进行初始化。 如果其他任务当前正在初始化 OnceCell，此调用将等待该其他任务完成，然后返回该其他任务产生的值。 如果所提供的操作被取消或发生 panic，则初始化尝试将被取消。如果有其他任务正在等待值的初始化，那么其中一个将开始另一次初始化该值的尝试。 如果 f 尝试递归初始化单元，则会发生死锁。 获取 OnceCell 中当前的值，或使用给定的","module_doc":"只能写入一次的线程安全单元。 OnceCell 通常用于需要在首次使用时初始化一次但不需要进一步更改的全局变量。Tokio 中的 OnceCell 允许异步执行初始化过程。","section_headers":[]},{"url":"tokio/sync/struct.OwnedMappedMutexGuard.html","crate":"tokio","title":"OwnedMappedMutexGuard","section":"struct","desc":"对持有 Mutex 的 owned 句柄，已通过 OwnedMutexGuard::map 对其应用了函数。 这可用于保存受保护数据的子字段。 为已锁定数据的某个组件创建一个新的 OwnedMappedMutexGuard。 此操作不会失败，因为传入的 OwnedMappedMutexGuard 已经锁定了 mutex。 这是一个关联函数，需要用作 OwnedMappedMutexGuard::map(...)。方法会与锁定数据内容上的同名方法冲突。 尝试为已锁定数据的某个组件创建一个新的 OwnedMappedMutexGuard。如果闭包返回 None，则返回原始守卫。 此操作不会失败，因为传入的 OwnedMutexGuard 已经锁定了互斥锁。 这是一个关联函数，需要以 OwnedMutexGuard::try_map(...) 的方式使用。如果使用方法，将与已锁定数据内容上同名的方法产生冲突。 使用给定的格式化器格式化此值。 更多信息 解引用后得到的类型。 解引用此值。 以可变方式解引用此值。 使用给定的格式化器格式化此值。 更多信息 执行此类型的析构函数。 更多信息 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 原样返回参数。 调用 U::from(self)。 也就是说，此转换是 From<T> for U 实现选择执行的操作。 Converts the given value to a String. 更多信息 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"对持有 Mutex 的 owned 句柄，已通过 OwnedMutexGuard::map 对其应用了函数。 这可用于保存受保护数据的子字段。","section_headers":[]},{"url":"tokio/sync/struct.OwnedMutexGuard.html","crate":"tokio","title":"OwnedMutexGuard","section":"struct","desc":"对持有 Mutex 的 owned 句柄。 此 guard 仅适用于包装在 Arc 中的 Mutex。它与 MutexGuard 相同，只是它不借用 Mutex，而是克隆 Arc，增加引用计数。这意味着与 MutexGuard 不同，它将具有 'static 生命周期。 只要你拥有此 guard，你就可以独占访问底层的 T。该 guard 在内部保持指向原始 Mutex 的引用计数指针，因此即使锁消失了，guard 仍然有效。 每当守卫被 drop 时，锁会自动释放，此时 lock 将再次成功。 为已锁定数据的某个组件创建一个新的 OwnedMappedMutexGuard。 此操作不会失败，因为传入的 OwnedMutexGuard 已经锁定了互斥锁。 这是一个关联函数，需要用作 OwnedMutexGuard::map(...)。方法会与锁定数据内容上的同名方法冲突。 §示例 use tokio::sync::{Mutex, OwnedMutexGuard}; use std::sync::Arc; #[derive(Debug, Clone, Copy, PartialEq, Eq)] struct Foo(u32); let foo = Arc::new(Mutex::new(Foo(1))); { let mut mapped = OwnedMutexGuard::map(foo.clone().lock_owned().await, |f| &mut f.0); *mapped = 2; } assert_eq!(Foo(2), *foo.lock().await); 尝试为已锁定数据的某个组件创建一个新的 OwnedMappedMutexGuard。如果闭包返回 None，则返回原始守卫。 此操作不会失败，因为传入的 OwnedMutexGuard 已经锁定了互斥锁。 这是一个关联函数，需要以 OwnedMutexGuard::try_map(...) 的方式使用。如果使用方法，将与已锁定数据内容上同名的方法产生冲突。 §示例 use tokio::sync::{Mutex, OwnedMutexGuard}; use std::sync::Arc; #[derive(Debug, Clone, Copy, PartialEq, Eq)] struct Foo(u32); let foo = Arc::new(Mutex::new(Foo(1))); { let mut mapped = OwnedMutexGuard::try_map(foo.clone().lock_owned().await, |f| Some(&mut f.0)) .expect(\"should not fail\"); *mapped = 2; } assert_eq!(Foo(2), *foo.lock().await); 返回对原始 Arc<Mutex> 的引用。 use std::sync::Arc; use tokio::sync::{Mutex, OwnedMutexGuard}; async fn unlock_and_relock(guard: OwnedMutexGuard<u32>) -> OwnedMutexGuard<u32> { println!(\"1. contains: {:?}\", *guard); let mutex: Arc<Mutex<u32>> = OwnedMutexGuard::mutex(&guard).clone(); drop(guard); let guard = mutex.lock_owned().await; println!(\"2. contains: {:?}\", *guard); guard } 使用给定的格式化器格式化此值。 更多信息 解引用后得到的类型。 解引用此值。 以可变方式解引用此值。 使用给定的格式化器格式化此值。 更多信息 执行此类型的析构函数。 更多信息 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 原样返回参数。 调用 U::from(self)。 也就是说，此转换是 From<T> for U 实现选择执行的操作。 Converts the given value to a String. 更多信息 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"对持有 Mutex 的 owned 句柄。 此 guard 仅适用于包装在 Arc 中的 Mutex。它与 MutexGuard 相同，只是它不借用 Mutex，而是克隆 Arc，增加引用计数。这意味着与 MutexGuard 不同，它将具有 'static 生命周期。 只要你拥有此 guard，你就可以独占访问底层的 T。该 guard 在内部保持指向原始 Mutex 的引用计数指针，因此即使锁消失了，guard 仍然有效。 每当守卫被 drop 时，锁会自动释放，此时 lock 将再次成功。","section_headers":[]},{"url":"tokio/sync/struct.OwnedRwLockMappedWriteGuard.html","crate":"tokio","title":"OwnedRwLockMappedWriteGuard","section":"struct","desc":"用于在 drop 时释放锁的独占写访问权的 owned RAII 结构。 此结构体通过对 OwnedRwLockWriteGuard 进行 映射 创建。它是与 OwnedRwLockWriteGuard 分开的类型，因为降级映射后的 guard 可能导致未定义行为，因此不允许这样做。 为锁定数据的组件创建一个新的 OwnedRwLockMappedWriteGuard。 此操作不会失败，因为传入的 OwnedRwLockMappedWriteGuard 已经锁定了数据。 这是一个关联函数，需要以 OwnedRwLockWriteGuard::map(..) 的方式使用。如果使用方法，将与已锁定数据内容上同名的方法产生冲突。 §示例 use std::sync::Arc; use tokio::sync::{RwLock, OwnedRwLockWriteGuard}; #[derive(Debug, Clone, Copy, PartialEq, Eq)] struct Foo(u32); let lock = Arc::new(RwLock::new(Foo(1))); { let lock = Arc::clone(&lock); let mut mapped = OwnedRwLockWriteGuard::map(lock.write_owned().await, |f| &mut f.0); *mapped = 2; } assert_eq!(Foo(2), *lock.read().await); 尝试为锁定数据的组件创建一个新的 OwnedRwLockMappedWriteGuard。如果闭包返回 None，则返回原始 guard。 此操作不会失败，因为传入的 OwnedRwLockMappedWriteGuard 已经锁定了数据。 这是一个关联函数，需要用作 OwnedRwLockMappedWriteGuard::try_map(...)。方法会与锁定数据内容上的同名方法冲突。 §示例 use std::sync::Arc; use tokio::sync::{RwLock, OwnedRwLockWriteGuard}; #[derive(Debug, Clone, Copy, PartialEq, Eq)] struct Foo(u32); let lock = Arc::new(RwLock::new(Foo(1))); { let guard = Arc::clone(&lock).write_owned().await; let mut guard = OwnedRwLockWriteGuard::try_map(guard, |f| Some(&mut f.0)).expect(\"should not fail\"); *guard = 2; } assert_eq!(Foo(2), *lock.read().await); 返回对原始 Arc<RwLock> 的引用。 §示例 use std::sync::Arc; use tokio::sync::{ RwLock, OwnedRwLockWriteGuard, OwnedRwLockMappedWriteGuard, }; let lock = Arc::new(RwLock::new(1)); let guard = lock.clone().write_owned().await; let guard = OwnedRwLockWriteGuard::map(guard, |x| x); assert!(Arc::ptr_eq(&lock, OwnedRwLockMappedWriteGuard::rwlock(&guard))); 使用给定的格式化器格式化此值。 更多信息 解引用后得到的类型。 解引用此值。 以可变方式解引用此值。 使用给定的格式化器格式化此值。 更多信息 执行此类型的析构函数。 更多信息 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 原样返回参数。 调用 U::from(self)。 也就是说，此转换是 From<T> for U 实现选择执行的操作。 Converts the given value to a String. 更多信息 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"用于在 drop 时释放锁的独占写访问权的 owned RAII 结构。 此结构体通过对 OwnedRwLockWriteGuard 进行 映射 创建。它是与 OwnedRwLockWriteGuard 分开的类型，因为降级映射后的 guard 可能导致未定义行为，因此不允许这样做。","section_headers":[]},{"url":"tokio/sync/struct.OwnedRwLockReadGuard.html","crate":"tokio","title":"OwnedRwLockReadGuard","section":"struct","desc":"Owned RAII 结构体，用于在 drop 时释放锁的共享读访问。 此结构体由 RwLock 上的 read_owned 方法创建。 为锁定数据的组件创建一个新的 OwnedRwLockReadGuard。此操作不会失败，因为传入的 OwnedRwLockReadGuard 已锁定数据。 这是一个关联函数，需要用作 OwnedRwLockReadGuard::map(...)。方法会与锁定数据内容上的同名方法冲突。 §示例 use std::sync::Arc; use tokio::sync::{RwLock, OwnedRwLockReadGuard}; #[derive(Debug, Clone, Copy, PartialEq, Eq)] struct Foo(u32); let lock = Arc::new(RwLock::new(Foo(1))); let guard = lock.read_owned().await; let guard = OwnedRwLockReadGuard::map(guard, |f| &f.0); assert_eq!(1, *guard); 尝试为锁定数据的组件创建一个新的 OwnedRwLockReadGuard。如果闭包返回 None，则返回原始 guard。 此操作不会失败，因为传入的 OwnedRwLockReadGuard 已锁定数据。 这是一个关联函数，需要用作 OwnedRwLockReadGuard::try_map(..)。方法会与锁定数据内容上的同名方法冲突。 §示例 use std::sync::Arc; use tokio::sync::{RwLock, OwnedRwLockReadGuard}; #[derive(Debug, Clone, Copy, PartialEq, Eq)] struct Foo(u32); let lock = Arc::new(RwLock::new(Foo(1))); let guard = lock.read_owned().await; let guard = OwnedRwLockReadGuard::try_map(guard, |f| Some(&f.0)).expect(\"should not fail\"); assert_eq!(1, *guard); 返回对原始 Arc<RwLock> 的引用。 §示例 use std::sync::Arc; use tokio::sync::{RwLock, OwnedRwLockReadGuard}; #[derive(Debug, Clone, Copy, PartialEq, Eq)] struct Foo(u32); let lock = Arc::new(RwLock::new(Foo(1))); let guard = lock.clone().read_owned().await; assert!(Arc::ptr_eq(&lock, OwnedRwLockReadGuard::rwlock(&guard))); let guard = OwnedRwLockReadGuard::map(guard, |f| &f.0); assert!(Arc::ptr_eq(&lock, OwnedRwLockReadGuard::rwlock(&guard))); 使用给定的格式化器格式化此值。 更多信息 解引用后得到的类型。 解引用此值。 使用给定的格式化器格式化此值。 更多信息 执行此类型的析构函数。 更多信息 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 原样返回参数。 调用 U::from(self)。 也就是说，此转换是 From<T> for U 实现选择执行的操作。 Converts the given value to a String. 更多信息 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"Owned RAII 结构体，用于在 drop 时释放锁的共享读访问。 此结构体由 RwLock 上的 read_owned 方法创建。","section_headers":[]},{"url":"tokio/sync/struct.OwnedRwLockWriteGuard.html","crate":"tokio","title":"OwnedRwLockWriteGuard","section":"struct","desc":"用于在 drop 时释放锁的独占写访问权的 owned RAII 结构。 此结构体由 RwLock 上的 write_owned 方法创建。 为锁定数据的组件创建一个新的 OwnedRwLockMappedWriteGuard。 此操作不会失败，因为传入的 OwnedRwLockWriteGuard 已经锁定了数据。 这是一个关联函数，需要以 OwnedRwLockWriteGuard::map(..) 的方式使用。如果使用方法，将与已锁定数据内容上同名的方法产生冲突。 §示例 use std::sync::Arc; use tokio::sync::{RwLock, OwnedRwLockWriteGuard}; #[derive(Debug, Clone, Copy, PartialEq, Eq)] struct Foo(u32); let lock = Arc::new(RwLock::new(Foo(1))); { let lock = Arc::clone(&lock); let mut mapped = OwnedRwLockWriteGuard::map(lock.write_owned().await, |f| &mut f.0); *mapped = 2; } assert_eq!(Foo(2), *lock.read().await); 为锁定数据的组件创建一个新的 OwnedRwLockReadGuard。 此操作不会失败，因为传入的 OwnedRwLockWriteGuard 已经锁定了数据。 这是一个关联函数，需要用作 OwnedRwLockWriteGuard::downgrade_map(..)。方法会与锁定数据内容上的同名方法冲突。 在 f 内部，即使只获得了 &T，你仍然保留对数据的独占访问权。交出 &mut T 将导致内存不安全，因为你可以使用内部可变性。 §示例 use std::sync::Arc; use tokio::sync::{RwLock, OwnedRwLockWriteGuard}; #[derive(Debug, Clone, Copy, PartialEq, Eq)] struct Foo(u32); let lock = Arc::new(RwLock::new(Foo(1))); let guard = Arc::clone(&lock).write_owned().await; let mapped = OwnedRwLockWriteGuard::downgrade_map(guard, |f| &f.0); let foo = lock.read_owned().await; assert_eq!(foo.0, *mapped); 尝试为锁定数据的组件创建一个新的 OwnedRwLockMappedWriteGuard。如果闭包返回 None，则返回原始 guard。 此操作不会失败，因为传入的 OwnedRwLockWriteGuard 已经锁定了数据。 这是一个关联函数，需要用作 OwnedRwLockWriteGuard::try_map(...)。方法会与锁定数据内容上的同名方法冲突。 §示例 use std::sync::Arc; use tokio::sync::{RwLock, OwnedRwLockWriteGuard}; #[derive(Debug, Clone, Copy, PartialEq, Eq)] struct Foo(u32); let lock = Arc::new(RwLock::new(Foo(1))); { let guard = Arc::clone(&lock).write_owned().await; let mut guard = OwnedRwLockWriteGuard::try_map(guard, |f| Some(&mut f.0)).expect(\"should not fail\"); *guard = 2; } assert_eq!(Foo(2), *lock.read().await); 尝试为锁定数据的组件创建一个新的 OwnedRwLockReadGuard。如果闭包返回 None，则返回原始 guard。 此操作不会失败，因为传入的 OwnedRwLockWriteGuard 已经锁定了数据。 这是一个关联函数，需要用作 OwnedRwLockWriteGuard::try_downgrade_map(...)。方法会与锁定数据内容上的同名方法冲突。 在 f 内部，即使只获得了 &T，你仍然保留对数据的独占访问权。交出 &mut T 将导致内存不安全，因为你可以使用内部可变性。 如果此函数返","module_doc":"用于在 drop 时释放锁的独占写访问权的 owned RAII 结构。 此结构体由 RwLock 上的 write_owned 方法创建。","section_headers":[]},{"url":"tokio/sync/struct.OwnedSemaphorePermit.html","crate":"tokio","title":"OwnedSemaphorePermit","section":"struct","desc":"来自信号量的 owned permit。 此类型由 acquire_owned 方法创建。 不将许可释放回信号量就将其遗忘。这可用于减少信号量中可用的许可数量。 §示例 use std::sync::Arc; use tokio::sync::Semaphore; let sem = Arc::new(Semaphore::new(10)); { let permit = sem.clone().try_acquire_many_owned(5).unwrap(); assert_eq!(sem.available_permits(), 5); permit.forget(); } // 因为我们忘记 permit，可用许可数将不会恢复到其初始值， // 即使 permit 已被丢弃。 assert_eq!(sem.available_permits(), 5); 将两个 OwnedSemaphorePermit 实例合并在一起，消费 other 而不释放它持有的 permit。 由 self 和 other 持有的许将在 self 被 drop 时一起释放。 §恐慌 如果合并来自不同 Semaphore 实例的许可，此函数会发生 panic。 §示例 use std::sync::Arc; use tokio::sync::Semaphore; let sem = Arc::new(Semaphore::new(10)); let mut permit = sem.clone().try_acquire_owned().unwrap(); for _ in 0..9 { let _permit = sem.clone().try_acquire_owned().unwrap(); // 将各个 permit 合并为单个 permit。 permit.merge(_permit) } assert_eq!(sem.available_permits(), 0); // 一次性释放所有 permit。 drop(permit); assert_eq!(sem.available_permits(), 10); 从 self 拆分 n 个 permit，并返回一个持有 n 个 permit 的新 OwnedSemaphorePermit 实例。 如果没有足够的许可且无法减少 n，则返回 None。 §注意 它将克隆所拥有的 Arc<Semaphore> 以构造新实例。 §示例 use std::sync::Arc; use tokio::sync::Semaphore; let sem = Arc::new(Semaphore::new(3)); let mut p1 = sem.try_acquire_many_owned(3).unwrap(); let p2 = p1.split(1).unwrap(); assert_eq!(p1.num_permits(), 2); assert_eq!(p2.num_permits(), 1); 返回获取此 permit 的 Semaphore。 返回 self 持有的许可数。 使用给定的格式化器格式化此值。 更多信息 执行此类型的析构函数。 更多信息 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 原样返回参数。 调用 U::from(self)。 也就是说，此转换是 From<T> for U 实现选择执行的操作。 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"来自信号量的 owned permit。 此类型由 acquire_owned 方法创建。","section_headers":[]},{"url":"tokio/sync/struct.RwLock.html","crate":"tokio","title":"RwLock","section":"struct","desc":"异步读写锁。 这种类型的锁允许任意数量的读者或在任意时刻最多一个写者。此锁的写部分通常允许修改底层数据（独占访问），而读部分通常允许只读访问（共享访问）。 相比之下，Mutex 不区分获取锁的读者或写者，因此会导致任何等待锁变得可用的任务让步。只要没有写者持有锁，RwLock 就允许任意数量的读者获取该锁。 Tokio 读写锁的优先级策略是公平的（或优先写者），以确保读者不会饿死写者。公平性通过对等待锁的任务使用先进先出队列来保证；在其之前排队的所有写锁请求被获取并释放之前，不会发出读锁。这与 Rust 标准库的 std::sync::RwLock 形成对比，后者的优先级策略取决于操作系统的实现。 类型参数 T 表示此锁保护的数据。要求 T 满足 Send 才能跨线程共享。加锁方法返回的 RAII guard 实现了 Deref（以及 write 方法的 DerefMut），以允许访问锁的内容。 §示例 use tokio::sync::RwLock; let lock = RwLock::new(5); // 可以同时持有多个读锁 { let r1 = lock.read().await; let r2 = lock.read().await; assert_eq!(*r1, 5); assert_eq!(*r2, 5); } // 读锁在此处被释放 // 但写锁一次只能持有一个 { let mut w = lock.write().await; *w += 1; assert_eq!(*w, 6); } // 写锁在此处被释放 创建一个未锁定的 RwLock<T> 新实例。 §示例 use tokio::sync::RwLock; let lock = RwLock::new(5); 创建一个未锁定的 RwLock<T> 新实例，允许最多 max_reads 个并发读取者。 §示例 use tokio::sync::RwLock; let lock = RwLock::with_max_readers(5, 1024);§恐慌 如果 max_reads 为 0 或大于 u32::MAX >> 3，则会发生 panic。 创建一个未锁定的 RwLock<T> 新实例。 当使用 tracing 不稳定功能 时，使用 const_new 创建的 RwLock 不会被检测。因此，它在 tokio-console 中不可见。如果需要，应改用 RwLock::new 创建被检测的对象。 §示例 use tokio::sync::RwLock; static LOCK: RwLock<i32> = RwLock::const_new(5); 创建一个未锁定的 RwLock<T> 新实例，允许最多 max_reads 个并发读取者。 §示例 use tokio::sync::RwLock; static LOCK: RwLock<i32> = RwLock::const_with_max_readers(5, 1024);§恐慌 如果 max_reads 为 0 或大于 u32::MAX >> 3，则会发生 panic。 使用共享读访问锁定此 RwLock，导致当前任务让出执行权，直到锁被获取。 调用任务将让出执行权，直到没有任何持有锁的写入者。当任务恢复时，锁内可能还有其他读取者。 请注意，根据 RwLock 的优先级策略，为防止饥饿，在先前的写锁释放之前不会授予读锁。因此，如果当前任务持有读锁，然后尝试获取写锁，再尝试获取读锁，则可能发生死锁。 返回一个 RAII 守卫，当它被 drop 时会释放此 RwLock 的读访问权。 §取消安全性 此方法使用队列按请求的顺序公平地分配锁。取消对 read 的调用会使你失去在队列中的位置。 §示例 use std::sync::Arc; use tokio::sync::RwLock; let lock = Arc::new(RwLock::new(1)); let c_lock = lock.clone(); let n = lock.read().await; assert_eq!(*n, 1); tokio::spawn(async move { // 当 main 持有一个活动的读锁时，我们再获取一个。 let r = c_lock.read().await; assert_eq!(*r, 1); }).await.expect(\"The spawned task has panicked\"); // 在派生任务结束后释放 guard。 drop(n); 以共享读访问阻塞方式锁定此 RwLock。 此方法适用于需要在异步代码和同步代码中都使用此 rwlock 的场景。 返回一个 RAI","module_doc":"异步读写锁。 这种类型的锁允许任意数量的读者或在任意时刻最多一个写者。此锁的写部分通常允许修改底层数据（独占访问），而读部分通常允许只读访问（共享访问）。 相比之下，Mutex 不区分获取锁的读者或写者，因此会导致任何等待锁变得可用的任务让步。只要没有写者持有锁，RwLock 就允许任意数量的读者获取该锁。 Tokio 读写锁的优先级策略是公平的（或优先写者），以确保读者不会饿死写者。公平性通过对等待锁的任务使用先进先出队列来保证；在其之前排队的所有写锁请求被获取并释放之前，不会发出读锁。这与 Rust 标准库的 std::sync::RwLock 形成对比，后者的优先级策略取决于操作系统的实现。 类型参数 T 表示此锁保护的数据。要求 T 满足 Send 才能跨线程共享。加锁方法返回的 RAII guard 实现了 Deref（以及 write 方法的 DerefMut），以允许访问锁的内容。","section_headers":[]},{"url":"tokio/sync/struct.RwLockMappedWriteGuard.html","crate":"tokio","title":"RwLockMappedWriteGuard","section":"struct","desc":"用于在 drop 时释放锁的独占写访问权的 RAII 结构。 此结构体通过对 RwLockWriteGuard 进行 映射 创建。它是与 RwLockWriteGuard 分开的类型，因为降级映射后的 guard 可能导致未定义行为，因此不允许这样做。 为锁定数据的组件创建一个新的 RwLockMappedWriteGuard。 此操作不会失败，因为传入的 RwLockMappedWriteGuard 已经锁定了数据。 这是一个关联函数，需要用作 RwLockMappedWriteGuard::map(..)。方法会与锁定数据内容上的同名方法冲突。 这是来自 parking_lot crate 的 RwLockWriteGuard::map 的异步版本。 §示例 use tokio::sync::{RwLock, RwLockWriteGuard}; #[derive(Debug, Clone, Copy, PartialEq, Eq)] struct Foo(u32); let lock = RwLock::new(Foo(1)); { let mut mapped = RwLockWriteGuard::map(lock.write().await, |f| &mut f.0); *mapped = 2; } assert_eq!(Foo(2), *lock.read().await); 尝试为已锁定数据的某个组件创建一个新的 RwLockMappedWriteGuard。如果闭包返回 None，则返回原始守卫。 此操作不会失败，因为传入的 RwLockMappedWriteGuard 已经锁定了数据。 这是一个关联函数，需要用作 RwLockMappedWriteGuard::try_map(...)。方法会与锁定数据内容上的同名方法冲突。 这是来自 parking_lot crate 的 RwLockWriteGuard::try_map 的异步版本。 §示例 use tokio::sync::{RwLock, RwLockWriteGuard}; #[derive(Debug, Clone, Copy, PartialEq, Eq)] struct Foo(u32); let lock = RwLock::new(Foo(1)); { let guard = lock.write().await; let mut guard = RwLockWriteGuard::try_map(guard, |f| Some(&mut f.0)).expect(\"should not fail\"); *guard = 2; } assert_eq!(Foo(2), *lock.read().await); 使用给定的格式化器格式化此值。 更多信息 解引用后得到的类型。 解引用此值。 以可变方式解引用此值。 使用给定的格式化器格式化此值。 更多信息 执行此类型的析构函数。 更多信息 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 原样返回参数。 调用 U::from(self)。 也就是说，此转换是 From<T> for U 实现选择执行的操作。 Converts the given value to a String. 更多信息 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"用于在 drop 时释放锁的独占写访问权的 RAII 结构。 此结构体通过对 RwLockWriteGuard 进行 映射 创建。它是与 RwLockWriteGuard 分开的类型，因为降级映射后的 guard 可能导致未定义行为，因此不允许这样做。","section_headers":[]},{"url":"tokio/sync/struct.RwLockReadGuard.html","crate":"tokio","title":"RwLockReadGuard","section":"struct","desc":"RAII 结构体，用于在 drop 时释放锁的共享读访问。 此结构体由 RwLock 上的 read 方法创建。 为锁定数据的组件创建一个新的 RwLockReadGuard。 此操作不会失败，因为传入的 RwLockReadGuard 已经锁定了数据。 这是一个关联函数，需要用作 RwLockReadGuard::map(...)。方法会与锁定数据内容上的同名方法冲突。 这是 parking_lot crate 中 RwLockReadGuard::map 的异步版本。 §示例 use tokio::sync::{RwLock, RwLockReadGuard}; #[derive(Debug, Clone, Copy, PartialEq, Eq)] struct Foo(u32); let lock = RwLock::new(Foo(1)); let guard = lock.read().await; let guard = RwLockReadGuard::map(guard, |f| &f.0); assert_eq!(1, *guard); 尝试为锁定数据的组件创建一个新的 RwLockReadGuard。如果闭包返回 None，则返回原始 guard。 此操作不会失败，因为传入的 RwLockReadGuard 已经锁定了数据。 这是一个关联函数，需要用作 RwLockReadGuard::try_map(..)。方法会与锁定数据内容上的同名方法冲突。 这是 parking_lot crate 中 RwLockReadGuard::try_map 的异步版本。 §示例 use tokio::sync::{RwLock, RwLockReadGuard}; #[derive(Debug, Clone, Copy, PartialEq, Eq)] struct Foo(u32); let lock = RwLock::new(Foo(1)); let guard = lock.read().await; let guard = RwLockReadGuard::try_map(guard, |f| Some(&f.0)).expect(\"should not fail\"); assert_eq!(1, *guard); 使用给定的格式化器格式化此值。 更多信息 解引用后得到的类型。 解引用此值。 使用给定的格式化器格式化此值。 更多信息 执行此类型的析构函数。 更多信息 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 原样返回参数。 调用 U::from(self)。 也就是说，此转换是 From<T> for U 实现选择执行的操作。 Converts the given value to a String. 更多信息 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"RAII 结构体，用于在 drop 时释放锁的共享读访问。 此结构体由 RwLock 上的 read 方法创建。","section_headers":[]},{"url":"tokio/sync/struct.RwLockWriteGuard.html","crate":"tokio","title":"RwLockWriteGuard","section":"struct","desc":"用于在 drop 时释放锁的独占写访问权的 RAII 结构。 此结构体由 RwLock 上的 write 方法创建。 为锁定数据的组件创建一个新的 RwLockMappedWriteGuard。 此操作不会失败，因为传入的 RwLockWriteGuard 已经锁定了数据。 这是一个关联函数，需要用作 RwLockWriteGuard::map(..)。方法会与锁定数据内容上的同名方法冲突。 这是来自 parking_lot crate 的 RwLockWriteGuard::map 的异步版本。 §示例 use tokio::sync::{RwLock, RwLockWriteGuard}; #[derive(Debug, Clone, Copy, PartialEq, Eq)] struct Foo(u32); let lock = RwLock::new(Foo(1)); { let mut mapped = RwLockWriteGuard::map(lock.write().await, |f| &mut f.0); *mapped = 2; } assert_eq!(Foo(2), *lock.read().await); 为锁定数据的组件创建一个新的 RwLockReadGuard。 此操作不会失败，因为传入的 RwLockWriteGuard 已经锁定了数据。 这是一个关联函数，需要用作 RwLockWriteGuard::downgrade_map(..)。方法会与锁定数据内容上的同名方法冲突。 这等效于来自 parking_lot crate 的异步 RwLockWriteGuard::map 和 RwLockWriteGuard::downgrade 的组合。 在 f 内部，即使只获得了 &T，你仍然保留对数据的独占访问权。交出 &mut T 将导致内存不安全，因为你可以使用内部可变性。 §示例 use tokio::sync::{RwLock, RwLockWriteGuard}; #[derive(Debug, Clone, Copy, PartialEq, Eq)] struct Foo(u32); let lock = RwLock::new(Foo(1)); let mapped = RwLockWriteGuard::downgrade_map(lock.write().await, |f| &f.0); let foo = lock.read().await; assert_eq!(foo.0, *mapped); 尝试为已锁定数据的某个组件创建一个新的 RwLockMappedWriteGuard。如果闭包返回 None，则返回原始守卫。 此操作不会失败，因为传入的 RwLockWriteGuard 已经锁定了数据。 这是一个关联函数，需要用作 RwLockWriteGuard::try_map(...)。方法会与锁定数据内容上的同名方法冲突。 这是来自 parking_lot crate 的 RwLockWriteGuard::try_map 的异步版本。 §示例 use tokio::sync::{RwLock, RwLockWriteGuard}; #[derive(Debug, Clone, Copy, PartialEq, Eq)] struct Foo(u32); let lock = RwLock::new(Foo(1)); { let guard = lock.write().await; let mut guard = RwLockWriteGuard::try_map(guard, |f| Some(&mut f.0)).expect(\"should not fail\"); *guard = 2; } assert_eq!(Foo(2), *lock.read().await); 尝试为锁定数据的组件创建一个新的 RwLockReadGuard。如果闭包返回 None，则返回原始 guard。 此操作不会失败，因为传入的 RwLockWriteGuard 已经锁定了数据。 这是一个关联函数，需要用作 RwLockWriteGuard::try_downgrade_map(...)。方法会与锁定数据内容上的同名方法冲突。 这等效于来自 parking_lot crate 的异步 RwLockWriteGuard::try_map 和 RwLockWriteGuard::downgrade 的组合。 在 f 内部，即使只获得了 &T，你仍然保留对数据的独占访问权。交出 &mut T 将导致内存不安全，因为你可以使用内部可变性。 如果此函数返回 Err(...)，则锁既不会被","module_doc":"用于在 drop 时释放锁的独占写访问权的 RAII 结构。 此结构体由 RwLock 上的 write 方法创建。","section_headers":[]},{"url":"tokio/sync/struct.Semaphore.html","crate":"tokio","title":"Semaphore","section":"struct","desc":"执行异步许可获取的计数信号量。 信号量维护一组许可。许可用于同步对共享资源的访问。信号量与互斥锁的区别在于，它允许一个以上的并发调用者同时访问共享资源。 当调用 acquire 并且信号量有剩余许可时，函数会立即返回一个许可。但是，如果没有剩余许可可用，acquire 会（异步地）等待，直到某个未完成的许可被 drop。此时，释放的许可将分配给调用者。 此 Semaphore 是公平的，这意味着许可按请求顺序发放。这种公平性也适用于涉及 acquire_many 的情况，因此如果队列前端的 acquire_many 调用请求的许可数超过当前可用的数量，即使信号量有足够的许可完成该 acquire 调用，也会阻止其完成。 要在 poll 函数中使用 Semaphore，可以使用 PollSemaphore 工具。 §示例 基本用法： use tokio::sync::{Semaphore, TryAcquireError}; let semaphore = Semaphore::new(3); let a_permit = semaphore.acquire().await.unwrap(); let two_permits = semaphore.acquire_many(2).await.unwrap(); assert_eq!(semaphore.available_permits(), 0); let permit_attempt = semaphore.try_acquire(); assert_eq!(permit_attempt.err(), Some(TryAcquireError::NoPermits));§限制程序中同时打开的文件数 大多数操作系统对打开的文件句柄数量有限制。即使在没有显式限制的系统中，资源约束也会隐式设置打开文件数量的上限。如果你的程序尝试打开大量文件并超出此限制，则会导致错误。 此示例使用具有 100 个许可的 Semaphore。通过在访问文件之前从 Semaphore 获取许可，你可以确保程序一次最多打开 100 个文件。当尝试打开第 101 个文件时，程序将等待直到有许可可用，然后再继续打开另一个文件。 use std::io::Result; use tokio::fs::File; use tokio::sync::Semaphore; use tokio::io::AsyncWriteExt; static PERMITS: Semaphore = Semaphore::const_new(100); async fn write_to_file(message: &[u8]) -> Result<()> { let _permit = PERMITS.acquire().await.unwrap(); let mut buffer = File::create(\"example.txt\").await?; buffer.write_all(message).await?; Ok(()) // permit 在此处离开作用域，可再次获取 }§限制同时发送的出站请求数 在某些情况下，可能需要限制并行发送的传出请求的数量。这可能是由于所使用 API 的限制或应用程序所运行系统的网络资源限制。 此示例使用具有 10 个许可的 Arc<Semaphore>。通过克隆 Arc<Semaphore>，每个生成的任务都会获得对信号量的引用。在任务发送请求之前，它必须通过调用 Semaphore::acquire 从信号量获取许可。这确保了在任何给定时间最多并行发送 10 个请求。任务发送请求后，它会 drop 该许可以允许其他任务发送请求。 use std::sync::Arc; use tokio::sync::Semaphore; // 定义并行请求的最大数量。 let semaphore = Arc::new(Semaphore::new(5)); // 派生许多任务来发送请求。 let mut jhs = Vec::new(); for task_id in 0..50 { let semaphore = semaphore.clone(); let jh = tokio::spawn(async move { // 在发送请求前获取 permit。 let _permit = semaphore.acquire().await.unwrap(); // 发送请求。 let response = send_request(task_id).await; // 请求发送完毕后释放 permit。 drop(_permit); // 处理响应。 // ... response }); j","module_doc":"执行异步许可获取的计数信号量。 信号量维护一组许可。许可用于同步对共享资源的访问。信号量与互斥锁的区别在于，它允许一个以上的并发调用者同时访问共享资源。 当调用 acquire 并且信号量有剩余许可时，函数会立即返回一个许可。但是，如果没有剩余许可可用，acquire 会（异步地）等待，直到某个未完成的许可被 drop。此时，释放的许可将分配给调用者。 此 Semaphore 是公平的，这意味着许可按请求顺序发放。这种公平性也适用于涉及 acquire_many 的情况，因此如果队列前端的 acquire_many 调用请求的许可数超过当前可用的数量，即使信号量有足够的许可完成该 acquire 调用，也会阻止其完成。 要在 poll 函数中使用 Semaphore，可以使用 PollSemaphore 工具。 基本用法：","section_headers":[]},{"url":"tokio/sync/struct.SemaphorePermit.html","crate":"tokio","title":"SemaphorePermit","section":"struct","desc":"来自信号量的 permit。 此类型由 acquire 方法创建。 不将许可释放回信号量就将其遗忘。这可用于减少信号量中可用的许可数量。 §示例 use std::sync::Arc; use tokio::sync::Semaphore; let sem = Arc::new(Semaphore::new(10)); { let permit = sem.try_acquire_many(5).unwrap(); assert_eq!(sem.available_permits(), 5); permit.forget(); } // 因为我们忘记 permit，可用许可数将不会恢复到其初始值， // 即使 permit 已被丢弃。 assert_eq!(sem.available_permits(), 5); 将两个 SemaphorePermit 实例合并在一起，消费 other 而不释放它持有的 permit。 由 self 和 other 持有的许将在 self 被 drop 时一起释放。 §恐慌 如果合并来自不同 Semaphore 实例的许可，此函数会发生 panic。 §示例 use std::sync::Arc; use tokio::sync::Semaphore; let sem = Arc::new(Semaphore::new(10)); let mut permit = sem.try_acquire().unwrap(); for _ in 0..9 { let _permit = sem.try_acquire().unwrap(); // 将各个 permit 合并为单个 permit。 permit.merge(_permit) } assert_eq!(sem.available_permits(), 0); // 一次性释放所有 permit。 drop(permit); assert_eq!(sem.available_permits(), 10); 从 self 拆分 n 个 permit，并返回持有 n 个 permit 的新 SemaphorePermit 实例。 如果没有足够的许可且无法减少 n，则返回 None。 §示例 use std::sync::Arc; use tokio::sync::Semaphore; let sem = Arc::new(Semaphore::new(3)); let mut p1 = sem.try_acquire_many(3).unwrap(); let p2 = p1.split(1).unwrap(); assert_eq!(p1.num_permits(), 2); assert_eq!(p2.num_permits(), 1); 返回 self 持有的许可数。 使用给定的格式化器格式化此值。 更多信息 执行此类型的析构函数。 更多信息 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 原样返回参数。 调用 U::from(self)。 也就是说，此转换是 From<T> for U 实现选择执行的操作。 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"来自信号量的 permit。 此类型由 acquire 方法创建。","section_headers":[]},{"url":"tokio/sync/struct.SetOnce.html","crate":"tokio","title":"SetOnce","section":"struct","desc":"只能写入一次的线程安全单元。 SetOnce 灵感来自 Python 的 asyncio.Event 类型。它可用于等待 SetOnce 的值被设置，类似于\"事件\"机制。 §示例 use tokio::sync::{SetOnce, SetOnceError}; static ONCE: SetOnce<u32> = SetOnce::const_new(); // 在某处派生一个任务来设置值... tokio::spawn(async move { ONCE.set(20) }); // 使用 .get 检查不会阻塞主线程 println!(\"{:?}\", ONCE.get()); // 等待值被设置会阻塞线程 println!(\"{:?}\", ONCE.wait().await); Ok(()) SetOnce 通常用于需要在首次使用时初始化一次但不需要进一步更改的全局变量。Tokio 中的 SetOnce 允许异步执行初始化过程。 §示例 use tokio::sync::{SetOnce, SetOnceError}; use std::sync::Arc; let once = SetOnce::new(); let arc = Arc::new(once); let first_cl = Arc::clone(&arc); let second_cl = Arc::clone(&arc); // 派生一个任务来设置值 tokio::spawn(async move { first_cl.set(20) }).await.unwrap()?; // 在任务内等待以免阻塞主线程 tokio::spawn(async move { // 在 async 上下文中等待值被设置 assert_eq!(*second_cl.wait().await, 20); }).await.unwrap(); // 后续的 set 调用将失败 assert!(arc.set(30).is_err()); println!(\"{:?}\", arc.get()); Ok(()) 创建一个新的空 SetOnce 实例。 创建一个新的空 SetOnce 实例。 等效于 SetOnce::new，但可以在静态变量中使用。 当使用 tracing 不稳定功能 时，使用 const_new 创建的 SetOnce 不会被检测。因此，它在 tokio-console 中不可见。如果需要，应改用 SetOnce::new 创建被检测的对象。 §示例 use tokio::sync::{SetOnce, SetOnceError}; static ONCE: SetOnce<u32> = SetOnce::const_new(); fn get_global_integer() -> Result<Option<&'static u32>, SetOnceError<u32>> { ONCE.set(2)?; Ok(ONCE.get()) } let result = get_global_integer()?; assert_eq!(result, Some(&2)); Ok(()) 创建一个新的 SetOnce，其中包含所提供的值（如果有）。 如果 Option 为 None，则等效于 SetOnce::new。 创建一个包含所提供值的新 SetOnce。 §示例 当使用 tracing 不稳定功能 时，使用 const_new_with 创建的 SetOnce 不会被检测。因此，它在 tokio-console 中不可见。如果需要，应改用 SetOnce::new_with 创建被检测的对象。 use tokio::sync::SetOnce; static ONCE: SetOnce<u32> = SetOnce::const_new_with(1); fn get_global_integer() -> Option<&'static u32> { ONCE.get() } let result = get_global_integer(); assert_eq!(result, Some(&1)); 如果 SetOnce 当前包含值，则返回 true，否则返回 false。 返回当前存储在 SetOnce 中的值的引用，如果 SetOnce 为空则返回 None。 如果 SetOnce 为空，则将其值设置为给定值。 如果 SetOnce 已经有值，则此调用将失败并返回 SetOnceError。 从单元中取出值，此过程会销毁单元。如果单元为空，则返回 None。 等待直到值被设置。 如果 SetOnce 已初始化，它将立即返回值。 §取消安全性 ","module_doc":"只能写入一次的线程安全单元。 SetOnce 灵感来自 Python 的 asyncio.Event 类型。它可用于等待 SetOnce 的值被设置，类似于\"事件\"机制。","section_headers":[]},{"url":"tokio/sync/struct.SetOnceError.html","crate":"tokio","title":"SetOnceError","section":"struct","desc":"可从 SetOnce::set 返回的错误。 此错误表示在调用 set 时 SetOnce 已被初始化 使用给定的格式化器格式化此值。 更多信息 使用给定的格式化器格式化此值。 更多信息 返回此错误的更底层来源（若有）。 更多信息 Provides type-based access to context intended for error reports. 更多信息 测试 self 与 other 值是否相等，供 == 运算符使用。 测试 != 运算符。默认实现几乎总是够用，除非有非常充分的理由，否则不应被覆盖。 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 原样返回参数。 调用 U::from(self)。 也就是说，此转换是 From<T> for U 实现选择执行的操作。 Converts the given value to a String. 更多信息 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"可从 SetOnce::set 返回的错误。 此错误表示在调用 set 时 SetOnce 已被初始化","section_headers":["元组字段"]},{"url":"tokio/sync/struct.TryLockError.html","crate":"tokio","title":"TryLockError","section":"struct","desc":"从 Mutex::try_lock、RwLock::try_read 和 RwLock::try_write 函数返回的错误。 仅当 mutex 已被锁定时，Mutex::try_lock 操作才会失败。 仅当锁当前由独占写入者持有时，RwLock::try_read 操作才会失败。 仅当锁当前由任何读取者或独占写入者持有时，RwLock::try_write 操作才会失败。 使用给定的格式化器格式化此值。 更多信息 使用给定的格式化器格式化此值。 更多信息 返回此错误的更底层来源（若有）。 更多信息 Provides type-based access to context intended for error reports. 更多信息 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 原样返回参数。 调用 U::from(self)。 也就是说，此转换是 From<T> for U 实现选择执行的操作。 Converts the given value to a String. 更多信息 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"从 Mutex::try_lock、RwLock::try_read 和 RwLock::try_write 函数返回的错误。 仅当 mutex 已被锁定时，Mutex::try_lock 操作才会失败。 仅当锁当前由独占写入者持有时，RwLock::try_read 操作才会失败。 仅当锁当前由任何读取者或独占写入者持有时，RwLock::try_write 操作才会失败。","section_headers":[]},{"url":"tokio/sync/broadcast/fn.channel.html","crate":"tokio","title":"channel","section":"fn","desc":"创建一个有界、多生产者、多消费者的 channel，其中每个发送的值都会广播给所有活动的接收器。 注意：实际容量可能大于所提供的 capacity。 在 Sender 上发送的所有数据将按发送顺序在每个活动的 Receiver 上可用。 可以克隆 Sender，以便从进程中的多个位置 send 到同一 channel，也可以从 Arc 中并发使用。通过调用 Sender::subscribe 创建新的 Receiver 句柄。 如果所有 Receiver 句柄都已被 drop，则 send 方法将返回 SendError。类似地，如果所有 Sender 句柄都已被 drop，则 recv 方法将返回 RecvError。 §示例 use tokio::sync::broadcast; let (tx, mut rx1) = broadcast::channel(16); let mut rx2 = tx.subscribe(); tokio::spawn(async move { assert_eq!(rx1.recv().await.unwrap(), 10); assert_eq!(rx1.recv().await.unwrap(), 20); }); tokio::spawn(async move { assert_eq!(rx2.recv().await.unwrap(), 10); assert_eq!(rx2.recv().await.unwrap(), 20); }); tx.send(10).unwrap(); tx.send(20).unwrap();§恐慌 如果 capacity 等于 0，这将 panic。 这会为 capacity 条消息预分配空间。分配失败可能导致 panic 或分配错误。","module_doc":"创建一个有界、多生产者、多消费者的 channel，其中每个发送的值都会广播给所有活动的接收器。 注意：实际容量可能大于所提供的 capacity。 在 Sender 上发送的所有数据将按发送顺序在每个活动的 Receiver 上可用。 可以克隆 Sender，以便从进程中的多个位置 send 到同一 channel，也可以从 Arc 中并发使用。通过调用 Sender::subscribe 创建新的 Receiver 句柄。 如果所有 Receiver 句柄都已被 drop，则 send 方法将返回 SendError。类似地，如果所有 Sender 句柄都已被 drop，则 recv 方法将返回 RecvError。","section_headers":[]},{"url":"tokio/sync/broadcast/struct.Receiver.html","crate":"tokio","title":"Receiver","section":"struct","desc":"broadcast channel 的接收端。 不能并发使用。可以使用 recv 检索消息。 要将此接收器转换为 Stream，可以使用 BroadcastStream 包装器。 §示例 use tokio::sync::broadcast; let (tx, mut rx1) = broadcast::channel(16); let mut rx2 = tx.subscribe(); tokio::spawn(async move { assert_eq!(rx1.recv().await.unwrap(), 10); assert_eq!(rx1.recv().await.unwrap(), 20); }); tokio::spawn(async move { assert_eq!(rx2.recv().await.unwrap(), 10); assert_eq!(rx2.recv().await.unwrap(), 20); }); tx.send(10).unwrap(); tx.send(20).unwrap(); 返回已发送到 channel 中但此 Receiver 尚未接收的消息数。 如果 len 返回的值大于 channel 容量的下一个最大 2 的幂，则对 recv 的任何调用都将返回 Err(RecvError::Lagged)，并且对 try_recv 的任何调用都将返回 Err(TryRecvError::Lagged)。例如，如果 channel 的容量为 10，则一旦 len 返回大于 16 的值，recv 将开始返回 Err(RecvError::Lagged)。 §示例 use tokio::sync::broadcast; let (tx, mut rx1) = broadcast::channel(16); tx.send(10).unwrap(); tx.send(20).unwrap(); assert_eq!(rx1.len(), 2); assert_eq!(rx1.recv().await.unwrap(), 10); assert_eq!(rx1.len(), 1); assert_eq!(rx1.recv().await.unwrap(), 20); assert_eq!(rx1.len(), 0); 如果 channel 中没有任何 Receiver 尚未接收的消息，则返回 true。 §示例 use tokio::sync::broadcast; let (tx, mut rx1) = broadcast::channel(16); assert!(rx1.is_empty()); tx.send(10).unwrap(); tx.send(20).unwrap(); assert!(!rx1.is_empty()); assert_eq!(rx1.recv().await.unwrap(), 10); assert_eq!(rx1.recv().await.unwrap(), 20); assert!(rx1.is_empty()); 如果这些 receiver 属于同一个通道，则返回 true。 §示例 use tokio::sync::broadcast; let (tx, rx) = broadcast::channel::<()>(16); let rx2 = tx.subscribe(); assert!(rx.same_channel(&rx2)); let (_tx3, rx3) = broadcast::channel::<()>(16); assert!(!rx3.same_channel(&rx2)); 返回 Sender 句柄的数量。 返回 WeakSender 句柄的数量。 检查通道是否已关闭。 如果 channel 已关闭，则此方法返回 true。当所有 Sender 都已被 drop 时，channel 关闭。 §示例 use tokio::sync::broadcast; let (tx, rx) = broadcast::channel::<()>(10); assert!(!rx.is_closed()); drop(tx); assert!(rx.is_closed()); 从当前尾部元素开始重新订阅 channel。 此 Receiver 句柄将接收在重新订阅之后发送的所有值的克隆。这不包括当前接收器队列中的元素。请考虑以下示例。 §示例 use tokio::sync::broadcast; let (tx, mut rx) = broadcast::channel(2); tx.send(1).unwrap(); let m","module_doc":"broadcast channel 的接收端。 不能并发使用。可以使用 recv 检索消息。 要将此接收器转换为 Stream，可以使用 BroadcastStream 包装器。","section_headers":[]},{"url":"tokio/sync/broadcast/struct.Sender.html","crate":"tokio","title":"Sender","section":"struct","desc":"broadcast channel 的发送端。 可以从多个线程使用。可以使用 send 发送消息。 §示例 use tokio::sync::broadcast; let (tx, mut rx1) = broadcast::channel(16); let mut rx2 = tx.subscribe(); tokio::spawn(async move { assert_eq!(rx1.recv().await.unwrap(), 10); assert_eq!(rx1.recv().await.unwrap(), 20); }); tokio::spawn(async move { assert_eq!(rx2.recv().await.unwrap(), 10); assert_eq!(rx2.recv().await.unwrap(), 20); }); tx.send(10).unwrap(); tx.send(20).unwrap(); 创建 broadcast channel 的发送端。 有关此方法的更多信息，请参阅 broadcast::channel 的文档。 尝试向所有活动的 Receiver 句柄发送一个值，如果无法发送则将其返回。 当至少有一个活动的 Receiver 句柄时，发送成功。发送失败是指所有关联的 Receiver 句柄都已被 drop。 §返回值 成功时，返回已订阅的 Receiver 句柄数。这并不意味着这么多接收者将看到该消息，因为接收者可能在接收消息之前 drop 或滞后（参见滞后）。 §注意 Ok 的返回值并不意味着所发送的值将被所有或任何活动的 Receiver 句柄观察到。Receiver 句柄可能在接收到发送的消息之前被 drop。 Err 的返回值并不意味着未来对 send 的调用会失败。可以通过调用 subscribe 来创建新的 Receiver 句柄。 §示例 use tokio::sync::broadcast; let (tx, mut rx1) = broadcast::channel(16); let mut rx2 = tx.subscribe(); tokio::spawn(async move { assert_eq!(rx1.recv().await.unwrap(), 10); assert_eq!(rx1.recv().await.unwrap(), 20); }); tokio::spawn(async move { assert_eq!(rx2.recv().await.unwrap(), 10); assert_eq!(rx2.recv().await.unwrap(), 20); }); tx.send(10).unwrap(); tx.send(20).unwrap(); 创建一个新的 Receiver 句柄，该句柄将接收在此次调用 subscribe 之后发送的值。 §示例 use tokio::sync::broadcast; let (tx, _rx) = broadcast::channel(16); // 不会看到 tx.send(10).unwrap(); let mut rx = tx.subscribe(); tx.send(20).unwrap(); let value = rx.recv().await.unwrap(); assert_eq!(20, value); 将 Sender 转换为 WeakSender，它不计入 RAII 语义，即如果 channel 的所有 Sender 实例都已被 drop，并且只剩下 WeakSender 实例，则 channel 已关闭。 返回已排队值的数量。 一个值在发送时被所有当时活动的接收者看到之前，或被超出队列容量的后续发送从队列中驱逐之前，一直处于排队状态。 §注意 与 Receiver::len 相比，此方法仅报告已排队的值，而不报告在被所有接收者看到之前已从队列中驱逐的值。 §示例 use tokio::sync::broadcast; let (tx, mut rx1) = broadcast::channel(16); let mut rx2 = tx.subscribe(); tx.send(10).unwrap(); tx.send(20).unwrap(); tx.send(30).unwrap(); assert_eq!(tx.len(), 3); rx1.recv().await.unwrap(); // 长度仍为 3，因为 rx2 尚未看到第一个值。 assert_eq!(tx.len(), 3); rx2.recv().await.unwrap()","module_doc":"broadcast channel 的发送端。 可以从多个线程使用。可以使用 send 发送消息。","section_headers":[]},{"url":"tokio/sync/broadcast/struct.WeakSender.html","crate":"tokio","title":"WeakSender","section":"struct","desc":"不会阻止通道被关闭的 sender。 如果 channel 的所有 Sender 实例都已被 drop，仅剩下 WeakSender 实例，则 channel 关闭。 为了发送消息，WeakSender 需要使用 WeakSender::upgrade 进行升级，该函数返回 Option<Sender>。如果所有 Sender 都已被 drop，则返回 None，否则返回 Sender。 §示例 use tokio::sync::broadcast::channel; let (tx, _rx) = channel::<i32>(15); let tx_weak = tx.downgrade(); // 升级会成功，因为 `tx` 仍然存在。 assert!(tx_weak.upgrade().is_some()); // 如果 drop 掉 `tx`，则升级会失败。 drop(tx); assert!(tx_weak.clone().upgrade().is_none()); 尝试将 WeakSender 转换为 Sender。 如果存在其他存活的 Sender 实例且 channel 之前未被 drop，则返回 Some，否则返回 None。 返回 Sender 句柄的数量。 返回 WeakSender 句柄的数量。 返回值的副本。 更多信息 Performs copy-assignment from source. 更多信息 使用给定的格式化器格式化此值。 更多信息 执行此类型的析构函数。 更多信息 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 Performs copy-assignment from self to dest. 更多信息 原样返回参数。 调用 U::from(self)。 也就是说，此转换是 From<T> for U 实现选择执行的操作。 获得所有权后的类型。 Creates owned data from borrowed data, usually by cloning. 更多信息 Uses borrowed data to replace owned data, usually by cloning. 更多信息 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"不会阻止通道被关闭的 sender。 如果 channel 的所有 Sender 实例都已被 drop，仅剩下 WeakSender 实例，则 channel 关闭。 为了发送消息，WeakSender 需要使用 WeakSender::upgrade 进行升级，该函数返回 Option<Sender>。如果所有 Sender 都已被 drop，则返回 None，否则返回 Sender。","section_headers":[]},{"url":"tokio/sync/broadcast/error/enum.RecvError.html","crate":"tokio","title":"RecvError","section":"enum","desc":"从 Receiver 上的 recv 函数返回的错误。 没有更多活动的 sender，这意味着不会再发送任何消息。 接收者落后太多。尝试再次接收将返回 channel 仍保留的最旧消息。 包含已跳过消息的数量。 返回值的副本。 更多信息 Performs copy-assignment from source. 更多信息 使用给定的格式化器格式化此值。 更多信息 使用给定的格式化器格式化此值。 更多信息 返回此错误的更底层来源（若有）。 更多信息 Provides type-based access to context intended for error reports. 更多信息 测试 self 与 other 值是否相等，供 == 运算符使用。 测试 != 运算符。默认实现几乎总是够用，除非有非常充分的理由，否则不应被覆盖。 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 Performs copy-assignment from self to dest. 更多信息 原样返回参数。 调用 U::from(self)。 也就是说，此转换是 From<T> for U 实现选择执行的操作。 获得所有权后的类型。 Creates owned data from borrowed data, usually by cloning. 更多信息 Uses borrowed data to replace owned data, usually by cloning. 更多信息 Converts the given value to a String. 更多信息 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"从 Receiver 上的 recv 函数返回的错误。","section_headers":["变体"]},{"url":"tokio/sync/broadcast/error/enum.TryRecvError.html","crate":"tokio","title":"TryRecvError","section":"enum","desc":"从 Receiver 上的 try_recv 函数返回的错误。 channel 当前为空。还有活动的 Sender 句柄，因此数据可能尚不可用。 没有更多活动的 sender，这意味着不会再发送任何消息。 接收者落后太多，已被强制断开连接。尝试再次接收将返回 channel 仍保留的最旧消息。 包含已跳过消息的数量。 返回值的副本。 更多信息 Performs copy-assignment from source. 更多信息 使用给定的格式化器格式化此值。 更多信息 使用给定的格式化器格式化此值。 更多信息 返回此错误的更底层来源（若有）。 更多信息 Provides type-based access to context intended for error reports. 更多信息 测试 self 与 other 值是否相等，供 == 运算符使用。 测试 != 运算符。默认实现几乎总是够用，除非有非常充分的理由，否则不应被覆盖。 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 Performs copy-assignment from self to dest. 更多信息 原样返回参数。 调用 U::from(self)。 也就是说，此转换是 From<T> for U 实现选择执行的操作。 获得所有权后的类型。 Creates owned data from borrowed data, usually by cloning. 更多信息 Uses borrowed data to replace owned data, usually by cloning. 更多信息 Converts the given value to a String. 更多信息 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"从 Receiver 上的 try_recv 函数返回的错误。","section_headers":["变体"]},{"url":"tokio/sync/broadcast/error/struct.SendError.html","crate":"tokio","title":"SendError","section":"struct","desc":"由 Sender 上的 send 函数返回的错误。 仅当没有活动的接收者时，send 操作才会失败，这意味着该消息永远无法被接收。错误包含作为有效负载的正在发送的消息，以便可以恢复它。 使用给定的格式化器格式化此值。 更多信息 使用给定的格式化器格式化此值。 更多信息 返回此错误的更底层来源（若有）。 更多信息 Provides type-based access to context intended for error reports. 更多信息 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 原样返回参数。 调用 U::from(self)。 也就是说，此转换是 From<T> for U 实现选择执行的操作。 Converts the given value to a String. 更多信息 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"由 Sender 上的 send 函数返回的错误。 仅当没有活动的接收者时，send 操作才会失败，这意味着该消息永远无法被接收。错误包含作为有效负载的正在发送的消息，以便可以恢复它。","section_headers":["元组字段"]},{"url":"tokio/sync/futures/struct.Notified.html","crate":"tokio","title":"Notified","section":"struct","desc":"从 Notify::notified() 返回的 Future。 此 future 是 fused 的，因此一旦它完成，后续任何对 poll 的调用都会立即返回 Poll::Ready。 将此 future 添加到已准备好通过调用 notify_one 接收唤醒的 future 列表中。 poll future 也会将其添加到列表中，因此仅当你想在第一次调用 poll 之前将 future 添加到列表中时才应使用此方法。（实际上，此方法等效于调用 poll，只是未注册 Waker。） 这对使用 notify_waiters 发送的通知没有影响，只要它们发生在 Notified 创建之后，就会被接收，无论是否已调用 enable 或 poll。 如果 Notified 已就绪，则此方法返回 true。在以下情况下会发生这种情况： The notify_waiters method was called between the creation of the Notified and the call to this method. This is the first call to enable or poll on this future, and the Notify was holding a permit from a previous call to notify_one. The call consumes the permit in that case. The future has previously been enabled or polled, and it has since then been marked ready by either consuming a permit from the Notify, or by a call to notify_one or notify_waiters that removed it from the list of futures ready to receive wakeups. 如果此方法返回 true，则对同一 future 的任何后续 poll 调用将立即返回 Poll::Ready。 §示例 无界多生产者多消费者 (mpmc) 通道。 对 enable 的调用很重要，因为否则如果你并行进行两次 recv 调用和两次 send 调用，则可能发生以下情况： Both calls to try_recv return None. Both new elements are added to the vector. The notify_one method is called twice, adding only a single permit to the Notify. Both calls to recv reach the Notified future. One of them consumes the permit, and the other sleeps forever. 如果在 try_recv 之前通过调用 enable 将 Notified future 添加到列表中，则第三步中的 notify_one 调用会从列表中移除这些 future 并将它们标记为已通知，而不是向 Notify 添加许可。这确保了两个 future 都会被唤醒。 use tokio::sync::Notify; use std::collections::VecDeque; use std::sync::Mutex; struct Channel<T> { messages: Mutex<VecDeque<T>>, notify_on_sent: Notify, } impl<T> Channel<T> { pub fn send(&self, msg: T) { let mut locked_queue = self.messages.lock().unwrap(); locked_queue.push_back(msg); drop(locked_queue); // 向当前正在调用 `recv` 的 // 其中一个调用发送通知。 self.notify_on_sent.notify_one(); } pub fn try_recv(&self) -> Option<T> { let mut locked_queue = self.messages.lock().unwrap(); locked_queue.pop_front() } pub async fn recv(&self) -> T { let future = self.notify_on","module_doc":"从 Notify::notified() 返回的 Future。 此 future 是 fused 的，因此一旦它完成，后续任何对 poll 的调用都会立即返回 Poll::Ready。","section_headers":[]},{"url":"tokio/sync/futures/struct.OwnedNotified.html","crate":"tokio","title":"OwnedNotified","section":"struct","desc":"从 Notify::notified_owned() 返回的 Future。 此 future 是 fused 的，因此一旦它完成，后续任何对 poll 的调用都会立即返回 Poll::Ready。 将此 future 添加到已准备好通过调用 notify_one 接收唤醒的 future 列表中。 有关更多详细信息，请参阅 Notified::enable。 使用给定的格式化器格式化此值。 更多信息 执行此类型的析构函数。 更多信息 Future 完成时产生的值的类型。 Attempts to resolve the future to a final value, registering the current task for wakeup if the value is not yet available. 更多信息 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 原样返回参数。 调用 U::from(self)。 也就是说，此转换是 From<T> for U 实现选择执行的操作。 Future 完成时产生的输出。 我们将要把此值转变成哪种 future？ Creates a future from a value. 更多信息 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"从 Notify::notified_owned() 返回的 Future。 此 future 是 fused 的，因此一旦它完成，后续任何对 poll 的调用都会立即返回 Poll::Ready。","section_headers":[]},{"url":"tokio/sync/mpsc/fn.channel.html","crate":"tokio","title":"channel","section":"fn","desc":"创建一个有界 mpsc channel，用于在异步任务之间通信，并提供背压。 channel 将缓冲最多提供数量的消息。一旦缓冲区已满，尝试发送新消息将等待，直到从 channel 接收一条消息为止。提供的缓冲区容量必须至少为 1。 在 Sender 上发送的所有数据将按发送顺序在 Receiver 上可用。 可以克隆 Sender 以便从多个代码位置 send 到同一 channel。仅支持一个 Receiver。 如果尝试 send 时 Receiver 已断开连接，则 send 方法将返回 SendError。类似地，如果尝试 recv 时 Sender 已断开连接，则 recv 方法将返回 None。 §恐慌 如果缓冲区容量为 0 或过大，则会 panic。目前最大容量为 Semaphore::MAX_PERMITS。 §示例 use tokio::sync::mpsc; let (tx, mut rx) = mpsc::channel(100); tokio::spawn(async move { for i in 0..10 { if let Err(_) = tx.send(i).await { println!(\"receiver dropped\"); return; } } }); while let Some(i) = rx.recv().await { println!(\"got = {}\", i); }","module_doc":"创建一个有界 mpsc channel，用于在异步任务之间通信，并提供背压。 channel 将缓冲最多提供数量的消息。一旦缓冲区已满，尝试发送新消息将等待，直到从 channel 接收一条消息为止。提供的缓冲区容量必须至少为 1。 在 Sender 上发送的所有数据将按发送顺序在 Receiver 上可用。 可以克隆 Sender 以便从多个代码位置 send 到同一 channel。仅支持一个 Receiver。 如果尝试 send 时 Receiver 已断开连接，则 send 方法将返回 SendError。类似地，如果尝试 recv 时 Sender 已断开连接，则 recv 方法将返回 None。 如果缓冲区容量为 0 或过大，则会 panic。目前最大容量为 Semaphore::MAX_PERMITS。","section_headers":[]},{"url":"tokio/sync/mpsc/fn.unbounded_channel.html","crate":"tokio","title":"unbounded_channel","section":"fn","desc":"创建一个无界 mpsc channel，用于在异步任务之间通信，没有背压。 只要接收端尚未关闭，此 channel 上的 send 始终会成功。如果接收者落后，消息将被任意缓冲。 注意 that the amount of available system memory is an implicit bound to the channel. Using an unbounded channel has the ability of causing the process to run out of memory. In this case, the process will be aborted.","module_doc":"创建一个无界 mpsc channel，用于在异步任务之间通信，没有背压。 只要接收端尚未关闭，此 channel 上的 send 始终会成功。如果接收者落后，消息将被任意缓冲。 注意 that the amount of available system memory is an implicit bound to the channel. Using an unbounded channel has the ability of causing the process to run out of memory. In this case, the process will be aborted.","section_headers":[]},{"url":"tokio/sync/mpsc/struct.OwnedPermit.html","crate":"tokio","title":"OwnedPermit","section":"struct","desc":"用于向 channel 发送一个值的 owned permit。 这与 Permit 类型相同，只是它移动 sender 而不是借用它。 OwnedPermit 值由 Sender::reserve_owned() 和 Sender::try_reserve_owned() 返回，用于在生成要发送的消息之前保证 channel 容量。 使用预留的容量发送一个值。 消息的容量已被预留。消息被发送到 receiver，并且许可被消费。即使 receiver 半部分已关闭，此操作也会成功。有关执行干净关闭的更多详细信息，请参阅 Receiver::close。 与 Permit::send 不同，此方法返回预留 OwnedPermit 的 Sender。 §示例 use tokio::sync::mpsc; let (tx, mut rx) = mpsc::channel(1); // 预留容量 let permit = tx.reserve_owned().await.unwrap(); // 在 permit 上发送一条消息，并返回发送端。 let tx = permit.send(456); // 接收在 permit 上发送的值 assert_eq!(rx.recv().await.unwrap(), 456); // 现在可以复用 `tx` 发送另一条消息。 tx.send(789).await.unwrap(); 不发送消息就释放预留的容量，并返回 Sender。 §示例 use tokio::sync::mpsc; let (tx, rx) = mpsc::channel(1); // 克隆发送端并预留容量 let permit = tx.clone().reserve_owned().await.unwrap(); // 在原始 `tx` 上发送会失败，因为 `permit` // 已预留了全部可用容量。 assert!(tx.try_send(123).is_err()); // 释放 permit 而不发送消息，并返回发送端的克隆。 let tx2 = permit.release(); // 现在可以复用 `tx` 发送另一条消息。 tx.send(789).await.unwrap(); 如果这些 permit 属于同一个 channel，则返回 true。 §示例 use tokio::sync::mpsc; let (tx, rx) = mpsc::channel::<()>(2); let permit1 = tx.clone().reserve_owned().await.unwrap(); let permit2 = tx.clone().reserve_owned().await.unwrap(); assert!(permit1.same_channel(&permit2)); let (tx2, rx2) = mpsc::channel::<()>(1); let permit3 = tx2.clone().reserve_owned().await.unwrap(); assert!(!permit3.same_channel(&permit2)); 如果此 permit 与给定的 Sender 属于同一个 channel，则返回 true。 §示例 use tokio::sync::mpsc; let (tx, rx) = mpsc::channel::<()>(1); let permit = tx.clone().reserve_owned().await.unwrap(); assert!(permit.same_channel_as_sender(&tx)); let (tx2, rx2) = mpsc::channel::<()>(1); assert!(!permit.same_channel_as_sender(&tx2)); 使用给定的格式化器格式化此值。 更多信息 执行此类型的析构函数。 更多信息 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 原样返回参数。 调用 U::from(self)。 也就是说，此转换是 From<T> for U 实现选择执行的操作。 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"用于向 channel 发送一个值的 owned permit。 这与 Permit 类型相同，只是它移动 sender 而不是借用它。 OwnedPermit 值由 Sender::reserve_owned() 和 Sender::try_reserve_owned() 返回，用于在生成要发送的消息之前保证 channel 容量。","section_headers":[]},{"url":"tokio/sync/mpsc/struct.Permit.html","crate":"tokio","title":"Permit","section":"struct","desc":"允许向 channel 发送一个值的 permit。 Permit 值由 Sender::reserve() 和 Sender::try_reserve() 返回，用于在生成要发送的消息之前保证 channel 容量。 使用预留的容量发送一个值。 消息的容量已被预留。消息被发送到 receiver，并且许可被消费。即使 receiver 半部分已关闭，此操作也会成功。有关执行干净关闭的更多详细信息，请参阅 Receiver::close。 §示例 use tokio::sync::mpsc; let (tx, mut rx) = mpsc::channel(1); // 预留容量 let permit = tx.reserve().await.unwrap(); // 直接在 `tx` 上发送会失败， // 因为没有可用容量。 assert!(tx.try_send(123).is_err()); // 在 permit 上发送一条消息 permit.send(456); // 接收在 permit 上发送的值 assert_eq!(rx.recv().await.unwrap(), 456); 使用给定的格式化器格式化此值。 更多信息 执行此类型的析构函数。 更多信息 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 原样返回参数。 调用 U::from(self)。 也就是说，此转换是 From<T> for U 实现选择执行的操作。 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"允许向 channel 发送一个值的 permit。 Permit 值由 Sender::reserve() 和 Sender::try_reserve() 返回，用于在生成要发送的消息之前保证 channel 容量。","section_headers":[]},{"url":"tokio/sync/mpsc/struct.PermitIterator.html","crate":"tokio","title":"PermitIterator","section":"struct","desc":"Permit 的 Iterator，可用于在 channel 中保留 n 个槽。 PermitIterator 值由 Sender::reserve_many() 和 Sender::try_reserve_many() 返回，用于在生成要发送的 n 条消息之前保证 channel 容量。 使用给定的格式化器格式化此值。 更多信息 执行此类型的析构函数。 更多信息 Returns the exact remaining length of the iterator. 更多信息 Returns true if the iterator is empty. 更多信息 被迭代元素的类型。 Advances the iterator and returns the next value. 更多信息 Returns the bounds on the remaining length of the iterator. 更多信息 Advances the iterator and returns an array containing the next N values. 更多信息 Consumes the iterator, counting the number of iterations and returning it. 更多信息 Consumes the iterator, returning the last element. 更多信息 Advances the iterator by n elements. 更多信息 Returns the nth element of the iterator. 更多信息 Creates an iterator starting at the same point, but stepping by the given amount at each iteration. 更多信息 Takes two iterators and creates a new iterator over both in sequence. 更多信息 ‘Zips up’ two iterators into a single iterator of pairs. 更多信息 Creates a new iterator which places an item generated by separator between adjacent items of the original iterator. 更多信息 Takes a closure and creates an iterator which calls that closure on each element. 更多信息 Calls a closure on each element of an iterator. 更多信息 Creates an iterator which uses a closure to determine if an element should be yielded. 更多信息 Creates an iterator that both filters and maps. 更多信息 Creates an iterator which gives the current iteration count as well as the next value. 更多信息 Creates an iterator which can use the peek and peek_mut methods to look at the next element of the iterator without consuming it. See their documentation for more information. 更多信息 Creates an iterator that skips elements based on a predicate. 更多信息 Creates an iterator that yields elements based on a predicate. 更多信息 Creates an iterator that both yields elements based on a predicate and maps. 更多信息 Creates an iterator that skips the first n elements. 更多信息 Creates an iterator that yields the first n elements, or fewer if the underlying iterat","module_doc":"Permit 的 Iterator，可用于在 channel 中保留 n 个槽。 PermitIterator 值由 Sender::reserve_many() 和 Sender::try_reserve_many() 返回，用于在生成要发送的 n 条消息之前保证 channel 容量。","section_headers":[]},{"url":"tokio/sync/mpsc/struct.Receiver.html","crate":"tokio","title":"Receiver","section":"struct","desc":"从关联的 Sender 接收值。 实例由 channel 函数创建。 此接收器可以使用 ReceiverStream 转换为 Stream。 接收此接收器的下一个值。 如果通道已关闭且通道缓冲区中没有剩余消息，则此方法返回 None。这表明从此 Receiver 不再可能接收到任何值。当所有 sender 都被 drop 或调用了 close 时，通道会被关闭。 如果 channel 的缓冲区中没有消息，但 channel 尚未关闭，则此方法将休眠，直到有消息被发送或 channel 被关闭。请注意，如果调用了 close，但仍有之前未完成的 Permits，则 recv 不会认为 channel 已关闭，直到这些 permit 被释放。 §取消安全性 此方法可安全取消。如果 recv 在 tokio::select! 语句中作为事件使用，并且其他分支先完成，则可以保证此通道没有接收到任何消息。 §示例 use tokio::sync::mpsc; let (tx, mut rx) = mpsc::channel(100); tokio::spawn(async move { tx.send(\"hello\").await.unwrap(); }); assert_eq!(Some(\"hello\"), rx.recv().await); assert_eq!(None, rx.recv().await); 值被缓冲： use tokio::sync::mpsc; let (tx, mut rx) = mpsc::channel(100); tx.send(\"hello\").await.unwrap(); tx.send(\"world\").await.unwrap(); assert_eq!(Some(\"hello\"), rx.recv().await); assert_eq!(Some(\"world\"), rx.recv().await); 为此 receiver 接收下一个值并扩展 buffer。 此方法以不超过 limit 指定的固定值数量扩展 buffer。如果 limit 为零，则函数立即返回 0。返回值是添加到 buffer 的值的数量。 对于 limit > 0，如果 channel 队列中没有消息，但 channel 尚未关闭，则此方法将休眠，直到有消息被发送或 channel 被关闭。请注意，如果调用了 close，但仍有之前未完成的 Permits，则 recv_many 不会认为 channel 已关闭，直到这些 permit 被释放。 对于 limit 的非零值，此方法永远不会返回 0，除非 channel 已关闭并且 channel 队列中没有剩余消息。这表明永远无法从此 Receiver 接收更多值。当所有 sender 都已被 drop 或调用 close 时，channel 关闭。 buffer 的容量会按需增加。 §取消安全性 此方法可安全取消。如果 recv_many 在 tokio::select! 语句中作为事件使用，并且其他分支先完成，则可以保证此通道没有接收到任何消息。 §示例 use tokio::sync::mpsc; let mut buffer: Vec<&str> = Vec::with_capacity(2); let limit = 2; let (tx, mut rx) = mpsc::channel(100); let tx2 = tx.clone(); tx2.send(\"first\").await.unwrap(); tx2.send(\"second\").await.unwrap(); tx2.send(\"third\").await.unwrap(); // 调用 `recv_many` 接收最多 `limit`（2）个值。 assert_eq!(2, rx.recv_many(&mut buffer, limit).await); assert_eq!(vec![\"first\", \"second\"], buffer); // 如果缓冲区已满，下一次 `recv_many` 调用会 // 预留额外的容量。 assert_eq!(1, rx.recv_many(&mut buffer, 1).await); tokio::spawn(async move { tx.send(\"fourth\").await.unwrap(); }); // 虽然 `tx` 已被丢弃，但 `recv_many` // 保证不返回 0，因为通道 // 尚未关闭。 assert_eq!(1, rx.recv_many(&mut buffer, 1).await); assert_eq!(ve","module_doc":"从关联的 Sender 接收值。 实例由 channel 函数创建。 此接收器可以使用 ReceiverStream 转换为 Stream。","section_headers":[]},{"url":"tokio/sync/mpsc/struct.Sender.html","crate":"tokio","title":"Sender","section":"struct","desc":"向关联的 Receiver 发送值。 实例由 channel 函数创建。 若要将 Sender 转换为 Sink 或在 poll 函数中使用它，可以使用 PollSender 工具。 发送一个值，一直等待直到有可用容量。 当确定通道的另一端尚未挂起时，发送成功。不成功的发送指的是相应的接收端已被关闭的情况。请注意，返回 Err 意味着数据将永远不会被接收，但返回 Ok 并不意味着数据一定会被接收。完全有可能在此函数返回 Ok 后，相应的接收端立即挂起。 §错误 如果通道的接收半部分已关闭（无论是由于调用了 close，还是由于 Receiver 句柄被 drop），则函数返回错误。错误中包含传递给 send 的值。 §取消安全性 如果在 tokio::select! 语句中将 send 用作事件，并且其他某个分支先完成，那么可以保证该消息未被发送。但是，在这种情况下，消息会被丢弃并丢失。 为避免消息丢失，请使用 reserve 预留容量，然后使用返回的 Permit 发送消息。 此通道使用一个队列来确保对 send 和 reserve 的调用按其请求顺序完成。取消对 send 的调用会使你失去在队列中的位置。 §示例 在以下示例中，每次调用 send 都会阻塞，直到先前发送的值被接收。 use tokio::sync::mpsc; let (tx, mut rx) = mpsc::channel(1); tokio::spawn(async move { for i in 0..10 { if let Err(_) = tx.send(i).await { println!(\"receiver dropped\"); return; } } }); while let Some(i) = rx.recv().await { println!(\"got = {}\", i); } 当 receiver 被 drop 时完成。 这允许生产者在对所产生值的兴趣被取消时收到通知，并立即停止工作。 §取消安全性 此方法可安全取消。一旦通道关闭，它将永远保持关闭状态，并且所有对 closed 的未来调用都将立即返回。 §示例 use tokio::sync::mpsc; let (tx1, rx) = mpsc::channel::<()>(1); let tx2 = tx1.clone(); let tx3 = tx1.clone(); let tx4 = tx1.clone(); let tx5 = tx1.clone(); tokio::spawn(async move { drop(rx); }); futures::join!( tx1.closed(), tx2.closed(), tx3.closed(), tx4.closed(), tx5.closed() ); println!(\"Receiver dropped\"); 尝试在此 Sender 上立即发送一条消息。 此方法与 send 的区别在于：如果通道的缓冲区已满或没有接收者等待获取数据，则会立即返回。与 send 相比，此函数有两种失败情况而不是一种（一种表示断开连接，一种表示缓冲区已满）。 §错误 如果已达到通道容量，即通道有 n 个已缓冲的值，其中 n 是传递给 channel 的参数，则会返回一个错误。 如果通道的接收半部分已关闭（无论是由于调用了 close，还是由于 Receiver 句柄被 drop），则函数返回错误。错误中包含传递给 send 的值。 §示例 use tokio::sync::mpsc; // 创建一个缓冲区大小为 1 的通道 let (tx1, mut rx) = mpsc::channel(1); let tx2 = tx1.clone(); tokio::spawn(async move { tx1.send(1).await.unwrap(); tx1.send(2).await.unwrap(); // 任务会一直等待直到接收端接收到值。 }); tokio::spawn(async move { // 如果缓冲区已满，这会返回错误 // 并且不发送任何消息。 let _ = tx2.try_send(3); }); let mut msg; msg = rx.recv().await.unwrap(); println!(\"message {} received\", msg); msg = rx.recv().await.unwrap(); println!(\"message {} received\", msg); // 第三条消息可能从未被发送 match rx.recv().await { Some(msg) => pri","module_doc":"向关联的 Receiver 发送值。 实例由 channel 函数创建。 若要将 Sender 转换为 Sink 或在 poll 函数中使用它，可以使用 PollSender 工具。","section_headers":[]},{"url":"tokio/sync/mpsc/struct.UnboundedReceiver.html","crate":"tokio","title":"UnboundedReceiver","section":"struct","desc":"从关联的 UnboundedSender 接收值。 实例由 unbounded_channel 函数创建。 此接收器可以使用 UnboundedReceiverStream 转换为 Stream。 接收此接收器的下一个值。 如果 channel 已关闭并且 channel 缓冲区中没有剩余消息，则此方法返回 None。这表明永远无法从此 Receiver 接收更多值。当所有 sender 都已被 drop 或调用 close 时，channel 关闭。 如果 channel 的缓冲区中没有消息，但 channel 尚未关闭，则此方法将休眠，直到有消息被发送或 channel 被关闭。 §取消安全性 此方法可安全取消。如果 recv 在 tokio::select! 语句中作为事件使用，并且其他分支先完成，则可以保证此通道没有接收到任何消息。 §示例 use tokio::sync::mpsc; let (tx, mut rx) = mpsc::unbounded_channel(); tokio::spawn(async move { tx.send(\"hello\").unwrap(); }); assert_eq!(Some(\"hello\"), rx.recv().await); assert_eq!(None, rx.recv().await); 值被缓冲： use tokio::sync::mpsc; let (tx, mut rx) = mpsc::unbounded_channel(); tx.send(\"hello\").unwrap(); tx.send(\"world\").unwrap(); assert_eq!(Some(\"hello\"), rx.recv().await); assert_eq!(Some(\"world\"), rx.recv().await); 为此 receiver 接收下一个值并扩展 buffer。 此方法以不超过 limit 指定的固定值数量扩展 buffer。如果 limit 为零，则函数立即返回 0。返回值是添加到 buffer 的值的数量。 对于 limit > 0，如果 channel 队列中没有消息，但 channel 尚未关闭，则此方法将休眠，直到有消息被发送或 channel 被关闭。 对于 limit 的非零值，此方法永远不会返回 0，除非 channel 已关闭并且 channel 队列中没有剩余消息。这表明永远无法从此 Receiver 接收更多值。当所有 sender 都已被 drop 或调用 close 时，channel 关闭。 buffer 的容量会按需增加。 §取消安全性 此方法可安全取消。如果 recv_many 在 tokio::select! 语句中作为事件使用，并且其他分支先完成，则可以保证此通道没有接收到任何消息。 §示例 use tokio::sync::mpsc; let mut buffer: Vec<&str> = Vec::with_capacity(2); let limit = 2; let (tx, mut rx) = mpsc::unbounded_channel(); let tx2 = tx.clone(); tx2.send(\"first\").unwrap(); tx2.send(\"second\").unwrap(); tx2.send(\"third\").unwrap(); // 调用 `recv_many` 接收最多 `limit`（2）个值。 assert_eq!(2, rx.recv_many(&mut buffer, limit).await); assert_eq!(vec![\"first\", \"second\"], buffer); // 如果缓冲区已满，下一次 `recv_many` 调用会 // 预留额外的容量。 assert_eq!(1, rx.recv_many(&mut buffer, limit).await); tokio::spawn(async move { tx.send(\"fourth\").unwrap(); }); // 虽然 `tx` 已被丢弃，但 `recv_many` // 保证不返回 0，因为通道 // 尚未关闭。 assert_eq!(1, rx.recv_many(&mut buffer, limit).await); assert_eq!(vec![\"first\", \"second\", \"third\", \"fourth\"], buffer); // 最后一个发送端被丢弃后，通道关闭， // `recv_many` 返回 0，容量保持不变。 drop(tx2); assert_eq","module_doc":"从关联的 UnboundedSender 接收值。 实例由 unbounded_channel 函数创建。 此接收器可以使用 UnboundedReceiverStream 转换为 Stream。","section_headers":[]},{"url":"tokio/sync/mpsc/struct.UnboundedSender.html","crate":"tokio","title":"UnboundedSender","section":"struct","desc":"向关联的 UnboundedReceiver 发送值。 实例由 unbounded_channel 函数创建。 尝试在不阻塞的情况下通过此 UnboundedSender 发送消息。 此方法未标记为 async，因为向无界 channel 发送消息永远不需要任何形式的等待。这是由于 channel 的无限容量，允许 send 操作立即完成。因此，send 方法可以在同步和异步代码中使用而不会出现问题。 如果 channel 的接收端已关闭（无论是由于调用了 close 还是 UnboundedReceiver 已被 drop），则此函数返回错误。错误包括传递给 send 的值。 当 receiver 被 drop 时完成。 这允许生产者在对所产生值的兴趣被取消时收到通知，并立即停止工作。 §取消安全性 此方法可安全取消。一旦通道关闭，它将永远保持关闭状态，并且所有对 closed 的未来调用都将立即返回。 §示例 use tokio::sync::mpsc; let (tx1, rx) = mpsc::unbounded_channel::<()>(); let tx2 = tx1.clone(); let tx3 = tx1.clone(); let tx4 = tx1.clone(); let tx5 = tx1.clone(); tokio::spawn(async move { drop(rx); }); futures::join!( tx1.closed(), tx2.closed(), tx3.closed(), tx4.closed(), tx5.closed() ); println!(\"Receiver dropped\"); 检查 channel 是否已关闭。当 UnboundedReceiver 被 drop，或调用 UnboundedReceiver::close 方法时，会发生这种情况。 let (tx, rx) = tokio::sync::mpsc::unbounded_channel::<()>(); assert!(!tx.is_closed()); let tx2 = tx.clone(); assert!(!tx2.is_closed()); drop(rx); assert!(tx.is_closed()); assert!(tx2.is_closed()); 如果这些 sender 属于同一个通道，则返回 true。 §示例 let (tx, rx) = tokio::sync::mpsc::unbounded_channel::<()>(); let tx2 = tx.clone(); assert!(tx.same_channel(&tx2)); let (tx3, rx3) = tokio::sync::mpsc::unbounded_channel::<()>(); assert!(!tx3.same_channel(&tx2)); 将 UnboundedSender 转换为 WeakUnboundedSender，后者不计入 RAII 语义，即如果 channel 的所有 UnboundedSender 实例都已被 drop，仅剩下 WeakUnboundedSender 实例，则 channel 关闭。 返回 UnboundedSender 句柄的数量。 返回 WeakUnboundedSender 句柄的数量。 返回值的副本。 更多信息 Performs copy-assignment from source. 更多信息 使用给定的格式化器格式化此值。 更多信息 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 Performs copy-assignment from self to dest. 更多信息 原样返回参数。 调用 U::from(self)。 也就是说，此转换是 From<T> for U 实现选择执行的操作。 获得所有权后的类型。 Creates owned data from borrowed data, usually by cloning. 更多信息 Uses borrowed data to replace owned data, usually by cloning. 更多信息 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"向关联的 UnboundedReceiver 发送值。 实例由 unbounded_channel 函数创建。","section_headers":[]},{"url":"tokio/sync/mpsc/struct.WeakSender.html","crate":"tokio","title":"WeakSender","section":"struct","desc":"不会阻止通道被关闭的 sender。 如果 channel 的所有 Sender 实例都已被 drop，仅剩下 WeakSender 实例，则 channel 关闭。 为了发送消息，WeakSender 需要使用 WeakSender::upgrade 进行升级，该函数返回 Option<Sender>。如果所有 Sender 都已被 drop，则返回 None，否则返回 Sender。 §示例 use tokio::sync::mpsc::channel; let (tx, _rx) = channel::<i32>(15); let tx_weak = tx.downgrade(); // 升级会成功，因为 `tx` 仍然存在。 assert!(tx_weak.upgrade().is_some()); // 如果 drop 掉 `tx`，则升级会失败。 drop(tx); assert!(tx_weak.clone().upgrade().is_none()); 尝试将 WeakSender 转换为 Sender。如果存在其他存活的 Sender 实例且 channel 之前未被 drop，则返回 Some，否则返回 None。 返回 Sender 句柄的数量。 返回 WeakSender 句柄的数量。 返回值的副本。 更多信息 Performs copy-assignment from source. 更多信息 使用给定的格式化器格式化此值。 更多信息 执行此类型的析构函数。 更多信息 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 Performs copy-assignment from self to dest. 更多信息 原样返回参数。 调用 U::from(self)。 也就是说，此转换是 From<T> for U 实现选择执行的操作。 获得所有权后的类型。 Creates owned data from borrowed data, usually by cloning. 更多信息 Uses borrowed data to replace owned data, usually by cloning. 更多信息 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"不会阻止通道被关闭的 sender。 如果 channel 的所有 Sender 实例都已被 drop，仅剩下 WeakSender 实例，则 channel 关闭。 为了发送消息，WeakSender 需要使用 WeakSender::upgrade 进行升级，该函数返回 Option<Sender>。如果所有 Sender 都已被 drop，则返回 None，否则返回 Sender。","section_headers":[]},{"url":"tokio/sync/mpsc/struct.WeakUnboundedSender.html","crate":"tokio","title":"WeakUnboundedSender","section":"struct","desc":"不会阻止 channel 被关闭的无界 sender。 如果 channel 的所有 UnboundedSender 实例都已被 drop，仅剩下 WeakUnboundedSender 实例，则 channel 关闭。 为了发送消息，WeakUnboundedSender 需要使用 WeakUnboundedSender::upgrade 进行升级，该函数返回 Option<UnboundedSender>。如果所有 UnboundedSender 都已被 drop，则返回 None，否则返回 UnboundedSender。 §示例 use tokio::sync::mpsc::unbounded_channel; let (tx, _rx) = unbounded_channel::<i32>(); let tx_weak = tx.downgrade(); // 升级会成功，因为 `tx` 仍然存在。 assert!(tx_weak.upgrade().is_some()); // 如果 drop 掉 `tx`，则升级会失败。 drop(tx); assert!(tx_weak.clone().upgrade().is_none()); 尝试将 WeakUnboundedSender 转换为 UnboundedSender。如果存在其他存活的 Sender 实例且 channel 之前未被 drop，则返回 Some，否则返回 None。 返回 UnboundedSender 句柄的数量。 返回 WeakUnboundedSender 句柄的数量。 返回值的副本。 更多信息 Performs copy-assignment from source. 更多信息 使用给定的格式化器格式化此值。 更多信息 执行此类型的析构函数。 更多信息 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 Performs copy-assignment from self to dest. 更多信息 原样返回参数。 调用 U::from(self)。 也就是说，此转换是 From<T> for U 实现选择执行的操作。 获得所有权后的类型。 Creates owned data from borrowed data, usually by cloning. 更多信息 Uses borrowed data to replace owned data, usually by cloning. 更多信息 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"不会阻止 channel 被关闭的无界 sender。 如果 channel 的所有 UnboundedSender 实例都已被 drop，仅剩下 WeakUnboundedSender 实例，则 channel 关闭。 为了发送消息，WeakUnboundedSender 需要使用 WeakUnboundedSender::upgrade 进行升级，该函数返回 Option<UnboundedSender>。如果所有 UnboundedSender 都已被 drop，则返回 None，否则返回 UnboundedSender。","section_headers":[]},{"url":"tokio/sync/mpsc/error/enum.SendTimeoutError.html","crate":"tokio","title":"SendTimeoutError","section":"enum","desc":"由 Sender::send_timeout 返回的错误。 无法在 channel 上发送数据，因为 channel 已满，并且发送超时已过。 通道的接收半部分已被显式关闭或已被 drop。 消耗 SendTimeoutError，返回未发送的值。 返回值的副本。 更多信息 Performs copy-assignment from source. 更多信息 使用给定的格式化器格式化此值。 更多信息 使用给定的格式化器格式化此值。 更多信息 返回此错误的更底层来源（若有）。 更多信息 Provides type-based access to context intended for error reports. 更多信息 测试 self 与 other 值是否相等，供 == 运算符使用。 测试 != 运算符。默认实现几乎总是够用，除非有非常充分的理由，否则不应被覆盖。 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 Performs copy-assignment from self to dest. 更多信息 原样返回参数。 调用 U::from(self)。 也就是说，此转换是 From<T> for U 实现选择执行的操作。 获得所有权后的类型。 Creates owned data from borrowed data, usually by cloning. 更多信息 Uses borrowed data to replace owned data, usually by cloning. 更多信息 Converts the given value to a String. 更多信息 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"由 Sender::send_timeout 返回的错误。","section_headers":["变体"]},{"url":"tokio/sync/mpsc/error/enum.TryRecvError.html","crate":"tokio","title":"TryRecvError","section":"enum","desc":"由 Receiver::try_recv 返回的错误。 此 channel 当前为空，但 Sender 尚未断开连接，因此数据可能尚不可用。 channel 的发送端已断开连接，将不会再接收到任何数据。 返回值的副本。 更多信息 Performs copy-assignment from source. 更多信息 使用给定的格式化器格式化此值。 更多信息 使用给定的格式化器格式化此值。 更多信息 返回此错误的更底层来源（若有）。 更多信息 Provides type-based access to context intended for error reports. 更多信息 测试 self 与 other 值是否相等，供 == 运算符使用。 测试 != 运算符。默认实现几乎总是够用，除非有非常充分的理由，否则不应被覆盖。 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 Performs copy-assignment from self to dest. 更多信息 原样返回参数。 调用 U::from(self)。 也就是说，此转换是 From<T> for U 实现选择执行的操作。 获得所有权后的类型。 Creates owned data from borrowed data, usually by cloning. 更多信息 Uses borrowed data to replace owned data, usually by cloning. 更多信息 Converts the given value to a String. 更多信息 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"由 Receiver::try_recv 返回的错误。","section_headers":["变体"]},{"url":"tokio/sync/mpsc/error/enum.TrySendError.html","crate":"tokio","title":"TrySendError","section":"enum","desc":"由 Sender::try_send 返回的错误。 无法在 channel 上发送数据，因为 channel 当前已满，发送需要阻塞。 通道的接收半部分已被显式关闭或已被 drop。 消耗 TrySendError，返回未发送的值。 返回值的副本。 更多信息 Performs copy-assignment from source. 更多信息 使用给定的格式化器格式化此值。 更多信息 使用给定的格式化器格式化此值。 更多信息 返回此错误的更底层来源（若有）。 更多信息 Provides type-based access to context intended for error reports. 更多信息 从输入类型转换为此类型。 测试 self 与 other 值是否相等，供 == 运算符使用。 测试 != 运算符。默认实现几乎总是够用，除非有非常充分的理由，否则不应被覆盖。 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 Performs copy-assignment from self to dest. 更多信息 原样返回参数。 调用 U::from(self)。 也就是说，此转换是 From<T> for U 实现选择执行的操作。 获得所有权后的类型。 Creates owned data from borrowed data, usually by cloning. 更多信息 Uses borrowed data to replace owned data, usually by cloning. 更多信息 Converts the given value to a String. 更多信息 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"由 Sender::try_send 返回的错误。","section_headers":["变体"]},{"url":"tokio/sync/mpsc/error/struct.SendError.html","crate":"tokio","title":"SendError","section":"struct","desc":"由 Sender::send 返回的错误。 返回值的副本。 更多信息 Performs copy-assignment from source. 更多信息 使用给定的格式化器格式化此值。 更多信息 使用给定的格式化器格式化此值。 更多信息 返回此错误的更底层来源（若有）。 更多信息 Provides type-based access to context intended for error reports. 更多信息 从输入类型转换为此类型。 测试 self 与 other 值是否相等，供 == 运算符使用。 测试 != 运算符。默认实现几乎总是够用，除非有非常充分的理由，否则不应被覆盖。 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 Performs copy-assignment from self to dest. 更多信息 原样返回参数。 调用 U::from(self)。 也就是说，此转换是 From<T> for U 实现选择执行的操作。 获得所有权后的类型。 Creates owned data from borrowed data, usually by cloning. 更多信息 Uses borrowed data to replace owned data, usually by cloning. 更多信息 Converts the given value to a String. 更多信息 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"由 Sender::send 返回的错误。","section_headers":["元组字段"]},{"url":"tokio/sync/oneshot/fn.channel.html","crate":"tokio","title":"channel","section":"fn","desc":"创建一个新的一次性 channel，用于跨异步任务发送单个值。 该函数返回单独的“发送”和“接收”句柄。Sender 句柄由生产者用于发送值。Receiver 句柄由消费者用于接收值。 每个句柄可以用于不同的任务。 §示例 use tokio::sync::oneshot; let (tx, rx) = oneshot::channel(); tokio::spawn(async move { if let Err(_) = tx.send(3) { println!(\"the receiver dropped\"); } }); match rx.await { Ok(v) => println!(\"got = {:?}\", v), Err(_) => println!(\"the sender dropped\"), }","module_doc":"创建一个新的一次性 channel，用于跨异步任务发送单个值。 该函数返回单独的“发送”和“接收”句柄。Sender 句柄由生产者用于发送值。Receiver 句柄由消费者用于接收值。 每个句柄可以用于不同的任务。","section_headers":[]},{"url":"tokio/sync/oneshot/struct.Receiver.html","crate":"tokio","title":"Receiver","section":"struct","desc":"从关联的 Sender 接收一个值。 一对 Sender 和 Receiver 由 channel 函数创建。 此通道没有 recv 方法，因为接收者本身实现了 Future trait。要接收 Result<T, error::RecvError>，请直接 .await Receiver 对象。 即使消息已发送，Future trait 上的 poll 方法也可能伪返回 Poll::Pending。如果发生此类伪失败，则当伪失败已解决时，调用者将被唤醒，以便调用者可以再次尝试接收消息。请注意，收到此类唤醒并不能保证下一次调用会成功——它可能以另一个伪失败而失败。（伪失败并不意味着消息已丢失。它只是被延迟了。） §取消安全性 Receiver 可安全取消。如果在 tokio::select! 语句中将其用作事件，并且其他某个分支先完成，则可以保证此通道上未接收到任何消息。 §示例 use tokio::sync::oneshot; let (tx, rx) = oneshot::channel(); tokio::spawn(async move { if let Err(_) = tx.send(3) { println!(\"the receiver dropped\"); } }); match rx.await { Ok(v) => println!(\"got = {:?}\", v), Err(_) => println!(\"the sender dropped\"), } 如果 sender 在未发送的情况下被 drop，则 receiver 将失败并返回 error::RecvError： use tokio::sync::oneshot; let (tx, rx) = oneshot::channel::<u32>(); tokio::spawn(async move { drop(tx); }); match rx.await { Ok(_) => panic!(\"This doesn't happen\"), Err(_) => println!(\"the sender dropped\"), } 要在 tokio::select! 循环中使用 Receiver，请在通道前面添加 &mut。 use tokio::sync::oneshot; use tokio::time::{interval, sleep, Duration}; let (send, mut recv) = oneshot::channel(); let mut interval = interval(Duration::from_millis(100)); tokio::spawn(async move { sleep(Duration::from_secs(1)).await; send.send(\"shut down\").unwrap(); }); loop { tokio::select! { _ = interval.tick() => println!(\"Another 100ms\"), msg = &mut recv => { println!(\"Got message: {}\", msg.unwrap()); break; } } } 阻止关联的 Sender 句柄发送值。 调用 close 之后发生的任何 send 操作都保证会失败。调用 close 后，如果有一个值在调用 close 之前已发送，则应调用 try_recv 来接收该值。 此函数可用于执行优雅关闭，并确保不会将值发送到通道中而永远不会被接收。 如果已接收到消息或通道已关闭，则 close 是空操作。 §示例 防止值被发送 use tokio::sync::oneshot; use tokio::sync::oneshot::error::TryRecvError; let (tx, mut rx) = oneshot::channel(); assert!(!tx.is_closed()); rx.close(); assert!(tx.is_closed()); assert!(tx.send(\"never received\").is_err()); match rx.try_recv() { Err(TryRecvError::Closed) => {} _ => unreachable!(), } 接收在调用 close 之前发送的值 use tokio::sync::oneshot; let (tx, mut rx) = oneshot::channel(); assert!(tx.send(\"will receive\").is_ok()); rx.clo","module_doc":"从关联的 Sender 接收一个值。 一对 Sender 和 Receiver 由 channel 函数创建。 此通道没有 recv 方法，因为接收者本身实现了 Future trait。要接收 Result<T, error::RecvError>，请直接 .await Receiver 对象。 即使消息已发送，Future trait 上的 poll 方法也可能伪返回 Poll::Pending。如果发生此类伪失败，则当伪失败已解决时，调用者将被唤醒，以便调用者可以再次尝试接收消息。请注意，收到此类唤醒并不能保证下一次调用会成功——它可能以另一个伪失败而失败。（伪失败并不意味着消息已丢失。它只是被延迟了。） Receiver 可安全取消。如果在 tokio::select! 语句中将其用作事件，并且其他某个分支先完成，则可以保证此通道上未接收到任何消息。","section_headers":[]},{"url":"tokio/sync/oneshot/struct.Sender.html","crate":"tokio","title":"Sender","section":"struct","desc":"向关联的 Receiver 发送一个值。 一对 Sender 和 Receiver 由 channel 函数创建。 §示例 use tokio::sync::oneshot; let (tx, rx) = oneshot::channel(); tokio::spawn(async move { if let Err(_) = tx.send(3) { println!(\"the receiver dropped\"); } }); match rx.await { Ok(v) => println!(\"got = {:?}\", v), Err(_) => println!(\"the sender dropped\"), } 如果 sender 在未发送的情况下被 drop，则 receiver 将失败并返回 error::RecvError： use tokio::sync::oneshot; let (tx, rx) = oneshot::channel::<u32>(); tokio::spawn(async move { drop(tx); }); match rx.await { Ok(_) => panic!(\"This doesn't happen\"), Err(_) => println!(\"the sender dropped\"), } 要在析构函数中使用 Sender，请将其放入 Option 中并调用 Option::take。 use tokio::sync::oneshot; struct SendOnDrop { sender: Option<oneshot::Sender<&'static str>>, } impl Drop for SendOnDrop { fn drop(&mut self) { if let Some(sender) = self.sender.take() { // 使用 `let _ =` 忽略发送错误。 let _ = sender.send(\"I got dropped!\"); } } } let (send, recv) = oneshot::channel(); let send_on_drop = SendOnDrop { sender: Some(send) }; drop(send_on_drop); assert_eq!(recv.await, Ok(\"I got dropped!\")); 尝试在此 channel 上发送一个值，如果无法发送则将其返回。 此方法会消费 self，因为 oneshot channel 上只能发送一个值。它未标记为 async，因为向 oneshot channel 发送消息永远不需要任何形式的等待。因此，send 方法可以在同步和异步代码中使用而不会出现问题。 当确定 channel 的另一端尚未挂起时，发送成功。发送失败是指相应的接收器已被释放。请注意，返回值 Err 意味着数据将永远不会被接收，但返回值 Ok 并不意味着数据将被接收。相应的接收器可能会在此函数返回 Ok 后立即挂起。 §示例 将值发送到另一个任务 use tokio::sync::oneshot; let (tx, rx) = oneshot::channel(); tokio::spawn(async move { if let Err(_) = tx.send(3) { println!(\"the receiver dropped\"); } }); match rx.await { Ok(v) => println!(\"got = {:?}\", v), Err(_) => println!(\"the sender dropped\"), } 等待关联的 Receiver 句柄关闭。 通过显式调用 close 或 drop Receiver 值来关闭 Receiver。 当与 select! 配合使用时，此函数很有用，可在接收器不再对结果感兴趣时中止计算。 §返回值 返回一个必须被 await 的 Future。 §示例 基本用法 use tokio::sync::oneshot; let (mut tx, rx) = oneshot::channel::<()>(); tokio::spawn(async move { drop(rx); }); tx.closed().await; println!(\"the receiver dropped\"); 与 select 配合使用 use tokio::sync::oneshot; use tokio::time::{self, Duration}; async fn compute() -> String","module_doc":"向关联的 Receiver 发送一个值。 一对 Sender 和 Receiver 由 channel 函数创建。","section_headers":[]},{"url":"tokio/sync/oneshot/error/enum.TryRecvError.html","crate":"tokio","title":"TryRecvError","section":"enum","desc":"由 Receiver 上的 try_recv 函数返回的错误。 channel 的发送端尚未发送值。 channel 的发送端已被 drop，而未发送值。 返回值的副本。 更多信息 Performs copy-assignment from source. 更多信息 使用给定的格式化器格式化此值。 更多信息 使用给定的格式化器格式化此值。 更多信息 返回此错误的更底层来源（若有）。 更多信息 Provides type-based access to context intended for error reports. 更多信息 测试 self 与 other 值是否相等，供 == 运算符使用。 测试 != 运算符。默认实现几乎总是够用，除非有非常充分的理由，否则不应被覆盖。 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 Performs copy-assignment from self to dest. 更多信息 原样返回参数。 调用 U::from(self)。 也就是说，此转换是 From<T> for U 实现选择执行的操作。 获得所有权后的类型。 Creates owned data from borrowed data, usually by cloning. 更多信息 Uses borrowed data to replace owned data, usually by cloning. 更多信息 Converts the given value to a String. 更多信息 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"由 Receiver 上的 try_recv 函数返回的错误。","section_headers":["变体"]},{"url":"tokio/sync/oneshot/error/struct.RecvError.html","crate":"tokio","title":"RecvError","section":"struct","desc":"由 Receiver 的 Future 实现返回的错误。 当发送端在未发送的情况下被 drop 时，接收端返回此错误。 返回值的副本。 更多信息 Performs copy-assignment from source. 更多信息 使用给定的格式化器格式化此值。 更多信息 使用给定的格式化器格式化此值。 更多信息 返回此错误的更底层来源（若有）。 更多信息 Provides type-based access to context intended for error reports. 更多信息 测试 self 与 other 值是否相等，供 == 运算符使用。 测试 != 运算符。默认实现几乎总是够用，除非有非常充分的理由，否则不应被覆盖。 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 Performs copy-assignment from self to dest. 更多信息 原样返回参数。 调用 U::from(self)。 也就是说，此转换是 From<T> for U 实现选择执行的操作。 获得所有权后的类型。 Creates owned data from borrowed data, usually by cloning. 更多信息 Uses borrowed data to replace owned data, usually by cloning. 更多信息 Converts the given value to a String. 更多信息 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"由 Receiver 的 Future 实现返回的错误。 当发送端在未发送的情况下被 drop 时，接收端返回此错误。","section_headers":[]},{"url":"tokio/sync/watch/fn.channel.html","crate":"tokio","title":"channel","section":"fn","desc":"创建一个新的 watch channel，返回“发送”和“接收”句柄。 由 Sender 发送的所有值将对 Receiver 句柄可见。只有最后发送的值可供 Receiver 端使用。所有中间值都会被 drop。 §示例 以下示例会打印 hello! world! 。 use tokio::sync::watch; use tokio::time::{Duration, sleep}; let (tx, mut rx) = watch::channel(\"hello\"); tokio::spawn(async move { // 使用等价的 \"do-while\" 循环，以便在等待 `changed()` future // 之前先处理初始值。 loop { println!(\"{}! \", *rx.borrow_and_update()); if rx.changed().await.is_err() { break; } } }); sleep(Duration::from_millis(100)).await; tx.send(\"world\")?;","module_doc":"创建一个新的 watch channel，返回“发送”和“接收”句柄。 由 Sender 发送的所有值将对 Receiver 句柄可见。只有最后发送的值可供 Receiver 端使用。所有中间值都会被 drop。 以下示例会打印 hello! world! 。","section_headers":[]},{"url":"tokio/sync/watch/struct.Receiver.html","crate":"tokio","title":"Receiver","section":"struct","desc":"从关联的 Sender 接收值。 实例由 channel 函数创建。 要将此接收者转换为 Stream，可以使用 WatchStream 包装器。 返回对最近发送值的引用。 此方法不会将返回的值标记为已读，因此对 changed 的后续调用可能会立即返回，即使你已经通过调用 borrow 看到了该值。 未释放的借用会对内部值保持一个读锁。这意味着长时间存活的借用可能会导致生产者一半被阻塞。建议尽量缩短借用的生命周期。此外，如果运行在允许 !Send future 的环境中，必须确保返回的 Ref 类型不会跨越 .await 点存活，否则可能会导致死锁。 锁的优先级策略取决于底层锁实现，本类型不保证使用任何特定策略。特别是，正在等待通过 send 获取锁的生产者可能会阻塞对 borrow 的并发调用，也可能不会，例如： Potential deadlock example // Task 1 (on thread A) | // Task 2 (on thread B) let _ref1 = rx.borrow(); | | // will block | let _ = tx.send(()); // may deadlock | let _ref2 = rx.borrow(); | 有关何时使用此方法与 borrow_and_update 的更多信息，请参阅此处。 §示例 use tokio::sync::watch; let (_, rx) = watch::channel(\"hello\"); assert_eq!(*rx.borrow(), \"hello\"); 返回对最近发送值的引用，并将该值标记为已读。 此方法会将当前值标记为已读。在 Sender 再次修改共享值之前，对 changed 的后续调用不会立即返回。 未释放的借用会对内部值保持一个读锁。这意味着长时间存活的借用可能会导致生产者一半被阻塞。建议尽量缩短借用的生命周期。此外，如果运行在允许 !Send future 的环境中，必须确保返回的 Ref 类型不会跨越 .await 点存活，否则可能会导致死锁。 锁的优先级策略取决于底层锁实现，本类型不保证使用任何特定策略。特别是，正在等待通过 send 获取锁的生产者可能会阻塞对 borrow 的并发调用，也可能不会，例如： Potential deadlock example // Task 1 (on thread A) | // Task 2 (on thread B) let _ref1 = rx1.borrow_and_update(); | | // will block | let _ = tx.send(()); // may deadlock | let _ref2 = rx2.borrow_and_update(); | 有关何时使用此方法与 borrow 的更多信息，请参阅此处。 检查此通道是否包含此接收者尚未看到的消息。当前值将不会被标记为已读。 虽然此方法称为 has_changed，但它不会检查消息的相等性，因此即使当前消息等于上一条消息，此调用也会返回 true。 §错误 当且仅当通道已关闭时返回 RecvError。 §示例§Basic usage use tokio::sync::watch; let (tx, mut rx) = watch::channel(\"hello\"); tx.send(\"goodbye\").unwrap(); assert!(rx.has_changed().unwrap()); assert_eq!(*rx.borrow_and_update(), \"goodbye\"); // 该值已被标记为已读 assert!(!rx.has_changed().unwrap());§Closed channel example use tokio::sync::watch; let (tx, rx) = watch::channel(\"hello\"); tx.send(\"goodbye\").unwrap(); drop(tx); // 通道已关闭 assert!(rx.has_changed().is_err()); 将状态标记为已更改。 调用此方法后，无论是否发送了新值，has_changed() 都会返回 true，并且 changed() 会立即返回。 这对于在订阅以同步新接收者之后触发初始更改通知很有用。 将状态标记为未更改。 当前值将被接收者视为已读。 如果你对接收者中可见的当前值不感兴趣，这将很有用。 等待更改通知，然后将当前值标记为已读。 如果调用此方法时通道中的当前值尚未标记为已读，则该方法会将该值标记为已读并立即返回。如果最新值已标记为已读，则","module_doc":"从关联的 Sender 接收值。 实例由 channel 函数创建。 要将此接收者转换为 Stream，可以使用 WatchStream 包装器。","section_headers":[]},{"url":"tokio/sync/watch/struct.Ref.html","crate":"tokio","title":"Ref","section":"struct","desc":"返回对内部值的引用。 未释放的借用会对内部值保持一个读锁。这意味着长时间存活的借用可能会导致生产者一半被阻塞。建议尽量缩短借用的生命周期。此外，如果运行在允许 !Send future 的环境中，必须确保返回的 Ref 类型不会跨越 .await 点存活，否则可能会导致死锁。 锁的优先级策略取决于底层锁实现，本类型不保证使用任何特定策略。特别是，正在等待通过 send 获取锁的生产者可能会阻塞对 borrow 的并发调用，也可能不会，例如： Potential deadlock example // Task 1 (on thread A) | // Task 2 (on thread B) let _ref1 = rx.borrow(); | | // will block | let _ = tx.send(()); // may deadlock | let _ref2 = rx.borrow(); | 指示自上次将借用值标记为已读取以来，该借用值是否被视为已更改。 与 Receiver::has_changed() 不同，此方法在 channel 关闭时不会失败。 从 Sender 借用时，此函数将始终返回 false。 §示例 use tokio::sync::watch; let (tx, mut rx) = watch::channel(\"hello\"); tx.send(\"goodbye\").unwrap(); // 发送端永远不会将该值视为已更改。 assert!(!tx.borrow().has_changed()); // 立即丢弃发送端，仅用于测试。 drop(tx); // 即使发送端已被丢弃... assert!(rx.has_changed().is_err()); // ...修改后的值仍可读取，并会被检测为已更改。 assert_eq!(*rx.borrow(), \"goodbye\"); assert!(rx.borrow().has_changed()); // 读取已更改的值并将其标记为已读。 { let received = rx.borrow_and_update(); assert_eq!(*received, \"goodbye\"); assert!(received.has_changed()); // 在离开此作用域时释放读锁。 } // 此时该值已被标记为已读，且在发送端被丢弃后 // 将不再被修改。 assert!(!rx.borrow().has_changed()); 使用给定的格式化器格式化此值。 更多信息 解引用后得到的类型。 解引用此值。 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 原样返回参数。 调用 U::from(self)。 也就是说，此转换是 From<T> for U 实现选择执行的操作。 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"返回对内部值的引用。 未释放的借用会对内部值保持一个读锁。这意味着长时间存活的借用可能会导致生产者一半被阻塞。建议尽量缩短借用的生命周期。此外，如果运行在允许 !Send future 的环境中，必须确保返回的 Ref 类型不会跨越 .await 点存活，否则可能会导致死锁。 锁的优先级策略取决于底层锁实现，本类型不保证使用任何特定策略。特别是，正在等待通过 send 获取锁的生产者可能会阻塞对 borrow 的并发调用，也可能不会，例如：","section_headers":[]},{"url":"tokio/sync/watch/struct.Sender.html","crate":"tokio","title":"Sender","section":"struct","desc":"将值发送给关联的 Receiver。 实例由 channel 函数创建。 创建 watch 通道的发送端。 有关调用此函数时的错误，请参阅 watch::channel 的文档。请注意，当没有接收者时尝试发送值将返回错误。 §示例 let sender = tokio::sync::watch::Sender::new(0u8); assert!(sender.send(3).is_err()); let _rec = sender.subscribe(); assert!(sender.send(4).is_ok()); 通过通道发送一个新值，通知所有接收者。 如果通道已关闭（即所有接收者都已 drop），则此方法将失败。可以使用 subscribe 方法重新打开通道。但是，当 send 失败时，该值不会提供给未来的接收者（但会随 SendError 返回）。 为了始终为未来的接收者提供一个新值，即使当前没有接收者，也可以使用其他发送方法之一（send_if_modified、send_modify 或 send_replace）。 在原地无条件修改被监视的值，通知所有接收者。 这对于修改被监视的值很有用，而无需分配新实例。此外，即使没有 receiver，此方法也允许发送值。 如果该值仅在可变借用期间被有条件地修改，则首选使用更通用的函数 Self::send_if_modified()，以防止对未修改的值发出不必要的更改通知。 §恐慌 当 modify 闭包被调用时发生 panic，此函数也会 panic。panic 时不会通知任何 receiver。在 panic 之前闭包对被监视的值所做的所有更改，都将在后续的 borrow 调用中可见。 §示例 use tokio::sync::watch; struct State { counter: usize, } let (state_tx, state_rx) = watch::channel(State { counter: 0 }); state_tx.send_modify(|state| state.counter += 1); assert_eq!(state_rx.borrow().counter, 1); 在原地有条件修改被监视的值，仅在修改后通知所有接收者。 这对于修改被监视的值很有用，而无需分配新实例。此外，即使没有 receiver，此方法也允许发送值。 如果该值在可变借用期间确实已被修改，则 modify 闭包必须返回 true。仅当尽管进行了可变借用但保证该值未被修改时，它才应返回 false。 仅当闭包返回 true 时，接收者才会收到通知。如果闭包已修改该值但返回了 false，则会导致静默修改，即修改后的值在后续对 borrow 的调用中可见，但接收者不会收到更改通知。 返回闭包的结果，即如果值已被修改则为 true，否则为 false。 §恐慌 当 modify 闭包被调用时发生 panic，此函数也会 panic。panic 时不会通知任何 receiver。在 panic 之前闭包对被监视的值所做的所有更改，都将在后续的 borrow 调用中可见。 §示例 use tokio::sync::watch; struct State { counter: usize, } let (state_tx, mut state_rx) = watch::channel(State { counter: 1 }); let inc_counter_if_odd = |state: &mut State| { if state.counter % 2 == 1 { state.counter += 1; return true; } false }; assert_eq!(state_rx.borrow().counter, 1); assert!(!state_rx.has_changed().unwrap()); assert!(state_tx.send_if_modified(inc_counter_if_odd)); assert!(state_rx.has_changed().unwrap()); assert_eq!(state_rx.borrow_and_update().counter, 2); assert!(!state_rx.has_changed().unwrap()); assert!(!state_tx.send_if_modified(inc_counter_if_odd)); assert!(!state_rx.has_changed().unwrap()); assert_eq!(state_rx.borrow_","module_doc":"将值发送给关联的 Receiver。 实例由 channel 函数创建。","section_headers":[]},{"url":"tokio/sync/watch/error/struct.RecvError.html","crate":"tokio","title":"RecvError","section":"struct","desc":"接收变更通知时产生的错误。 返回值的副本。 更多信息 Performs copy-assignment from source. 更多信息 使用给定的格式化器格式化此值。 更多信息 使用给定的格式化器格式化此值。 更多信息 返回此错误的更底层来源（若有）。 更多信息 Provides type-based access to context intended for error reports. 更多信息 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 Performs copy-assignment from self to dest. 更多信息 原样返回参数。 调用 U::from(self)。 也就是说，此转换是 From<T> for U 实现选择执行的操作。 获得所有权后的类型。 Creates owned data from borrowed data, usually by cloning. 更多信息 Uses borrowed data to replace owned data, usually by cloning. 更多信息 Converts the given value to a String. 更多信息 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"接收变更通知时产生的错误。","section_headers":[]},{"url":"tokio/sync/watch/error/struct.SendError.html","crate":"tokio","title":"SendError","section":"struct","desc":"发送值失败时产生的错误。 返回值的副本。 更多信息 Performs copy-assignment from source. 更多信息 使用给定的格式化器格式化此值。 更多信息 使用给定的格式化器格式化此值。 更多信息 返回此错误的更底层来源（若有）。 更多信息 Provides type-based access to context intended for error reports. 更多信息 测试 self 与 other 值是否相等，供 == 运算符使用。 测试 != 运算符。默认实现几乎总是够用，除非有非常充分的理由，否则不应被覆盖。 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 Performs copy-assignment from self to dest. 更多信息 原样返回参数。 调用 U::from(self)。 也就是说，此转换是 From<T> for U 实现选择执行的操作。 获得所有权后的类型。 Creates owned data from borrowed data, usually by cloning. 更多信息 Uses borrowed data to replace owned data, usually by cloning. 更多信息 Converts the given value to a String. 更多信息 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"发送值失败时产生的错误。","section_headers":["元组字段"]},{"url":"tokio/task/fn.block_in_place.html","crate":"tokio","title":"block_in_place","section":"fn","desc":"在当前线程上运行提供的阻塞函数， 而不阻塞执行器。 一般来说，发出阻塞调用或在 future 中执行大量计算而不让出是有问题的， 因为它可能会阻止执行器 推进其他任务。调用此函数 通知执行器当前正在执行的任务即将阻塞线程， 以便执行器能够在此之前 将其拥有的任何其他任务移交给 新的工作线程。有关更多信息， 请参阅CPU 密集型任务和阻塞代码部分。 请注意，尽管此函数避免了饿死其他独立 生成的任务，但在调用 block_in_place 期间， 同一任务中并发运行的任何其他代码 都将被挂起。例如在使用 join! 宏时 可能发生这种情况。为避免此问题， 请使用 spawn_blocking 代替 block_in_place。 请注意，此函数不能在 current_thread 运行 时内使用，因为在这种情况下 没有其他工作线程可以移交任务。 另一方面，允许在运行时外部调用此函数。 在这种情况下，block_in_place 只是正常地 调用所提供的闭包。 block_in_place 后运行的代码 无法被取消。当你关闭 执行器时，它将无限期等待所有阻塞操作完成。 你可以使用 shutdown_timeout 在 超时后停止等待它们。请注意，这 仍然不会取消这些任务â它们 只是在方法返回后继续运行。 §示例 use tokio::task; task::block_in_place(move || { // do some compute-heavy work or call synchronous code }); block_in_place 内运行的代码可以使用 block_on 重新进入 async 上下文。 use tokio::task; use tokio::runtime::Handle; task::block_in_place(move || { Handle::current().block_on(async move { // do something async }); });§Panics 如果从 current_thread 运行时调用，此函数会 panic。","module_doc":"在当前线程上运行提供的阻塞函数， 而不阻塞执行器。 一般来说，发出阻塞调用或在 future 中执行大量计算而不让出是有问题的， 因为它可能会阻止执行器 推进其他任务。调用此函数 通知执行器当前正在执行的任务即将阻塞线程， 以便执行器能够在此之前 将其拥有的任何其他任务移交给 新的工作线程。有关更多信息， 请参阅CPU 密集型任务和阻塞代码部分。 请注意，尽管此函数避免了饿死其他独立 生成的任务，但在调用 block_in_place 期间， 同一任务中并发运行的任何其他代码 都将被挂起。例如在使用 join! 宏时 可能发生这种情况。为避免此问题， 请使用 spawn_blocking 代替 block_in_place。 请注意，此函数不能在 current_thread 运行 时内使用，因为在这种情况下 没有其他工作线程可以移交任务。 另一方面，允许在运行时外部调用此函数。 在这种情况下，block_in_place 只是正常地 调用所提供的闭包。 block_in_place 后运行的代码 无法被取消。当你关闭 执行器时，它将无限期等待所有阻塞操作完成。 你可以使用 shutd","section_headers":[]},{"url":"tokio/task/fn.id.html","crate":"tokio","title":"id","section":"fn","desc":"返回当前正在运行的任务的 Id。 §恐慌 如果在任务外部调用此函数，则会发生 panic。请注意，对 block_on 的调用没有任务 ID，因此如果从 block_on 调用内调用此方法，则会发生 panic。有关不发生 panic 的此函数版本，请参阅 task::try_id()。","module_doc":"返回当前正在运行的任务的 Id。 如果在任务外部调用此函数，则会发生 panic。请注意，对 block_on 的调用没有任务 ID，因此如果从 block_on 调用内调用此方法，则会发生 panic。有关不发生 panic 的此函数版本，请参阅 task::try_id()。","section_headers":[]},{"url":"tokio/task/fn.spawn.html","crate":"tokio","title":"spawn","section":"fn","desc":"生成一个新的异步任务，并为其返回一个 JoinHandle。 调用 spawn 时，所提供的 future 将立即开始在后台运行，即使你没有 await 返回的 JoinHandle。 生成任务使该任务能够与其他任务并发执行。生成的任务可能在当前线程上执行，也可能被发送到另一个线程执行。具体细节取决于当前的 Runtime 配置。在正在运行的运行时中，任务将立即在后台启动。在阻塞的运行时上，用户必须推动运行时向前（例如，通过调用 Runtime::block_on）。 保证 spawn 不会同步轮询正在生成的任务。这意味着在持有锁时调用 spawn 不会与生成的任务产生死锁风险。 不能保证生成的任务一定会执行到完成。关闭运行时时，所有未完成的任务都将被 drop，无论该任务的生命周期如何。 此函数必须从 Tokio 运行时的上下文中调用。在 Tokio 运行时上运行的任务始终在其上下文中，但你也可以使用 Runtime::enter 方法进入该上下文。 §示例 在此示例中，启动了服务器，并使用 spawn 启动一个新任务来处理每个接收到的连接。 use tokio::net::{TcpListener, TcpStream}; use std::io; async fn process(socket: TcpStream) { // ... } #[tokio::main] async fn main() -> io::Result<()> { let listener = TcpListener::bind(\"127.0.0.1:8080\").await?; loop { let (socket, _) = listener.accept().await?; tokio::spawn(async move { // 并发处理每个 socket。 process(socket).await }); } } 要并行运行多个任务并接收它们的结果，可以将 join handle 存储在向量中。 async fn my_background_op(id: i32) -> String { let s = format!(\"Starting background task {}.\", id); println!(\"{}\", s); s } let ops = vec![1, 2, 3]; let mut tasks = Vec::with_capacity(ops.len()); for op in ops { // 此调用会让它们在后台开始运行 // 立即开始。 tasks.push(tokio::spawn(my_background_op(op))); } let mut outputs = Vec::with_capacity(tasks.len()); for task in tasks { outputs.push(task.await.unwrap()); } println!(\"{:?}\", outputs); 此示例按任务启动顺序将任务推送到 outputs。如果你不关心输出的顺序，则也可以使用 JoinSet。 §恐慌 如果从 Tokio 运行时的外部调用，则会发生 panic。 §在 task 中使用 !Send 值 提供给 spawn 的任务必须实现 Send。但是，只要 !Send 值仅存在于对 .await 的调用之间，就可以从任务中使用它们。 例如，这是可行的： use tokio::task; use std::rc::Rc; fn use_rc(rc: Rc<()>) { // 用 rc 做些操作 } tokio::spawn(async { // 强制让 `Rc` 留在没有 `.await` 的作用域中 { let rc = Rc::new(()); use_rc(rc.clone()); } task::yield_now().await; }).await.unwrap(); 这是不可行的： ⓘuse tokio::task; use std::rc::Rc; fn use_rc(rc: Rc<()>) { // 用 rc 做些操作 } #[tokio::main] async fn main() { tokio::spawn(async { let rc = Rc::new(()); task::yield_now().await; use_rc(rc.clone()); }).await.unwrap(); } 在跨 .await 调用之间持有 !Send 值将导致类似于以下的、不友好的编译错误消息： `[... some type ...]` cannot be sent between t","module_doc":"生成一个新的异步任务，并为其返回一个 JoinHandle。 调用 spawn 时，所提供的 future 将立即开始在后台运行，即使你没有 await 返回的 JoinHandle。 生成任务使该任务能够与其他任务并发执行。生成的任务可能在当前线程上执行，也可能被发送到另一个线程执行。具体细节取决于当前的 Runtime 配置。在正在运行的运行时中，任务将立即在后台启动。在阻塞的运行时上，用户必须推动运行时向前（例如，通过调用 Runtime::block_on）。 保证 spawn 不会同步轮询正在生成的任务。这意味着在持有锁时调用 spawn 不会与生成的任务产生死锁风险。 不能保证生成的任务一定会执行到完成。关闭运行时时，所有未完成的任务都将被 drop，无论该任务的生命周期如何。 此函数必须从 Tokio 运行时的上下文中调用。在 Tokio 运行时上运行的任务始终在其上下文中，但你也可以使用 Runtime::enter 方法进入该上下文。 在此示例中，启动了服务器，并使用 spawn 启动一个新任务来处理每个接收到的连接。","section_headers":[]},{"url":"tokio/task/fn.spawn_blocking.html","crate":"tokio","title":"spawn_blocking","section":"fn","desc":"在可以接受阻塞的线程上运行所提供的闭包。 一般来说，在不让步的 future 中发出阻塞调用或执行大量计算是有问题的，因为它可能会阻止执行器驱动其他 future 向前。此函数在专用于阻塞操作的线程上运行所提供的闭包。有关更多信息，请参阅CPU-bound 任务和阻塞代码部分。 当通过此函数请求阻塞线程时，Tokio 将生成更多的阻塞线程，直到达到 Builder 上配置的上限为止。达到上限后，任务将放入队列中。默认情况下，线程上限非常大，因为 spawn_blocking 通常用于无法异步执行的各种 IO 操作。当你使用 spawn_blocking 运行 CPU-bound 代码时，应牢记这个大上限。当运行许多 CPU-bound 计算时，应使用信号量或其他同步原语来限制并行执行的计算数量。专门的 CPU-bound 执行器（例如 rayon）也可能是合适的选择。 此函数用于最终自行完成的非异步操作。如果要生成普通线程，应改用 thread::spawn。 请注意，使用 spawn_blocking 生成的任务无法中止，因为它们不是异步的。如果对 spawn_blocking 任务调用 abort，那么这将没有任何效果，任务将继续正常运行。例外情况是如果任务尚未开始运行；在这种情况下，调用 abort 可能会阻止任务启动。 关闭执行器时，它将尝试 abort 所有任务，包括 spawn_blocking 任务。但是，spawn_blocking 任务一旦开始运行就无法中止，这意味着运行时关闭将无限期地等待所有已开始的 spawn_blocking 完成运行。你可以使用 shutdown_timeout 在某个超时后停止等待它们。请注意，这仍然不会取消任务——只是允许它们在该方法返回后继续运行。如果阻塞任务尚未开始运行，则可能会被取消，但这并不能保证。 §何时使用 spawn_blocking，何时使用专用线程 spawn_blocking 用于最终完成的有界阻塞工作。每次调用都会在任务的整个持续时间内占用运行时阻塞线程池中的一个线程。因此，长期运行的任务会降低线程池的有效容量，一旦线程池饱和并且工作被排队，这可能会延迟其他阻塞操作。 对于无限运行或长时间运行的负载（例如后台工作进程或持久处理循环），首选使用 thread::spawn 创建的专用线程。 经验法则是： Use spawn_blocking for short-lived blocking operations Use dedicated threads for long-lived or persistent blocking workloads 请注意，如果你使用的是单线程运行时，此函数仍会为阻塞操作生成其他线程。当前线程调度器的单个线程仅用于异步代码。 §桥接异步与阻塞代码的相关 API 与模式 在简单情况下，让闭包在创建时接受输入参数并返回单个值（或结构体/元组等）就足够了。 对于希望将数据流入或流出同步上下文的更复杂情况，mpsc channel 具有 blocking_send 和 blocking_recv 方法，可用于由 spawn_blocking 创建的线程等非异步代码。 另一种选择是 SyncIoBridge，用于同步上下文在字节流上操作的情况。例如，你可能使用 hyper 等异步 HTTP 客户端获取数据，但使用为同步 I/O 编写的库对有效负载正文执行复杂的解析。 最后，另请参阅与同步代码桥接，其中讨论了将 Tokio 作为更大同步代码库一部分使用的相反情况。 §示例 传递输入值并接收计算结果： use tokio::task; // 初始输入 let mut v = \"Hello, \".to_string(); let res = task::spawn_blocking(move || { // 代表计算密集型工作或使用同步 API v.push_str(\"world\"); // 将该值的所有权交还给异步上下文 v }).await?; // `res` 是从线程返回的值 assert_eq!(res.as_str(), \"Hello, world\"); 使用 channel： use tokio::task; use tokio::sync::mpsc; let (tx, mut rx) = mpsc::channel(2); let start = 5; let worker = task::spawn_blocking(move || { for x in 0..10 { // 代表复杂计算 tx.blocking_send(start + x).unwrap(); } }); let mut acc = 0; while ","module_doc":"在可以接受阻塞的线程上运行所提供的闭包。 一般来说，在不让步的 future 中发出阻塞调用或执行大量计算是有问题的，因为它可能会阻止执行器驱动其他 future 向前。此函数在专用于阻塞操作的线程上运行所提供的闭包。有关更多信息，请参阅CPU-bound 任务和阻塞代码部分。 当通过此函数请求阻塞线程时，Tokio 将生成更多的阻塞线程，直到达到 Builder 上配置的上限为止。达到上限后，任务将放入队列中。默认情况下，线程上限非常大，因为 spawn_blocking 通常用于无法异步执行的各种 IO 操作。当你使用 spawn_blocking 运行 CPU-bound 代码时，应牢记这个大上限。当运行许多 CPU-bound 计算时，应使用信号量或其他同步原语来限制并行执行的计算数量。专门的 CPU-bound 执行器（例如 rayon）也可能是合适的选择。 此函数用于最终自行完成的非异步操作。如果要生成普通线程，应改用 thread::spawn。 请注意，使用 spawn_blocking 生成的任务无法中止，因为它们不是异步的。如果对 spawn_blocking 任务","section_headers":[]},{"url":"tokio/task/fn.spawn_local.html","crate":"tokio","title":"spawn_local","section":"fn","desc":"在当前 LocalSet 或 LocalRuntime 上生成一个 !Send future。 当显式使用这些类型之一，或者选择在 tokio::main 中使用 \"local\" 运行时风格时，这是可能的： ⓘ#[tokio::main(flavor = \"local\")] 生成的 future 将在调用 spawn_local 的同一线程上运行。 当调用 spawn_local 时，所提供的 future 将立即开始在后台运行，即使你没有 await 返回的 JoinHandle。 §恐慌 如果在 LocalSet 或 LocalRuntime 之外调用此函数，则会发生 panic。 请注意，如果在 LocalSet 内使用 tokio::spawn，则生成的新任务将不会位于 LocalSet 内，因此如果你想停留在 LocalSet 内，必须使用 spawn_local。 §示例 使用 LocalSet： use std::rc::Rc; use tokio::task; let nonsend_data = Rc::new(\"my nonsend data...\"); let local = task::LocalSet::new(); // 运行本地任务集。 local.run_until(async move { let nonsend_data = nonsend_data.clone(); task::spawn_local(async move { println!(\"{}\", nonsend_data); // ... }).await.unwrap(); }).await; 使用 local 运行时风格。 #[tokio::main(flavor = \"local\")] async fn main() { let join = tokio::task::spawn_local(async { println!(\"my nonsend data...\") }); join.await.unwrap() }","module_doc":"在当前 LocalSet 或 LocalRuntime 上生成一个 !Send future。 当显式使用这些类型之一，或者选择在 tokio::main 中使用 \"local\" 运行时风格时，这是可能的：","section_headers":[]},{"url":"tokio/task/fn.try_id.html","crate":"tokio","title":"try_id","section":"fn","desc":"返回当前正在运行的任务的 Id，如果在任务外部调用，则返回 None。 此函数类似于 task::id()，只不过它在任务上下文外部调用时返回 None 而不是 panic。","module_doc":"返回当前正在运行的任务的 Id，如果在任务外部调用，则返回 None。 此函数类似于 task::id()，只不过它在任务上下文外部调用时返回 None 而不是 panic。","section_headers":[]},{"url":"tokio/task/fn.yield_now.html","crate":"tokio","title":"yield_now","section":"fn","desc":"将执行权让回 Tokio 运行时。 任务通过 await yield_now() 让步，并可能在该 future 完成时恢复（无输出）。当前任务将作为挂起任务重新添加到挂起队列的末尾。任何其他挂起任务都将被调度。任务继续不需要其他唤醒。 另请参阅 task 模块中的使用示例。 §非保证 如果调用栈中此函数之上有任何特殊的组合器，则该函数可能不会一直让步到执行器。例如，如果 tokio::select! 在与 yield_now() 相同的 poll 期间有另一个分支完成，则让步不会一直传播到运行时。 通常不能保证运行时的行为符合你的预期，特别是在调用 yield_now() 后决定下一个调度哪个任务时。尤其运行时可能选择立即再次 poll 刚刚运行 yield_now() 的任务，而不先 poll 任何其他任务。例如，运行时不会在每次 poll 任务之间驱动 IO 驱动，这可能导致运行时立即再次 poll 当前任务，即使有另一个任务可以通过 IO 驱动的通知取得进展。 一般来说，更改运行时 poll 任务的顺序不被视为破坏性更改，你的程序应该在运行时以任何顺序 poll 你的任务时都是正确的。","module_doc":"将执行权让回 Tokio 运行时。 任务通过 await yield_now() 让步，并可能在该 future 完成时恢复（无输出）。当前任务将作为挂起任务重新添加到挂起队列的末尾。任何其他挂起任务都将被调度。任务继续不需要其他唤醒。 另请参阅 task 模块中的使用示例。 如果调用栈中此函数之上有任何特殊的组合器，则该函数可能不会一直让步到执行器。例如，如果 tokio::select! 在与 yield_now() 相同的 poll 期间有另一个分支完成，则让步不会一直传播到运行时。 通常不能保证运行时的行为符合你的预期，特别是在调用 yield_now() 后决定下一个调度哪个任务时。尤其运行时可能选择立即再次 poll 刚刚运行 yield_now() 的任务，而不先 poll 任何其他任务。例如，运行时不会在每次 poll 任务之间驱动 IO 驱动，这可能导致运行时立即再次 poll 当前任务，即使有另一个任务可以通过 IO 驱动的通知取得进展。 一般来说，更改运行时 poll 任务的顺序不被视为破坏性更改，你的程序应该在运行时以任何顺序 poll 你的任务时都是正确的。","section_headers":[]},{"url":"tokio/task/struct.AbortHandle.html","crate":"tokio","title":"AbortHandle","section":"struct","desc":"中止已生成任务的所拥有权限，无需等待其完成。 与 JoinHandle 不同，AbortHandle 不表示等待任务完成的权限，只表示终止它的权限。 可以通过调用 AbortHandle::abort 方法中止任务。Drop AbortHandle 会释放终止任务的权限——它不会中止任务。 请注意，使用 spawn_blocking 生成的任务无法中止，因为它们不是异步的。如果对 spawn_blocking 任务调用 abort，那么这将没有任何效果，任务将继续正常运行。例外情况是如果任务尚未开始运行；在这种情况下，调用 abort 可能会阻止任务启动。 中止与该句柄关联的任务。 await 一个已取消的任务可能会像往常一样完成（如果任务在被取消时已经完成），但最有可能的是失败并返回已取消的 JoinError。 如果任务已被取消（例如通过 JoinHandle::abort），则此方法将不执行任何操作。 请注意，使用 spawn_blocking 派生的任务无法被中止，因为它们不是异步的。如果对 spawn_blocking 任务调用 abort，则不会产生任何效果，任务将继续正常运行。例外情况是任务尚未开始运行；此时调用 abort 可能会阻止任务启动。 有关取消的更多信息，另请参阅 模块级文档。 检查与此 AbortHandle 关联的任务是否已完成。 请注意，即使已对任务调用了 abort，此方法也可能返回 false。这是因为取消过程可能需要一些时间，并且此方法在取消完成之前不会返回 true。 返回相对于其他当前已派生任务能唯一标识此任务的 任务 ID。 返回一个克隆的 AbortHandle，可用于远程中止此任务。 Performs copy-assignment from source. 更多信息 使用给定的格式化器格式化此值。 更多信息 执行此类型的析构函数。 更多信息 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 Performs copy-assignment from self to dest. 更多信息 原样返回参数。 调用 U::from(self)。 也就是说，此转换是 From<T> for U 实现选择执行的操作。 获得所有权后的类型。 Creates owned data from borrowed data, usually by cloning. 更多信息 Uses borrowed data to replace owned data, usually by cloning. 更多信息 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"中止已生成任务的所拥有权限，无需等待其完成。 与 JoinHandle 不同，AbortHandle 不表示等待任务完成的权限，只表示终止它的权限。 可以通过调用 AbortHandle::abort 方法中止任务。Drop AbortHandle 会释放终止任务的权限——它不会中止任务。 请注意，使用 spawn_blocking 生成的任务无法中止，因为它们不是异步的。如果对 spawn_blocking 任务调用 abort，那么这将没有任何效果，任务将继续正常运行。例外情况是如果任务尚未开始运行；在这种情况下，调用 abort 可能会阻止任务启动。","section_headers":[]},{"url":"tokio/task/struct.Id.html","crate":"tokio","title":"Id","section":"struct","desc":"唯一标识一个任务（相对于所有其他当前正在运行的任务）的不透明 ID。 仅在同时满足以下两个条件时，任务的 ID 才能被另一个任务重用： The task itself exits. There is no active JoinHandle associated with this task. 在以下情况下，JoinHandle 被视为活动状态： You are explicitly holding a JoinHandle, AbortHandle, or tokio_util::task::AbortOnDropHandle. The task is being tracked by a JoinSet or tokio_util::task::JoinMap. §注意 Task IDs are not sequential, and do not indicate the order in which tasks are spawned, what runtime a task is spawned on, or any other data. The task ID of the currently running task can be obtained from inside the task via the task::try_id() and task::id() functions and from outside the task via the JoinHandle::id() function. 返回值的副本。 更多信息 Performs copy-assignment from source. 更多信息 使用给定的格式化器格式化此值。 更多信息 使用给定的格式化器格式化此值。 更多信息 Feeds this value into the given Hasher. 更多信息 Feeds a slice of this type into the given Hasher. 更多信息 This method returns an Ordering between self and other. 更多信息 Compares and returns the maximum of two values. 更多信息 Compares and returns the minimum of two values. 更多信息 Restrict a value to a certain interval. 更多信息 测试 self 与 other 值是否相等，供 == 运算符使用。 测试 != 运算符。默认实现几乎总是够用，除非有非常充分的理由，否则不应被覆盖。 若存在，此方法返回 self 与 other 值之间的排序关系。 更多信息 测试小于（针对 self 与 other），供 < 运算符使用。 更多信息 Tests less than or equal to (for self and other) and is used by the <= operator. 更多信息 Tests greater than (for self and other) and is used by the > operator. 更多信息 Tests greater than or equal to (for self and other) and is used by the >= operator. 更多信息 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 Performs copy-assignment from self to dest. 更多信息 原样返回参数。 调用 U::from(self)。 也就是说，此转换是 From<T> for U 实现选择执行的操作。 获得所有权后的类型。 Creates owned data from borrowed data, usually by cloning. 更多信息 Uses borrowed data to replace owned data, usually by cloning. 更多信息 Converts the given value to a String. 更多信息 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"唯一标识一个任务（相对于所有其他当前正在运行的任务）的不透明 ID。 仅在同时满足以下两个条件时，任务的 ID 才能被另一个任务重用： 在以下情况下，JoinHandle 被视为活动状态：","section_headers":[]},{"url":"tokio/task/struct.JoinError.html","crate":"tokio","title":"JoinError","section":"struct","desc":"任务未能执行完成。 如果错误是由任务被取消引起的，则返回 true。 有关取消的更多信息，请参阅模块级文档。 如果错误是由任务 panic 引起的，则返回 true。 §示例 use std::panic; #[tokio::main] async fn main() { let err = tokio::spawn(async { panic!(\"boom\"); }).await.unwrap_err(); assert!(err.is_panic()); } 消费 join error，返回任务 panic 的对象。 §恐慌 如果 Error 不表示底层任务以 panic 终止，则 into_panic() 会 panic。使用 is_panic 检查错误原因，或使用 try_into_panic 获取不会 panic 的变体。 §示例 ⓘuse std::panic; #[tokio::main] async fn main() { let err = tokio::spawn(async { panic!(\"boom\"); }).await.unwrap_err(); if err.is_panic() { // 在主任务上恢复 panic panic::resume_unwind(err.into_panic()); } } 消费 join error，如果任务因 panic 而终止，则返回任务 panic 的对象。否则，返回 self。 §示例 ⓘuse std::panic; #[tokio::main] async fn main() { let err = tokio::spawn(async { panic!(\"boom\"); }).await.unwrap_err(); if let Ok(reason) = err.try_into_panic() { // 在主任务上恢复 panic panic::resume_unwind(reason); } } 返回一个 task ID，用于标识相对于其他当前已生成任务出错的 task。 使用给定的格式化器格式化此值。 更多信息 使用给定的格式化器格式化此值。 更多信息 返回此错误的更底层来源（若有）。 更多信息 Provides type-based access to context intended for error reports. 更多信息 从输入类型转换为此类型。 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 原样返回参数。 调用 U::from(self)。 也就是说，此转换是 From<T> for U 实现选择执行的操作。 Converts the given value to a String. 更多信息 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"任务未能执行完成。","section_headers":[]},{"url":"tokio/task/struct.JoinHandle.html","crate":"tokio","title":"JoinHandle","section":"struct","desc":"用于加入任务（等待其终止）的所有权权限。 这可以看作是 Tokio 任务的 std::thread::JoinHandle 等价物，而不是线程的等价物。请注意，与此 JoinHandle 关联的后台任务在你调用 spawn 时立即开始运行，即使你尚未 await 此 JoinHandle。 当 JoinHandle 被 drop 时，它会分离关联的任务，这意味着不再有该任务的句柄，也无法再 join 它。 此 struct 由 task::spawn 和 task::spawn_blocking 函数创建。 保证在通过 JoinHandle await、JoinHandle::is_finished 或 AbortHandle::is_finished 观察到任务完成之前，已生成任务的析构函数已经运行完毕。 §取消安全性 &mut JoinHandle<T> 类型是取消安全的。如果在 tokio::select! 语句中将其用作事件，并且其他某个分支先完成，则可以保证任务的输出不会丢失。 如果 JoinHandle 被 drop，则任务将继续在后台运行，并且其返回值将丢失。 §示例 从 task::spawn 创建： use tokio::task; let join_handle: task::JoinHandle<_> = task::spawn(async { // 此处为一些工作 }); 从 task::spawn_blocking 创建： use tokio::task; let join_handle: task::JoinHandle<_> = task::spawn_blocking(|| { // 此处为一些阻塞工作 }); JoinHandle<T> 中的泛型参数 T 是已生成任务的返回类型。如果返回值是 i32，则 join handle 的类型为 JoinHandle<i32>： use tokio::task; let join_handle: task::JoinHandle<i32> = task::spawn(async { 5 + 3 }); 如果任务没有返回值，则 join handle 的类型为 JoinHandle<()>： use tokio::task; let join_handle: task::JoinHandle<()> = task::spawn(async { println!(\"I return nothing.\"); }); 请注意，handle.await 不会直接给出返回类型。它被包装在 Result 中，因为已生成任务中的 panic 会被 Tokio 捕获。必须双重链接 ? 运算符才能提取返回值： use tokio::task; use std::io; let join_handle: task::JoinHandle<Result<i32, io::Error>> = tokio::spawn(async { Ok(5 + 3) }); let result = join_handle.await??; assert_eq!(result, 8); Ok(()) 如果任务发生 panic，则错误是包含该 panic 的 JoinError： use tokio::task; use std::io; use std::panic; #[tokio::main] async fn main() -> io::Result<()> { let join_handle: task::JoinHandle<Result<i32, io::Error>> = tokio::spawn(async { panic!(\"boom\"); }); let err = join_handle.await.unwrap_err(); assert!(err.is_panic()); Ok(()) } 子任务被分离并比其父任务活得更久： use tokio::task; use tokio::time; use std::time::Duration; let original_task = task::spawn(async { let _detached_task = task::spawn(async { // 这里休眠以确保第一个任务先返回。 time::sleep(Duration::from_millis(10)).await; // 即使 JoinHandle 已被丢弃，此函数仍会被调用。 println!(\"♫ Still alive ♫\"); }); }); original_task.await.expect(\"The task being joined h","module_doc":"用于加入任务（等待其终止）的所有权权限。 这可以看作是 Tokio 任务的 std::thread::JoinHandle 等价物，而不是线程的等价物。请注意，与此 JoinHandle 关联的后台任务在你调用 spawn 时立即开始运行，即使你尚未 await 此 JoinHandle。 当 JoinHandle 被 drop 时，它会分离关联的任务，这意味着不再有该任务的句柄，也无法再 join 它。 此 struct 由 task::spawn 和 task::spawn_blocking 函数创建。 保证在通过 JoinHandle await、JoinHandle::is_finished 或 AbortHandle::is_finished 观察到任务完成之前，已生成任务的析构函数已经运行完毕。 &mut JoinHandle<T> 类型是取消安全的。如果在 tokio::select! 语句中将其用作事件，并且其他某个分支先完成，则可以保证任务的输出不会丢失。 如果 JoinHandle 被 drop，则任务将继续在后台运行，并且其返回值将丢失。 从 task::spaw","section_headers":[]},{"url":"tokio/task/struct.JoinSet.html","crate":"tokio","title":"JoinSet","section":"struct","desc":"在 Tokio 运行时上生成的任务的集合。 JoinSet 可用于等待集合中部分或全部任务的完成。该集合是无序的，任务将按其完成的顺序返回。 所有任务必须具有相同的返回类型 T。 当 JoinSet 被 drop 时，JoinSet 中的所有任务都将立即中止。 §示例 生成多个任务并等待它们。 use tokio::task::JoinSet; let mut set = JoinSet::new(); for i in 0..10 { set.spawn(async move { i }); } let mut seen = [false; 10]; while let Some(res) = set.join_next().await { let idx = res.unwrap(); seen[idx] = true; } for i in 0..10 { assert!(seen[i]); }§Task ID 保证 当一个任务在 JoinSet 中被跟踪时，该任务的 ID 相对于 Tokio 中所有其他正在运行的任务是唯一的。为此，在 JoinSet 中跟踪任务等同于持有该任务的 JoinHandle。有关详细信息，请参阅任务 ID 文档。 创建一个新的 JoinSet。 返回 JoinSet 中当前的任务数。 返回 JoinSet 是否为空。 在 JoinSet 上生成提供的任务，返回一个可用于远程取消任务的 AbortHandle。 当调用此方法时，提供的 future 将立即开始在后台运行，即使你未在 JoinSet 上 await 任何东西也是如此。 §恐慌 如果在 Tokio 运行时之外调用，此方法会发生 panic。 在所提供的运行时上生成提供的任务，并将其存储在此 JoinSet 中，返回一个可用于远程取消任务的 AbortHandle。 当调用此方法时，提供的 future 将立即开始在后台运行，即使你未在 JoinSet 上 await 任何东西也是如此。 在当前 LocalSet 或 LocalRuntime 上生成提供的任务，并将其存储在此 JoinSet 中，返回一个可用于远程取消任务的 AbortHandle。 当调用此方法时，提供的 future 将立即开始在后台运行，即使你未在 JoinSet 上 await 任何东西也是如此。 §恐慌 如果在 LocalSet 或 LocalRuntime 之外调用此方法，则会发生 panic。 在所提供的 LocalSet 上生成提供的任务，并将其存储在此 JoinSet 中，返回一个可用于远程取消任务的 AbortHandle。 与 spawn_local 方法不同，此方法可用于在不当前正在运行的 LocalSet 上生成本地任务。所提供的 future 将在下次启动 LocalSet 时开始运行。 在阻塞线程池上生成阻塞代码，并将其存储在此 JoinSet 中，返回一个可用于远程取消任务的 AbortHandle。 §示例 生成多个阻塞任务并等待它们。 use tokio::task::JoinSet; #[tokio::main] async fn main() { let mut set = JoinSet::new(); for i in 0..10 { set.spawn_blocking(move || { i }); } let mut seen = [false; 10]; while let Some(res) = set.join_next().await { let idx = res.unwrap(); seen[idx] = true; } for i in 0..10 { assert!(seen[i]); } }§恐慌 如果在 Tokio 运行时之外调用，此方法会发生 panic。 在所提供的运行时的阻塞线程池上生成阻塞代码，并将其存储在此 JoinSet 中，返回一个可用于远程取消任务的 AbortHandle。 等待集合中某个任务完成并返回其输出。 如果集合为空，则返回 None。 §Cancel Safety 此方法是可安全取消的。如果在 tokio::select! 语句中将 join_next 用作事件，并且其他某个分支先完成，则可以保证没有任务从此 JoinSet 中移除。 等待集合中某个任务完成，并返回其输出以及已完成任务的任务 ID。 如果集合为空，则返回 None。 当此方法返回错误时，失败任务的 ID 可以通过 JoinError::id 方法访问。 §Cancel Safety 此方法是可安全取消的。如果在 tokio::select! 语句中将 join_next_with_id 用作事件，并且其","module_doc":"在 Tokio 运行时上生成的任务的集合。 JoinSet 可用于等待集合中部分或全部任务的完成。该集合是无序的，任务将按其完成的顺序返回。 所有任务必须具有相同的返回类型 T。 当 JoinSet 被 drop 时，JoinSet 中的所有任务都将立即中止。 生成多个任务并等待它们。","section_headers":[]},{"url":"tokio/task/struct.LocalEnterGuard.html","crate":"tokio","title":"LocalEnterGuard","section":"struct","desc":"LocalSet 的上下文守卫 使用给定的格式化器格式化此值。 更多信息 执行此类型的析构函数。 更多信息 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 原样返回参数。 调用 U::from(self)。 也就是说，此转换是 From<T> for U 实现选择执行的操作。 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"LocalSet 的上下文守卫","section_headers":[]},{"url":"tokio/task/struct.LocalKey.html","crate":"tokio","title":"LocalKey","section":"struct","desc":"用于任务本地数据的键。 此类型由 task_local! 宏生成。 与 std::thread::LocalKey, tokio::task::LocalKey 不会在首次访问时惰性初始化该值。而是在包含任务本地的 future 首次由 futures 执行器（如 Tokio）poll 时才初始化该值。 §示例 tokio::task_local! { static NUMBER: u32; } NUMBER.scope(1, async move { assert_eq!(NUMBER.get(), 1); }).await; NUMBER.scope(2, async move { assert_eq!(NUMBER.get(), 2); NUMBER.scope(3, async move { assert_eq!(NUMBER.get(), 3); }).await; }).await; 将值 T 设置为 future F 的任务本地值。 在 scope 完成时，任务本地将被 drop。 §恐慌 如果在同一个 LocalKey 上调用 with 或 try_with 时 poll 返回的 future，则对 poll 的调用将发生 panic。 §示例 tokio::task_local! { static NUMBER: u32; } NUMBER.scope(1, async move { println!(\"task local value: {}\", NUMBER.get()); }).await; 将值 T 设置为闭包 F 的任务本地值。 在 sync_scope 完成时，任务本地将被 drop。 §恐慌 如果在同一个 LocalKey 上调用 with 或 try_with 时调用此方法，则会发生 panic。 §示例 tokio::task_local! { static NUMBER: u32; } NUMBER.sync_scope(1, || { println!(\"task local value: {}\", NUMBER.get()); }); 访问当前 task-local 并运行所提供的闭包。 §恐慌 如果 task local 没有设置值，此函数会发生 panic。 访问当前 task-local 并运行所提供的闭包。 如果具有关联键的任务本地不存在，则此方法将返回 AccessError。有关 panicking 变体，请参见 with。 如果 task-local 值实现了 Clone，则返回该值的一份副本。 §恐慌 如果 task local 没有设置值，此函数会发生 panic。 如果 task-local 值实现了 Clone，则返回该值的一份副本。 如果具有关联键的任务本地不存在，则此方法将返回 AccessError。有关 panicking 变体，请参见 get。 使用给定的格式化器格式化此值。 更多信息 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 原样返回参数。 调用 U::from(self)。 也就是说，此转换是 From<T> for U 实现选择执行的操作。 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"用于任务本地数据的键。 此类型由 task_local! 宏生成。 与 std::thread::LocalKey, tokio::task::LocalKey 不会在首次访问时惰性初始化该值。而是在包含任务本地的 future 首次由 futures 执行器（如 Tokio）poll 时才初始化该值。","section_headers":[]},{"url":"tokio/task/struct.LocalSet.html","crate":"tokio","title":"LocalSet","section":"struct","desc":"在同一线程上执行的任务的集合。 在某些情况下，有必要运行一个或多个未实现 Send 的 future，因此在线程之间发送它们是不安全的。在这些情况下，可以使用本地任务集来调度一个或多个 !Send future 在同一线程上一起运行。 例如，以下代码将无法编译： ⓘuse std::rc::Rc; #[tokio::main] async fn main() { // `Rc` 没有实现 `Send`，因此不能在线程间 // 安全地发送。 let nonsend_data = Rc::new(\"my nonsend data...\"); let nonsend_data = nonsend_data.clone(); // 因为这里的 `async` 块会移动 `nonsend_data`，所以 future 是 `!Send` 的。 // 由于 `tokio::spawn` 要求派生的 future 实现 `Send`， // 这段代码无法编译。 tokio::spawn(async move { println!(\"{}\", nonsend_data); // ... }).await.unwrap(); }§与 run_until 一起使用 要生成 !Send future，我们可以使用本地任务集来在调用 Runtime::block_on 的线程上调度它们。在本地任务集中运行时，我们可以使用 task::spawn_local，它可以生成 !Send future。例如： use std::rc::Rc; use tokio::task; let nonsend_data = Rc::new(\"my nonsend data...\"); // 构造一个可以运行 `!Send` future 的本地任务集。 let local = task::LocalSet::new(); // 运行本地任务集。 local.run_until(async move { let nonsend_data = nonsend_data.clone(); // `spawn_local` 确保 future 被派生到本地 // 任务集上。 task::spawn_local(async move { println!(\"{}\", nonsend_data); // ... }).await.unwrap(); }).await; 注意：run_until 方法只能在 #[tokio::main]、#[tokio::test] 中使用，或者直接在 Runtime::block_on 调用中使用。它不能在通过 tokio::spawn 生成的任务内部使用。 §Awaiting a LocalSet 此外，LocalSet 本身实现了 Future，当在 LocalSet 上生成的所有任务都完成时完成。这可用于在 LocalSet 上运行多个 future 并驱动整个集合直至它们完成。例如： use tokio::{task, time}; use std::rc::Rc; let nonsend_data = Rc::new(\"world\"); let local = task::LocalSet::new(); let nonsend_data2 = nonsend_data.clone(); local.spawn_local(async move { // ... println!(\"hello {}\", nonsend_data2) }); local.spawn_local(async move { time::sleep(time::Duration::from_millis(100)).await; println!(\"goodbye {}\", nonsend_data) }); // ... local.await; 注意：等待 LocalSet 只能在 #[tokio::main]、#[tokio::test] 中进行，或者直接在 Runtime::block_on 调用中进行。它不能在通过 tokio::spawn 生成的任务内部使用。 §Use inside tokio::spawn 上面提到的两个方法不能在 tokio::spawn 内部使用，因此要在 tokio::spawn 内部生成 !Send future，我们需要采用其他方法。解决方案是在其他地方创建 LocalSet，并使用 mpsc 通道与之通信。 下面的示例将 LocalSet 放在一个新线程中。 use tokio::runtime::Builder; use tokio::sync::{mpsc, oneshot}; use tokio::task:","module_doc":"在同一线程上执行的任务的集合。 在某些情况下，有必要运行一个或多个未实现 Send 的 future，因此在线程之间发送它们是不安全的。在这些情况下，可以使用本地任务集来调度一个或多个 !Send future 在同一线程上一起运行。 例如，以下代码将无法编译：","section_headers":[]},{"url":"tokio/task/coop/fn.consume_budget.html","crate":"tokio","title":"consume_budget","section":"fn","desc":"消耗一个单位的预算，如果任务的 coop 预算已用尽，则将执行返回给 Tokio 运行时。 仅当任务的整个 coop 预算已用尽时，任务才会让步。此函数可用于在不使用诸如套接字或信号量等 Tokio 资源的长时间计算中插入可选的让步点，而无需每次都重复让步给运行时。 §示例 确保一个返回（可能很多）迭代值之和的函数是协作式的。 async fn sum_iterator(input: &mut impl std::iter::Iterator<Item=i64>) -> i64 { let mut sum: i64 = 0; while let Some(i) = input.next() { sum += i; tokio::task::consume_budget().await } sum }","module_doc":"消耗一个单位的预算，如果任务的 coop 预算已用尽，则将执行返回给 Tokio 运行时。 仅当任务的整个 coop 预算已用尽时，任务才会让步。此函数可用于在不使用诸如套接字或信号量等 Tokio 资源的长时间计算中插入可选的让步点，而无需每次都重复让步给运行时。 确保一个返回（可能很多）迭代值之和的函数是协作式的。","section_headers":[]},{"url":"tokio/task/coop/fn.cooperative.html","crate":"tokio","title":"cooperative","section":"fn","desc":"创建一个 wrapper future，使内部 future 与 Tokio 调度器协作。 被 poll 时，wrapper 将首先调用 poll_proceed 来消耗任务预算，如果预算已用尽，则立即让步。如果预算可用，则 poll 内部 future。如果内部 future 解析为其最终值，则将使用 RestoreOnPending::made_progress 确定预算消耗。 §示例 当在 tokio::sync::mpsc channel 的 Receiver 上调用 recv 时，返回下一个值时会自动消耗任务预算。这使得使用 Tokio mpsc channel 的任务自动成为协作式的。 如果改用 futures::channel::mpsc，则不会自动消耗任务预算。此示例演示了如何使用 cooperative 使 futures::channel::mpsc channel 以与 Tokio channel 相同的方式与调度器协作。 use tokio::task::coop::cooperative; use futures::channel::mpsc::Receiver; use futures::stream::StreamExt; async fn receive_next<T>(receiver: &mut Receiver<T>) -> Option<T> { // 使用 `StreamExt::next` 获取一个解析为下一个值的 `Future` let recv_future = receiver.next(); // 用协作包装器包装它 let coop_future = cooperative(recv_future); // 并等待 coop_future.await }","module_doc":"创建一个 wrapper future，使内部 future 与 Tokio 调度器协作。 被 poll 时，wrapper 将首先调用 poll_proceed 来消耗任务预算，如果预算已用尽，则立即让步。如果预算可用，则 poll 内部 future。如果内部 future 解析为其最终值，则将使用 RestoreOnPending::made_progress 确定预算消耗。 当在 tokio::sync::mpsc channel 的 Receiver 上调用 recv 时，返回下一个值时会自动消耗任务预算。这使得使用 Tokio mpsc channel 的任务自动成为协作式的。 如果改用 futures::channel::mpsc，则不会自动消耗任务预算。此示例演示了如何使用 cooperative 使 futures::channel::mpsc channel 以与 Tokio channel 相同的方式与调度器协作。","section_headers":[]},{"url":"tokio/task/coop/fn.has_budget_remaining.html","crate":"tokio","title":"has_budget_remaining","section":"fn","desc":"如果任务还有剩余预算，则返回 true。 §示例 此示例定义了一个 Timeout future，它要求给定的 future 在指定的持续时间过去之前完成。如果完成，则返回其结果；否则，返回错误并取消该 future。 请注意，future 可能会在我们评估超时之前耗尽预算。使用 has_budget_remaining，我们可以检测到此场景并确保始终检查超时。 pub struct Timeout<T> { future: T, delay: Pin<Box<Sleep>>, } impl<T> Future for Timeout<T> where T: Future + Unpin, { type Output = Result<T::Output, ()>; fn poll(self: Pin<&mut Self>, cx: &mut Context<'_>) -> Poll<Self::Output> { let this = Pin::into_inner(self); let future = Pin::new(&mut this.future); let delay = Pin::new(&mut this.delay); // 检查 future 是否就绪 let had_budget_before = coop::has_budget_remaining(); if let Poll::Ready(v) = future.poll(cx) { return Poll::Ready(Ok(v)); } let has_budget_now = coop::has_budget_remaining(); // 评估 timeout if let (true, false) = (had_budget_before, has_budget_now) { // 是底层 future 耗尽了预算 ready!(pin!(coop::unconstrained(delay)).poll(cx)); } else { ready!(delay.poll(cx)); } return Poll::Ready(Err(())); } }","module_doc":"如果任务还有剩余预算，则返回 true。 此示例定义了一个 Timeout future，它要求给定的 future 在指定的持续时间过去之前完成。如果完成，则返回其结果；否则，返回错误并取消该 future。 请注意，future 可能会在我们评估超时之前耗尽预算。使用 has_budget_remaining，我们可以检测到此场景并确保始终检查超时。","section_headers":[]},{"url":"tokio/task/coop/fn.poll_proceed.html","crate":"tokio","title":"poll_proceed","section":"fn","desc":"如果预算已用尽，则递减任务预算并返回 Poll::Pending。这表明任务应让步给调度器。否则，返回 RestoreOnPending，可用于提交预算消耗。 返回的 RestoreOnPending 在 drop 时会将预算恢复为之前的值，除非调用 RestoreOnPending::made_progress。调用方有责任在调用 poll_proceed 后能够取得进展时执行此操作。恢复预算会自动确保任务可以尝试以其他方式取得进展。 请注意，RestoreOnPending 将预算恢复为调用 poll_proceed 之前的状态。因此，如果在 poll_proceed 返回到 RestoreOnPending 被 drop 之间预算被进一步调整，则这些调整将被擦除，除非调用方表明已取得进展。 §示例 此示例包装了 futures::channel::mpsc::UnboundedReceiver，以与 Tokio 调度器协作。每次接收到一个值时，都会消耗任务预算。如果预算不可用，则任务将让步给调度器。 use std::pin::Pin; use std::task::{ready, Context, Poll}; use tokio::task::coop; use futures::stream::{Stream, StreamExt}; use futures::channel::mpsc::UnboundedReceiver; struct CoopUnboundedReceiver<T> { receiver: UnboundedReceiver<T>, } impl<T> Stream for CoopUnboundedReceiver<T> { type Item = T; fn poll_next( mut self: Pin<&mut Self>, cx: &mut Context<'_> ) -> Poll<Option<T>> { let coop = ready!(coop::poll_proceed(cx)); match self.receiver.poll_next_unpin(cx) { Poll::Ready(v) => { // 我们收到了一个值，因此消耗预算。 coop.made_progress(); Poll::Ready(v) } Poll::Pending => Poll::Pending, } } }","module_doc":"如果预算已用尽，则递减任务预算并返回 Poll::Pending。这表明任务应让步给调度器。否则，返回 RestoreOnPending，可用于提交预算消耗。 返回的 RestoreOnPending 在 drop 时会将预算恢复为之前的值，除非调用 RestoreOnPending::made_progress。调用方有责任在调用 poll_proceed 后能够取得进展时执行此操作。恢复预算会自动确保任务可以尝试以其他方式取得进展。 请注意，RestoreOnPending 将预算恢复为调用 poll_proceed 之前的状态。因此，如果在 poll_proceed 返回到 RestoreOnPending 被 drop 之间预算被进一步调整，则这些调整将被擦除，除非调用方表明已取得进展。 此示例包装了 futures::channel::mpsc::UnboundedReceiver，以与 Tokio 调度器协作。每次接收到一个值时，都会消耗任务预算。如果预算不可用，则任务将让步给调度器。","section_headers":[]},{"url":"tokio/task/coop/fn.unconstrained.html","crate":"tokio","title":"unconstrained","section":"fn","desc":"为 future 关闭协作调度。该 future 将永远不会被 Tokio 强制让步。如果 unconstrained future 永远不会让步，则使用它将使你的服务面临饥饿的风险。 另请参阅 task 模块中的使用示例。","module_doc":"为 future 关闭协作调度。该 future 将永远不会被 Tokio 强制让步。如果 unconstrained future 永远不会让步，则使用它将使你的服务面临饥饿的风险。 另请参阅 task 模块中的使用示例。","section_headers":[]},{"url":"tokio/task/coop/struct.Coop.html","crate":"tokio","title":"Coop","section":"struct","desc":"由 cooperative 创建的用于确保协作调度的 future 包装器。 Future 完成时产生的值的类型。 Attempts to resolve the future to a final value, registering the current task for wakeup if the value is not yet available. 更多信息 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 原样返回参数。 调用 U::from(self)。 也就是说，此转换是 From<T> for U 实现选择执行的操作。 Future 完成时产生的输出。 我们将要把此值转变成哪种 future？ Creates a future from a value. 更多信息 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"由 cooperative 创建的用于确保协作调度的 future 包装器。","section_headers":[]},{"url":"tokio/task/coop/struct.RestoreOnPending.html","crate":"tokio","title":"RestoreOnPending","section":"struct","desc":"由 poll_proceed 方法返回的值。 表示获取此 RestoreOnPending 的任务能够取得进展。这可以防止在 drop 时将任务预算恢复为获取此实例之前的值。 使用给定的格式化器格式化此值。 更多信息 执行此类型的析构函数。 更多信息 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 原样返回参数。 调用 U::from(self)。 也就是说，此转换是 From<T> for U 实现选择执行的操作。 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"由 poll_proceed 方法返回的值。","section_headers":[]},{"url":"tokio/task/coop/struct.Unconstrained.html","crate":"tokio","title":"Unconstrained","section":"struct","desc":"unconstrained 方法的 future。 Future 完成时产生的值的类型。 Attempts to resolve the future to a final value, registering the current task for wakeup if the value is not yet available. 更多信息 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 原样返回参数。 调用 U::from(self)。 也就是说，此转换是 From<T> for U 实现选择执行的操作。 Future 完成时产生的输出。 我们将要把此值转变成哪种 future？ Creates a future from a value. 更多信息 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"unconstrained 方法的 future。","section_headers":[]},{"url":"tokio/task/futures/struct.TaskLocalFuture.html","crate":"tokio","title":"TaskLocalFuture","section":"struct","desc":"在 future F 执行期间为其设置任务本地值 T 的 future。 任务本地的值必须为 'static，并将在 future 完成时被 drop。 由函数 LocalKey::scope 创建。 §示例 tokio::task_local! { static NUMBER: u32; } NUMBER.scope(1, async move { println!(\"task local value: {}\", NUMBER.get()); }).await; 返回此 TaskLocalFuture 存储在任务本地中的值。 函数返回： Some(T) if the task local value exists. None if the task local value has already been taken. 请注意，即使 future 尚未完成，此函数也会尝试获取任务本地值。在这种情况下，在调用 take_value 之后，该值将不再可通过任务本地获得。 §示例 tokio::task_local! { static KEY: u32; } let fut = KEY.scope(42, async { // 执行一些异步工作 }); let mut pinned = Box::pin(fut); // 完成 TaskLocalFuture let _ = pinned.as_mut().await; // 这里可以取到 task 本地值 let value = pinned.as_mut().take_value(); assert_eq!(value, Some(42)); 使用给定的格式化器格式化此值。 更多信息 执行此类型的析构函数。 更多信息 Future 完成时产生的值的类型。 Attempts to resolve the future to a final value, registering the current task for wakeup if the value is not yet available. 更多信息 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 原样返回参数。 调用 U::from(self)。 也就是说，此转换是 From<T> for U 实现选择执行的操作。 Future 完成时产生的输出。 我们将要把此值转变成哪种 future？ Creates a future from a value. 更多信息 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"在 future F 执行期间为其设置任务本地值 T 的 future。 任务本地的值必须为 'static，并将在 future 完成时被 drop。 由函数 LocalKey::scope 创建。","section_headers":[]},{"url":"tokio/time/enum.MissedTickBehavior.html","crate":"tokio","title":"MissedTickBehavior","section":"enum","desc":"定义 Interval 错过 tick 时的行为。 有时，Interval 的 tick 会被错过。例如，请考虑以下情况： use tokio::time::{self, Duration}; // 每 2 毫秒 tick 一次 let mut interval = time::interval(Duration::from_millis(2)); for _ in 0..5 { interval.tick().await; // 如果这耗时超过 2 毫秒，tick 会被延迟 task_that_takes_one_to_three_millis().await; } 一般来说，如果在调用 Interval::tick() 之前花费了太多时间，则会错过 tick。 默认情况下，当错过 tick 时，Interval 会尽快触发 tick，直到它在时间上\"赶上\"它应该在的位置。可以使用 MissedTickBehavior 来为 Interval 指定不同的行为。每个变体代表一种不同的策略。 请注意，由于执行器无法保证计时器的精确精度，因此这些策略仅在延迟大于 5 毫秒时适用。 尽可能快地触发 tick，直到赶上为止。 当使用此策略时，Interval 以\"正常\"方式调度 tick（与 tick 没有延迟时的方式相同），这会导致它尽快触发 tick，直到它在时间上赶上它应该在的位置。与 Delay 和 Skip 不同，使用 Burst 时产生的 tick（tick 产生的 Instant）与未错过 tick 时没什么不同。与 Skip 类似，与 Delay 不同，tick 可能会被缩短。 大致如下： Expected ticks: | 1 | 2 | 3 | 4 | 5 | 6 | Actual ticks: | work -----| delay | work | work | work -| work -----| 代码： use tokio::time::{interval, Duration}; let mut interval = interval(Duration::from_millis(50)); // 第一次 tick 在创建后立即解析 interval.tick().await; task_that_takes_200_millis().await; // `Interval` 错过了一次 tick // 由于已超过 timeout，这会立即解析 interval.tick().await; // 由于距 `interval` 开始已超过 100ms，因此 // 也会立即解析。 interval.tick().await; // 也会立即解析，因为它本应在 `interval` 开始后 // 150ms 时解析 interval.tick().await; // 立即解析 interval.tick().await; // 由于距 `interval` 开始已过去 200ms，因此这 // 将在 50ms 后解析 interval.tick().await; 这是使用 interval 和 interval_at 创建 Interval 时的默认行为。 从调用 tick 开始以 period 的倍数触发 tick，而不是从 start 开始。 当使用此策略并且 Interval 错过一个 tick 时，它不会从 start（第一个 tick 触发的时间）开始以 period 的倍数调度 tick 触发，而是将所有未来的 tick 调度为从调用 tick 的点开始以规则 period 发生。与 Burst 和 Skip 不同，tick 不会被缩短，也不再保证以从 start 开始的 period 的倍数发生。 大致如下： Expected ticks: | 1 | 2 | 3 | 4 | 5 | 6 | Actual ticks: | work -----| delay | work -----| work -----| work -----| 代码： use tokio::time::{interval, Duration, MissedTickBehavior}; let mut interval = interval(Duration::from_millis(50)); interval.set_missed_tick_behavior(MissedTickBehavior::Delay); task_that_takes_more_than_50_millis().await; // `Interval` 错过了一次 tick // 由于已超过 timeout，这会立即解析 interval.tick","module_doc":"定义 Interval 错过 tick 时的行为。 有时，Interval 的 tick 会被错过。例如，请考虑以下情况：","section_headers":["变体"]},{"url":"tokio/time/fn.interval.html","crate":"tokio","title":"interval","section":"fn","desc":"创建一个新的 Interval，以 period 间隔产生值。第一个 tick 立即完成。默认的 MissedTickBehavior 是 Burst，但可以通过调用 set_missed_tick_behavior 进行配置。 interval 将无限期地 tick。在任何时候都可以 drop 该 Interval 值。这将取消该 interval。 此函数等效于 interval_at(Instant::now(), period)。 §恐慌 如果 period 为零，此函数会发生 panic。 §示例 use tokio::time::{self, Duration}; let mut interval = time::interval(Duration::from_millis(10)); interval.tick().await; // 立即 tick interval.tick().await; // 在 10ms 后 tick interval.tick().await; // 在 10ms 后 tick // 大约已过去 20ms。 使用 interval 每两秒执行一个任务的简单示例。 interval 和 sleep 的区别在于，Interval 测量自上一个 tick 以来经过的时间，这意味着如果两次调用 .tick().await 之间经过了一些时间，则 .tick().await 可能等待的时间比为间隔指定的时间更短。 如果将下面示例中的 tick 替换为 sleep，那么该任务将每 3 秒才执行一次，而不是每 2 秒执行一次。 use tokio::time; async fn task_that_takes_a_second() { println!(\"hello\"); time::sleep(time::Duration::from_secs(1)).await } let mut interval = time::interval(time::Duration::from_secs(2)); for _i in 0..5 { interval.tick().await; task_that_takes_a_second().await; }","module_doc":"创建一个新的 Interval，以 period 间隔产生值。第一个 tick 立即完成。默认的 MissedTickBehavior 是 Burst，但可以通过调用 set_missed_tick_behavior 进行配置。 interval 将无限期地 tick。在任何时候都可以 drop 该 Interval 值。这将取消该 interval。 此函数等效于 interval_at(Instant::now(), period)。 如果 period 为零，此函数会发生 panic。","section_headers":[]},{"url":"tokio/time/fn.interval_at.html","crate":"tokio","title":"interval_at","section":"fn","desc":"创建一个新的 Interval，以 period 间隔产生值，第一个 tick 在 start 完成。默认的 MissedTickBehavior 是 Burst，但可以通过调用 set_missed_tick_behavior 进行配置。 interval 将无限期地 tick。在任何时候都可以 drop 该 Interval 值。这将取消该 interval。 §恐慌 如果 period 为零，此函数会发生 panic。 §示例 use tokio::time::{interval_at, Duration, Instant}; let start = Instant::now() + Duration::from_millis(50); let mut interval = interval_at(start, Duration::from_millis(10)); interval.tick().await; // 在 50ms 后 tick interval.tick().await; // 在 10ms 后 tick interval.tick().await; // 在 10ms 后 tick // 大约已过去 70ms。","module_doc":"创建一个新的 Interval，以 period 间隔产生值，第一个 tick 在 start 完成。默认的 MissedTickBehavior 是 Burst，但可以通过调用 set_missed_tick_behavior 进行配置。 interval 将无限期地 tick。在任何时候都可以 drop 该 Interval 值。这将取消该 interval。 如果 period 为零，此函数会发生 panic。","section_headers":[]},{"url":"tokio/time/fn.sleep.html","crate":"tokio","title":"sleep","section":"fn","desc":"等待直到 duration 已过。 等效于 sleep_until(Instant::now() + duration)。std::thread::sleep 的异步模拟。 在等待 sleep future 完成时不会执行任何工作。Sleep 以毫秒粒度运行，不应用于需要高分辨率定时器的任务。实现是平台特定的，某些平台（特别是 Windows）将提供大于 1 毫秒分辨率的定时器。 若要按计划定期运行某些任务，请参阅 interval。 §取消 取消 sleep 实例的方法是 drop 返回的 future。无需进行额外的清理工作。 §示例 等待 100 毫秒并打印 “已过 100 毫秒”。 use tokio::time::{sleep, Duration}; sleep(Duration::from_millis(100)).await; println!(\"100 ms have elapsed\"); 有关更多示例，请参阅 Sleep 类型的文档。 §恐慌 如果未设置当前定时器，此函数会发生 panic。 当 Builder::enable_time 或 Builder::enable_all 未包含在 builder 中时，可能会触发 panic。 每当在 Tokio 运行时之外创建定时器时，它也可能会发生 panic。这就是 rt.block_on(sleep(...)) 会发生 panic 的原因，因为该函数在运行时之外执行。而 rt.block_on(async {sleep(...).await}) 不会 panic，这是因为将函数包装在 async 中使其变为惰性的，从而可以在运行时内成功执行而不会发生 panic。","module_doc":"等待直到 duration 已过。 等效于 sleep_until(Instant::now() + duration)。std::thread::sleep 的异步模拟。 在等待 sleep future 完成时不会执行任何工作。Sleep 以毫秒粒度运行，不应用于需要高分辨率定时器的任务。实现是平台特定的，某些平台（特别是 Windows）将提供大于 1 毫秒分辨率的定时器。 若要按计划定期运行某些任务，请参阅 interval。 取消 sleep 实例的方法是 drop 返回的 future。无需进行额外的清理工作。 等待 100 毫秒并打印 “已过 100 毫秒”。","section_headers":[]},{"url":"tokio/time/fn.sleep_until.html","crate":"tokio","title":"sleep_until","section":"fn","desc":"等待直到到达 deadline。 在等待 sleep future 完成时不会执行任何工作。Sleep 以毫秒粒度运行，不应用于需要高分辨率定时器的任务。 若要按计划定期运行某些任务，请参阅 interval。 §取消 取消 sleep 实例的方法是 drop 返回的 future。无需进行额外的清理工作。 §示例 等待 100 毫秒并打印 “已过 100 毫秒”。 use tokio::time::{sleep_until, Instant, Duration}; sleep_until(Instant::now() + Duration::from_millis(100)).await; println!(\"100 ms have elapsed\"); 有关更多示例，请参阅 Sleep 类型的文档。 §恐慌 如果未设置当前定时器，此函数会发生 panic。 当 Builder::enable_time 或 Builder::enable_all 未包含在 builder 中时，可能会触发 panic。 每当在 Tokio 运行时之外创建定时器时，它也可能会发生 panic。这就是 rt.block_on(sleep(...)) 会发生 panic 的原因，因为该函数在运行时之外执行。而 rt.block_on(async {sleep(...).await}) 不会 panic，这是因为将函数包装在 async 中使其变为惰性的，从而可以在运行时内成功执行而不会发生 panic。","module_doc":"等待直到到达 deadline。 在等待 sleep future 完成时不会执行任何工作。Sleep 以毫秒粒度运行，不应用于需要高分辨率定时器的任务。 若要按计划定期运行某些任务，请参阅 interval。 取消 sleep 实例的方法是 drop 返回的 future。无需进行额外的清理工作。 等待 100 毫秒并打印 “已过 100 毫秒”。","section_headers":[]},{"url":"tokio/time/fn.timeout.html","crate":"tokio","title":"timeout","section":"fn","desc":"要求 Future 在指定的持续时间过去之前完成。 如果 future 在持续时间过去之前完成，则返回已完成的 future 值。否则，返回错误并取消该 future。 请注意，timeout 是在 poll future 之前检查的，因此如果 future 在执行期间不让步，则 future 可能会完成并超过 timeout，而不会返回错误。 此函数返回一个 future，其返回类型为 Result<T,Elapsed>，其中 T 是所提供 future 的返回类型。 如果所提供的 future 立即完成，则无论提供的持续时间如何，此函数返回的 future 保证立即以 Ok 变体完成。 §取消 取消 timeout 的方法是 drop 该 future。无需进行额外的清理或其他工作。 可以通过调用 Timeout::into_inner 来获取原始 future。这会消费 Timeout。 §示例 创建一个设置为在 10 毫秒后到期的新 Timeout。 use tokio::time::timeout; use tokio::sync::oneshot; use std::time::Duration; let (tx, rx) = oneshot::channel(); // 将 future 包装在一个 `Timeout` 中，设置为 10 毫秒后过期。 if let Err(_) = timeout(Duration::from_millis(10), rx).await { println!(\"did not receive value within 10 ms\"); }§恐慌 如果未设置当前定时器，此函数会发生 panic。 当 Builder::enable_time 或 Builder::enable_all 未包含在 builder 中时，可能会触发 panic。 每当在 Tokio 运行时之外创建定时器时，它也可能会发生 panic。这就是 rt.block_on(sleep(...)) 会发生 panic 的原因，因为该函数在运行时之外执行。而 rt.block_on(async {sleep(...).await}) 不会 panic，这是因为将函数包装在 async 中使其变为惰性的，从而可以在运行时内成功执行而不会发生 panic。","module_doc":"要求 Future 在指定的持续时间过去之前完成。 如果 future 在持续时间过去之前完成，则返回已完成的 future 值。否则，返回错误并取消该 future。 请注意，timeout 是在 poll future 之前检查的，因此如果 future 在执行期间不让步，则 future 可能会完成并超过 timeout，而不会返回错误。 此函数返回一个 future，其返回类型为 Result<T,Elapsed>，其中 T 是所提供 future 的返回类型。 如果所提供的 future 立即完成，则无论提供的持续时间如何，此函数返回的 future 保证立即以 Ok 变体完成。 取消 timeout 的方法是 drop 该 future。无需进行额外的清理或其他工作。 可以通过调用 Timeout::into_inner 来获取原始 future。这会消费 Timeout。 创建一个设置为在 10 毫秒后到期的新 Timeout。","section_headers":[]},{"url":"tokio/time/fn.timeout_at.html","crate":"tokio","title":"timeout_at","section":"fn","desc":"要求 Future 在指定的时间点之前完成。 如果 future 在到达时间点之前完成，则返回已完成的 future 值。否则，返回错误。 此函数返回一个 future，其返回类型为 Result<T,Elapsed>，其中 T 是所提供 future 的返回类型。 如果所提供的 future 立即完成，则无论提供的截止时间如何，此函数返回的 future 保证立即以 Ok 变体完成。 §取消 取消 timeout 的方法是 drop 该 future。无需进行额外的清理或其他工作。 可以通过调用 Timeout::into_inner 来获取原始 future。这会消费 Timeout。 §示例 创建一个设置为在 10 毫秒后到期的新 Timeout。 use tokio::time::{Instant, timeout_at}; use tokio::sync::oneshot; use std::time::Duration; let (tx, rx) = oneshot::channel(); // 将 future 包装在一个 `Timeout` 中，设置为 10 毫秒后 // future。 if let Err(_) = timeout_at(Instant::now() + Duration::from_millis(10), rx).await { println!(\"did not receive value within 10 ms\"); }§恐慌 如果未设置当前定时器，此函数会发生 panic。 当 Builder::enable_time 或 Builder::enable_all 未包含在 builder 中时，可能会触发 panic。 每当在 Tokio 运行时之外创建定时器时，它也可能会发生 panic。这就是 rt.block_on(sleep(...)) 会发生 panic 的原因，因为该函数在运行时之外执行。而 rt.block_on(async {sleep(...).await}) 不会 panic，这是因为将函数包装在 async 中使其变为惰性的，从而可以在运行时内成功执行而不会发生 panic。","module_doc":"要求 Future 在指定的时间点之前完成。 如果 future 在到达时间点之前完成，则返回已完成的 future 值。否则，返回错误。 此函数返回一个 future，其返回类型为 Result<T,Elapsed>，其中 T 是所提供 future 的返回类型。 如果所提供的 future 立即完成，则无论提供的截止时间如何，此函数返回的 future 保证立即以 Ok 变体完成。 取消 timeout 的方法是 drop 该 future。无需进行额外的清理或其他工作。 可以通过调用 Timeout::into_inner 来获取原始 future。这会消费 Timeout。 创建一个设置为在 10 毫秒后到期的新 Timeout。","section_headers":[]},{"url":"tokio/time/struct.Instant.html","crate":"tokio","title":"Instant","section":"struct","desc":"单调不减时钟的测量值。不透明，仅与 Duration 配合使用时有用。 在创建时，Instant 始终保证不小于任何先前测量的 Instant，并且通常用于测量基准测试或操作耗时之类的任务。 但请注意，Instant 不保证是稳定的。换句话说，底层时钟的每个 tick 长度可能不同（例如，某些秒可能比其他秒长）。Instant 可能会向前跳跃或经历时间膨胀（变慢或变快），但绝不会向后走。 Instant 是不透明类型，只能相互比较。没有方法可以从 Instant 获取\"秒数\"。相反，它只允许测量两个 Instant 之间的持续时间（或比较两个 Instant）。 Instant 结构体的大小可能因目标操作系统而异。 §注意 此类型包装内部的 std 变体，用于对齐 Tokio 时钟以用于 now()。这对于测试非常有用，你可以利用 time::pause() 和 time::advance()。 返回对应于\"现在\"的 Instant。 §示例 use tokio::time::Instant; let now = Instant::now(); 从 std::time::Instant 创建 tokio::time::Instant。 将该值转换为 std::time::Instant。 返回从另一时刻到此刻所经过的时间量，如果该时刻晚于此刻，则返回零时长。 返回从另一个 Instant 到此 Instant 已经过去的时间量；如果那个 Instant 比此 Instant 晚，则返回 None。 §示例 use tokio::time::{Duration, Instant, sleep}; let now = Instant::now(); sleep(Duration::new(1, 0)).await; let new_now = Instant::now(); println!(\"{:?}\", new_now.checked_duration_since(now)); println!(\"{:?}\", now.checked_duration_since(new_now)); // None 返回从另一时刻到此刻所经过的时间量，如果该时刻晚于此刻，则返回零时长。 §示例 use tokio::time::{Duration, Instant, sleep}; let now = Instant::now(); sleep(Duration::new(1, 0)).await; let new_now = Instant::now(); println!(\"{:?}\", new_now.saturating_duration_since(now)); println!(\"{:?}\", now.saturating_duration_since(new_now)); // 0ns } 返回自创建此 Instant 以来已经过去的时间量；如果此 Instant 在未来，则返回零持续时间。 §示例 use tokio::time::{Duration, Instant, sleep}; let instant = Instant::now(); let three_secs = Duration::from_secs(3); sleep(three_secs).await; assert!(instant.elapsed() >= three_secs); 如果 t 可以表示为 Instant（这意味着它位于底层数据结构的边界内），则返回 Some(t)，其中 t 是时间 self + duration；否则返回 None。 如果 t 可以表示为 Instant（这意味着它位于底层数据结构的边界内），则返回 Some(t)，其中 t 是时间 self - duration；否则返回 None。 应用 + 运算符后得到的类型。 Performs the + operation. 更多信息 Performs the += operation. 更多信息 返回值的副本。 更多信息 Performs copy-assignment from source. 更多信息 使用给定的格式化器格式化此值。 更多信息 从输入类型转换为此类型。 从输入类型转换为此类型。 Feeds this value into the given Hasher. 更多信息 Feeds a slice of this type into the given Hasher. 更多信息 This method returns an Ordering between self and other. 更多信息 Compares and returns the maxi","module_doc":"单调不减时钟的测量值。不透明，仅与 Duration 配合使用时有用。 在创建时，Instant 始终保证不小于任何先前测量的 Instant，并且通常用于测量基准测试或操作耗时之类的任务。 但请注意，Instant 不保证是稳定的。换句话说，底层时钟的每个 tick 长度可能不同（例如，某些秒可能比其他秒长）。Instant 可能会向前跳跃或经历时间膨胀（变慢或变快），但绝不会向后走。 Instant 是不透明类型，只能相互比较。没有方法可以从 Instant 获取\"秒数\"。相反，它只允许测量两个 Instant 之间的持续时间（或比较两个 Instant）。 Instant 结构体的大小可能因目标操作系统而异。 此类型包装内部的 std 变体，用于对齐 Tokio 时钟以用于 now()。这对于测试非常有用，你可以利用 time::pause() 和 time::advance()。","section_headers":[]},{"url":"tokio/time/struct.Interval.html","crate":"tokio","title":"Interval","section":"struct","desc":"由 interval 和 interval_at 返回的 Interval。 此类型允许你等待一系列瞬时，每个瞬时之间有一定的时间间隔。与在循环中调用 sleep 不同，这还允许你计算两次调用 sleep 之间所花费的时间。 Interval 可以通过 IntervalStream 转换为 Stream。 当达到间隔中的下一个瞬时时完成。 §取消安全性 此方法是可安全取消的。如果 tick 用作 tokio::select! 中的分支，并且另一个分支先完成，则不会消耗任何 tick。 §示例 use tokio::time; use std::time::Duration; let mut interval = time::interval(Duration::from_millis(10)); interval.tick().await; // 大约已过去 0ms。第一次 tick 立即完成。 interval.tick().await; interval.tick().await; // 大约已过去 20ms。 轮询以达到间隔中的下一个瞬时。 此方法可以返回以下值： Poll::Pending if the next instant has not yet been reached. Poll::Ready(instant) if the next instant has been reached. 当此方法返回 Poll::Pending 时，当前任务被调度以在该瞬时到达时接收唤醒。请注意，在多次调用 poll_tick 时，只有传递给最近一次调用的 Context 中的 Waker 会被调度接收唤醒。 重置间隔，使其在当前时间之后的一个周期完成。 此方法忽略 MissedTickBehavior 策略。 这等效于调用 reset_at(Instant::now() + period)。 §示例 use tokio::time; use std::time::Duration; let mut interval = time::interval(Duration::from_millis(100)); interval.tick().await; time::sleep(Duration::from_millis(50)).await; interval.reset(); interval.tick().await; interval.tick().await; // 大约已过去 250ms。 立即重置间隔。 此方法忽略 MissedTickBehavior 策略。 这等效于调用 reset_at(Instant::now())。 §示例 use tokio::time; use std::time::Duration; let mut interval = time::interval(Duration::from_millis(100)); interval.tick().await; time::sleep(Duration::from_millis(50)).await; interval.reset_immediately(); interval.tick().await; interval.tick().await; // 大约已过去 150ms。 在指定的 std::time::Duration 之后重置间隔。 此方法忽略 MissedTickBehavior 策略。 这等效于调用 reset_at(Instant::now() + after)。 §示例 use tokio::time; use std::time::Duration; let mut interval = time::interval(Duration::from_millis(100)); interval.tick().await; time::sleep(Duration::from_millis(50)).await; let after = Duration::from_millis(20); interval.reset_after(after); interval.tick().await; interval.tick().await; // 大约已过去 170ms。 将间隔重置为 crate::time::Instant 截止时间。 将下一个 tick 设置为在给定的瞬时到期。如果该瞬时在过去，则将使用 MissedTickBehavior 策略进行追赶。如果该瞬时在未来，则下一个 tick 将在给定的瞬时完成，即使这意味着它将睡眠超过此 Interval 的持续时间。如果在调用此方法之前 Interval 已有任何丢失","module_doc":"由 interval 和 interval_at 返回的 Interval。 此类型允许你等待一系列瞬时，每个瞬时之间有一定的时间间隔。与在循环中调用 sleep 不同，这还允许你计算两次调用 sleep 之间所花费的时间。 Interval 可以通过 IntervalStream 转换为 Stream。","section_headers":[]},{"url":"tokio/time/struct.Sleep.html","crate":"tokio","title":"Sleep","section":"struct","desc":"由 sleep 和 sleep_until 返回的 Future。 此类型不实现 Unpin trait，这意味着如果将其与 select! 一起使用或通过调用 poll 来使用，则必须先将其 pin。如果使用 .await，则不适用。 §示例 等待 100 毫秒并打印 “已过 100 毫秒”。 use tokio::time::{sleep, Duration}; sleep(Duration::from_millis(100)).await; println!(\"100 ms have elapsed\"); 与 select! 一起使用。当对同一个 Sleep 进行多次选择时，需要使用 tokio::pin! 将其 pin。 use tokio::time::{self, Duration, Instant}; let sleep = time::sleep(Duration::from_millis(10)); tokio::pin!(sleep); loop { tokio::select! { () = &mut sleep => { println!(\"timer elapsed\"); sleep.as_mut().reset(Instant::now() + Duration::from_millis(50)); }, } } 在带装箱的结构体中使用。通过使用 Box 将 Sleep pin，HasSleep 结构体实现 Unpin，即使 Sleep 没有实现。 use std::future::Future; use std::pin::Pin; use std::task::{Context, Poll}; use tokio::time::Sleep; struct HasSleep { sleep: Pin<Box<Sleep>>, } impl Future for HasSleep { type Output = (); fn poll(mut self: Pin<&mut Self>, cx: &mut Context<'_>) -> Poll<()> { self.sleep.as_mut().poll(cx) } } 在带 pin 投影的结构体中使用。这种方法避免了 Box，但 HasSleep 结构体因此将不实现 Unpin。 use std::future::Future; use std::pin::Pin; use std::task::{Context, Poll}; use tokio::time::Sleep; use pin_project_lite::pin_project; pin_project! { struct HasSleep { #[pin] sleep: Sleep, } } impl Future for HasSleep { type Output = (); fn poll(self: Pin<&mut Self>, cx: &mut Context<'_>) -> Poll<()> { self.project().sleep.poll(cx) } } 返回 future 将完成的时刻。 如果 Sleep 已过去，则返回 true。 当所请求的持续时间已过去时，Sleep 实例已过去。 将 Sleep 实例重置为新的截止时间。 调用此函数允许在不创建新的关联状态的情况下，更改 Sleep future 完成的时间点。 此函数可以在 future 完成之前和之后调用。 要调用此方法，通常需要将调用与 Pin::as_mut 结合使用，这样可以调用方法而无需消费 Sleep 本身。 §示例 use tokio::time::{Duration, Instant}; let sleep = tokio::time::sleep(Duration::from_millis(10)); tokio::pin!(sleep); sleep.as_mut().reset(Instant::now() + Duration::from_millis(20)); 另请参阅顶级示例。 使用给定的格式化器格式化此值。 更多信息 Future 完成时产生的值的类型。 Attempts to resolve the future to a final value, registering the current task for wakeup if the value is not yet available. 更多信息 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably","module_doc":"由 sleep 和 sleep_until 返回的 Future。 此类型不实现 Unpin trait，这意味着如果将其与 select! 一起使用或通过调用 poll 来使用，则必须先将其 pin。如果使用 .await，则不适用。 等待 100 毫秒并打印 “已过 100 毫秒”。","section_headers":[]},{"url":"tokio/time/struct.Timeout.html","crate":"tokio","title":"Timeout","section":"struct","desc":"由 timeout 和 timeout_at 返回的 Future。 获取对此 timeout 中底层值的引用。 获取对此 timeout 中底层值的可变引用。 消耗此 timeout，返回底层值。 使用给定的格式化器格式化此值。 更多信息 Future 完成时产生的值的类型。 Attempts to resolve the future to a final value, registering the current task for wakeup if the value is not yet available. 更多信息 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 原样返回参数。 调用 U::from(self)。 也就是说，此转换是 From<T> for U 实现选择执行的操作。 Future 完成时产生的输出。 我们将要把此值转变成哪种 future？ Creates a future from a value. 更多信息 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"由 timeout 和 timeout_at 返回的 Future。","section_headers":[]},{"url":"tokio/time/error/struct.Elapsed.html","crate":"tokio","title":"Elapsed","section":"struct","desc":"由 Timeout 返回的错误。 当函数能够完成之前超时到期时，返回此错误。 使用给定的格式化器格式化此值。 更多信息 使用给定的格式化器格式化此值。 更多信息 返回此错误的更底层来源（若有）。 更多信息 Provides type-based access to context intended for error reports. 更多信息 从输入类型转换为此类型。 测试 self 与 other 值是否相等，供 == 运算符使用。 测试 != 运算符。默认实现几乎总是够用，除非有非常充分的理由，否则不应被覆盖。 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 原样返回参数。 调用 U::from(self)。 也就是说，此转换是 From<T> for U 实现选择执行的操作。 Converts the given value to a String. 更多信息 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"由 Timeout 返回的错误。 当函数能够完成之前超时到期时，返回此错误。","section_headers":[]},{"url":"tokio/time/error/struct.Error.html","crate":"tokio","title":"Error","section":"struct","desc":"计时器实现遇到的错误。 目前，可能发生两种不同的错误： 当尝试计时器操作，但计时器实例已被 drop 时，会发生 shutdown。在这种情况下，操作将永远无法完成，并返回 shutdown 错误。这是一个永久性错误，即一旦观察到该错误，将来计时器操作将永远不会成功。 当尝试计时器操作，但计时器实例当前正在处理最大数量的未完成 sleep 实例时，会发生 at_capacity。在这种情况下，操作当前无法执行，并返回 at_capacity。这是一个暂时性错误，即在将来的某个时刻，如果再次尝试该操作，它可能会成功。观察到该错误的调用者应尝试减载。一种方法是 drop 发出计时器操作的 future。 创建一个表示已关闭计时器的错误。 如果错误是由计时器关闭引起的，则返回 true。 创建一个表示计时器已达到容量的错误。 如果错误是由计时器达到容量引起的，则返回 true。 创建一个表示计时器配置错误的错误。 如果错误是由计时器配置错误引起的，则返回 true。 返回值的副本。 更多信息 Performs copy-assignment from source. 更多信息 使用给定的格式化器格式化此值。 更多信息 使用给定的格式化器格式化此值。 更多信息 返回此错误的更底层来源（若有）。 更多信息 Provides type-based access to context intended for error reports. 更多信息 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 Performs copy-assignment from self to dest. 更多信息 原样返回参数。 调用 U::from(self)。 也就是说，此转换是 From<T> for U 实现选择执行的操作。 获得所有权后的类型。 Creates owned data from borrowed data, usually by cloning. 更多信息 Uses borrowed data to replace owned data, usually by cloning. 更多信息 Converts the given value to a String. 更多信息 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"计时器实现遇到的错误。 目前，可能发生两种不同的错误： 当尝试计时器操作，但计时器实例已被 drop 时，会发生 shutdown。在这种情况下，操作将永远无法完成，并返回 shutdown 错误。这是一个永久性错误，即一旦观察到该错误，将来计时器操作将永远不会成功。 当尝试计时器操作，但计时器实例当前正在处理最大数量的未完成 sleep 实例时，会发生 at_capacity。在这种情况下，操作当前无法执行，并返回 at_capacity。这是一个暂时性错误，即在将来的某个时刻，如果再次尝试该操作，它可能会成功。观察到该错误的调用者应尝试减载。一种方法是 drop 发出计时器操作的 future。","section_headers":[]}]