[{"url":"bytes/struct.Bytes.html","crate":"bytes","title":"Bytes","section":"struct","desc":"一段可廉价克隆、可切片访问的连续内存。 Bytes 是一个用于存储和操作连续内存切片的高效容器。 它主要面向网络编程场景，但 在其他场景下也同样适用。 Bytes 通过允许多个 Bytes 对象 指向同一段底层内存来简化零拷贝网络编程。 Bytes 并没有单一的具体实现。它是一个接口， 其具体行为由若干 Bytes 底层实现 通过动态分派来提供。 所有 Bytes 实现都必须满足以下要求： 可廉价克隆，从而能在任意数量的组件之间共享（例如通过修改引用计数实现）。 可以切片，以引用原缓冲区的一个子集。 use bytes::Bytes; let mut mem = Bytes::from(\"Hello world\"); let a = mem.slice(0..5); assert_eq!(a, \"Hello\"); let b = mem.split_to(6); assert_eq!(mem, \"world\"); assert_eq!(b, \"Hello \");§Memory layout The Bytes struct itself is fairly small, limited to 4 usize fields used to track information about which segment of the underlying memory the Bytes handle has access to. Bytes keeps both a pointer to the shared state containing the full memory slice and a pointer to the start of the region visible by the handle. Bytes also tracks the length of its view into the memory. §Sharing Bytes contains a vtable, which allows implementations of Bytes to define how sharing/cloning is implemented in detail. When Bytes::clone() is called, Bytes will call the vtable function for cloning the backing storage in order to share it behind multiple Bytes instances. For Bytes implementations which refer to constant memory (e.g. created via Bytes::from_static()) the cloning implementation will be a no-op. For Bytes implementations which point to a reference counted shared storage (e.g. an Arc<[u8]>), sharing will be implemented by increasing the reference count. Due to this mechanism, multiple Bytes instances may point to the same shared memory region. Each Bytes instance can point to different sections within that memory region, and Bytes instances may or may not have overlapping views into the memory. The following diagram visualizes a scenario where 2 Bytes instances make use of an Arc-based backing storage, and provide access to different views: Arc ptrs ┌─────────┐ ________________________ / │ Bytes 2 │ / └─────────┘ / ┌───────────┐ | | |_________/ │ Bytes 1 │ | | | └───────────┘ | | | | | ___/ data | tail | data | tail |/ | v v v v ┌─────┬","module_doc":"一段可廉价克隆、可切片访问的连续内存。 Bytes 是一个用于存储和操作连续内存切片的高效容器。 它主要面向网络编程场景，但 在其他场景下也同样适用。 Bytes 通过允许多个 Bytes 对象 指向同一段底层内存来简化零拷贝网络编程。 Bytes 并没有单一的具体实现。它是一个接口， 其具体行为由若干 Bytes 底层实现 通过动态分派来提供。 所有 Bytes 实现都必须满足以下要求：","section_headers":[]},{"url":"bytes/struct.BytesMut.html","crate":"bytes","title":"BytesMut","section":"struct","desc":"对一段连续内存切片的独占引用。 BytesMut 表示对一段可能共享的内存区域的独占视图。 凭借该独占性保证，BytesMut 句柄的持有者 可以修改该内存。 可以把 BytesMut 视为包含一个 buf: Arc<Vec<u8>>、其在 buf 中的偏移量、 切片长度，并保证同一 buf 上没有其他 BytesMut 与它的切片重叠。这条保证意味着 不需要写锁。 §Growth BytesMut’s BufMut 实现会在必要时隐式扩容。 不过，若在一连串插入之前预先 预留出所需空间，效率会更高。 §示例 use bytes::{BytesMut, BufMut}; let mut buf = BytesMut::with_capacity(64); buf.put_u8(b'h'); buf.put_u8(b'e'); buf.put(&b\"llo\"[..]); assert_eq!(&buf[..], b\"hello\"); // Freeze the buffer so that it can be shared let a = buf.freeze(); // This does not allocate, instead `b` points to the same memory. let b = a.clone(); assert_eq!(&a[..], b\"hello\"); assert_eq!(&b[..], b\"hello\"); 创建具有指定容量的新 BytesMut。 返回的 BytesMut 在不重新分配的情况下至少可容纳 capacity 字节。 需要注意，本函数不指定返回的 BytesMut 的长度，只指定容量。 §示例 use bytes::{BytesMut, BufMut}; let mut bytes = BytesMut::with_capacity(64); // `bytes` contains no data, even though there is capacity assert_eq!(bytes.len(), 0); bytes.put(&b\"hello world\"[..]); assert_eq!(&bytes[..], b\"hello world\"); 使用默认容量创建新的 BytesMut。 生成的对象长度为 0，容量未指定。本函数不分配内存。 §示例 use bytes::{BytesMut, BufMut}; let mut bytes = BytesMut::new(); assert_eq!(0, bytes.len()); bytes.reserve(2); bytes.put_slice(b\"xy\"); assert_eq!(&b\"xy\"[..], &bytes[..]); 返回此 BytesMut 所包含的字节数。 §示例 use bytes::BytesMut; let b = BytesMut::from(&b\"hello\"[..]); assert_eq!(b.len(), 5); 若 BytesMut 长度为 0，返回 true。 §示例 use bytes::BytesMut; let b = BytesMut::with_capacity(64); assert!(b.is_empty()); 返回 BytesMut 在不重新分配的情况下可容纳的字节数。 §示例 use bytes::BytesMut; let b = BytesMut::with_capacity(64); assert_eq!(b.capacity(), 64); 将 self 转换为不可变的 Bytes。 此转换零开销，用于表示句柄所引用的切片将不再被修改。转换完成后，句柄可被克隆并在线程间共享。 §示例 ⓘuse bytes::{BytesMut, BufMut}; use std::thread; let mut b = BytesMut::with_capacity(64); b.put(&b\"hello world\"[..]); let b1 = b.freeze(); let b2 = b1.clone(); let th = thread::spawn(move || { assert_eq!(&b1[..], b\"hello world\"); }); assert_eq!(&b2[..], b\"hello world\"); th.join().unwrap(); 创建一个包含 len 个零字节的新 BytesMut。 生成对象的长度为 len，容量大于或等于 len。对象的整个长度将填充为零。 在某些平台或分配器上，本函数可能比手动实现更快。 §示例 us","module_doc":"对一段连续内存切片的独占引用。 BytesMut 表示对一段可能共享的内存区域的独占视图。 凭借该独占性保证，BytesMut 句柄的持有者 可以修改该内存。 可以把 BytesMut 视为包含一个 buf: Arc<Vec<u8>>、其在 buf 中的偏移量、 切片长度，并保证同一 buf 上没有其他 BytesMut 与它的切片重叠。这条保证意味着 不需要写锁。 BytesMut’s BufMut 实现会在必要时隐式扩容。 不过，若在一连串插入之前预先 预留出所需空间，效率会更高。","section_headers":[]},{"url":"bytes/struct.TryGetError.html","crate":"bytes","title":"TryGetError","section":"struct","desc":"Buf 的 try_get_ 方法的错误类型。 表明在尝试使用某个 try_get_ 方法从 Buf 获取值时， 缓冲区中没有足够的剩余字节。 获取该值所需的字节数 缓冲区中可用的字节数 使用给定的格式化器格式化此值。 更多信息 使用给定的格式化器格式化此值。 更多信息 返回此错误的更底层来源（若有）。 更多信息 Provides type-based access to context intended for error reports. 更多信息 从输入类型转换为此类型。 测试 self 与 other 值是否相等，供 == 运算符使用。 测试 != 运算符。默认实现几乎总是够用，除非有非常充分的理由，否则不应被覆盖。 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 原样返回传入的参数。 调用 U::from(self)。 That is, this conversion is whatever the implementation of From<T> for U 的实现方式。 Converts the given value to a String. 更多信息 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"Buf 的 try_get_ 方法的错误类型。 表明在尝试使用某个 try_get_ 方法从 Buf 获取值时， 缓冲区中没有足够的剩余字节。","section_headers":["字段"]},{"url":"bytes/buf/struct.Chain.html","crate":"bytes","title":"Chain","section":"struct","desc":"Chain 将两个缓冲区串联起来。 Chain 是一个适配器，它将两个底层缓冲区链接起来，并提供一个跨越两者的连续视图。它可以将不可变缓冲区（Buf 值）或可变缓冲区（BufMut 值）串联起来。 此结构体通常通过调用 Buf::chain 创建。详情请参阅该函数的文档。 §示例 use bytes::{Bytes, Buf}; let mut buf = (&b\"hello \"[..]) .chain(&b\"world\"[..]); let full: Bytes = buf.copy_to_bytes(11); assert_eq!(full[..], b\"hello world\"[..]); 获取对第一个底层 Buf 的引用。 §示例 use bytes::Buf; let buf = (&b\"hello\"[..]) .chain(&b\"world\"[..]); assert_eq!(buf.first_ref()[..], b\"hello\"[..]); 获取对第一个底层 Buf 的可变引用。 §示例 use bytes::Buf; let mut buf = (&b\"hello\"[..]) .chain(&b\"world\"[..]); buf.first_mut().advance(1); let full = buf.copy_to_bytes(9); assert_eq!(full, b\"elloworld\"[..]); 获取对最后一个底层 Buf 的引用。 §示例 use bytes::Buf; let buf = (&b\"hello\"[..]) .chain(&b\"world\"[..]); assert_eq!(buf.last_ref()[..], b\"world\"[..]); 获取对最后一个底层 Buf 的可变引用。 §示例 use bytes::Buf; let mut buf = (&b\"hello \"[..]) .chain(&b\"world\"[..]); buf.last_mut().advance(1); let full = buf.copy_to_bytes(10); assert_eq!(full, b\"hello orld\"[..]); 消耗此 Chain，返回底层的值。 §示例 use bytes::Buf; let chain = (&b\"hello\"[..]) .chain(&b\"world\"[..]); let (first, last) = chain.into_inner(); assert_eq!(first[..], b\"hello\"[..]); assert_eq!(last[..], b\"world\"[..]); Returns the number of bytes between the current position and the end of the buffer. 更多信息 Returns a slice starting at the current position and of length between 0 and Buf::remaining(). Note that this can return a shorter slice (this allows non-continuous internal representation). 更多信息 Advance the internal cursor of the Buf 更多信息 Fills dst with potentially multiple slices starting at self’s current position. 更多信息 Consumes len bytes inside self and returns new instance of Bytes with this data. 更多信息 Returns true if there are any more bytes to consume 更多信息 Copies bytes from self into dst. 更多信息 Gets an unsigned 8 bit integer from self. 更多信息 Gets a signed 8 bit integer from self. 更多信息 Gets an unsigned 16 bit integer from self in big-endian byte order. 更多信息 Gets an unsigned 16 bit integer from self in little-endian byte order. 更多信息 Gets an u","module_doc":"Chain 将两个缓冲区串联起来。 Chain 是一个适配器，它将两个底层缓冲区链接起来，并提供一个跨越两者的连续视图。它可以将不可变缓冲区（Buf 值）或可变缓冲区（BufMut 值）串联起来。 此结构体通常通过调用 Buf::chain 创建。详情请参阅该函数的文档。","section_headers":[]},{"url":"bytes/buf/struct.IntoIter.html","crate":"bytes","title":"IntoIter","section":"struct","desc":"遍历缓冲区所包含字节的迭代器。 §示例 基本用法： use bytes::Bytes; let buf = Bytes::from(&b\"abc\"[..]); let mut iter = buf.into_iter(); assert_eq!(iter.next(), Some(b'a')); assert_eq!(iter.next(), Some(b'b')); assert_eq!(iter.next(), Some(b'c')); assert_eq!(iter.next(), None); 创建一个迭代器，遍历缓冲区中包含的字节。 §示例 use bytes::Bytes; let buf = Bytes::from_static(b\"abc\"); let mut iter = buf.into_iter(); assert_eq!(iter.next(), Some(b'a')); assert_eq!(iter.next(), Some(b'b')); assert_eq!(iter.next(), Some(b'c')); assert_eq!(iter.next(), None); 消耗此 IntoIter，返回底层的值。 §示例 use bytes::{Buf, Bytes}; let buf = Bytes::from(&b\"abc\"[..]); let mut iter = buf.into_iter(); assert_eq!(iter.next(), Some(b'a')); let buf = iter.into_inner(); assert_eq!(2, buf.remaining()); 获取对底层 Buf 的引用。 不建议直接从底层 Buf 读取。 §示例 use bytes::{Buf, Bytes}; let buf = Bytes::from(&b\"abc\"[..]); let mut iter = buf.into_iter(); assert_eq!(iter.next(), Some(b'a')); assert_eq!(2, iter.get_ref().remaining()); 获取对底层 Buf 的可变引用。 不建议直接从底层 Buf 读取。 §示例 use bytes::{Buf, BytesMut}; let buf = BytesMut::from(&b\"abc\"[..]); let mut iter = buf.into_iter(); assert_eq!(iter.next(), Some(b'a')); iter.get_mut().advance(1); assert_eq!(iter.next(), Some(b'c')); 使用给定的格式化器格式化此值。 更多信息 Returns the exact remaining length of the iterator. 更多信息 Returns true if the iterator is empty. 更多信息 被迭代元素的类型。 Advances the iterator and returns the next value. 更多信息 Returns the bounds on the remaining length of the iterator. 更多信息 Advances the iterator and returns an array containing the next N values. 更多信息 Consumes the iterator, counting the number of iterations and returning it. 更多信息 Consumes the iterator, returning the last element. 更多信息 Advances the iterator by n elements. 更多信息 Returns the nth element of the iterator. 更多信息 Creates an iterator starting at the same point, but stepping by the given amount at each iteration. 更多信息 Takes two iterators and creates a new iterator over both in sequence. 更多信息 ‘Zips up’ two iterators into a single iterator of pairs. 更多信息 Creates a new iterator which pla","module_doc":"遍历缓冲区所包含字节的迭代器。 基本用法：","section_headers":[]},{"url":"bytes/buf/struct.Limit.html","crate":"bytes","title":"Limit","section":"struct","desc":"一个限制可写入到底层缓冲区字节数的 BufMut 适配器。 消耗该 Limit，返回底层的值。 获得底层 BufMut 的引用。 不建议直接对底层 BufMut 进行写入。 获得底层 BufMut 的可变引用。 不建议直接对底层 BufMut 进行写入。 返回可写入的最大字节数。 §注意 如果底层 BufMut 可用的字节数少于本方法返回的值， 则以实际可用字节数为准。 设置可写入的最大字节数。 §注意 如果底层 BufMut 可用的字节数少于 lim， 则以实际可用字节数为准。 Returns the number of bytes that can be written from the current position until the end of the buffer is reached. 更多信息 Returns a mutable slice starting at the current BufMut position and of length between 0 and BufMut::remaining_mut(). Note that this can be shorter than the whole remainder of the buffer (this allows non-continuous implementation). 更多信息 Advance the internal cursor of the BufMut 更多信息 Returns true if there is space in self for more bytes. 更多信息 Transfer bytes into self from src and advance the cursor by the number of bytes written. 更多信息 Transfer bytes into self from src and advance the cursor by the number of bytes written. 更多信息 Put cnt bytes val into self. 更多信息 Writes an unsigned 8 bit integer to self. 更多信息 Writes a signed 8 bit integer to self. 更多信息 Writes an unsigned 16 bit integer to self in big-endian byte order. 更多信息 Writes an unsigned 16 bit integer to self in little-endian byte order. 更多信息 Writes an unsigned 16 bit integer to self in native-endian byte order. 更多信息 Writes a signed 16 bit integer to self in big-endian byte order. 更多信息 Writes a signed 16 bit integer to self in little-endian byte order. 更多信息 Writes a signed 16 bit integer to self in native-endian byte order. 更多信息 Writes an unsigned 32 bit integer to self in big-endian byte order. 更多信息 Writes an unsigned 32 bit integer to self in little-endian byte order. 更多信息 Writes an unsigned 32 bit integer to self in native-endian byte order. 更多信息 Writes a signed 32 bit integer to self in big-endian byte order. 更多信息 Writes a signed 32 bit integer to self in little-endian byte order. 更多信息 Writes a signed 32 bit integer to self in native-endian byte order. 更多信息 Writes an unsigned 64 bit integer to self in the big-endian byte order. 更多信息 Writes an unsigned 64 b","module_doc":"一个限制可写入到底层缓冲区字节数的 BufMut 适配器。","section_headers":[]},{"url":"bytes/buf/struct.Reader.html","crate":"bytes","title":"Reader","section":"struct","desc":"一个为内部值实现 io::Read 的 Buf 适配器。 此结构体通常通过对 Buf 调用 reader() 创建。详情请参阅 reader() 的文档。 获取对底层 Buf 的引用。 不建议直接从底层 Buf 读取。 §示例 use bytes::Buf; let buf = b\"hello world\".reader(); assert_eq!(b\"hello world\", buf.get_ref()); 获得底层 Buf 的可变引用。 不建议直接从底层 Buf 进行读取。 消耗此 Reader，返回底层的值。 §示例 use bytes::Buf; use std::io; let mut buf = b\"hello world\".reader(); let mut dst = vec![]; io::copy(&mut buf, &mut dst).unwrap(); let buf = buf.into_inner(); assert_eq!(0, buf.remaining()); Returns the contents of the internal buffer, filling it with more data, via Read methods, if empty. 更多信息 Marks the given amount of additional bytes from the internal buffer as having been read. Subsequent calls to read only return bytes that have not been marked as read. 更多信息 Checks if there is any data left to be read. 更多信息 Reads all bytes into buf until the delimiter byte or EOF is reached. 更多信息 Skips all bytes until the delimiter byte or EOF is reached. 更多信息 Reads all bytes until a newline (the 0xA byte) is reached, and append them to the provided String buffer. 更多信息 Returns an iterator over the contents of this reader split on the byte byte. 更多信息 Returns an iterator over the lines of this reader. 更多信息 使用给定的格式化器格式化此值。 更多信息 Pull some bytes from this source into the specified buffer, returning how many bytes were read. 更多信息 Like read, except that it reads into a slice of buffers. 更多信息 Determines if this Reader has an efficient read_vectored implementation. 更多信息 Reads all bytes until EOF in this source, placing them into buf. 更多信息 Reads all bytes until EOF in this source, appending them to buf. 更多信息 Reads the exact number of bytes required to fill buf. 更多信息 Pull some bytes from this source into the specified buffer. 更多信息 Reads the exact number of bytes required to fill cursor. 更多信息 Creates a “by reference” adapter for this instance of Read. 更多信息 Transforms this Read instance to an Iterator over its bytes. 更多信息 Creates an adapter which will chain this stream with another. 更多信息 Creates an adapter which will read at most l","module_doc":"一个为内部值实现 io::Read 的 Buf 适配器。 此结构体通常通过对 Buf 调用 reader() 创建。详情请参阅 reader() 的文档。","section_headers":[]},{"url":"bytes/buf/struct.Take.html","crate":"bytes","title":"Take","section":"struct","desc":"一个限制从底层缓冲区读取字节数的 Buf 适配器。 此结构体通常通过对 Buf 调用 take() 创建。详情请参阅 take() 的文档。 消耗此 Take，返回底层的值。 §示例 use bytes::{Buf, BufMut}; let mut buf = b\"hello world\".take(2); let mut dst = vec![]; dst.put(&mut buf); assert_eq!(*dst, b\"he\"[..]); let mut buf = buf.into_inner(); dst.clear(); dst.put(&mut buf); assert_eq!(*dst, b\"llo world\"[..]); 获取对底层 Buf 的引用。 不建议直接从底层 Buf 读取。 §示例 use bytes::Buf; let buf = b\"hello world\".take(2); assert_eq!(11, buf.get_ref().remaining()); 获取对底层 Buf 的可变引用。 不建议直接从底层 Buf 读取。 §示例 use bytes::{Buf, BufMut}; let mut buf = b\"hello world\".take(2); let mut dst = vec![]; buf.get_mut().advance(2); dst.put(&mut buf); assert_eq!(*dst, b\"ll\"[..]); 返回可读取的最大字节数。 §Note 若内部 Buf 的字节数少于本方法所指示的数量，则以实际可用字节数为准。 §示例 use bytes::Buf; let mut buf = b\"hello world\".take(2); assert_eq!(2, buf.limit()); assert_eq!(b'h', buf.get_u8()); assert_eq!(1, buf.limit()); 设置可读取的最大字节数。 §Note 若内部 Buf 的字节数少于 lim，则以实际可用字节数为准。 §示例 use bytes::{Buf, BufMut}; let mut buf = b\"hello world\".take(2); let mut dst = vec![]; dst.put(&mut buf); assert_eq!(*dst, b\"he\"[..]); dst.clear(); buf.set_limit(3); dst.put(&mut buf); assert_eq!(*dst, b\"llo\"[..]); Returns the number of bytes between the current position and the end of the buffer. 更多信息 Returns a slice starting at the current position and of length between 0 and Buf::remaining(). Note that this can return a shorter slice (this allows non-continuous internal representation). 更多信息 Advance the internal cursor of the Buf 更多信息 Consumes len bytes inside self and returns new instance of Bytes with this data. 更多信息 Fills dst with potentially multiple slices starting at self’s current position. 更多信息 Returns true if there are any more bytes to consume 更多信息 Copies bytes from self into dst. 更多信息 Gets an unsigned 8 bit integer from self. 更多信息 Gets a signed 8 bit integer from self. 更多信息 Gets an unsigned 16 bit integer from self in big-endian byte order. 更多信息 Gets an unsigned 16 bit integer from self in little-endian byte order. 更多信息 Gets an unsigned 16 bit integer from self","module_doc":"一个限制从底层缓冲区读取字节数的 Buf 适配器。 此结构体通常通过对 Buf 调用 take() 创建。详情请参阅 take() 的文档。","section_headers":[]},{"url":"bytes/buf/struct.UninitSlice.html","crate":"bytes","title":"UninitSlice","section":"struct","desc":"未初始化的字节切片。 由 BufMut::chunk_mut() 返回，所引用的字节切片可能是未初始化的。此包装器提供安全访问，不会引入未定义行为。 此包装器的安全不变量为： Reading from an UninitSlice is undefined behavior. Writing uninitialized bytes to an UninitSlice is undefined behavior. &mut UninitSlice 与 &mut [MaybeUninit<u8>] 的区别在于：在安全代码中，可以向 &mut [MaybeUninit<u8>] 写入未初始化的字节，而本类型禁止这样做。 创建一个 &mut UninitSlice，包装一段已初始化的内存切片。 §示例 use bytes::buf::UninitSlice; let mut buffer = [0u8; 64]; let slice = UninitSlice::new(&mut buffer[..]); 创建一个 &mut UninitSlice，包装一段未初始化的内存切片。 §示例 use bytes::buf::UninitSlice; use core::mem::MaybeUninit; let mut buffer = [MaybeUninit::uninit(); 64]; let slice = UninitSlice::uninit(&mut buffer[..]); let mut vec = Vec::with_capacity(1024); let spare: &mut UninitSlice = vec.spare_capacity_mut().into(); 从指针和长度创建 &mut UninitSlice。 §Safety 调用方必须确保 ptr 引用了由调用方拥有的有效内存区域，且在 'a 的整个生命周期内代表一个字节切片。 §示例 use bytes::buf::UninitSlice; let bytes = b\"hello world\".to_vec(); let ptr = bytes.as_ptr() as *mut _; let len = bytes.len(); let slice = unsafe { UninitSlice::from_raw_parts_mut(ptr, len) }; 在指定偏移处写入单个字节。 §Panics 若 index 越界，本函数会 panic。 §示例 use bytes::buf::UninitSlice; let mut data = [b'f', b'o', b'o']; let slice = unsafe { UninitSlice::from_raw_parts_mut(data.as_mut_ptr(), 3) }; slice.write_byte(0, b'b'); assert_eq!(b\"boo\", &data[..]); 从 src 复制字节到 self。 src 的长度必须与 self 相同。 §Panics 若 src 与 self 长度不同，本函数会 panic。 §示例 use bytes::buf::UninitSlice; let mut data = [b'f', b'o', b'o']; let slice = unsafe { UninitSlice::from_raw_parts_mut(data.as_mut_ptr(), 3) }; slice.copy_from_slice(b\"bar\"); assert_eq!(b\"bar\", &data[..]); 返回指向切片缓冲区的裸指针。 §Safety 调用方不得读取所引用的内存，也不得向切片写入未初始化的字节。 §示例 use bytes::BufMut; let mut data = [0, 1, 2]; let mut slice = &mut data[..]; let ptr = BufMut::chunk_mut(&mut slice).as_mut_ptr(); 返回此切片缓冲区的 &mut [MaybeUninit<u8>]。 §Safety 调用方不得读取所引用的内存，也不得向切片写入未初始化的字节。这是因为创建 UninitSlice 的 BufMut 实现知道哪些部分是初始化的。向切片写入未初始化的字节可能导致 BufMut 之后读取这些字节并触发未定义行为。 §示例 use bytes::BufMut; let mut data = [0, 1, 2]; let mut slice = &mut data[..]; unsafe ","module_doc":"未初始化的字节切片。 由 BufMut::chunk_mut() 返回，所引用的字节切片可能是未初始化的。此包装器提供安全访问，不会引入未定义行为。 此包装器的安全不变量为： &mut UninitSlice 与 &mut [MaybeUninit<u8>] 的区别在于：在安全代码中，可以向 &mut [MaybeUninit<u8>] 写入未初始化的字节，而本类型禁止这样做。","section_headers":[]},{"url":"bytes/buf/struct.Writer.html","crate":"bytes","title":"Writer","section":"struct","desc":"一个为内部值实现 io::Write 的 BufMut 适配器。 此结构体通常通过对 BufMut 调用 writer() 创建。详情请参阅 writer() 的文档。 获取对底层 BufMut 的引用。 不建议直接写入底层 BufMut。 §示例 use bytes::BufMut; let buf = Vec::with_capacity(1024).writer(); assert_eq!(1024, buf.get_ref().capacity()); 获取对底层 BufMut 的可变引用。 不建议直接写入底层 BufMut。 §示例 use bytes::BufMut; let mut buf = vec![].writer(); buf.get_mut().reserve(1024); assert_eq!(1024, buf.get_ref().capacity()); 消耗此 Writer，返回底层的值。 §示例 use bytes::BufMut; use std::io; let mut buf = vec![].writer(); let mut src = &b\"hello world\"[..]; io::copy(&mut src, &mut buf).unwrap(); let buf = buf.into_inner(); assert_eq!(*buf, b\"hello world\"[..]); 使用给定的格式化器格式化此值。 更多信息 Writes a buffer into this writer, returning how many bytes were written. 更多信息 Flushes this output stream, ensuring that all intermediately buffered contents reach their destination. 更多信息 Like write, except that it writes from a slice of buffers. 更多信息 Determines if this Writer has an efficient write_vectored implementation. 更多信息 Attempts to write an entire buffer into this writer. 更多信息 Attempts to write multiple buffers into this writer. 更多信息 Writes a formatted string into this writer, returning any error encountered. 更多信息 Creates a “by reference” adapter for this instance of Write. 更多信息 Gets the TypeId of self. 更多信息 Immutably borrows from an owned value. 更多信息 Mutably borrows from an owned value. 更多信息 原样返回传入的参数。 调用 U::from(self)。 That is, this conversion is whatever the implementation of From<T> for U 的实现方式。 转换出错时返回的类型。 转换出错时返回的类型。","module_doc":"一个为内部值实现 io::Write 的 BufMut 适配器。 此结构体通常通过对 BufMut 调用 writer() 创建。详情请参阅 writer() 的文档。","section_headers":[]},{"url":"bytes/buf/trait.Buf.html","crate":"bytes","title":"Buf","section":"trait","desc":"从缓冲区读取字节。 缓冲区将字节存储在内存中，以便读取操作不会失败。底层存储可能连续也可能不连续。Buf 值是指向缓冲区的游标。从 Buf 读取会推进游标位置。可以将其视为字节集合的高效 Iterator。 最简单的 Buf 是 &[u8]。 use bytes::Buf; let mut buf = &b\"hello world\"[..]; assert_eq!(b'h', buf.get_u8()); assert_eq!(b'e', buf.get_u8()); assert_eq!(b'l', buf.get_u8()); let mut rest = [0; 8]; buf.copy_to_slice(&mut rest); assert_eq!(&rest[..], &b\"lo world\"[..]); 返回从当前位置到缓冲区末尾的字节数。 此值大于或等于 chunk() 返回切片的长度。 §示例 use bytes::Buf; let mut buf = &b\"hello world\"[..]; assert_eq!(buf.remaining(), 11); buf.get_u8(); assert_eq!(buf.remaining(), 10);§Implementer notes remaining 的实现应确保返回值不会改变，除非调用了 advance 或任何其他文档明确会改变 Buf’s 当前位置的函数。 返回从当前位置开始、长度介于 0 和 Buf::remaining() 之间的切片。注意这可能返回较短的切片（允许非连续的内部表示）。 这是一个较低级别的函数。大多数操作通过其他函数完成。 §示例 use bytes::Buf; let mut buf = &b\"hello world\"[..]; assert_eq!(buf.chunk(), &b\"hello world\"[..]); buf.advance(6); assert_eq!(buf.chunk(), &b\"world\"[..]);§Implementer notes 此函数不应 panic。chunk() 返回空切片当且仅当 remaining() 返回 0。换言之，chunk() 返回空切片意味着 remaining() 将返回 0，而 remaining() 返回 0 意味着 chunk() 将返回空切片。 推进 Buf 的内部游标 下一次调用 chunk() 时将返回一个从底层缓冲区再深入 cnt 字节开始的切片。 §示例 use bytes::Buf; let mut buf = &b\"hello world\"[..]; assert_eq!(buf.chunk(), &b\"hello world\"[..]); buf.advance(6); assert_eq!(buf.chunk(), &b\"world\"[..]);§Panics 若 cnt > self.remaining()，此函数可能会 panic。 §Implementer notes 建议 advance 的实现在 cnt > self.remaining() 时 panic。若实现不 panic，则调用行为必须如同 cnt == self.remaining()。 调用 cnt == 0 时绝不应 panic，且应为无操作。 从 self 的当前位置开始， 向 dst 中填充可能的多段切片。 如果 Buf 由不相邻的字节切片支撑，chunk_vectored 允许 一次性取出多段切片。dst 是一个 IoSlice 引用数组， 其中的切片可直接配合 writev 使用而无需额外转换。所有写入 dst 的缓冲区 长度之和应小于或等于 Buf::remaining()。 dst 中的元素会被覆盖，但切片包含的数据 不会被修改。返回值是写入 dst 的切片个数。 若 Buf::remaining() 非零，则本方法至少会向 dst 写入一段非空切片。 这是一个底层函数，大多数操作应使用其他方法完成。 §实现者须知 本方法绝不能 panic。一旦到达缓冲区末尾， 即 Buf::remaining 返回 0，对 chunk_vectored 的调用必须返回 0， 且不得修改 dst。 实现还需注意正确处理 dst 为零长度切片的调用情形。 若还有更多字节可供消费则返回 true 这等价于 self.remaining() != 0。 §示例 use bytes::Buf; let mut buf = &b\"a\"[..]; assert!(buf.has_remaining()); buf.get_u8(); assert!(!buf.has_remaining());","module_doc":"从缓冲区读取字节。 缓冲区将字节存储在内存中，以便读取操作不会失败。底层存储可能连续也可能不连续。Buf 值是指向缓冲区的游标。从 Buf 读取会推进游标位置。可以将其视为字节集合的高效 Iterator。 最简单的 Buf 是 &[u8]。","section_headers":[]},{"url":"bytes/buf/trait.BufMut.html","crate":"bytes","title":"BufMut","section":"trait","desc":"为值提供对字节的顺序写访问的 trait。 向缓冲区写入字节 缓冲区将字节存储在内存中，以便写入操作不会失败。底层存储可能连续也可能不连续。BufMut 值是指向缓冲区的游标。向 BufMut 写入会推进游标位置。 最简单的 BufMut 是 Vec<u8>。 use bytes::BufMut; let mut buf = vec![]; buf.put(&b\"hello world\"[..]); assert_eq!(buf, b\"hello world\"); 返回从当前位置到缓冲区末尾可写入的字节数。 此值大于或等于 chunk_mut() 返回切片的长度。 向 BufMut 写入可能涉及按需分配更多内存。若遇到分配失败，实现可能在该方法指示的字节数之前就失败。 §示例 use bytes::BufMut; let mut dst = [0; 10]; let mut buf = &mut dst[..]; let original_remaining = buf.remaining_mut(); buf.put(&b\"hello\"[..]); assert_eq!(original_remaining - 5, buf.remaining_mut());§Implementer notes remaining_mut 的实现应确保返回值不会改变，除非调用了 advance_mut 或任何其他文档明确会改变 BufMut’s 当前位置的函数。 §Note remaining_mut 可能返回比实际可用空间更小的值。 推进 BufMut 的内部游标 下一次调用 chunk_mut() 时将返回一个从底层缓冲区再深入 cnt 字节开始的切片。 §Safety 调用方必须确保 chunk 的下 cnt 字节已初始化。 §示例 use bytes::BufMut; let mut buf = Vec::with_capacity(16); // Write some data buf.chunk_mut()[0..2].copy_from_slice(b\"he\"); unsafe { buf.advance_mut(2) }; // write more bytes buf.chunk_mut()[0..3].copy_from_slice(b\"llo\"); unsafe { buf.advance_mut(3); } assert_eq!(5, buf.len()); assert_eq!(buf, b\"hello\");§Panics 若 cnt > self.remaining_mut()，此函数可能会 panic。 §Implementer notes 建议 advance_mut 的实现在 cnt > self.remaining_mut() 时 panic。若实现不 panic，则调用行为必须如同 cnt == self.remaining_mut()。 调用 cnt == 0 时绝不应 panic，且应为无操作。 返回从当前 BufMut 位置开始、长度介于 0 和 BufMut::remaining_mut() 之间的可变切片。注意这可能比缓冲区剩余总量更短（允许非连续的内部实现）。 这是一个较低级别的函数。大多数操作通过其他函数完成。 返回的字节切片可能表示未初始化的内存。 §示例 use bytes::BufMut; let mut buf = Vec::with_capacity(16); unsafe { // MaybeUninit::as_mut_ptr buf.chunk_mut()[0..].as_mut_ptr().write(b'h'); buf.chunk_mut()[1..].as_mut_ptr().write(b'e'); buf.advance_mut(2); buf.chunk_mut()[0..].as_mut_ptr().write(b'l'); buf.chunk_mut()[1..].as_mut_ptr().write(b'l'); buf.chunk_mut()[2..].as_mut_ptr().write(b'o'); buf.advance_mut(3); } assert_eq!(5, buf.len()); assert_eq!(buf, b\"hello\");§Implementer notes 此函数不应 panic。chunk_mut() 返回空切片当且仅当 remaining_mut() 返回 0。换言之，chunk_mut() 返回空切片意味着 remaining_mut() 将返回 0，而 remaining_mut() 返回 0 意味着 chunk_","module_doc":"为值提供对字节的顺序写访问的 trait。 向缓冲区写入字节 缓冲区将字节存储在内存中，以便写入操作不会失败。底层存储可能连续也可能不连续。BufMut 值是指向缓冲区的游标。向 BufMut 写入会推进游标位置。 最简单的 BufMut 是 Vec<u8>。","section_headers":[]}]